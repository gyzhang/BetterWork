# 审阅待决事项

> 本文件是**审阅与编辑阶段的活清单**。
> 规则：每一轮审阅、编辑、改稿开始前**必须先打开这份清单核对一遍**，把未决项摆到明面上提醒作者，直到作者明确说"完成"。

最后更新：**2026-09-18（v1.0 全稿定稿，三形态齐备）**

---

## 1. ✅ 挂起项已关闭（2026-09-18 由作者确认）

### 1. "代码已通过测试验证"这一表述的强度

| 项 | 内容 |
|---|---|
| 出处 | 作者本次任务要求第 2 条 |
| 作者答复（2026-09-18） | **"这些代码都是能正常运行的，也是通过了测试验证的"** |
| **关闭（2026-09-18）** | 作者回复：**"我本机跑通了，用 B（强表述）"** |
| 状态 | ✅ **已关闭** —— 采用强表述：正文可写"测试已通过" |
| 落地位置 | 附录 B「代码来源登记」给出验证命令与结果；`00-架构与编辑说明.md` §7.2 同步改写 |

**为什么这条重要**：全书的核心纪律是"可复核"。作者说代码都过了测试，但那是**第三方陈述**；书里若写"以下代码已通过测试验证"，读者会拿它当结论用。

**保留的诚实边界（不回退，也不省略）**：

代笔方本会话实测（受限环境）为 `2 failed | 75 passed (77)` / `14 failed | 568 passed (582)`，14 项失败全部落在 macOS 进程组监管器一条链上，根因是执行环境不允许调用 `ps`（`operation not permitted`，退出码 127；对照实验 `/bin/echo` 派生正常）。作者在本机（`ps` 可用）全量跑通。

**差异如实写进附录 B**——因为这本身就是一条好的工程论点：**依赖操作系统进程工具的测试是环境敏感的；这不是缺陷，是必须被写下来的前提。**它恰好是全书第 13 章"脚本化提供方不是线上模型质量的证据"的同类命题。

#### 校准记录

| 日期 | 作者反馈 | 对应动作 |
|---|---|---|
| 2026-09-18 | 要求第 2 条 | 记为挂起项 |
| 2026-09-18 | （代笔方实测）`npx vitest run` → `14 failed \| 568 passed (582)` | 定位根因为 `ps` 被环境阻断；给出 A/B 两个表述方案 |
| 2026-09-18 | **"我本机跑通了，用 B（强表述）"** | ✅ 关闭；采用强表述；环境差异写入附录 B |

---

## 1b. 语气校准：已完成（2026-09-18）

| 项 | 内容 |
|---|---|
| 校准章 | 第 1 章（`parts/01-ch01.md`） |
| 作者裁定 | **"这就是我的调子"** |
| 落地动作 | 第 1 章定为**全书语气基准**。后续每章交稿前对照自查：断言式标题、拿数字说话、不注水、章末给动作不给思考题 |
| 追记入口 | 若后续某章被指认"不像我"，追记在本节并当轮回改 |

---

## 2. 其他待作者确认的开放问题

| # | 问题 | 状态 |
|---|---|---|
| 2 | 书名：主推《智能体工程：从一次工具调用到可交付的系统》 | ✅ 已定（作者："书名很不错，就是我喜欢的"） |
| 3 | 语气校准 | ✅ 已完成，见 §1b |
| 4 | 案例基调：校园/通用主线 + 每章「行业视角」小节，行业变体用机构类型代称 | ✅ 按默认执行 |
| 5 | 篇幅：31.9 万字 / 38 图 / A4 约 340 页 | ⚠️ **见图与页数已达成（39 图 / A4 339 页），字数未达成（27.9 万 vs 31.9 万）——见 §2b** |
| 6 | 交付节奏：分批（第 0–5 章 → 第 6–13 章 → 第 14–20 章 + 附录） | ✅ 按默认执行 |
| 7 | 署名 **Kevin+AI**；保留参考实现 GitHub 地址（MIT 许可要求保留版权声明） | ✅ 已定 |
| 8 | HTML 阅读版与打印版 PDF：正文定稿后一次性产出 | ✅ **已完成**：HTML 1005.8 KB（39 图内联为矢量）、A4 打印版 339 页 |
| 9 | **是否补足字数** | ✅ **已裁定（2026-09-18）：篇幅不作验收指标**；只按"是否缺内容"补——已据此补第 19 章（那一章缺的是可跑示例，不是字数）。立为 D-07 |

---

## 2b. ⏳ 待作者决定：字数差额（唯一未决项）

| 项 | 内容 |
|---|---|
| 实际 | **总字符 273,453**（v2.0 两轮落地后实测；此数会变，引用前重算） |
| 原定目标 | 约 31.9 万字 |
| 差额 | 约 5.3 万总字符（**-17%**） |
| 差在哪 | **不是漏写章节**，21 章体例齐全、39 图齐备。差额集中在第五、六、七篇的单章体量：交付篇 41,000→29,148、综合篇 29,000→16,666、迁移篇 25,000→14,941 |
| 不影响什么 | **A4 打印版实测 339 页**，符合"一本书要有书的样子"；校验 18 项全通过 |
| 补足需要追加什么 | 这三篇的**深度**——更多手算示例、更强的行业变体、更长的可操作清单。**不需要新增章节。** 预计追加 4–6 万字可到原定规模 |
| 作者的三个选项 | **① 接受现状**（已是一本 339 页的完整书）· **② 指定补哪些章**（我按章加厚）· **③ 全部按原定规模补齐**（我按篇重做这三篇的深度） |

**我的建议**：选 **①** 或 **②**。理由：这三篇的**论点已经收敛**，当前每章 1–2 张图、5 块体例、章末行动清单都已到位；单纯为了凑字数加厚，会让原本能一页说完的事变成三页——**而"删掉一个说不清的数字，比留下一个说不清的数字更专业"，这句话对篇幅同样成立。** 如果确实要补，我建议只补第五篇（交付）与第六篇（综合），因为那两篇的手算示例最经得起加厚。

---

## 3. 已决策并落地

| # | 事项 | 决定 | 落地 |
|---|---|---|---|
| 1 | 交付目录 | `book/cn/` 即书名目录，遵循书稿工坊目录契约 | ✅ `parts/` `images/` `svg/` `assets/` `refs/` 已建 |
| 2 | 定位 | 工程实践书 + 可教学证据链，不是课程教材 | ✅ `00-架构与编辑说明.md` §7.1 D-01 |
| 3 | 篇章结构 | 第 0 章 + 七篇 20 章 + 附录 A–E | ✅ `00-架构与编辑说明.md` §5.1 |
| 4 | 脱敏 | 真实客户名、内部产品线名、作者姓名一律不出现；案例用机构类型代称 | ✅ `sensitive.txt` |
| 5 | 术语 | 一词一义，核心词表 27 条 | ✅ `00-架构与编辑说明.md` §2.2 |
| 6 | 代码来源 | 全部锚定本地仓库 HEAD `8ce7b776` | ✅ 附录 B（待写） |
| 7 | 时效快照 | 统一声明快照时点 2026-09-18 | ✅ `00-架构与编辑说明.md` §4 |

---

## 4. 勘误记录（由作者追问触发）

> 尚无。本节在作者提出第一次质疑后启用。
> 启用时的纪律：**必须同时列出"复核通过、无需修改的项"**——只说错的不说对的，是另一种不诚实。

---

## 5. 本轮顺带修掉的问题（记录备查）

| 问题 | 处理 |
|---|---|
| 英文稿引用的 MCP 规范版本为 `2025-11-25`，已落后于 `2026-07-28` 正式版 | 中文版第 7 章直接写 2026-07-28 版，并把版本差异写成"协议在变，职责不变"的论证材料 |
| 英文稿 `references.md` 的访问日期为 2026-09-17/18，与本轮一致，无需重取 | 沿用并复核关键条目；新增的 2026 年事实另行登记入附录 C |
| 英文稿把课程考核语言（Outcome / Gate）写进正文，中文读者阅读负担重 | 下移到附录 E 与各章"读完后你能回答"，正文改用工程语言 |

---

## 6. v1.0 交付轮（第三批）修掉的问题（记录备查）

### 6.1 正文与体例

| 问题 | 处理 |
|---|---|
| 新写的第 14–20 章用了 `## N.N 本章小结` 章节标题，与前 13 章的 `**本章小结**` 独立成行不一致 → 体例校验报 7 章缺块 | 统一为 `**本章小结**`；复跑后**体例 21 章 × 5 块全通过** |
| ch14 的「读图指引」指向了改版前的列（"这一步失败时，谁会先发现"），而最终图上没有这一列 | **改文不改图**（图是已过闸门且已目检的成品）：重写指引，改为指向图上真实的五阶段与结论行 |
| ch15 / ch16 / ch17 / ch18 / ch19 / ch20 的读图指引同属"文字与图对不上" | 逐条重写，全部改指图上真实存在的元素 |

### 6.2 配图

| 问题 | 处理 |
|---|---|
| 新图用了 `#E8EFF9`（不在 spec_lock 声明的书稿色板内）→ 配色闸门拒绝导出整批 | 全量替换为已声明的面板色 `#EEF4FB` |
| 5 处文字距卡片边框仅 3.3–5.3px（低于 6px 下限）→ 会切字 | 基线下移；复跑后"距边框过近"告警**清零** |
| `fig-15-2` 上移文字时压到了上一行 → 卡片内**两行文字互撞**（机械闸门查不出，只能目检） | 三行重新等距排布（352 / 380 / 406），复跑并重新目检通过 |

### 6.3 三形态构建（本轮新暴露，两类都只有走到构建才会看见）

| 问题 | 处理 |
|---|---|
| **图注重复**：正文 `alt` 已含"图 N-M"前缀，`build_html.py` 的 `caption_for` 又加一次 → 全 39 张图注变成「图 1-1　图 1-1 …」 | 修 `caption_for`：先剥 `alt` 的前缀再加图号。已核对 39 条图注全部正常 |
| **内联后 id 冲突**：39 张图各自都用了 `fig-header` / `fig-conclusion` / `fig-table` 等分组 id，内联进一个 HTML 后产生 **10 个重复 id**（非法 HTML） | 修 `inline_svg`：**全部 id 加"图名前缀 + 双下划线"**；已确认所有 SVG 内不存在 `url(#...)` 引用，重命名安全。复检**重复 id = 0** |
| 第二轮 PDF 用了重命名 id 之前的 HTML 生成 → **三形态不同源** | 从最终 HTML **重导一次**，实测 328 页 A4 |
| 实拍复核的第一轮 5 张截图 md5 完全相同 | 根因是 **zsh 不对未加引号的变量做分词**，`set -- $spec` 没拆开参数，5 张拍的全是同一张首屏。改为显式传参重拍 |

> **6.3 全部四条都是"局部合法、整体才暴露"的一类。** 它们共同说明：**"每张图单独过闸门"不等于"39 张图放进一个文档里没问题"；"每个分片单独合规"也不等于"合稿合规"。**

---

## 7. 作者质疑触发的勘误（2026-09-18）

> 按纪律：**必须同时列出"复核通过、无需修改的项"。**

### 7.1 作者质疑："预计阅读 约 4 小时 35 分钟"——这个数字不成立

**作者原话**："一本 328 页的技术书籍，为什么会在约 4 小时 35 分钟内完成？我从来没有遇到过看技术书籍这么快的人，就算是看修仙小说也看不到这么快吧！无法预估时间，咱们就不要写这个时间！"

**复核结论：作者完全正确，这是我的缺陷，已修。**

| 项 | 内容 |
|---|---|
| 数字从哪来 | `build_html.py` 的 `mins = 汉字数 / 480` |
| 480 是什么 | 一个**隐含的阅读速度**：480 汉字/分钟 = **每秒 8 个字** |
| 折算 | 132,068 汉字 ÷ 480 ≈ 275 分钟 ≈ 4 小时 35 分；**328 页 ÷ 275 分钟 = 每分钟 1.2 页** |
| 为什么错 | ① 这个速度**对任何书都不成立**，更不用说要停下来看代码、核对 API 名、对照配图的技术书；② 它的**分子分母都交代不清**——"480 字/分钟"没有任何口径来源；③ 按本书自己的纪律（写作纪律第 1 条数据三问），**这类数字根本不该写** |
| 修法 | **不是换个系数重算**（换成 200 字/分钟仍是假的），而是**整条去掉**，换成**可数的结构事实** |
| 替换成什么 | `中文 132,068 字 · 配图 39 张 · 代码 180 段 · 表格 270 个` —— 四个都能用一条命令数出来 |
| 落地范围 | HTML 阅读版统计条、外壳 `<title>` 无关处、控制台输出；**PDF 已从新 HTML 重导**（仍 328 页，三形态同源） |
| 全书复查 | 正文、附录、overview、架构说明**均无其他时间估算**（唯一命中"小时+分钟"的是第 14 章讲单位不能混加的教材例子，正确） |
| 技能修复 | ① 本书 `build_html.py` 已改；② **`md-to-html-book` 技能内的同源脚本也同步修正**（否则下一本书会继承同一缺陷），并新增铁律 10；③ `book-studio` 写作纪律新增第 8 条 |

**为什么值得记进勘误**：这不是一个算错的数，而是一个**看不出错的数**。它格式规范、有小数、放在最显眼的位置（封面统计条），而且**没有人会去验它**——直到作者凭常识指出"比读小说还快"。这与本书第 13 章"脚本化假模型跑通了不构成线上质量证据"、第 20 章"漂亮的包也能带关键缺陷"是同一条纪律：**一个数字可信，不是因为它看起来精确，是因为它的分子分母能被交代。**

**已写进技能的判定线索**：**如果某个数字看起来比常识更好，它不是估算，是算错了。**

### 7.2 复核通过、无需修改的项（同时列出）

| 项 | 复核结果 |
|---|---|
| 全书 21 章 × 5 块体例 | ✅ 完整 |
| 39 张配图与正文引用 | ✅ 正反向全通过，无孤立图 |
| 图注编号 | ✅ 39 条全部正常（上一轮修掉的重复问题未回退） |
| 内联 SVG 的分组 id | ✅ 重复 id = 0（上一轮修掉的问题未回退） |
| `check_manuscript.py` 八项 | ✅ 0 错误 0 提醒 |
| 打印版 328 页 / A4 595×842 / 页码 | ✅ 正确 |
| 脱敏、中英间距 | ✅ 零命中、零缺 |
| 封面上"39 张手绘矢量配图"与"参考实现 · 已通过测试" | ✅ 均为**可数事实**或**作者已授权的表述**，非估算，不动 |

---

## 8. 作者质疑触发的勘误（第二轮）：PDF 必须有书签

**作者原话**："我的 pdf 不会只是打印版，很多时候可能是在线阅读版，所以得要有书签。"

**复核结论：作者完全正确。** 命中一个我漏掉的根本认识——**我把 PDF 的交付形态定义错了。**

| 项 | 内容 |
|---|---|
| 缺陷 | 生成的 328 页 PDF **书签 0 条**，元数据只有 Chrome 自动带的一个 title |
| 根因 | 我在概念上把这份 PDF 当成"打印件"，于是只关心纸张相关的事（A4、页码、色带不着色反白）。**而实际使用场景里，读者多数是在在线阅读器/预览器里读它**——那样一份 328 页没有目录树的书，只能一页页翻 |
| 命名也带偏 | 文件名叫 `…·打印版.pdf`，这个命名本身就在暗示"它只用于打印"，进一步固化了错误的形态定位 |
| 修法 | ① 注入 **445 条三层书签**（27 篇/章 + 218 节 + 200 子节）；② 补齐**元数据**（title / author / subject / keywords / creator）；③ **改名 `<书名>.pdf`**，去掉"打印版"三字 |
| 技术要点 | Chrome 的 `--print-to-pdf` **不会**从 HTML 标题生成 PDF 大纲，必须事后注入 |
| 定位方法（不猜页码） | 从 HTML 取标题权威清单 → 在 PDF 里按**字号带**抓候选（L1 15.5–17.5pt / L2 12.70–12.95pt / L3 10.90–11.15pt；正文 10.5pt、表 9.1pt、代码 8.7pt 均不重叠）→ **排除封面页**（封面 SVG 的副标题会落进 L3，制造假标题）→ 两份清单**逐条比对归一化文本 + 层级，对不上就报错退出** |
| 验证 | 书签 445 条 = HTML 标题 445 条；**逐条回查"该页是否真含该标题"，错误 0 条** |
| 脚本 | `add_pdf_bookmarks.py`（已同步并入 `md-to-html-book` 技能，供后续所有书复用） |

**这次的教训比上一次更深一层**：上一次的"预计阅读时间"是**一个算错的数**；这一次是**一个想错的交付形态**——我没有算错任何东西，我把整份产物的用途理解窄了。两个错误的共同点是：**都通过了全部机械校验**（八项校验、闸门、目检、结构校验、实拍复核，一条不落），因为**它们都不在检查项里**。

**由此新增的检查项**：三形态交付时，PDF **必须**核对两项——① 书签条数 = 标题条数；② 每条书签指向的那页确实含该标题。已写进 `book-studio` 与 `md-to-html-book` 两个技能。

### 8.1 复核通过、无需修改的项（同时列出）

| 项 | 复核结果 |
|---|---|
| 328 页 / A4 595×842 / 页码盖章 / 封面无页码 | ✅ 未受书签注入影响 |
| 39 张配图在图册工程里的矢量渲染（`images=0`，纯矢量路径） | ✅ |
| 图注 39 条编号 | ✅ 无重复 |
| 内联 SVG 分组 id | ✅ 重复 id = 0 |
| `check_manuscript.py` 八项 | ✅ 0 错误 0 提醒 |
| HTML 阅读版（另有三层目录 + 滚动高亮 + 关键词筛选 + 配图索引） | ✅ 侧栏导航本就完整，不受影响 |
| 封面统计条四个可数事实 | ✅ 仍为可数事实（中文字数 / 配图 / 代码段 / 表格数） |

---

## 9. 作者质疑触发的勘误（第三轮）：一句读不懂的话，与一次缺失的回读

**作者原话**："这个文件中的'结果不对接不上责任'是啥意思？你写完书稿后有回读检查吗？"

### 9.1 第一问：那句话的意思，以及它为什么写坏了

**我想表达的**：出了错，却找不到该负责的环节。前面那个助手给了错答案，但三处错误（信息边界、计算边界、交付边界）**没有一处能归到"模型不够聪明"头上**——所以换更强的模型不会解决，你得改系统。这正是紧接着那条「核心判断 1」的引子。

**写坏了两个地方**：① 把一整条逻辑压成 6 个字；②「接不上」在中文里首先会被读成"电话接不通"。**读者要先做一次语义解压才能懂，这就是写坏了。**

**已改为**：「一个是**结果错了，却找不到该负责的环节**；一个是**过程越界了，却没有东西拦得住**。」

### 9.2 第二问：**没有。这是我的真实缺口。**

**我做了什么**（都必须说清，因为它们是真实投入的）：

| 检查 | 覆盖 |
|---|---|
| `check_manuscript.py` | 脱敏 / 图片正反向 / SVG-PNG 配对 / 中英间距 / 体例 / 图号连续 / 合稿漏拼 |
| 配图三道闸门 | 配色 / 版式适配 / 数据几何 |
| 配图目检 | **39 张全量读完，不抽查** |
| HTML 结构校验 | 目录链接、重复 id、残留 img、markdown 残留 |
| HTML 实拍复核 | 首屏 / 表格 / 代码 / 配图 / 夜读，五视角 |
| PDF 核验 | 页数 / 纸型 / 图注 / 页码 / 445 条书签逐条回查 |

**我没做的**：**没有把正文通读一遍。** 读过的正文只有：写每一章时看自己的草稿、以及为修「读图指引」对照图看的那几段。

**后果就是那句话活到了 v1.0**：它不含数字、不涉及图片、不涉及体例、中英间距也正常——**八项校验全部通过，因为它语法完全正确，只是读不懂。**

**三轮质疑的共同点越来越清楚**：
1. 第一轮"预计阅读时间" = **一个算错的数**
2. 第二轮"PDF 无书签" = **一个想错的交付形态**
3. 第三轮"这句话什么意思" = **一句写坏的话**

三轮全部通过了当时**全部**机械校验。结论不是"校验没用"，而是：**机械校验守住的是"已经想到的规则"；没想到的维度，只能靠人或靠作者指出来。**

### 9.3 本轮回读实际发现并修掉的六类问题

回读范围：**前置章全篇 + 第 20 章全篇**（共 15,000 余字），外加两类机械补查。

| # | 问题 | 位置 | 为什么会漏 |
|---|---|---|---|
| 1 | 「结果不对接不上责任」——压缩过度、有歧义 | 前置章 §1 | 语法正确 |
| 2 | 表头写「谁负责」，格子里填的是「需求判断 / 运行时 / Harness…」等**层级名** | 前置章 §2 表 | 表格结构合法 |
| 3 | 「智能体」定义格：五样东西并列后无连接词，读起来是名词堆 | 前置章 §7.3 | —— |
| 4 | 积木表未区分「必备 / 按需」，读者会以为八块缺一不可 | 前置章 §6.3 | —— |
| 5 | **前置章约定「拆成 3 个问题」，而第 14–20 章实际都是 4 条** | 7 章 | 体例校验只看积木在不在，不看内部形态 |
| 6 | §20.2.1 断言「它按'能不能用'排的」与其自身给的分值顺序不符；且「40 分」指代不明 | 第 20 章 | 表格与断言各自都对，凑在一起才错 |
| 7 | §20.3「解释这三个区分」，而表里列的是三个**概念** | 第 20 章 | 数量对得上，词性错了 |
| 8 | 6 处全角破折号前多一个空格 | 3 个文件 | —— |

**第 5 条的处置说明**：「3 个」这条约定是我早先随手定的，本身没有依据。第 14–20 章的第四问都是实质问题（如"系统交付之后，哪些东西必须被指派责任人？"）。**为了满足一个任意数目而删掉一个好问题，是更大的损失。** 因此改为 **3–4 条**，并把范围写进机械校验——**改成"3–4"是在修正一条任意规则，不是为了让现状通过。**

### 9.4 顺手补进机械校验的三项（新增第 9 项，共 11 项通过）

`check_manuscript.py` 新增 `check_declared_counts()`：

| 项 | 规则 | 为什么能机械化 |
|---|---|---|
| 9a | 「读完后你能回答」每章 3–4 条 | 条目数可数 |
| 9b | 「N 道自检题」声明的 N = 实际条目数 | 数目对不上就是错的 |
| 9c | 全角破折号前后无空格 | 纯排版 |

**并且新增了不可机械化的那一步**：`book-studio` 技能加了 **第 5.5 步「回读检查」**，明确写出这是**唯一一步机器查不了、必须人读**的检查，附六类缺陷特征、读哪些、怎么读、怎么记，以及一条自查纪律——**交付前能说出"我通读过"才算读完。**

### 9.5 复核通过、无需修改的项（同时列出）

| 项 | 复核结果 |
|---|---|
| 39 张配图与正文引用（正反向） | ✅ 无孤立图 |
| 图注 39 条编号 | ✅ 无重复 |
| 内联 SVG 分组 id | ✅ 重复 id = 0 |
| PDF 445 条书签 | ✅ 逐条回查指向正确率 100% |
| 各章「N 道自检题」声明数 | ✅ **21 章全部与实际条目数一致**（0 处不符） |
| 中英间距、脱敏 | ✅ 零缺、零命中 |
| 全书「本章承诺」均为单句 | ✅ 无一处超长或含多段 |

---

## 10. 全书编辑回读（作者要求：以专业编辑身份回读全书所有章节、举一反三、修改）

**作者要求**："你以专业编辑的身份，仔细再次回读全书所有章节，举一反三的检查，然后修改。"

### 10.1 方法：分派读稿人精读 → 我逐条复核 → 我亲自改

全书 27 个文件、26.8 万字符，超出单次通读的可行范围。按专业编辑的实际做法：

1. **派 6 位读稿人**分章精读（第 0–2 章 / 第 3–6 章 / 第 7–10 章 / 第 11–14 章 / 第 15–18 章 / 第 19 章 + 五份附录），每人拿到一份**统一的缺陷判据**（11 类，就是前几轮真实踩出来的那些）。
2. 读稿人**只读不改**，产出「文件:行号 / 精确原句 / 类别 / 为什么是缺陷 / 可直接替换的改法」。
3. **我逐条去原文核对**，不采信未验证的结论（下面 10.3 有两条被推翻）。
4. **我亲自执行全部修改**，并逐条写探针验证改动确实进了成品。

### 10.2 结果：约 42 处修改

| 组 | 文件 | 修 |
|---|---|---|
| A | 第 0–2 章 | 13 处（含 ch01 整章标题重编号） |
| B | 第 3–6 章 | 5 处 |
| C | 第 7–10 章 | 3 处 |
| D | 第 11–14 章 | 8 处 |
| E | 第 15–18 章 | 8 处 |
| F | 第 19 章 + 附录 A–E | 10 处 |
| 图 | fig-17-1 重画、fig-15-1 改词 | 2 处 |

**28 条探针全部通过**（新文本已进入成品、旧文本已消失）；三形态已重建（md 548.6 KB / html 968.1 KB / **pdf 328 页 + 445 条书签、指向错误 0**）。

### 10.3 举一反三：读稿人报告里最有价值的不是单点，是**四类系统性缺陷**

| # | 系统性缺陷 | 表现 | 处置 |
|---|---|---|---|
| 1 | **章节编号体系偏离** | 全书 21 章都是 `## N.M`，**只有第 1 章是 `## 1.`–`## 7.`**，其子节也写成 `### 2.1`（应为 `### 1.2.1`） | 整章重编号（13 个标题）；已核查全书无处引用 ch01 节号，无连带影响 |
| 2 | **"出处见附录 X"承诺落空** | 正文引用 6 个参考实现文件声称"出处见附录 B"，**附录 B 一个都没登记**：`docs/03-system-architecture.md`、`docs/05-capability-system.md`、`docs/06-knowledge-workflows.md`、`office-parser.ts`、`agent-engine.test.ts`、`package-lock.json` | 6 行全部补登记；并**把这项核查固化进 `build_book.py`** |
| 3 | **跨章前向引用落空** | 5 处"第 N 章讲过/会讲 X"，去第 N 章找不到：ch03→ch06（两份 schema 组织、按工具定制摘要格式，均不存在）、ch07 引"第 0 章那句方法论比数字耐久"（第 0 章无此句）、ch10 引"第 1 章那个'工具'是能力的最小单位"（第 1 章无此断言） | 全部改写：能内联说清的就内联，不该有的引用删掉 |
| 4 | **同一概念在书内有两个数字** | "责任人"：第 19 章说**四个**（代码/依赖/数据定义/模板），第 20 章与附录 E 说**七个**（依赖/模型配置/固定输入/指标定义/模板/保留策略/事件处理），附录 D 两处又各说一个 | 不做"取其一、灭其二"——两者**范围确实不同**（产出责任 vs 维护责任）。改为显式分层并互相指路：第 19 章=「四类产出责任人」，第 20 章/附录 E=「七项维护责任人」 |

**另外两类值得记下**：同一句话在相邻两段**逐字重复**（ch18:222/224，删）;同一句话内部**自相矛盾**（ch07"前两个是时间，第一个是体积"；ch12"第 12、11、5 三章"与"这一章、上一章和上上章"）。

### 10.4 两条被推翻的读稿人结论（复核的价值）

| 报告 | 我的复核结论 |
|---|---|
| ch19"**停车位**"是错字（"与决策所有者毫无关系"） | **不成立**。"停车位"是第 17 章定义过的比喻（停下来问人的位置），fig-17-1 里就是它的视觉符号。**真问题不是错字，是这个词在第 19 章孤立出现、读者无从理解**。已改为自足表述：`决定了"什么时候必须停下来问人"设在哪一步` |
| ch15"**漏法**"疑似错字 | **部分成立**。"漏法"是自造压缩词（意为"易漏之处"），图上文字与读图指引一致，所以不是笔误，**但确实难读**。图与文一并改为"**易漏**" |

**这两条说明：读稿人的"错字"判断要回到原文核实——但也正是它们让我找到了比"错字"更准的问题。**

### 10.5 机械校验从 11 项扩到 13 项（把能机械化的当场固化）

| 新增 | 规则 | 这一轮它抓到了什么 |
|---|---|---|
| `check_manuscript.py` 第 10 项 | **交叉引用完整性**：「第 N.M 节」必须真实存在；「第 N 章」必须有对应章文件 | 复核后确认：**335 个节标题全部索引，所有引用都能对上**（这是"举一反三"的直接价值——修完 3 处后证明其余 332 处是好的） |
| `build_book.py` 新增 | **附录 B 一致性**：正文引用的参考实现文件必须在附录 B 登记 | 首次运行即抓出 6 个漏登记 |

### 10.6 复核通过、无需修改的项（同时列出）

| 项 | 复核结果 |
|---|---|
| 四章共用合成数据的数字/单位/比例（第 14/15/16/18 章） | ✅ **逐项核对全部自洽**：P1 25000/30000/-5000/-1/6；分项 A 20000/22000、B 5000/8000；P2 27000/31000/-4000/-4/31；8% 四处一致；「不是各项目比率的平均」经手算成立 |
| 第 13 章 pass^k 数值 | ✅ 0.9¹=90%、0.9³=72.9%、0.9⁵=59.0%、0.9¹⁰=34.9%，全对 |
| 第 13 章运行时事件计数 | ✅ run+message×3+reasoning+tool×5+run×3 = 13 种，与"恰好 13 种、无 usage/cost 事件"一致 |
| 第 6 章负基数用例与测试预言 | ✅ 手算复核无误 |
| 各章「N 道自检题」声明数 | ✅ 21 章全部与实际条目数一致 |
| 各章「分 N 步/件/类」声明 | ✅ 逐章核对与条目数一致 |
| 附录 E 编号连续性 | ✅ 闸门一–七、条内 1.1–7.14、E.9/E.10 全部连续无重号 |
| 附录 B 测试数字 | ✅ 77 文件 / 582 用例 在 B.1、B.3.1、B.4、B.4.1 之间完全自洽 |
| 附录 A 术语译法与正文用词 | ✅ Harness / Skill / 专家 / Agent Loop 首次定义处一致 |
| 39 张配图的"短标签" | ✅ 全量扫描，未发现同类自造缩写（26 个标签全部是常规词） |
| 中英间距 / 脱敏 / 全角破折号 | ✅ 零缺、零命中、零多余空格 |

### 10.7 残留风险（必须说清）

**这次是"分派精读 + 逐条复核"，不是我自己通读 26.8 万字。** 差别在于：

- 覆盖是**真的全**（27 个文件全部被精读，含附录）；
- 但"发现"依赖读稿人，我的复核是**对报告逐条回原文核对**（选择性验证），不是独立重读。

所以仍可能存在**读稿人和我都没看出来的问题**，尤其是需要跨整章比较才能发现的（如"A 章给的定义与 F 章的使用不一致"）。要彻底消除，只能我自己逐章通读一遍——那是更大的工作量，需要作者确认是否值得。

---

## 11. v2.0 审稿意见落地（第一轮：定点修改）

**来源**：`98-审稿意见-v2.0.md`（对 `98-审稿意见-v1.0.md` 的独立复核）。作者授权"按你说的执行"。

### 11.1 本轮改了什么（共 35 处，跨 12 个文件）

| 类 | 项 | 结果 |
|---|---|---|
| **A1** | `grain` 统一为**颗粒度**（方案①，依据附录 A §A.4"以本表为准"） | 改 **8 处**（词表 + 6 处正文 + 1 处架构说明）+ **1 张配图**（`fig-10-1` 图内是同一句，已改并重渲染） |
| **A2** | ch13 交叉引用 `第 10 章` → `第 0 章` | 1 处 |
| **A4** | 架构说明 `63 个测试文件` → `77`；附录 C T13 标注**文件计数**口径 | 2 处 |
| **B1** | 术语表补 `token` 词条 | 1 处 |
| **B2** | ch19 延伸阅读 `唯一比较对象` → `主要依据`（并列出三家） | 1 处 |
| **B3** | 架构说明 `五大运行时` → **三家**（并指向附录 C T28/T29） | 1 处 |
| **B4** | ch18 统一为**预算执行率** | 2 处 |
| **B6/B11/B12/B13** | 体例表四个声明改为与实际一致：核心判断 1–3 处、行动清单 5–8 条、定义框与数据卡标"按需" | 4 处 |
| **B7** | ch07 §7.1 客户端职责：`握手` → `连接建立与协商（2026-07-28 起不再有 initialize 握手）` | 1 处 |
| **B8** | 附录 C T23 压缩句改为「从不足 1% 到约 26% 不等」 | 1 处 |
| **B9** | ch09 装配片段：正文说明只含前四类 + 注释加「槽位 #」前缀 + 补排序说明 | 7 处 |
| **B10** | 架构说明 §4.4 评估快照表补 GAIA 74.5% | 1 处 |
| **C1** | ch19 §19.3 加回溯引用（指回第 5 章） | 1 处 |
| **C2** | ch06 MCP 命名处加指路（详见第 7 章） | 1 处 |
| **C3** | ch14 开场首次出现的数字补单位口径（见 14.3 节） | 1 处 |
| **C4** | ch12 引第 5 章的话由截断引文改为转述 | 1 处 |
| **附带** | 架构说明 §5 篇幅配比表：**全部实际字数重算**；修正「前置」行多算的 1 张图（`fig-0-1` 同时被算进前置与第 0 章，导致配图列合计 40 而实际 39） | 12 处 |

**31 条探针全部通过**（新文本已进入稿件、旧文本已消失）。

### 11.2 顺手做掉的两件"制度性"修补

1. **新增机械校验第 11 项：体例表声明的数量区间必须被守住。**
   这一项是 §7 那个"类"的直接产物——旧校验只查"块在不在"，不查"数量对不对"，所以体例表漂了 21 章都没人发现。现在 `核心判断 1–3 处`、`行动清单 5–8 条` 会被逐章校验。**`check_manuscript.py` 现为 14 项全通过。**
2. **取消架构说明里的字数副本。**
   发现同一件事有两个数：原始字符口径 133,481、阅读版解析口径 132,909。**改法不是对齐，是取消副本**——`00-架构` 不再手写字数，改为"以 HTML 阅读版统计条为准"。理由：一处手写的、会随每轮变动的数字必然会漂。

### 11.3 一处必须更正的判断（对 v2.0 自身）

v2.0 §8.1 原先把"交付篇 29,148 / 综合篇 16,666 / 迁移篇 14,941 不可复现"记为 **v1.0 的偏差**。复核后发现：**这三个数字与 `00-架构与编辑说明.md` §5 篇幅配比表"实际总字符"一列逐位相同**——v1.0 是照抄我们的口径文件，不是它算错。

**所以结论要改**：问题不在 v1.0，而在**我们自己的会变的数字被当成静态结论引用了**。已在配比表下加硬提醒：**凡会变的数，要么当轮重算，要么不引用。** v2.0 §8.1 已同步更正。

### 11.4 仍未落地（第二轮）

| 项 | 内容 | 为什么留到第二轮 |
|---|---|---|
| **A3** | 附录 C 补登记 P11–P14（ExcelJS / `docx` / PptxGenJS / LangGraph 文档），并把 PptxGenJS"文档站点访问失败"写进 C.4 | 需要新写条目 |
| **A5** | 行业视角补 **9 章**（第 0–6、8、20 章） | 需要新写正文 |
| **C5** | 行业视角固定一个机构类型代称并沿用 | 与 A5 同属一组，一起做 |
| §12 | 篇幅补充（第 20 章行业视角、第 19 章可跑示例、第 16 章 §16.4 推导、第 15 章目视实例） | 视作者对篇幅的决定 |

### 11.5 一个待作者知道的既存告警（本轮查明，非新引入）

配图闸门 `quality_gate=passed`（blocking=0），但 39/39 张图带**同一类告警**：

> `<text> ... exceeds <g id="fig-header"> data-pptx-bounds`：垂直溢出 **4.3%**，而该检查的阈值是「**超过 5% 才算 error**」。

即：图的导语行（y=192）的含 descender 文本盒比 `fig-header` 分组下界多出约 3px。**它自第一张图起就存在、39 张完全一致、多次全量目检均无视觉问题**（下界之外是空白，未压到任何元素）。**建议保持不动**，并已把技能文档里"这一类必须处理"的表述改准（它只在 ≥5% 时才是错误）。

---

---

## 12. v2.0 审稿意见落地（第二轮：新写内容）

**范围**：A3（附录 C 补登记）· A5（行业视角补 9 章）· C5（机构代称约定）。§12 篇幅补充**未执行**，等作者决定（见 §11.4）。

### 12.1 A3：附录 C 新增「实现与库文档」一类

| 改动 | 内容 |
|---|---|
| **C.1 类别表补一类** | `实现与库文档｜受版本约束`——原先的分类表里**没有这一类**，这正是那四个库引用被漏掉的**机制性原因**：它们既不是"原理与设计（不过期）"，也不是"时效敏感事实"，落不进任何一格 |
| **新增 §C.2.1** | 登记 **P11 ExcelJS（v4.4.0）· P12 `docx` · P13 PptxGenJS · P14 LangGraph**，每条带**版本列**与"未经本书验证"声明 |
| **C.4 补一行** | `未能访问的文档｜P13`——"写作期间文档站点访问失败，依据为仓库 README"，**从正文散落归位到未验证清单** |
| **正文四处引用点名条目号** | "见附录 C" → "**见附录 C 的 P11/P12/P13/P14**"，可核查性从"能查到附录"进到"能查到条目" |

**一个实施中的判断**：这四条**没有放进 C.2（原理与设计类·不过期）**——库文档会随版本变，放进标着"不过期"的表里，等于把会过期的东西标成不会过期。**分类表里缺的那一类，比漏掉的四个条目更值得修。**

### 12.2 A5：行业视角补 9 章，覆盖率 21/21

| 章 | 新节 | 标题（断言式） |
|---|---|---|
| 0 | §0.8 | 机构里没有"我的机器上跑通了"这句话 |
| 1 | §1.7 | 机构里加的不是流程，是一道前置问题——谁有权批 |
| 2 | §2.7 | 模型"不知道"不是错误，"装作知道"才是 |
| 3 | §3.8 | 在机构里，一个工具是一次变更，不是一个提交 |
| 4 | §4.11 | 一次"停下"在机构里要能被结账 |
| 5 | §5.8 | 机构里的"人在环路"是授权，不是礼貌 |
| 6 | §6.9 | 机构里，"给不出数"是一个需要被接受的答案 |
| 8 | §8.9 | 机构里的检索要能回答"依据是哪一版" |
| 20 | §20.5 | 接手的人不止工程师 |

- 体例与既有 12 节一致：断言式标题 + 一张表 + 一句判断；共约 **2,800 汉字**（272–389 字/节）
- 原各章的「这一章在整本书里的位置」**统一让号**（0.8→0.9 … 20.5→20.6）；**已确认全书无按号引用这些节**，改号零连带
- **一个实施中的取舍**：这 9 节**不引入任何新的外部事实**，因此**不需要往附录 C 增登记条目**——在附录 C 刚被指出漏登记的同一天，往里加更多未登记的引用，是错的

### 12.3 C5：机构代称约定

`00-front.md` §4 在原"机构类型不可混淆"纪律之下新增**「代称约定」**：

- **主线机构固定为「某城商行」**，跨章沿用同一代称
- 其他行业只作**对照**，用行业名代称（"某制造企业""某券商"），**不与主线混写**
- 泛称"机构""受监管场景"的地方不受此限
- 理由写进了正文：**不固定，同一批数字在不同章就会落到规模不同的机构上，读者无法横向对照**——而那正是本书反复强调的那类错误

### 12.4 制度性修补：`stamp_arch.py`（字数副本改为生成）

架构说明 §5 配比表的"实际总字符"列会随每轮编辑变动，手写必然漂（已发生过一次）。新增 `stamp_arch.py`：**重算并写回**该表的实际字数、配图数、合计行与差额段。**匹配不上就报错退出，不做猜测**。已在 `00-架构` 交付脚本表登记，并写明"**每轮改完正文都要跑一次**"。

本轮实测：全书总字符 **273,453** · 配图 39 · 相对目标 319,000 差 **14%**。

### 12.5 本轮的验证

| 项 | 结果 |
|---|---|
| `check_manuscript.py` | ✅ **14 项全通过** · 0 错误 0 提醒 |
| 行业视角覆盖率 | ✅ **21/21 章**（原 12/21） |
| 三形态 | md 562 KB · html 988.8 KB · **PDF 336 页 · 455 条书签 · 指向错误 0** |
| 成品探针 | ✅ 第二轮 **17 条全部通过** |
| 新节目检 | ✅ 抽样目检（§0.8）排版与既有体例一致 |

---

---

## 13. 铁律 11 立：篇幅不作验收指标 + 第 19 章实测补充（作者裁定后执行）

**作者裁定（2026-09-18）**："我们不是必须要求多少字数，但 19 章缺内容，还是要补上。即便补上后，后续也只能从是否缺内容方面来审核编辑。你要加一条铁律，不能因为凑字数而添加内容。"

### 13.1 铁律落地（三处）

| 位置 | 内容 |
|---|---|
| `book-studio` SKILL.md **铁律 11** | **不因凑字数而添加内容**。篇幅不是验收指标；新增一节的唯一合法理由是"缺了这一块，论证或示例就不完整"。审核编辑只问"缺不缺"，不问"够不够"。凡遇到"字数不达标"的压力，正确动作是核对内容完整性并如实报告差额（技能 → v20260918.11） |
| `00-架构` 编辑决定 **D-07** | 同一裁定的书稿侧记录 |
| `99` §2b | 原挂起的"是否补足字数"问题就此关闭 |

### 13.2 第 19 章补的是内容，不是字数（两次真实实验）

**§19.2.2 本书实测：一次干净的安装（锚定提交 `8ce7b776`）**——照本章自己的清单，`git archive` 取匿名树、`npm ci --ignore-scripts`：

| 实测结果 | 说明 |
|---|---|
| 安装 exit 0 · 829 包 · **3 秒** | **3 秒不是安装时间**——温热缓存；写进交接文档就是假信息 |
| `npm run typecheck` **exit 2**（TS2561 ×2） | **最有价值的发现**：`--ignore-scripts` 跳过 `patch-package`，补丁（3 段，**含 `.d.ts` 段**）未打 → `onlyFontDirs` 类型缺失 → **typecheck 从绿变红**；`index.cjs` 运行时行为同样不同。一个"只想跳过 Electron 下载"的参数同时改变了类型检查与运行时行为，**且两件事都不在安装时报错** |
| 核心三包测试 15 文件 / 102 用例 **全绿** | 测试全绿不代表其他门绿——"能装"是一组门，每道都要单独跑 |

清单据此补一行：**构建期补丁 / postinstall 步骤**（它们改变依赖的类型面与运行时行为）。§19.2.2 末尾如实列出四条局限（--ignore-scripts 不代表完整安装、缓存温热、未测 Electron 本体、单平台）。

**§19.5 本书实测：把本章的规则对着参考实现跑**——发现比实验本身更重：

| 实验 | 结果 |
|---|---|
| 核心包缺配置行为（3 用例真实运行） | 空 `baseUrl` 抛错但**归因错**（配置非法被报成"无法连接"）；空 `apiKey` **无任何配置校验**——Authorization 头被条件拼装静默省略、请求照发，失败在网络层。好消息三条：不泄露 key、不静默成功、无假提供方介入（核心包层面） |
| **桌面装配路径（只读核查）** | `run-service.ts:1393`：未配置语言模型时**静默回退 `FakeModelProvider`**（`fallbackModel`），其事件流按脚本产出、**无"这是假的"标记**，运行记录也未携带提供方标识——**这正是 §19.5 明文禁止的"绝对不能做"**。写规则的人没发现自己守不住规则，**这就是规则必须配一个会失败的检查、而不是配一段自律的原因** |

该缺口已登记**附录 B「已知的实现缺口」**（含修法：预检查拦截或显式标记"演示运行"）；交付冒烟 fixture（`smoke-missing-config.test.ts`）随书给出，**3 用例 11ms 全过，从此是交接检查的一部分**。

### 13.3 锚定号前移：`2d8a79f` → `8ce7b776`

写作期间仓库 HEAD 前移（两提交：中/英文稿入库）。**已核对区间内 `packages/`、`apps/` 代码零改动**，全书 12 处锚定号替换，附录 B 补"锚定号前移说明"并立规矩：**每轮交付前重验一次锚定号与代码的对应关系**。

### 13.4 校验与产物

`check_manuscript.py` **14 项全通过**（节标题索引 345）；总字符 **279,144**（+5,691，全部来自第 19 章实测内容与登记行）；三形态重建（PDF 见 §13.5 注）。

### 13.5 未补的两处（按铁律 11 复核后的决定）

`98-审稿意见-v2.0.md` §12 曾建议三处补充。**只补了第 19 章**（它缺的是"没有任何一次真实安装/冒烟记录"，是论证缺块）。另两处——第 16 章 §16.4 推导密度、第 15 章目视实例——按铁律 11 复核属**密度与呈现偏好**，不是论证缺块，**不补**；作者逐页验收时若认为缺，再按同一标准补。

---


## 14. 事故与恢复：2026-09-18 22:16 未提交改动被整体回退

### 14.1 发生了什么

另一个会话为把 `sensitive.txt` 从 GitHub 历史中移除，执行了 `git filter-repo --invert-paths --force`。该命令要求工作树干净，收尾会 **`git reset --hard` + `reflog expire` + `gc --prune=now`**。当时 `book/cn/` 下整轮 v2.0 编辑改动尚未提交，于是被整体丢弃，同时全书 327 个提交号被重写。**未跟踪文件不受影响**——正因如此 `stamp_arch.py` 与两份审稿意见活了下来。

**教训**：动手改写历史之前，先给未提交改动打一个 WIP 提交。看到几十处 `M` 而没有先建议存档，是流程漏，不是工具漏。

### 14.2 恢复口径与实证

| 项 | 做法 | 结果 |
|---|---|---|
| 正文 | 重放 WorkBuddy trace 里的写入序列 | 合稿与 22:11 实测**字节一致**（585,764 B） |
| 阅读端 UI | trace 里只剩一条 680 字符的编辑；改取 `changes-detail` 最后一次检查点的完整 diff（`build_html.py` +117/−3，10 个 hunk）重建 | 重建出的 HTML 与丢失文件**同尺寸**（1,029,888 B），`node --check` 通过 |
| 独立复核 | 用 16:09 已分享出去的 HTML 反向比对：剥掉 `style` / `script` / `svg` 后逐行 diff | **只剩 5 行差异，全部是阅读端 UI**——印证"那一版之后只做样式、不做内容" |
| 逐章 | 20 组章节纯文本长度对比 | 229,696 == 229,696，0 组差异 |
| 受损范围 | 当日 WorkBuddy 检查点 + Qoder 本地历史：`book/cn/` 之外每个文件"最后一份快照 = 磁盘 = HEAD" | 受损范围仅 `book/cn/` |
| 校验 | `check_manuscript.py` | 18 项全通过 · 0 错误 · 0 提醒 |

### 14.3 制度性修补：`stamp_pdf_stats.py`

页数、书签条数、三个产物的体积是**每轮重建都会变**的数，全仓十余处手写副本本轮一次漂齐（328 → 339 页、445 → 456 条）。新增 `stamp_pdf_stats.py`：量 → 写回 → 每条规则**断言恰好命中一次**，命中 0 次或多次即报错退出——与 `stamp_arch.py` 同一条判据。**轮次记录与作者原话里的数字是当时那一轮的取证，脚本明确不动**（overview §3.1 第 10、11 条，本文 §7 / §12 / §13 的核验行均在内）。本轮共写回 20 处。

### 14.4 待作者裁定

| # | 事项 | 现状 |
|---|---|---|
| 1 | **全书 46 处锚定提交号已失效** | `2d8a79f…` → `c41184f…`、`8ce7b776…` → `f0245d3…`（映射取自 `.git/filter-repo/commit-map`，两个目标提交已核可解析）。等 GitHub 历史最后一次改写定稿后再一次性替换，否则再改写一次又作废 |
| 2 | 00-架构 §8.1「`check_manuscript.py` 八项全通过」及其后的中文列举 | 校验器现为 18 项，且那句只列了 7 项。改项数等于改措辞，脚本不代劳 |
| 3 | 三个未跟踪文件 `98-审稿意见-v1.0.md`、`98-审稿意见-v2.0.md`、`stamp_arch.py` | 事故中因未跟踪而幸存，至今不在版本控制里；是否入库请裁定 |
| 4 | 三个生成物是否从 GitHub 历史中彻底清除 | 已加 `.gitignore` 并 `git rm --cached`，但历史里那三份仍在；要清干净需再一次 `filter-repo` + 强推，**必须在工作树干净时执行** |
