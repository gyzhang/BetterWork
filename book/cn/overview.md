# 本轮概览 · v1.0 全稿定稿

> 本文件每轮交付后**整份重写**。它只回答四件事：这轮改了什么、现在到哪一步、哪里可能有问题、需要作者定什么。

**书名**：智能体工程：从一次工具调用到可交付的系统
**署名**：Kevin + AI　·　**定稿日**：2026 年 9 月 18 日　·　**状态**：**全稿定稿，三形态齐备**

---

## 1. 这轮改了什么

### 1.1 新写的正文与附录（第三批，本轮主体工作）

| 文件 | 内容 | 总字符 |
|---|---|---|
| `parts/15-ch14.md` | 第 14 章　Excel 分析：颗粒度错了，后面全错 | 11,548 |
| `parts/16-ch15.md` | 第 15 章　Markdown 与 Word：内容模型决定文档能不能改 | 8,849 |
| `parts/17-ch16.md` | 第 16 章　可编辑 PPT：最后一页的数字要和第一页对得上 | 8,751 |
| `parts/18-ch17.md` | 第 17 章　综合案例 A：从研究到一份可辩护的方案 | 7,775 |
| `parts/19-ch18.md` | 第 18 章　综合案例 B：从运营数据到可复用的期报 | 8,891 |
| `parts/20-ch19.md` | 第 19 章　打包与迁移：职责边界决定可迁移性 | 7,588 |
| `parts/21-ch20.md` | 第 20 章　独立交付、评估与复述（全书收束） | 7,353 |
| `parts/89-appendix-a.md` | 附录 A　术语表 | 5,685 |
| `parts/92-appendix-d.md` | 附录 D　2026 年智能体产业与政策图景 | 5,434 |
| `parts/93-appendix-e.md` | 附录 E　工程检查清单汇编 | 7,214 |

另更新 `parts/91-appendix-c.md`（附录 C）：把智能体互联标准补到精确的 **GB/Z 185—2026**（2026-05-22 发布、7 部分、SAC/TC 28 归口），并新增 T37（身份码落地进度）、T38（智能体互联协议 AIP）。

### 1.2 配图

新增 **14 张**（fig-14-1 至 fig-20-2），全书累计 **39 张**。全部走 SVG 原生管线，过三道闸门（`quality_gate=passed`），PNG 2816×1584 与 SVG 矢量源成对，并**全量逐张目检**。

### 1.3 合稿与三形态（本轮新建的能力）

| 新增文件 | 作用 |
|---|---|
| `build_book.py` | 合稿脚本。**合稿顺序写死在脚本里**，双路复算校验一致，并拒绝未纳入顺序的分片 |
| `build_html.py` | HTML 构建脚本（从 `md-to-html-book` 拷入并按本书适配） |
| `智能体工程：从一次工具调用到可交付的系统.md` | 合稿主稿 · 573 KB |
| `智能体工程：从一次工具调用到可交付的系统.html` | 单文件离线阅读版 · 1008.6 KB · 39 图内联为矢量 |
| `<书名>.pdf` | A4 · **344 页** · 页码居中盖章 · **三层书签 456 条** · 元数据齐备 |

---

## 2. 现在到哪一步

**21 章 + 5 份附录全部完成，三形态齐备。**

| 项 | 结果 |
|---|---|
| 规模 | 总字符 **279,940** · 配图 **39** · A4 **344 页**（字数以 HTML 阅读版统计条为准；第 19 章实测补充后重算） |
| `check_manuscript.py` | **18 项全通过 · 0 错误 · 0 提醒** |
| 配图闸门 | 39 页 `quality_gate=passed`，blocking = 0，"距边框过近"告警清零 |
| 配图目检 | **39 张全量读完，不抽查** |
| HTML 结构校验 | 目录链接 495 ⊆ 标题 id 456 + 图锚 39 · **重复 id 0** · 残留 `<img>` 0 · 未解析 markdown 残留 0 |
| HTML 实拍复核 | 五个视角全部通过：首屏 / 表格 / 代码块 / 配图 / 夜读；**统计条已去掉"预计阅读"** |
| PDF（随时可在线读） | 344 页 · 595×842（A4）· 图注无重复 · 页码正确、封面无页码 |
| **PDF 书签** | **456 条三层书签（27 篇/章 + 227 节 + 202 子节）· 逐条验证指向正确率 100%** · 元数据（title/author/subject/keywords） |

---

## 3. 哪里可能有问题（以及本轮已修掉的）

### 3.1 已修掉的九类真缺陷

| # | 缺陷 | 抓它的方式 |
|---|---|---|
| 1 | 第 14–20 章体例块写成 `## N.N 本章小结`，与前 13 章不一致 | 机械校验（体例） |
| 2 | 7 章的「读图指引」指向改版后图上**不存在**的列 | 交付前人工复读 |
| 3 | 新图用了色板外的 `#E8EFF9`，配色闸门拒绝导出**整批 39 张** | 机械闸门 |
| 4 | 5 处文字距卡片边框仅 3.3–5.3px（低于 6px 下限，会切字） | 机械闸门 |
| 5 | `fig-15-2` 卡片内**两行文字互撞** | **目检**（闸门查不出） |
| 6 | **39 条图注全部重复**：`图 1-1　图 1-1 …` | 构建后读 PDF |
| 7 | **内联后 10 个重复 id**（39 张图各自都叫 `fig-header` / `fig-conclusion`） | 构建后结构校验 |
| 8 | 第二轮 PDF 用了改名前的 HTML → 三形态不同源 | 流程自查 |
| 9 | 实拍复核第一轮 5 张截图 md5 完全相同（zsh 不分词，参数没传进去） | **对比截图 md5** |
| 11 | **PDF 只有 0 条书签**——328 页的 PDF 在线阅读时无法导航 | **作者指出"PDF 很多时候是在线阅读版"** — 已补三层书签 + 元数据，并去掉"打印版"这个误导性命名 |
| 12 | **一句读不懂的话「结果不对接不上责任」**（压缩过度 + 歧义） | **作者问"这是啥意思"** — 已改写；并补做了一次真实回读 |
| 13 | **前置章约定「3 个问题」而第 14–20 章实际都是 4 条** | 回读时发现（体例校验只看积木在不在） |
| 14 | §20.2.1 断言与自身分值不符、"40 分"指代不明；§20.3「三个区分」实为三个概念；表头「谁负责」与层级名不匹配；§7.3 定义格生硬；6 处破折号空格 | 回读时发现，逐条已修 |
| 15 | **全书编辑回读（第 0–20 章 + 五份附录，27 个文件）**：约 42 处修改 | 6 位读稿人分章精读 + 我逐条复核 + 我亲自改；28 条探针全部通过 |
| 16 | **第 1 章整章章节号偏离全书约定**（`## 1.` 而全书是 `## N.M`） | 读稿人发现 → 整章重编号 13 处 |
| 17 | **附录 B 漏登记 6 个被正文引用的文件** | 读稿人发现 → 补登记，并把该核查固化进 `build_book.py` |
| 18 | **5 处跨章前向引用落空** + **"责任人"在书内有两个数字（4 vs 7）** | 分别改写 / 显式分层并互相指路 |
| 19 | **审稿意见 v2.0**：对 v1.0 的 31 条逐条回原文复核 + 三类系统检查（引文归属 / 附录引用闭合 / 体例声明 vs 实际） | 生成 `98-审稿意见-v2.0.md`；查出全书**唯一**一处归属错误、**3 处**库引用落空、体例表**四个数字**全部失准 |
| 20 | **v2.0 第一轮落地**：A1 颗粒度统一 / A2 ch13 指向 / A4 测试口径 / B 类 12 条 / C 类 4 条，共 **35 处跨 12 个文件**，含 1 张配图 | 31 条探针全通过；新增校验第 11 项 |
| 21 | **体例表声明的数量与实际不符**（核心判断 / 行动清单 / 定义框 / 数据卡） | 改声明为与实际一致，**并把区间做成机械校验**（check_manuscript 第 11 项，现 14 项） |
| 22 | **架构说明存在字数副本，两个口径相差数百到数千** | **取消副本**，改为"以 HTML 阅读版统计条为准" |
| 23 | **篇幅配比表把 fig-0-1 同时算进「前置」与「第 0 章」**（配图列合计 40，实际 39） | 已修正；全表字数重算 |
| 24 | **v2.0 第二轮落地**：附录 C 新增「实现与库文档」类并登记 P11–P14（含 PptxGenJS 访问失败归位 C.4）；**行业视角补 9 章，覆盖率 21/21**；`00-front` 新增**机构代称约定**（主线固定为「某城商行」） | 17 条成品探针全通过 |
| 25 | **架构说明的字数副本改为生成**：新增 `stamp_arch.py`，重算并写回配比表与差额段 | 判据：**会随每轮变动的数字，必须有生成器，不能手写** |
| 26 | **铁律 11 立（作者裁定）**：篇幅不作验收指标，**不能因凑字数而添加内容**；审核编辑只问"缺不缺内容"，不问"够不够字数" | 写进 `book-studio`（→ v20260918.11）与 `00-架构` D-07；"是否补足字数"挂起项就此关闭 |
| 27 | **第 19 章实测补充（补内容不是补字数）**：§19.2.2 一次干净的安装（发现 `--ignore-scripts` 同时改变类型面与运行时行为，typecheck 红/测试绿的根因）；§19.5 缺配置冒烟 + **发现 `run-service.ts:1393` 静默回退 FakeModelProvider**——正是该节明文禁止的行为 | 附录 B 缺口 +2；交付冒烟 fixture 随书给出（3 用例 11ms 全过） |
| 28 | **锚定号前移** `2d8a79f` → `8ce7b776`（区间内 packages/apps 零改动，等价性已核） | 全书 12 处替换；附录 B 立"每轮重验锚定号"规矩 |
| 29 | **一次数据事故与全量恢复**：22:16 的历史改写把 `book/cn` 未提交的整轮改动 `git reset --hard` 掉，并将 327 个提交号全部重写；已从 WorkBuddy trace / file-history / changes-detail 检查点全量恢复（含阅读端沉浸按钮），再用 16:09 已分享的 HTML 反向核对 | 判据同 25：**会随每轮变动的实测数必须有生成器，不能手写** → 新增 `stamp_pdf_stats.py`；历史改写前先打一个 WIP 提交 |
| 10 | **"预计阅读 约 4 小时 35 分钟"**（由 `字数 / 480` 除出，即 8 字/秒）→ 对 328 页技术书完全失真 | **作者凭常识指出"比读小说还快"** — 已整条删除，换成可数事实 |

**第 6–9 条属于"局部合法、整体才暴露"的一类**：每张图单独过闸门没问题，每个分片单独合规，**但 39 张图放进一个文档、39 个分片拼成一本书，问题才出现。**

### 3.2 两项残留告警（已判定无害，未修）

- `Noncanonical compact authoring` —— 技能已明确列为无害。
- 每图 1 条 `<text> exceeds fig-header bounds on the vertical axis`（**超出 0.4px**）—— 标题组内**没有容器矩形**，不存在裁切；且首批 25 张交付时即为此状态。

### 3.3 一条必须摆明的内容风险

**参考实现里有两处缺口，本书没有掩盖，而是标注为"必须自行补"**：

| 缺口 | 在哪一章 | 书的处理 |
|---|---|---|
| **没有 token / 成本计量**（运行时事件词汇恰好 13 种，无任何 usage/cost 事件） | 第 13 章 | 要求自建用量事件，并立"无数据即未知"规则 |
| **没有嵌入向量检索**（知识库只有 FTS5 词法检索 + `LIKE` 回退） | 第 8 章 | 要求自建并对比两种检索 |

两条都在附录 B §B.3.1「已知的实现缺口」里列明。

---

## 4. 需要作者决定什么

**只有一件未决**：字数差额。

| 项 | 值 |
|---|---|
| 实际 | 总字符 **273,453**（v2.0 两轮落地后实测；此数会变，引用前重算） |
| 原定目标 | 约 31.9 万字 |
| 差额 | 约 5.3 万总字符（**-17%**） |
| 差在哪 | **不是漏写章节**。差额集中在第五、六、七篇的单章体量（交付篇 41,000→29,148、综合篇 29,000→16,666、迁移篇 25,000→14,941） |

**三个选项**：

1. **接受现状** —— 已是一本 344 页、21 章体例齐全、39 图完整的书。
2. **指定补哪几篇** —— 建议只补第五篇（交付）与第六篇（综合），那两篇的手算示例最经得起加厚。
3. **按原定规模全部补齐** —— 我按篇重做这三篇的深度，预计追加 4–6 万字。

**我的建议：选 1 或 2。** 这三篇的论点已经收敛；单纯凑字数会让"一页能说完的事变成三页"。**"删掉一个说不清的数字，比留下一个说不清的数字更专业"——这句话对篇幅同样成立。** 详见 `99-审阅待决事项.md` §2b。

---

## 5. 已关闭的挂起项

| 项 | 状态 |
|---|---|
| "代码已通过测试验证"的表述强度 | ✅ **已关闭**（作者裁定：强表述 B）。环境差异（代笔方受限环境 `14 failed / 568 passed`，根因 `ps` 被阻断）如实写入附录 B |
| 语气校准 | ✅ 第 1 章定为全书基准（作者："这就是我的调子"） |
| 书名 / 署名 / 案例基调 / 交付节奏 | ✅ 全部按作者裁定落地 |

---

## 6. 三形态各自怎么改

| 想改什么 | 怎么做 |
|---|---|
| 正文 | 改 `parts/*.md` → 跑 `build_book.py` → 跑 `build_html.py` → 重导 PDF。**合稿产物不手改。** |
| 配图 | 改 `$PPTM_HOME/projects/book_figures_ppt169_20260918/svg_output/*.svg` → 跑 `svg_native_export.py` → 重渲染 PNG → 同步 `svg/` → **重跑后两步**（图被内联进 HTML，必须重建） |
| 只改 PDF 页码 | 重跑 `make_print_pdf.py`（用 `md-to-html-book` 里的脚本，指定 `PDF_SRC` / `PDF_OUT`） |
