---
name: "Sample Assistant"
description: "A minimal built-in Skill that demonstrates the packaging pattern."
version: "0.0.1"
---

# Sample Assistant

This is a placeholder built-in Skill used to verify the packaging and
startup registration pipeline. It does not perform any real work.

When you are ready to ship a real built-in Skill, replace this directory
with the actual Skill package and update `release-manifest.json` accordingly.
