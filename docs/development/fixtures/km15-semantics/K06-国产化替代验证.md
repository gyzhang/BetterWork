# 自研组件替换的验证清单

替换数据库、中间件或操作系统时，按六项依次验证：语法与函数兼容性（含存储过程、窗口函数、字符集排序规则）、性能基准（与旧组件同数据量的对比压测报告）、主备切换与容灾演练、备份恢复的完整性校验、驱动与客户端在目标环境的适配、应用日志与监控指标的采集连续性。

每项给出通过标准与实测值，任一未达标不得进入生产变更窗口。灰度期间保留旧组件回退包，回退触发条件写入方案，例如响应时间劣化超过两成或错误率翻倍。验证报告随变更单一并归档，供后续版本复用比对。
