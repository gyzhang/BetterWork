# Qwen3.8-Flash：记忆改进逐卡交接

- 日期：2026-09-25；状态：D1–D5 推荐方案已批准，文档交接不代表已经开工。
- 唯一任务入口：[MI00–MI10](tasks-memory-improvements.md)。产品行为见[Spec](../designs/memory-improvements.md)，技术字段/预算/迁移/IPC 见[契约 §11](memory-contracts.md#11-mi-改进契约proposed)，设计决策见[ADR-0028](../adr/0028-memory-reliability-improvements.md)。
- Qwen3.8-Flash 负责代码和测试执行，不因此成为 BetterWork 内部默认模型；自动测试禁止真实网络调用。

## 1. 第一次：只核查 MI00

```text
请使用当前 Qwen3.8-Flash 执行 BetterWork 的 MI00，只核对基线与开工条件，不改业务代码。
先读 AGENTS.md、docs/12-engineering-standards.md、docs/designs/memory-improvements.md、docs/development/memory-contracts.md 第11节、docs/development/tasks-memory-improvements.md 的MI00。
执行 date、git status --short、git rev-parse --short HEAD，核对真实迁移版本与本卡关键链路，不使用文档旧测试数冒充本次结果。
按任务卡检查既有实现、并行工作树与审批事实；D1–D5推荐方案已于2026-09-25获批，未变化的方案无需重复询问；设计批准不等于后续卡开发或ADR正式Accepted。
只更新MI00证据和当日日志，保留其他会话已有改动，不改WM16/其他系列状态。
不启动真实模型调用，不提交、不推送、不发布。给出下一卡的前置条件，未获开发指令不要自行继续。
```

## 2. 评审后：单卡实施模板

D1–D5 已批准；MI00 核对完成、对应前置满足且光哥准备授权该卡开工时，粘贴本段。编号每次替换成一张卡，无需重新批准未变化的方案。

```text
依据已批准的记忆改进Spec D1–D5推荐方案，我授权你仅实施MI01及其必要测试和文档。
使用当前Qwen3.8-Flash；不自动执行下一卡，不委派其他编码智能体，不修改算台模型配置。
先读AGENTS.md、docs/12-engineering-standards.md、docs/designs/memory-improvements.md、docs/development/memory-contracts.md第11节、docs/development/tasks-memory-improvements.md的MI01。
先 date + git status --short + 核对前置/HEAD，列本卡文件范围；尊重并行未提交改动，迁移用最新连续版本号，不覆盖、不清理用户文件。
按本卡先加可失败回归，再实现生产闭环。协议唯一在agent-protocol；边界Zod；真实SQLite；禁止源码豁免、假数据仓储、空实现、第二套配置或权限放宽。
仅用合成数据和可注入Provider验证，自动测试不得触网或调用真实模型。
按卡片运行定向测试、typecheck、npm run verify、git diff --check，保留真实退出码，不管道截尾。中文修改后Grep回读。
若是UI卡，按脚本启动应用，给我“动作→预期”清单并记录实际使用结果；未完成UI验证保持doing，不以组件测试冒充。
更新本卡状态/证据和当日日志。遇契约冲突、既有改动冲突或需扩大授权时停止相关工作并说明，不自己猜新产品规则。
不提交、不推送、不发布，不自动把ADR改为Accepted，不改变WM16人工验收状态。
最后按任务板第7节交接，注明实际测试结果、未验证部分和下一卡。
```

更换卡号时，必须同时更换授权行与必读行，不能只改标题。若 MI00 已核实且方案没有变化，无需重复询问已批准细节。

## 3. 每卡追加提醒

将下表对应内容加在模板末尾；完整需求仍以任务卡/契约为准，不能只执行这张摘要表。

| 卡 | 附加执行约束 |
| --- | --- |
| MI01 | 不是只补sourceSelector。检查最终无工具消息、真实workspace、完整Run审计与直接/重放依赖；ResolvedMemorySource不能把memoryDependencies写空；缺记录不是合法空数组。共用checkpoint/artifact分支无法证明时拒绝。不要修猜历史manual。 |
| MI02 | 必须传真实message.completed eventId；来源摘录与编辑正文分开；Markdown渲染选区不等于原文坐标。不能唯一定位就原文重选；不允许无来源静默变自主。非BMP与重复句各补测试。 |
| MI03 | 排除列表独立查TaskContext，不靠selectedItems或前50个摘要；空prompt/preview失败也能恢复。范围外统一占位；保留全部上下文绑定；CAS失败不可自动覆盖，双击不能toggle两次。 |
| MI04 | 只做recallPolicy持久化与迁移；默认relevant，不自动提升旧记忆。不先开放无运行效果的pinned写接口；旧回执与原请求哈希必须可重放，不改历史迁移。 |
| MI05 | 同卡开放策略写入与v2实际召回。优先不能穿安全门禁；预算包括包装；keep-both连通组整体计费/整体落选。旧v1快照不可因修改literal常量解析失败；断言真正ModelRequest。 |
| MI06 | 明确“优先带入”不是“保证使用”；不合格给原因，保存影响下次Run；投影失败仍是保存成功警告。不要新建页面或全局开关。 |
| MI07 | 保持冲突算法；只补来源与精确修订的applicabilityNote展示；保存/刷新/重启均可回看。编辑修订后裁决失效，禁止无条件忽略冲突。 |
| MI08 | 不恢复不安全历史、不自动摘要、不自动换latest或发送Run。只读来源执行器IPC与真实消费同卡交付；读精确版本Run快照，不读旧Task最新executor。专家身份与当前修订分开，配置变化需确认，不复制旧授权；跳转回来保留原任务草稿。 |
| MI09 | 分别跑Q1–Q8无词面问法、R1–R8技术正例、N1–N8负例，分母不混用；以真实SQLite和生产准备/发送链路验证。自动测试零外网；变异只还原自己的改动，不用git reset。离线通过不代表真实工作效果通过。 |
| MI10 | 仅按已批准模型配置/调用预算做合成场景；自动提炼已关闭且无活动作业，否则先停。S5原Task带历史续跑，S4实际工具读取；后台请求也计数。分别记录请求与遵从，到额即停，不自动重试或换模型。 |

## 4. MI-M0 下午验收提示词

在 MI01–MI03 自动化完成后使用，不代表授权调用模型。

```text
请准备记忆改进MI-M0的人工验收，不扩大代码范围，不调用真实模型。
先核对MI01/MI02/MI03的自动化证据和当前工作树，按bash scripts/dev-start.sh启动开发应用。
给我两组“动作→该看到什么”的步骤：
A. 从合成已完成回答选择原文→编辑正文→保留来源保存→查看真实来源与材料/记忆依赖；错误选区必须能重选而不是变成自主口径。
B. 本任务不用→重新试算→关闭重开/重启→恢复参与选择；无词面命中仍可能不入选，但恢复入口不能消失，模型和材料绑定不丢。
由我操作真实应用；你按我反馈和只读DB/日志核实，不操作我其他应用，不把尚未执行步骤写成已通过。
发现问题回对应卡补回归；通过后只更新对应卡证据和当日日志，不自动开始MI04、提交或推送。
```

上面 A/B 两组步骤已连同 MI06/MI07/MI08 的 UI 门槛与任务板 §6.1 八条路径，逐格展开为 [MI 人工验收清单](memory-mi10-checklist.md)（§1–§7 人工走查，§8 真实模型场景保持未测）。走查用那份清单，不在这里重复维护第二份步骤。

## 5. 真实模型验收授权模板（MI10 专用）

填写实际配置与预算后才执行。它是未来授权模板，本文件本身不授权任何网络请求。

```text
我授权本次MI10使用算台中已存在的【模型配置显示名】和合成测试资料，执行tasks-memory-improvements.md第6.2节六个成对场景。
最大模型请求数为【明确整数，建议18；理想工具回合下限14】，工具后续回合、重试及意外后台请求同样计数，发出前核对额度，到额立即停止；本授权仅限本轮场景，不自动续期、不暗换服务。
先核实MI09通过、当前模型配置可用且范围匹配；专用合成空间自动提炼必须已关闭，且无排队或运行中的提炼作业，不满足就停。不要修改默认模型、提炼开关、凭据或取消其他作业；发现后台提炼启动停止本轮并记录污染。
使用相同模型/参数和固定合成材料。S1–S4每对两个独立新Task，基线为本任务排除目标记忆；S5两侧先用确定性Provider经真实服务在隔离库建立带审计历史及派生记忆，再只排除父记忆，在各自原Task续跑；S6两侧均守隔离，不发送越权内容。不回滚代码，不破坏其他任务。
S4两侧都须实际调用工具读取本期资料及精确参考成果，fixture不等于已读取；准备轮零真实调用且不计语义得分，严格区分fixture证据与真实结果。
记录实际调用数、Run ID、上下文修订、请求阶段、规则得分、重复纠正次数和失败原因，不打印密钥、完整请求或真实公司资料。
安全两项必须全通过，总分至少5/6；未测/超额/模型不可用就如实记录，不用FakeProvider冒充真实通过。
结果交给我确认，未经新指令不提交、推送、发布，也不自动关闭WM16或其他任务板的验收卡。
```

## 6. 失败后的定向返工模板

```text
仅返工MIxx的【失败场景ID】。先读本卡与契约对应条款，核实SQLite/日志/实际请求证据，区分实现错误、测试错误、语义不遵从和环境问题。
先补一个能证明根因的失败回归，再最小修复；不能通过删除断言、放宽类型、减小样本、跳门禁或改验收口径消除失败。
只重测必要范围后跑完整门禁，UI/真实模型复测仍遵守原授权预算，不自行增加调用或改模型。
保留其他会话文件，记录失败→修复→证据；未满足原门槛不标done。
```
