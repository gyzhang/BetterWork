# MI 人工验收清单（记忆可靠性改进）

状态：**待验**。自动化证据已在 MI01–MI09 登记（见 [MI 任务板](tasks-memory-improvements.md) 状态表），本清单只覆盖必须由光哥在真实窗口里走查的部分；不得以 jsdom 用例、代码阅读或原型评审代替这里的任何一格。

- 覆盖卡片：MI-M0 门槛（MI02/MI03 的人工路径）、MI06、MI07、MI08 的 UI 门槛，以及 [MI 任务板](tasks-memory-improvements.md) §6.1 的八条实际窗口路径。
- 真相源：产品行为见 [改进 Spec](../designs/memory-improvements.md)；字段、预算、错误码见 [记忆实施契约 §11](memory-contracts.md#11-mi-改进契约proposed)；状态只写任务板，本清单不复写状态。
- 三栏缺一不收口：自动证据（已登记）／人工 UI（本清单 §1–§7）／真实模型语义（§8，另需光哥单独授权模型配置与调用预算）。
- 应用启停只用项目启停脚本（`npm run dev`，见交接说明第 3 节）。Qoder 侧负责起应用并把清单交到这里，界面操作与结果判定由光哥完成；不用自动化驱动应用界面。
- 建议材料：一个专用合成工作空间＋1 份只含虚构数字的 Markdown/TXT 经营数据；记忆样本至少准备「已确认＋用户口径＋无依赖的工作要求」「派生自资料的记忆」「事实或经验类记忆」「同 topicKey 的互补规则两条」各一份。任何一格不合格就先记现象，不当场改代码。

## 0. 走查前的造数（不做这段，后面几节会撞「没有对象可点」）

按下面顺序在界面里造出样本，全部只用虚构内容；每造好一项在该行末记「已具备」。条件取自 Main 的真实门禁，不是设想。

| # | 要造什么 | 怎么造（走真实入口） | 支撑哪几格 | 已具备 |
| --- | --- | --- | --- | --- |
| 0.1 | 一次完成的运行＋最终回答 | 在合成空间新建任务，选上那份虚构材料，提一个短问题让运行跑到完成 | §1 全部、0.3 的派生记忆 | ☐ |
| 0.2 | 一条「可设优先」的自主工作要求 | 在记忆页手工新建（不走回答捕获，Main 就按你的口径登记）：分类选「约束」，正文写成一条长期工作要求，不设依赖 | 3.1–3.4、3.7、3.8、6.1–6.3 | ☐ |
| 0.3 | 一条派生自回答的记忆 | 用 §1 的捕获流程保存一条：捕获入口一定带来源，不会变成自主口径 | 1.4–1.5、3.5 | ☐ |
| 0.4 | 一条事实类＋一条经验类记忆 | 手工新建，分类分别选「事实」与「历史经验」 | 3.6 | ☐ |
| 0.5 | 一条未到生效期的记忆 | 手工新建时把「生效日期（可选）」填成明天 | 3.6 的后半 | ☐ |
| 0.6 | 两条会构成「潜在冲突」的已确认规则 | 手工新建两条：「议题（可选）」填同一个词（如「报表口径」），分类都选「约束」，**正文必须写成两句不同的话**（同文会被归一成同一条），适用范围相同或交叠、有效期重叠 | 4.1–4.8 | ☐ |
| 0.7 | 七条可设优先的工作要求（或多于 2,000 码点的一条） | 复制 0.2 再改数字，或把正文写长到超过 2,000 码点 | 3.8 | ☐ |
| 0.8 | 一条已被本任务排除的记忆 | 在对话页面板点「本任务不用」 | 2.1–2.8 | ☐ |
| 0.9 | 上期成果的一个精确版本＋换过版的材料 | 上一期任务里保存成果并对它做一次人工修订；把材料文件在本地改一版后在资料库点「刷新」 | 5.1–5.3 | ☐ |
| 0.10 | 专家跑出的成果版本，之后把该任务的专家换掉 | 先用某专家跑一次并保存成果，再回原任务改选另一位专家 | 5.5–5.7 | ☐ |

造数过程本身就是 §1、§2、§4 的走查动作；哪一步造不出来（入口拒绝、报错不合理）直接记在该行末并按「不通过」处理，不要绕过界面写库。

## 1. 回答捕获与来源完整（§6.1 路径 1–2／MI01+MI02）

前置：在合成空间跑完一次任务，得到一条完成的最终回答（未结束时入口会提示“这条回答还没有可定位的最终事件”）。

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 1.1 | 在最新回答里用鼠标选中一段原文 → 点回答下方「记住这段经验」 | 出现捕获表单，提示「来源摘录取自这条回答的原文；正文可以另行改写，改写不会解除来源与依赖。」；只读原文框展示回答全文；唯一命中时记忆正文已预填所选片段 | 待验 |
| 1.2 | 什么都不选直接点「记住这段经验」，或选中渲染后找不到的文本 | 正文留空并内联提示需回原文重选；原文框可手工拖选后点「确认选区」，之后 1.1 才成立 | 待验 |
| 1.3 | 选中回答里出现两次的同一短句（重复片段）→ 确认选区 | 提示「所选内容在这条回答里出现多次，无法确定位置，请在原文重新选择」，不生成选择器、不能提交 | 待验 |
| 1.4 | 确认选区后改写记忆正文（例如换成更概括的口径），选适用范围，点「保留来源并记住」 | 保存成功、表单关闭；适用范围下拉里不出现“全局/跨空间”选项，只有本空间（含专家×空间）粒度 | 待验 |
| 1.5 | 记忆页打开 1.4 那条详情 | 来源显示为已复核的用户口径回答摘录；「依赖：资料 N 项 · 既有记忆 M 条」与那次运行真实读取的材料/父记忆数量一致，不是 0 项空依赖 | 待验 |
| 1.6 | 在捕获表单里点取消/关闭，随后到记忆页确认 | 库里没有新增记录（取消不落库）；重开入口仍可再次捕获 | 待验 |
| 1.7 | 故意让保存失败（例如改到超长正文上限、或在另一窗口先改过同一条制造修订冲突），再重试 | 失败信息内联可见且草稿原样保留；重试成功后不出现双修订 | 待验 |
| 1.8 | 回答含 Markdown 标记、生僻汉字（如“䷀”）或 emoji 时重复 1.1–1.4 | 原文框选区与预填一致，码点计数随输入变化；不出现按 UTF-16 长度截断导致的半个字符 | 待验 |

## 2. 本任务排除与恢复（§6.1 路径 3–4／MI03，MI-M0 门槛）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 2.1 | 对话页右侧上下文面板「下次运行可用」里，对一条记忆点「本任务不用」 | 按钮变「正在调整…」后该行显示为已排除状态；该行仍留在面板中，不被隐藏 | 待验 |
| 2.2 | 换一个完全不同的问法重新预览 | 「本任务已排除」区块里仍能看到该条（小节副标题写明“换问法、重启或预览失败都保留在这里”）；本次选择里没有它 | 待验 |
| 2.3 | 关闭面板→完全退出应用→重启→打开同一任务 | 排除项仍在独立清单里，其他任务的记忆不受影响（切到同空间另一任务确认它没被排除） | 待验 |
| 2.4 | 在清单里点「恢复参与选择」后再预览 | 该条恢复被选资格；如果仍词面不相关或来源失效，它出现在落选原因里而不是硬塞回选择——恢复不等于重新确认 | 待验 |
| 2.5 | 把输入框清空（无 prompt）或让预览失败一次，再看排除清单 | 清单仍能加载并给出恢复入口（读取失败时显示可重试的内联错误，不显示空态假象） | 待验 |
| 2.6 | 排除一条已删除/越范围的记录（可用另一空间或“以后不用”制造），刷新清单 | 该行只显示「此项当前不可查看 · 不在本任务可管理范围内，或记录已被删除」，不泄露正文；按钮是「移除此排除」 | 待验 |
| 2.7 | 在两个窗口同时改同一任务（一处排除、一处改材料绑定），后提交者再保存 | 后提交者看到冲突要求刷新，不静默覆盖对方的绑定；双击同一行不出现状态反转 | 待验 |
| 2.8 | 「本任务已排除」点「刷新」 | 加载中显示「正在读取…」，成功后条目与持久化状态一致 | 待验 |

## 3. 优先带入策略（§6.1 路径 5／MI04+MI05+MI06）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 3.1 | 记忆页打开一条「已确认＋用户口径＋无依赖」的工作要求详情 | 策略行显示「按相关性选择」，按钮「设为优先带入」，下方说明写明只免「词面命中」这一道门槛，不免范围、有效期、本任务排除、来源与冲突门禁，且“只影响下次运行，也不表示模型一定采用” | 待验 |
| 3.2 | 点「设为优先带入」 | 徽标变「优先带入」，按钮变「取消优先带入」；未新增第二条记忆（修订号 +1，正文不变） | 待验 |
| 3.3 | 回到该空间新任务，用与记忆正文不同词的问法预览（例如记忆写“金额按万元保留两位”，问“做份经营回顾”） | 该条出现在选择里，理由标注「优先带入」；同空间一条无关的相关记忆不因这次改动而带入 | 待验 |
| 3.4 | 取消优先后重复 3.3 的问法 | 该条不再入选，面板把它归入「与本任务不相关」一类落选原因，说明状态与真实选择一致 | 待验 |
| 3.5 | 对派生自资料的记忆点「设为优先带入」 | 内联拒绝并提示先「作为我的工作口径重新保存」；不产生修订 | 待验 |
| 3.6 | 对事实类或经验类记忆、来源待复核的记忆、已过期或未到生效期的记忆分别尝试设优先 | 各自给出对应原因（只用于工作要求／来源尚未复核／只有已确认且当前生效的记忆可设），全部在写入时被拒；未到生效期的规则即使设了优先也不会提前带入 | 待验 |
| 3.7 | 把一条已设为优先的记忆改成含材料依赖或改成事实 | 提示「请先取消优先，再修改会使其不再符合资格的内容、分类或范围」，不允许静默降级 | 待验 |
| 3.8 | 制造预算边界：优先带入 7 条以上，或把某条写到接近 2,000 码点 | 超出优先池额度（6 条／2,000 码点）的条目在面板里显示「超出本次预算」落选原因；没有任何一条被截成半条正文 | 待验 |

## 4. 潜在冲突与并存条件（§6.1 路径 6／MI07）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 4.1 | 用 0.6 造的那两条同议题规则，打开记忆页的冲突卡片 | 卡片抬头只称「可能冲突：两条口径指向同一议题，需要你澄清」，并列双方正文、适用范围与有效期，不替用户判定真假 | 待验 |
| 4.2 | 点「查看两侧来源」 | 展开后分别显示两侧的摘要（来源类型、修订、依赖计数、摘录前若干字），不显示原始事件载荷；再点变「收起两侧来源」 | 待验 |
| 4.3 | 在「适用条件说明」里写出两条各自的适用场景 → 点「确认两条并存」 | 卡片显示「已确认并存（成对带入，适用条件见裁决记录）」并回显所填条件 | 待验 |
| 4.4 | 关闭卡片、刷新页面或重启应用后再打开 | 并存条件仍然显示，不丢、不显示成未裁决 | 待验 |
| 4.5 | 条件留空或写得极长后确认并存 | 拒绝保存并内联给出原因（条件必填、长度上限）；界面提示写不出条件时不要确认并存 | 待验 |
| 4.6 | 并存生效后编辑其中任意一条（改正文或有效期） | 旧裁决对该对失效，冲突重新出现为待裁决，不套用旧条件 | 待验 |
| 4.7 | 冲突另一侧不可查看（删除/越范围）时打开卡片 | 不泄露对方正文与来源，也不自动裁决；提示到其合法管理范围内处理 | 待验 |
| 4.8 | 走一遍既有能力：确认“替代”裁决、候选重复提示 | 行为与改进前一致，无回归 | 待验 |

## 5. 换期恢复与来源执行身份（§6.1 路径 7／MI08）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 5.1 | 在同一任务里先跑一期（选择材料 A 旧版），换新版材料后再跑一次 | 「历史上下文调整」小节列出哪些旧轮次这次没带、原因是什么；写明“旧轮次只是这次不发送，对话没有被删除” | 待验 |
| 5.2 | 点该小节里的「选择本期材料」 | 面板切到来源页并打开文件材料选择；历史仍可读但不重放，不会自动把旧版材料加回本次运行 | 待验 |
| 5.3 | 点「查看上期成果版本」→ 在成果页选定精确版本 v1，再引用它 / 「从此版本开始新任务」 | 保持用户所选的那一版（不是 latest），既有用途标注保留；仅“标记参考”不产生读取关系，实际读取才记来源 | 待验 |
| 5.4 | 点「到记忆详情保留方法」 | 切到记忆页供你手动以你的口径新建或重述；界面不自动摘要旧回答、不自动落库 | 待验 |
| 5.5 | 用专家 A 跑出成果，把该任务的专家换成 B，再从成果某版本「从此版本开始新任务」 | 新任务默认身份来自该版本来源 Run 的 A 快照，不暗用 B，也不继承 B 的技能/模型/MCP 绑定；若 A 的当前修订已变化，先明确提示按当前配置使用 | 待验 |
| 5.6 | 对来源快照缺失、专家已停用或不可用的版本重复 5.5 | 按原因显式提示并回退通用助手（或要求显式选择），不伪造身份、不改取 latest 版本 | 待验 |
| 5.7 | 跨成果/不存在的版本 ID（例如从另一成果复制来的版本）尝试 | 不改动当前任务，给出可解释错误或明确不可用；无新增 Run、无网络请求 | 待验 |
| 5.8 | 在 5.5 的选择过程点返回/取消 | 原任务的 prompt 草稿、专家、材料与能力绑定原样保留 | 待验 |

## 6. 运行中改策略与旧审计（§6.1 路径 8／MI05+MI06）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 6.1 | 发起一次较长任务，运行中到记忆页把某条设为/取消优先带入 | 界面明确说明只影响下次运行；当前这次运行的「本次运行记忆」清单不变，旧审计不被改写 | 待验 |
| 6.2 | 取消当前 Run 后重跑同一问题 | 新 Run 按新策略选择（或明确不落选并给原因）；被取消的 Run 有终态、不留下半截运行 | 待验 |
| 6.3 | 回看被取消那次运行的记忆信息 | 仍显示它当时实际带出的记忆与原因，修订号/正文哈希与当时一致（快照不被后续更新覆盖） | 待验 |
| 6.4 | 「以后不用」（删除/终态）一条记忆后立刻看正在运行的任务 | 运行中任务不热更新；提示需要取消后重跑才立刻生效 | 待验 |

## 7. 同轮体验检查（走查 1–6 时随手确认，不重做全站视觉验收）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 7.1 | 浅/深色各走一遍捕获、排除、优先、冲突四张卡片 | 文字与徽标对比度足够，无一处只在一种主题下可读；无硬编码色块残留 | 待验 |
| 7.2 | 把窗口拖窄（约 900px 以内） | 表单、来源框、按钮不重叠不被裁掉；不靠 9–10px 小字换空间 | 待验 |
| 7.3 | 只用键盘 Tab/Enter/Esc 走完捕获与冲突卡片 | 焦点顺序可预期、焦点可见；Esc 关闭时不落库、不产生修订 | 待验 |
| 7.4 | 任意一步失败时看反馈形态 | 当前表单的错误是内联红条，跨页可回看的结果进消息中心；不出现自造成功横幅 | 待验 |

## 8. 真实模型成对场景（任务板 §6.2，未授权前整节保持“未测”）

前置缺一不可：MI09 自动化证据保留；专用合成空间**自动提炼已关闭且无排队/运行中作业**；光哥书面给出**模型配置显示名**与**整数调用预算**（下限 `2×(1+1+1+2+1+1)=14`，建议上限 18 以容纳 S4 分开读取）。前置不满足时本节约整体记 blocked，Qoder 不擅自改开关、取消他人作业或开始调用。

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 8.1 | S1 同义约束、S2 专家方法、S3 纠正复用、S4 连续两期、S5 撤销不回流、S6 跨空间隔离：每对两个独立 Task，同模型同输入，基线侧只做“本任务排除该记忆” | 改进侧 ≥5/6 达成判定规则，且 S5/S6 必须为 1；S4 两侧材料经真实工具读取；每对独立上下文 | 未测 |
| 8.2 | 逐次记录 | 按 §6.3 格式记 HEAD、模型显示名/非敏感指纹、Run ID、上下文修订、入选与排除身份、请求阶段、0/1、重复纠正次数、实际请求数、失败归因 | 未测 |
| 8.3 | 额度与污染 | 每次真实请求前核对剩余额度，工具回合/重试/意外后台请求都计数，到额即停；发现后台提炼启动立即停止并记录污染，不自行补测、不自动换模型 | 未测 |
| 8.4 | 失败归因 | 区分“未带入”（装配问题）与“带入未遵从”（语义问题）；不把 dispatch-attempted 当作模型已读 | 未测 |

## 9. 收尾核对

- 每完成一格把「结果记录」改成 通过／不通过＋一句话现象；未走到的写“未测”，不得留空或按“应该没问题”记通过。
- §1–§7 全部有实际记录后：MI02/MI03 与 MI-M0 门槛才能收口，MI06/MI07/MI08 才能从 doing 改 done；任何“不通过”回对应卡片补回归测试后重测，不当场弱化验收标准。
- §8 需单独授权，未执行时 MI10 保持 todo；本节完成与否都不自动关闭 WM16、E55、KM15，也不构成提交、推送或发布授权。
- 验收期间不导入真实用户数据库、不使用公司真实材料；合成数据里的数字与名称保持虚构。
