# 知识基础闭环编码交接（Qwen3.8-Flash）

- 日期：2026-09-24。
- 唯一职责：给新会话可复制的单卡指令；不维护任务状态、不重复字段或算法。
- 状态只看 [KM 任务板](tasks-knowledge.md)，实施只按[知识契约](knowledge-contracts.md)，产品行为见[产品规范](../designs/knowledge-foundation.md)，技术授权见 [ADR-0027](../adr/0027-knowledge-foundation.md)。
- 当前仅文档归档，全部 KM 卡未开工。模型名称由光哥在新会话选择，不由仓库配置暗中切换。

## 1. 第一次新会话：先核对，不编码

```text
请使用当前选定的 Qwen3.8-Flash 执行 BetterWork 的 KM00，只做开工核对，不写业务代码。
工作区：/Users/kevin/Dev4AI/BetterWork。
必读 AGENTS.md、docs/12-engineering-standards.md、docs/designs/knowledge-foundation.md、docs/adr/0027-knowledge-foundation.md、docs/development/knowledge-contracts.md、docs/development/tasks-knowledge.md。
先核对 git status --short、HEAD、两个数据库当前迁移号和关键已有实现；保留所有工作树改动，不回滚未知代码。
检查 ADR 的技术方案及产品规范 §5 文档线框是否有我的明确批准；没有就列出待批准项，保持未开工，不自行将 Proposed 改为 Accepted。
只报告：可复用能力、实质冲突、KM01 文件边界、前置与验收检查。没有冲突也不自动实施 KM01。
本次不调用真实模型、不启动应用、不做 UI 自动化、不安装依赖、不提交推送发布。更新 KM00 核对证据和当天日志；无批准则标 blocked 而非 done。
```

核对后的批准消息可以明确写“我批准 ADR-0027 与产品规范 §5 文档线框，并仅授权 KM01”。批准技术方案不代表一次授权全部卡；单卡消息仍应按下面模板。

## 2. 通用单卡实施指令

把【KM编号】替换为实际卡号，并同时粘贴 §3 对应聚焦行；只有已批准技术方案后使用本段。它不是本轮已经发生的授权记录。

```text
我已批准 BetterWork 知识基础闭环的 ADR-0027 及产品规范 §5 文档线框，本次仅授权执行【KM编号】，不要自动实施下一卡。
使用当前选定的 Qwen3.8-Flash；工作区 /Users/kevin/Dev4AI/BetterWork。
必读 AGENTS.md、docs/12-engineering-standards.md、docs/designs/knowledge-foundation.md、docs/development/knowledge-contracts.md、docs/development/tasks-knowledge.md、本卡指定 ADR 和现有测试。
先核对 git status --short、HEAD、前置卡证据和每个库最新迁移号；保留其他会话已有改动。执行 date 后才能写日期。
开始前列出：本卡行为/不做项、字段与 Schema、IPC 或 Tool、Service、数据库映射、UI/Hook、正常/异常/取消/恢复验收清单。
按契约完整接通本卡生产者和消费者，不只导出类型或写替身测试。已有等价实现直接复用，不新增第二套 MaterialReference/Evidence/模型密钥入口/编码规范。
缺陷诊断按 GATE-0；缺现场 DB 或日志证据如实说明，并用合成隔离测试复现，不访问用户材料来填 fixture。
只实现本卡和必要测试/文档，不委派其他智能体、不升级无关依赖、不修改 E/CF/WM 状态。实质契约冲突先停下说明，不自行换技术选型。
真实 SQLite 测试；HTTP 和模型测试注入替身且不触网。Renderer IPC 经 Hook 收口，遵守现有类型、错误、主题与导入规则，不加 any/非空断言/源码豁免。
完成本卡定向测试、npm run typecheck、npm run verify、git diff --check，记录命令与真实退出码，不接管道截尾。每次中文编辑后回读。
不要擅自启动应用或操作 Browser/ComputerUse；提供人工动作与预期，人工 UI、真实模型由我另行安排，缺证据明确标待验。
只更新本卡状态、必要契约和当天日志；半接线或测试失败不得 done。不要提交、推送、打包发布。
结束报告：卡号/状态；实际行为（含失败、取消、恢复）；修改文件；命令/退出码/用例；两个库迁移号；文档日志；人工待验；风险；下一卡前置。报告后停止。
```

构建作为 verify 的现有环节允许；打包/安装/真实模型/发布不在通用授权内。KM07/14 若需要任务指定的本地打包预检，先说明精确命令和产物目录并取得授权，不能把预检等同发布。

## 3. 逐卡聚焦补充

以下每行可复制到通用指令后；完整边界见任务卡，数字/Schema 始终回查契约。

| 卡号 | 聚焦指令 |
| --- | --- |
| KM01 | 保留知识修订 ID、原始 contentHash 与旧文本；同原文件不同解析版本可以追加修订。先接管理搜索固定引用供 KM03 使用，不能等 KM08 或按 latest 补身份。分页按保存 section 与码点，管理预览不写 Run 足迹，不提前建语义作业和集合表。 |
| KM02 | 接 read_knowledge 的完整 Run 链路与精确 Evidence/读取足迹；按契约迁移两张表的唯一约束，覆盖同段分页、不同调用重读、同调用结果重复消费的预算差异；先审计后返回，空材料零 HTTP/全库回退，Expert 不扩权，回归 WM 依赖。 |
| KM03 | 消费 KM01 搜索固定引用，原子创建完整 TaskContext 草稿；幂等回执先于首次创建校验，覆盖响应丢失后资料被移除的重试；成功后才导航，失败不清空原任务，迟到不抢导航，不自动发送模型。 |
| KM04 | 默认当前 Run，历史显式切换；来源预览严格只回看已返回证据范围，不续读未返回全文；旧数据不编造 span，预览与打开本机原件分开。 |
| KM05 | 移除“所有读过材料自动算采用”；自动 Markdown、文件登记、用户保存三条声明链闭合。按契约继承矩阵处理：legacy 连续编辑仍未核实，none 不产生声明，主动重选才变用户声明；不将声明称为正确性批准。 |
| KM06 | embedding 使用现有模型与凭据入口，真实消费响应形状/维度；modelFingerprint 不用 updatedAt，不含密钥；无 Fake、无暗换 profile、无自动重试，所有 HTTP 测试注入。 |
| KM07 | 完成 job/item、共享 space/维度锁、Worker、staging→active、取消/中断/attempt 防迟到；普通重建保留同空间旧代次，强制重建先退役旧空间；失败/中断/取消项可手动重试且不再 reset，成功项不重跑；开发与构建入口一致，不在 Main 跑长循环。 |
| KM08 | 知识页和 Run 统一异步搜索；先过滤再 topK，固定 RRF 与同分规则，摘要范围回算 section；同次只用一个仍有效空间，强制重建交错不返回旧向量；部分覆盖/故障明确降级，用户取消不能转成功。 |
| KM09 | 复用知识页骨架、模型设置和三路反馈；配置、覆盖、实际检索方式分别展示；普通重建与使全部旧向量失效的强制重建明确区别和确认；sequence 与请求代号防迟到，不新增一级导航或全局成功横幅。 |
| KM10 | 历史提取文本、原件状态与当前文件分别展示；手动检查不自动刷新/调用模型；来源缺失不丢历史，预览不冒充模型读取，取消不把未检查项标正常。 |
| KM11 | 单层集合、多对多成员，规范化重名和 CAS；分类只组织不授权，不移动本机文件，不改变既有 Task 材料；集合过滤必须在检索截取前。 |
| KM12 | 复用 OfficeParserService 的 bytes 核心；修 presentation 关系页序、备注与 Sheet locator 往返，预算在大分配前执行，统一取消；不扩格式枚举、不复制解析器。 |
| KM13 | XLSX/CSV/PPTX 从格式枚举/选择器到提取/修订/关键词/向量/工具读取全部接通；显式解析分支，警告可见，不把图片或无缓存公式伪造为已理解内容。 |
| KM14 | 用合成资料、真实两库和请求替身验证整个闭环；按契约规模测 p95/RSS/取消并记录环境，预检 Worker 构建路径；不降低样本量冒充通过，不运行真实模型。 |
| KM15 | 先核对独立授权；应用只按脚本启动，给光哥“动作→预期”清单并由光哥操作。自动、人工 UI、真实语义三栏缺一不收口，不代办 E55/WM16、不发布。 |

## 4. 中断后恢复

```text
恢复【KM编号】，只恢复这张卡。
先读任务板、契约、当天和前次日志，核对 git status、HEAD、实际迁移、已有用户改动和授权。
输出已完成/未完成/基线差异/需重验清单。不回滚未知修改，不把上次测试视为本次仍通过。
仍在本卡授权且无实质冲突时完成剩余工作；否则停下说明。
索引只验证取消、interrupted 和手动重试机制，不自动触发真实 embedding、不扫描用户历史资料补跑。
按单卡模板报告并更新本卡证据和当天日志；不继续下一卡、不提交推送。
```

## 5. 只核验里程碑

```text
只核验【KM-M编号】，不授权新增功能或下一卡。
只读核对当前 HEAD、任务卡、测试记录和人工证据，列出自动测试、用户 UI、真实模型三类结果与缺口。
需要补跑本地测试时先说明命令；真实模型、启动应用或桌面操作先等待独立授权。
不要把模型已配置写成语义已生效，不把搜索/读取写成成果已采用，不把 fixture 通过写成业务质量通过。
实施状态只在 tasks-knowledge.md 维护，不改 E/CF/WM，不提交发布。
```
