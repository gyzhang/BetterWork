# KM15 人工验收清单（知识管理与使用基础闭环）

状态：**待验**。本清单由 KM14 自动证据派生，供光哥亲自走查；不以自动化操作界面代替人工结果，真实模型栏未获授权前保持待验。

- 真相源：产品行为见 [知识库与记忆](../04-knowledge-and-memory.md) 与[产品规范](../designs/knowledge-foundation.md)；字段/预算/降级见 [知识实施契约](knowledge-contracts.md)；状态只写 [KM 任务板](tasks-knowledge.md)。
- 三栏缺一不收口：自动证据（KM14 已登记）、人工 UI（本清单）、真实模型语义（§12，另需授权）。
- 2026-09-25 按当前代码逐屏实核改写：所有按钮名、文案与落点取自实际渲染文本，不写「应该有的界面」。同日补齐了关键词重建入口、全库来源检查入口、作业条目回看与最近作业留档、Esc 与窄窗堆叠（证据见任务板 KM15 行），这些项在补齐前是**无法人工验收**的。
- 应用启停只用项目启停脚本（`bash scripts/dev-start.sh` / `bash scripts/dev-stop.sh`）；打包 .app 由光哥亲自打开。本清单只描述「操作 → 应看到什么」。
- 建议材料：自备 1 份 Markdown、1 份 TXT、1 份 PDF、1 份 DOCX、1 份 XLSX（含公式与缓存值）、1 份 CSV、1 份 PPTX（含备注、表格和**一页只有图片的幻灯片**），另加 1 份 `.ppt` 或损坏文件、1 份 0 字节 .md 作失败样本。操作前记下每份文件的标题关键词。

## 0. 前置条件（缺任何一条，后面的走查结论不成立）

| # | 前置 | 状态 |
| --- | --- | --- |
| 0.1 | 开发实例能起来（`bash scripts/dev-start.sh`，日志 `/tmp/betterwork-dev.log`） | **已通过**：2026-09-26 光哥确认能启动并可操作、无报错 |
| 0.2 | 打包态知识 Worker 可用：`npm run dist:mac --workspace @betterwork/desktop` 已重跑（退出 0），包内 `app.asar:/out/main` 含 `knowledge-worker.js` 与 `chunks/`；用打包二进制以 `ELECTRON_RUN_AS_NODE=1` 直跑包内 worker，CSV/XLSX 提取、向量扫描、伪 nonce 拒绝、正常退出全部通过（AI 侧无界面完成，探针输出已登记任务板） | 命令行侧**已核**；**.app 冷启动导入仍需 §13 人工确认** |
| 0.3 | 真实模型调用授权 + 一个可用的嵌入角色模型 | **已通过（2026-09-26）**：系统内 3 个模型均可用，其中只有 1 个向量模型；光哥已在知识页「索引与模型 → 嵌入模型」下拉核对，选中的就是它。该确认视为本轮 §12 的真实向量调用授权范围 |
| 0.4 | §12 的 20 个同义改写问题与「问题↔相关资料」标注，在跑之前共同固定 | **草稿已出，待光哥改定**：见[语义验收题集](knowledge-km15-semantic-set.md)（合成语料 10 份在 `fixtures/km15-semantics/`，20 题已预标注，判定口径为「标注资料进入前 5 即命中」）。不阻断 §1–§11 与 §13；开跑后不得回改题面与标注，执行载体（界面手跑／探针脚本）在那份文件 §5 二选一 |

## 1. 导入与资料库管理

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 1.1 | 侧栏「知识」→ 页头右上「导入资料」，一次选入 §建议的 7 种有效格式 | 按钮短暂变「正在处理…」；出现内联「已提交导入作业，索引正在后台建立。」；「索引作业」区出现「资料导入：进行中 x/y」行；列表逐条出现标题与 MD/TXT/PDF/DOC/XLS/CSV/PPT 徽标；没有全局成功横幅刷屏 | 待验 |
| 1.2 | 导入 `.ppt` 或损坏文件（注意：`.ppt` 不在默认过滤器里，要在文件对话框把类型切成「所有文件」） | 该条目失败且原因逐条可见（出现在「以下条目未完成」清单里），已成功的条目不受影响，库里没有空壳资料；作业行收口后进入「最近作业（含已取消）」并可「查看条目」看到「解析提取 · 失败 · 原因」 | 待验 |
| 1.3 | 导入一个 0 字节的 .md | 条目失败，原因为无文本可提取（`no-extractable-text`），不建立可检索的空知识 | 待验 |
| 1.4 | 移除某资料（卡片「更多」→「移出资料库」，确认框标题「移出「X」？」），再以同文件重导入 | 确认框写明不删原件；库里条目消失；重导入后作为新登记出现，且不复活此前的集合成员（在旧集合的「未分类」视角能看到它） | 待验 |
| 1.5 | 导入进行中点作业行「取消」，然后回到资料页 | 内联「已请求取消，作业将停在当前条目边界。」；该作业随后出现在「最近作业（含已取消）」里，状态显示「已取消」而不是消失；已完成条目保留；重启应用后不出现永远「进行中」的行 | 待验 |

## 2. 详情、分页与原件状态

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 2.1 | 打开长文档：卡片「更多」→「查看详情」 | 详情头部一行给出「第 N 版 · 来源一致（检查于…）或（尚未检查）· 关键词索引就绪 · 向量索引未启用/待建索引/…」；正文分页「本页 N 字，仍有后续内容」→ 下一页，读完显示「已读到结尾（本页 N 字）」；每段前显示定位（「第 N 页」「段落 N」「sheet:名!A1:C1」「rows:N-N」「slide:N/notes」）；页面提示写明预览不产生任务访问记录、不调用模型 | 待验 |
| 2.2 | 对 PDF/DOCX/PPTX 打开详情 | 定位与实际页/段/幻灯片对应；PPTX 每页文本、表格、备注各自成节；DOCX 段落号与原文一致 | 待验 |
| 2.3 | XLSX 含公式单元格 | 公式与缓存值同时可见（形如 `C3=B3*2（缓存值：240）`）；无缓存值时给出警告且不用空值冒充结果 | 待验 |
| 2.4 | 详情点「检查来源」 | 内联「已提交来源检查…」；作业收口后头部状态变成「来源一致 / 原件已变化 / 原件缺失 / 原件不可读」之一并带检查时间；不自动改内容 | 待验 |
| 2.5 | 把某个已导入原件 `chmod 000`（或移到外接盘断开）后点「检查来源」 | 显示「原件不可读」或「原件缺失」，两者不混用；旧保存文本仍可翻页阅读 | 待验 |
| 2.6 | 详情点「打开本机原件」；卡片上是「打开原文」 | 两个动作都只打开当前本机文件，不改变版本；失败时局部报错，不伪装成功 | 待验 |
| 2.7 | 详情打开时点「刷新内容」，等作业完成（不用关开详情） | 内联先给「已提交…刷新作业，索引正在后台重建。」（**不再有「已刷新」的提前成功提示**）；作业完成后「保存版本」自动多出「第 N+1 版」，正文默认回到最新版 | 待验 |
| 2.8 | 「返回列表」/按 Esc | 列表回到原搜索条件与结果，不丢勾选状态的解释：切搜索/筛选会清空勾选（见 4.4） | 待验 |

## 3. 集合分类（先点工具栏「索引与模型」展开面板）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 3.1 | 「集合管理」输入名 →「新建」，行内改名 →「改名」，删除 → 确认框「删除集合「X」？」 | 重名（含全半角/空格差异）被拒并给中文原因；删除确认写明「只解除这层分类，不会删除资料或本机原件」；删除后另一集合与资料本身不受影响 | 待验 |
| 3.2 | 详情「集合」勾 2 个 →「保存分类」；再打开另一文档保存 | 「保存分类」在没改动前是禁用的；保存后持久；若这期间另一窗口改过同一文档，再保存会报冲突要求刷新，而不是静默覆盖 | 待验 |
| 3.3 | 工具栏下拉「按集合筛选资料」切 集合/未分类/全部 | 列表与后续搜索范围同步收窄；当前正在筛选的集合被删除后自动回落到「全部资料」 | 待验 |

## 4. 检索、勾选与研究草稿

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 4.1 | 输入关键词点「搜索」 | 摘要条显示「找到 N 条相关资料」，右侧给出「本次检索方式：关键词／关键词＋语义／语义（向量） · 降级原因（中文） · 向量覆盖 a/b」；未检索时提示「提交搜索后可见检索方式与覆盖状态」 | 待验 |
| 4.2 | 勾选命中的卡片复选框（标签是「选择「X」用于研究」），或点「全选结果」 | 「用已选资料研究（N）」的 N 与实际份数一致；同一份资料的多个命中按修订去重（勾一个会把同修订的其他命中一起打上勾，这是不重复占材料名额的预期行为，请确认能接受） | 待验 |
| 4.3 | 点「用已选资料研究（N）」 | 直接进入新任务草稿：任务材料显示所选资料与默认「背景」用途，可改可删；**不会自动发送**；当前任务与选择都不被清空 | 待验 |
| 4.4 | 勾选后换关键词再搜，或切集合筛选 | 此前的结果勾选被清空并给出局部提示（与降级原因合成一条内联消息，不互相覆盖） | 待验 |
| 4.5 | 当前任务正在运行时点「用已选资料研究」 | 顶部给出可解释拒绝「当前任务仍在执行，请等待完成后再开始新的研究。」，任务与勾选都保留 | 待验 |
| 4.6 | 不选任何资料直接问知识相关问题 | 得到可解释结果（本次运行没有选定资料 → 空范围，不回退全库），不出现未选资料的内容 | 待验 |
| 4.7 | 快速重复点击「用已选资料研究」 | 不产生重复任务（同操作回执幂等） | 待验 |
| 4.8 | 断网或让主进程报错后再点（可用移除当前勾选的资料制造失败） | 失败提示「创建研究草稿失败；当前任务与已勾选资料保持不变。」，不留下可发送的半成品 | 待验 |

## 5. 来源回看（工作页「资料」面板）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 5.1 | 让助手读资料后，在「查看上下文 → 资料 → 已查阅来源」里点「查看区间」 | 只回看那一次实际返回的固定修订正文，页面写「共返回 N 字，止于该区间；不提供续读。」；没有续读入口 | 待验 |
| 5.2 | 对比「摘要来源」与「正文来源」标签 | 两种行为分开标注，各带修订哈希与「第 N 段 a–b 字」；搜索摘要不被说成读过全文 | 待验 |
| 5.3 | 展开「历史运行来源 · N 条」并切换 | 默认只显示「本次运行」；历史条目要显式展开；标「历史范围未记录」的旧来源不给「查看区间」按钮 | 待验 |
| 5.4 | 原件已被改过后回看旧 Run 来源 | 区间仍是当时那份文本，界面说明是已保存版本，不冒称实时原文 | 待验 |

## 6. 成果采用声明

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 6.1 | 打开一个由任务生成的成果详情 | 「声明采用依据」与「运行访问记录」分区显示；读过但没声明的资料只出现在访问记录里，成果标题区显示「未声明采用依据」而不是把访问当依据 | 待验 |
| 6.2 | 在成果编辑态勾「采用来源」（可选「摘要依据/正文依据」与用途）后保存 | 新版本显示「用户选择采用」；旧版本不回写；「改为沿用上一版声明」保存为「继承前版声明」 | 待验 |
| 6.3 | 全部不勾选并保存 | 显示「未声明采用依据」，不伪造采用关系 | 待验 |
| 6.4 | 让助手在回答里明确声明所用来源后保存成果 | 显示「模型声明采用」；这条只能由真实运行产生（界面上没有手工触发入口，见 §11） | 待验 |

## 7. 刷新与历史修订

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 7.1 | 本机改写已导入文件 → 卡片「更多」→「刷新索引」 | 详情「保存版本」出现新版；旧任务/旧来源仍回看得到旧内容 | 待验 |
| 7.2 | 删除源文件后刷新 | 逐条目可解释失败（缺失/不可读），已保存修订仍可读取 | 待验 |

## 8. 语义索引与模型（知识页「索引与模型」面板，不在设置页）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 8.1 | 设置 → 模型 → 添加模型：类型选「嵌入模型」，填 endpoint/key，保存并「测试」 | 只有启用且已配凭据的嵌入 profile 会出现在知识页「嵌入模型」下拉里；模型配置不会自称可用于聊天 | 待验 |
| 8.2 | 知识页勾「语义检索」 | 先弹「启用语义检索？」确认框，写清用哪个模型、现有几份资料、文本会发送到模型服务且可能产生费用、历史资料需手动重建；确认前不发起任何调用 | 待验 |
| 8.3 | 没有合格嵌入模型时勾「语义检索」 | 复选框禁用并给出原因（如「还没有可用的嵌入模型…」），不是无声禁用 | 待验 |
| 8.4 | 点「重建语义索引」 | 确认框「重建语义索引？」；作业行「向量索引重建：进行中 x/y」；中途「取消」后旧兼容索引仍可用 | 待验 |
| 8.5 | 点「强制重建语义索引」 | 确认框文案与普通重建不同，明确「全部旧语义索引会立即停用且不会恢复；取消或部分失败也不会退回旧结果」；重建期间检索仍给关键词结果并显示降级原因 | 待验 |
| 8.6 | 切换「嵌入模型」下拉的选项 | 立即提示需手动重建；旧向量不会混入新查询（搜索侧覆盖计数会掉下来） | 待验 |
| 8.7 | 语义关闭或有资料未索引时搜索 | 结果照常返回，摘要条给出中文降级原因（未启用语义检索 / 尚未建立语义索引 / 部分资料未完成向量索引 / 嵌入服务调用失败 / 索引代次已过期，需要重建 / 嵌入模型不可用 / 索引规模已达上限），界面任何地方都不出现英文枚举 | 待验 |
| 8.8 | 页面切走再回来（或重启应用） | 进行中的作业仍在面板上并继续推进；已收口的出现在「最近作业」，「查看条目」能看到每条的阶段（读取原件/解析提取/切分索引块/生成向量/发布索引/比对原件）与原因 | 待验 |

## 9. 本机索引维护（本轮新增入口）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 9.1 | 「索引与模型」→ 点「重建关键词索引」 | 内联「已提交关键词索引重建；不调用模型，向量索引与覆盖状态不受影响。」；作业行「关键词索引重建：进行中 x/y」；期间不需要任何模型，向量覆盖计数不变 | 待验 |
| 9.2 | 重建完成后搜索 | 之前能搜到的仍能找到；FTS 派生索引重建不影响已保存修订 | 待验 |
| 9.3 | 点「检查当前列表来源（N）」 | 内联「已提交 N 份资料的原件检查（分 M 批）…」；每批一个作业行；资料超过 200 份时能看到分批；未检查的条目显示「来源未检查」而不是「来源一致」 | 待验 |
| 9.4 | 库里没有资料时看这个按钮 | 「检查当前列表来源（0）」是禁用态，不给出可空转的按钮；切到某个集合后按钮上的 N 随当前列表变化（它检查的是当前筛选范围，不是全库） | 待验 |

## 10. Office 导入走查（KM13 人工部分）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 10.1 | 搜索词只出现在 PPTX 备注 / XLSX 某行 / CSV 某记录时检索 | 命中的是「slide:N/notes」「sheet:名!行区间」「rows:N-N」等对应节，详情定位一致 | 待验 |
| 10.2 | 用真实 Excel/WPS/PPT 产出的工作簿与演示稿（不是脚本生成的）导入 | 工作表名含中文/特殊字符时定位能回读；播放顺序与文件名顺序不一致时按实际播放顺序取页 | 待验 |
| 10.3 | 大表（数千行 XLSX/CSV）导入 | 要么成功且逐行分节可回看，要么在「以下条目未完成」里给出可解释原因；不出现半截「完整」知识 | 待验 |
| 10.4 | 导入只有图片的幻灯片页/图表 | 界面明示未提取到的部分（提取警告可见），不生成虚构内容 | 待验 |
| 10.5 | 处理大 Office 文件中途取消 | 有明确终态，原件与已导入资料无损 | 待验 |

## 11. 本轮不列入人工栏（写清楚，别当已验收）

- **Run 级采用声明账本**（`artifact:get-run-declarations`）与手工触发「模型声明采用」：界面没有入口，只能靠一次真实运行间接观察（6.4）。要人工验收它，需要先补界面。
- **来源检查的批量入口是「检查当前列表来源」**：它随工具栏的集合筛选走，不是一次覆盖全库；单份仍可在详情里点「检查来源」。
- 历史作业只保留最近 10 条，不是完整作业台账；更早的历史在数据库里，需要时另开界面。

## 12. 真实模型语义栏（题集待改定，未跑保持待验）

前置：真实调用授权与向量模型已具备（见 §0.3）；题目、标注与语料见[语义验收题集](knowledge-km15-semantic-set.md)——**20 个同义改写问题与「问题↔相关资料」标注在开跑前由光哥确认冻结，跑完不得回改**。

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 12.1 | 对 20 个问题分别跑关键词基线与混合检索，记录 top5 | 混合 top5 命中 ≥ 16/20 才算过栏；两列并列报告，含耗时 | 待验 |
| 12.2 | 记录失败问题与原因 | 失败可解释（降级原因/无覆盖），不回改标注 | 待验 |
| 12.3 | 用真实模型跑一次索引 | 索引与查询确实走所选 endpoint 与 model（可让对方在服务端访问日志核对），没有换模型的静默回退 | 待验 |

## 13. 打包与安装（.app 冷启动）

| # | 操作 | 预期结果 | 结果记录 |
| --- | --- | --- | --- |
| 13.1 | 打开 `apps/desktop/dist/mac-arm64/BetterWork.app`（未签名，Gatekeeper 可能要右键「打开」；`BetterWork-0.0.1-arm64.dmg` 同目录） | 应用能起、知识页能开；首次导入即触发提取子进程：若报「Worker 不可用」类错误，说明包内 ESM 入口在真实启动路径下解析失败（AI 侧已用命令行直跑通过，界面冷启动仍需确认） | 待验 |
| 13.2 | 在 .app 里导入 1 份 XLSX 和 1 份 PPTX | 与开发实例同样分节可检索；打包态依赖（exceljs/jszip/fast-xml-parser）在 asar 内解析正常 | 待验 |
| 13.3 | 用**已用过旧版本的用户数据**启动 .app | 两个库自动升级（Vault → v7、应用库 → v33）；旧资料、旧任务、旧来源仍可回看；不出现半迁移状态 | 待验 |

## 14. 收尾核对

- 每完成一节把「结果记录」改为 通过/不通过＋一句话现象；不通过项附复现步骤。
- 人工 UI 全部通过＋§12 达标后，KM15 才可 done；任何一栏缺证据时保持待验并登记缺口。
- 走查期间的界面问题按反馈通道判定：短确认应只在局部自消，跨页面长操作结果进消息中心；发现「提前报成功」或「英文枚举漏到界面」按缺陷记录，不算通过。
- 验收期间不改 E55/WM16 状态，不发布。
