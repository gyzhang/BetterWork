# 阶段 A 代码补救记录（2026-09-11）

承接 [09-10 审查](2026-09-10-phase-a-progress-quality.md)，本轮已修正运行绑定、输出可信性、文件边界与终态收口。完整工程门禁通过，但不能据此认定 A21 或阶段 A 验收完成。工作区基线为 `a67629f`，本轮未提交、推送或发布。

## 修正与证据

| 审查项 | 已实现修正 | 验证范围 |
| --- | --- | --- |
| R1 适配器无法命中 | 注册本地核验的原包内容 hash，导入受支持包时提供五命令 profile；执行前复核资源 hash | 预设与适配测试；尚未完成原包端到端生成验收 |
| R2 运行绑定不完整 | v8 保存授权的依赖选择；Run 固定修订、profile、环境、快照与 grant，使用受管 Python；校验快照清单和文件 hash | 迁移、绑定、依赖与篡改回归测试 |
| R3 无可信输出句柄 | 清理完成后由宿主收集文件及报告，保存专属副本、hash、验证状态，再提交成功结果 | 真实受控 Node 子进程到执行元数据、成果登记的组合测试，使用合成材料 |
| R4 质量门可被空报告或模型绕过 | 仅接受已识别版本的完整单文件结构报告，拒绝 issues、空白、伪 JSON、截断；登记读取宿主状态 | 报告与输出篡改测试；原校验器本地冒烟通过 |
| R5 文件父目录逃逸 | 检查父目录和叶节点，拒绝软/硬链接与越界；已有文件覆盖校验 expectedHash，原子替换 | 路径、链接、并发覆盖、中文与空格路径测试 |
| R6 清理失败仍成功 | finishRun 报告纳入终态；等待迟到启动句柄并取消；清理失败明确 run.failed | Run 与执行服务取消、撤销、迟到启动、清理失败测试 |
| R7 退出不等待清理 | before-quit 等待异步 shutdown 后关闭存储再退出，合并重复退出请求 | 退出顺序与失败重试测试 |
| R8 登记竞态与重复 | 事务内重查 Run/授权，校验文件与报告 hash；同执行同输出幂等；失败清理新副本 | 并发登记、取消/撤销竞态、篡改及事务失败测试 |

另补独立生成尝试目录，避免读取历史 exports；模板和项目以副本执行。执行日志改为有体积上限的本地持久化日志。取消信任或停用 Skill 时取消关联运行。

## 测试结果

- macOS arm64：`npm run verify` 退出 0，lint、format:check、typecheck、44 个测试文件 / 375 项测试、build 均通过。日志：`/tmp/betterwork-fix-verify-final.log`。构建仍有依赖 Zod 的 Rollup 注释警告，未导致失败。
- 原样本 `validate_pptx.py` 冒烟：临时合成两页 PPT 的完整 OK 报告被识别；关系断裂文件虽然进程退出 0，但报告包含两项 issues，不能当成功。校验脚本 hash 前后不变；临时文件已移除。日志：`/tmp/betterwork-native-validation.log`。
- 上述合成 PPT 仅用于校验器契约验证，使用 python-pptx 创建，不作为原 SVG/公司模板生成链路的替代验收。
- GATE-0 指定 SQLite 与开发日志不可用；本轮没有启动桌面应用、修改用户数据库、调用真实模型或安装依赖。

## 升级与剩余验收

- 旧授权未记录依赖选择时，需要在依赖配置中重新确认；不会猜测全局最新快照。已有自定义 profile 不自动覆盖，旧导入缺少样本预设时需检查配置或重新导入。
- 历史 execution 没有可信输出元数据时，不追认其文件为已验证成果，需要重新执行校验。
- 通用脚本路径限于受授权的 `scripts/*.py` 和绑定解释器；其他 CLI 仍需明确适配。当前文件检查不是原生进程沙箱。
- A16 仍需验证完整原工具链生成与兼容细节；A17 缺真实模型与公司模板运行记录；A19 缺 PowerPoint 无修复提示、逐元素编辑、保存重开和导出回归；A20/A21 缺冷安装完整旅程及制品证据；A09 Windows 仍受阻。
- 视觉、人工编辑保持 `not-checked`，不随结构通过升级。任务板将缺少必需验收的 A16/A17/A19/A20 记为 doing，A21 保持 todo；暂不推进 B/C。
