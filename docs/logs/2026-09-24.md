# Work Log: 2026-09-24

## 20:33 Spec §5.1–§5.5 逐条款闭合：补四条边界用例，删一条假守卫（状态：完成）

- 背景：目标模式第 10 轮，工作型记忆 Spec 的收口复核。前九轮扫到 §9.1／§11（任务板 §15.28）为止，剩下「通用实施契约」§5.1–§5.5 这五节正文一直没做逐条款闭合——它是整个记忆族的地基（类型、长度、来源、治理动作、冲突判定），也是「完成度必须有真实证据」最容易被「有强制点＝算完成」糊过去的一节。
- 变更内容：
  1. **41 条逐条款取证写进任务板 §15.29**：三条并行扫描回料后，我按行号抽查 45 处强制点（`sed -n "Np"` 逐行打印比对），只保留命中的行；用例一律写 `it()` 标题原文。
  2. **补三条用例，覆盖四处「契约写了数字／错误码，实现有强制点却没有任何用例走过」**：`packages/agent-protocol/src/index.test.ts:719`（人工正文 2,000／2,001 与 `keep-both` 说明 300／301 双向卡住）、`memory-service.test.ts`（终态记忆的编辑与治理动作都收口成 `TERMINAL_MEMORY` 且不再追加修订）、`work-centered-memory.integration.test.ts`（真实 Run 的助手消息派生出空间记录后改成全局口径被拒 `WORKSPACE_FACT_CANNOT_BE_GLOBAL`）。前两处是 §15.21 数字轴的残留，第三处是 §15.18「无生产者错误码」的反面：**有生产者、零用例**。
  3. **删掉一条假守卫**：`memory-provenance.ts` 的 `assertNoDependencyCycle` 导出但生产零调用，唯一那条用例标题写着「derived provenance refuses …」，而 `buildDerivedProvenance` 从不调它——比没有守卫更糟，因为它让后来人以为写时已挡自引用。真实环检测在 `memory-extraction-service.ts:188-205`（入队 `:750`、执行 `:870` 都有生产者和用例），读侧另有 `memory-recall-service.ts:331` 递归护栏；新建记录的身份先于依赖存在，写时自引用结构上不可能，所以按「不为用户不存在的场景造失败路径」处理为删除。
  4. **两处口径写进契约而不是改代码**：§5.1「稳定序列化不含临时消息 ID」——`modelRequestHash` 确实把每次现场 `randomUUID()` 的消息 id 哈希进去了，但它只在同一次调用内写库后回读比对（`run-memory-context-repository.ts:198-215`），从不跨请求复算，没有任何判定被削弱；§5.5「候选冲突提示不阻塞 confirmed」——召回装载集合按构造只取 confirmed，这半条无法被真实驱动。两处都记为已知边界，前者「要不要剥成可复算指纹」归光哥，后者明确不补假用例。
- 关键决策：测试断言继续写死字面量（2,000／300），变异验证因此才有意义——把 `MEMORY_CONTENT_MAX_CODE_POINTS` 放宽到 2,001、把说明上界放宽到 301，新用例各红一次；`TERMINAL_MEMORY`→`INVALID_TRANSITION`、`authority === 'derived'`→永不匹配，两次都只有新用例红、其余 16／11 条不受影响。全部还原后 `git status` 只剩本轮文件。
- 教训：**「有强制点」不等于「有证据」，「有导出」也不等于「接线」**——本轮两处新缺陷（`WORKSPACE_FACT_CANNOT_BE_GLOBAL` 有生产者零用例、`assertNoDependencyCycle` 有测试零生产调用）都是 §15.28 那条「双向核对」的换面孔版本；而且假守卫必须删，因为它在文档之外伪造了一份安全感。
- 测试证据：`npx eslint` 5 份改动文件退出 0；`npx vitest run memory-provenance.test.ts` 14 条全绿（删除后）；四次变异各自红一次并全部还原；`npm run format:check` 全仓通过；`npm run verify`（lint＋format:check＋typecheck＋全量测试＋build）退出码 **0**，112 文件／**1011 用例**全绿——用例数自洽：上一节 1009 ＋ 新增 3 − 删掉的假守卫 1 ＝ 1011。
- 相关文件：`packages/agent-protocol/src/index.ts`（变异还原后无改动）、`packages/agent-protocol/src/index.test.ts`、`apps/desktop/src/main/services/memory-service.test.ts`、`apps/desktop/src/main/services/work-centered-memory.integration.test.ts`、`apps/desktop/src/main/services/memory-provenance.ts`、`apps/desktop/src/main/services/memory-provenance.test.ts`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：Spec 还剩 §7.1／§7.2 正文逐条款（§7.1 已抽查到 `model-provider-factory.ts` 存在、`finishReason`／`usage` 已进 done chunk、`MODEL_FINISH_UNKNOWN` 有生产者、endpoint 只留 scheme＋host＋path；§7.2 数字面由 §15.21 覆盖）、§13.2 里程碑三栏记录、§16 假设与边界逐条核对。WM16 五项人工验收与真实模型语义确认仍等光哥；待拍板口径新增两条（`modelRequestHash` 要不要剥临时消息 ID、候选×已确认冲突这半条是否算已闭合），连同 §15.12／§15.15／§15.17／§15.18／§15.20／§15.22／§15.26／§15.27／§15.28 一起归光哥。

## 21:03 Spec §7.1／§7.2 逐条款闭合：把一条从未实现的模型口径补上（状态：完成）

- 背景：目标模式第 11 轮。§15.29 收完地基（§5.1–§5.5）后，本轮扫模型面：Spec §7.1「模型与 Provider」九条、§7.2「作业输入、输出及保密」八条行为面，外加 §13.2 里程碑三栏记录与 §16 假设与边界逐条——写进任务板 §15.30。
- 变更内容：
  1. **实现一条契约有、代码无的口径**：§7.1「无 Run 的讨论反馈使用当前 TaskContext 的模型」。`extraction-source-reader.ts` 里检查点没有 `runId` 时 `modelProfileId` 恒为 `undefined`，于是这类提炼悄悄走应用级默认模型；现在按 `taskContexts.getLatest(taskId).modelReference` → 执行专家修订 `modelReference` 的同一口径解析（与 `run-service.ts:1527` 给真实 Run 选模型的取法一致），只有两处都不是 `profile` 模式才交给默认。集成用例「没有来源 Run 的讨论反馈用当前 TaskContext 钉住的模型，不暗换应用级默认」两侧都断：钉住时作业带该 profile；把这条 profile 停用后同一来源可见地拒绝（`MODEL_UNAVAILABLE`、零作业登记），而应用级默认此刻仍然可用——回落等于把同一段用户反馈送给另一个服务。
  2. **补三条分支用例**：节点摘要单独撑不住一条候选（摘要只是背景）、空正文候选让整次结果失败（下限 1 码点）、契约写死的六条提炼规则确实随每次请求送出（逐句 `toContain` 原文，改词即红）。
  3. **§13.2 里程碑原来只有两栏**：自动证据／人工证据。按 Spec「三栏分列、缺一项不伪造」补第三栏「模型语义证据」，WM-M2 与 WM-M4 两行明写**未验证**（本轮全部用可捕获请求的替身模型），人工两栏全部标待光哥。
- 关键决策：只回传 `modelProfileId`、不另存一份模型引用，因为「专家说用应用级默认」与「没有专家」在解析结果上是同一个模型，多存一份反而造出第二套口径；界面重试弹窗「展示当前模型」这一半无测试，按纪律只记为边界（不为凑测试改生产代码），人工验收时在弹窗里核对模型名即是该栏证据。
- 教训：本轮抓到的这条不是「声明了没接线」而是**整句口径没落地**——前两轮的扫描轴都在问「协议里的值／码／字段有没有生产者」，而它藏在契约散文里、既没有枚举也没有错误码，只有把 §7.1 拆成九条逐条找强制点才会露出来。反面教训同时出现：我上一节写「全仓无 Embedding／向量库实现」时，`model-connectivity.ts:34-43` 里就有 `embeddings` 字样（既有模型角色连通性检查，与记忆无关）——绝对化措辞要么删掉，要么把「谁不依赖它」写清楚，本节已改成指向 `memory-retrieval.ts:24-28` 的具体证据。
- 测试证据：`npx eslint` 三份改动文件退出 0；`npm run typecheck` 通过（期间暴露 `discussionCheckpoints.create` 的入参类型需要 `taskId`、`findBySource` 要两个参数，均已修正）；`memory-extraction-prompt.test.ts` 18 条全绿（15＋3）；集成文件 13 条全绿（12＋1）。四处变异各红一次：还原 profileId 取法 → 新集成用例红且其余 12 条绿；`HUMAN_EVIDENCE_ROLES` 加 `checkpoint-summary`、`candidateContentMinCodePoints` 改 0、改掉第一条规则措辞 → 各自那一条红；全部还原后 `git diff --numstat` 生产文件只剩本轮新增。`npm run verify` 退出码 **0**，112 文件／**1015 用例**全绿（上一轮 1011 ＋ 本轮新增 4，数目自洽）。
- 相关文件：`apps/desktop/src/main/services/extraction-source-reader.ts`、`apps/desktop/src/main/services/work-centered-memory.integration.test.ts`、`apps/desktop/src/main/services/memory-extraction-prompt.test.ts`、`docs/development/memory-contracts.md`（§7.1 落点）、`docs/development/tasks-memory.md`（§15.30）。
- 下一步：Spec 剩余面收口到 §15.30 后，逐条款轴已覆盖 §2.2／§3／§5／§7.2／§7.3／§8／§9／§10／§11／§13.1／§13.2／§13.3／§16；余下可做的是 §1／§4／§6／§12／§14 的条款级复核（§6 的召回算法与预算已被 §15.5／§15.21／§15.23 分散覆盖，尚未做一次成表闭合）。WM16 五项人工验收、真实模型语义确认与 ADR-0026 批准仍等光哥；本轮新增一条待拍板（无 Run 的讨论反馈到底该走 TaskContext 钉住的模型，还是固定走应用级默认）。

## 22:08 Knowledge design and implementation review (completed)

- Scope: explain how imported local knowledge is selected and used in tasks; no implementation changes.
- Findings: original files stay read-only; the vault stores extracted text, locators and immutable revisions. Normal UI sends a saved TaskContext, so knowledge search is limited to explicitly selected revisions; an empty selection does not search the whole vault.
- Gaps: the knowledge research shortcut clears task materials and only prefills a prompt. Knowledge tools return up to eight short search excerpts, with no scoped knowledge-body reader; workspace text and Office readers do not accept knowledge references.
- Documentation drift: docs/04 still says parser/chunking versions are absent, but knowledge revisions persist both. Library search uses FTS5 with LIKE fallback; scoped revision search uses substring matching.
- Evidence: static review of KnowledgeView, ComposerCapabilityPicker, App startRun/startResearchFromKnowledge, TaskMaterialService, KnowledgeVault and RunService; no UI, database, test or real-model validation performed. Fixes remain unimplemented pending a separate request.

## 22:18 Embedding configuration usage check (completed)

- Finding: embedding profiles can be saved and connection-tested, but knowledge indexing, retrieval and task execution do not consume them. The connectivity probe sends a fixed test string to the embeddings endpoint; runtime model selection uses the language role only.
- Evidence: embedding references across desktop and packages, model-connectivity.ts, model-provider-factory.ts and use-model-settings.ts; static inspection only, no model requests or code changes.
- Collaboration preference: saved the requested model-endpoint privacy guidance in user memory; avoid repeating local-storage-versus-model-request warnings in ordinary product discussions.

## 22:22 Knowledge completion scope discussion (completed)

- Request: summarize missing knowledge capabilities and propose a focused two-day completion scope for the knowledge-worker desktop product.
- Recommendation: preserve existing import/index/revision/material infrastructure; first close task handoff, scoped body reading and source traceability, then connect embedding-based retrieval and improve lightweight management/index lifecycle. Treat additional knowledge formats and OCR as explicit scope choices, not already-approved implementation work.
- Status: scope recommendation only; distinguish verified code gaps from proposed usability additions. No code, schema, configuration or formal design changes, and no runtime verification performed.

## 23:01 知识基础闭环规范与 Qwen3.8-Flash 开发交接（状态：完成）

- 背景：光哥接受知识管理与使用补齐方向，明确本轮只形成规范和计划，业务代码将在新会话由 Qwen3.8-Flash 逐卡实现。
- 变更内容：新增产品规范、ADR-0027、实施契约、KM00–KM15 唯一任务板和编码提示词；更新文档总览、知识现状、架构、路线图、UI 与开发/ADR 导航。更正旧文档中解析版本未存、哈希不参与修订去重、范围搜索等过时说明。
- 关键决策：材料身份、正文读取、Evidence 与显式成果声明分层；嵌入消费现有模型/凭据入口，拟采用有规模上限的 SQLite Float32 精确扫描与确定性混合排序；索引分代、取消/中断不自动重试；Office 扩展独立卡，成果回收和 OCR 后置。技术方案 Proposed，所有新卡 todo，未改 E/CF/WM 状态。
- 推进建议：按资料使用、语义检索、管理与 Office 三批推进，两天只作为推进窗口，不承诺整批复杂度能在两天内完成。
- 交叉审查修订：前移管理搜索的固定修订身份；补读取足迹唯一约束迁移与重复调用预算；区分跨修订向量空间和单修订代次，强制重建不混旧空间；legacy 连续编辑不变采用声明；来源预览限定证据区间；研究幂等回执先于首次登记校验。两轮只读审查后，另补失败/中断/取消项均可手动重试、成功项不重跑的遗漏；对应产品规则、任务和提示词同步并回读。
- 检查证据：最新只读脚本检查 13 份改动文档中的新增/修改本地链接与锚点 59 项通过；新增五份文档围栏/尾随空白/尾换行、改动文本替换字符检查通过；16 卡全 todo、83 Points 汇总与依赖无环检查通过；git diff --check 退出 0。均为文档检查，不是产品功能测试。
- 基线差异：23:20 核对 HEAD 已由 f59c71a 前进至 9b93312；git diff --name-only 仅列 MemoryEditor、PopoverMenu、FieldSelect、styles 四个并行 UI 文件，不涉及知识服务。文档保留原静态核对基线并要求 KM00 开工重核，本会话未触碰这些 UI 文件。
- 执行边界：只修改 docs；未修改业务代码、配置或迁移，未启动应用、运行测试/构建、安装依赖、调用真实模型、提交或发布。
- 收尾：23:23 文档任务完成；下一会话使用编码交接的 KM00 核对技术与线框批准，再按独立授权实施，不因本日志完成而启动任何 KM 卡。
- 相关文件：docs/designs/knowledge-foundation.md、docs/adr/0027-knowledge-foundation.md、docs/development/knowledge-contracts.md、docs/development/tasks-knowledge.md、docs/development/knowledge-coding-prompts.md，以及七份既有文档入口。

## 23:42 知识文档提交与目标模式交接（状态：已授权提交推送）

- 授权：光哥要求提交所有文件，并提供 Qwen3.8-Flash 目标模式提示词；23:49 按最新指令直接提交并推送，不再运行门禁。本次不启动知识功能编码。
- 格式修正：首次 npm run verify 的 lint 通过，format:check 被既有 FieldSelect.tsx 的 JSX 换行格式挡住；经光哥批准，仅用 Prettier 合并一处属性换行，不改行为。
- 验证事实：第二次完整验证的 lint、format:check、typecheck 通过；测试为 11 个文件失败、101 个通过，24 条失败、967 条通过、26 条跳过，退出码 1，build 未执行。失败主要是超时，另有性能断言及清理错误；系统高负载仅为观察，未证实全部失败根因。
- 最新执行边界：低并发复跑被光哥中止；按明确指令跳过本次提交和推送钩子，不再测试，不修改断言、超时或业务代码，也不将跳过记为验证通过。此授权不自动延续到后续知识开发。
- 提交范围：十三份知识文档及上述一份获准的格式修正；推送 main 时包含已有未推送的 UI 提交。目标模式提示词作为后续可复制授权模板，不在本会话执行；KM 卡保持未开工，ADR 仍为 Proposed。
