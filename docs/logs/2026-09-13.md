# 2026-09-13 工作日志

## 00:46 B00-1 StartRun 协议与绑定解析改 1:N（状态：完成）

- 背景：ADR-0012 在 2026-09-12 定下设计后一直未落地。并行的阶段 A 收尾（幻灯片缩略图链路）已于 `4bf4485` 提交干净，本轮开工 B0 前置切片。基线先跑 `npm run verify` 退出 0，再确认 `agent-protocol`、`run-service.ts`、`App.tsx`、`register-ipc.ts`、preload 未被对方改动。
- 变更内容：
  - `packages/agent-protocol/src/index.ts`：`StartRunRequest.skillBinding?: SkillBinding` → `skillBindings?: SkillBinding[]`，新增 `MAX_RUN_SKILL_BINDINGS = 6`，数组在 Schema 层做 `.min(1).max(6)` 与 `skillId` 去重；不保留旧字段名。
  - `apps/desktop/src/main/services/run-service.ts`：`resolveBindingId` + `resolveSkillInstructions` 合并为 `resolveSkillBindings`，返回 `{ bindingIds, instructions }`；`ActiveRun.skillId?/bindingId?` → `skillIds: string[]`，`cancelRunsForSkill` 由相等判断改成员判断；删除「无显式绑定时延续同 Task 最近一次绑定」；`readSkillResource` / `executeSkillCommand` 改为按模型传入的 `input.bindingId` 寻址，并先校验该绑定属于本 Run；命令表每项带上自身 `bindingId`。
  - `apps/desktop/src/main/persistence/skill-execution-repository.ts`：删除只服务隐式继承的 `findLatestBindingByTask`。
  - `register-ipc.ts` 试运行通道、`App.tsx` 提交接线、`App.test.tsx` 断言同步为数组形态。
  - 文档：`docs/02` §4.1 当前实现状态、`docs/10` §7.1 能力选择条目、`docs/07` §0 指向任务板、`docs/adr/0012` 新增 §实现进度、`docs/development/README.md` 新增 B00 任务板行并把 ADR-0012 状态从「实现尚未落地」改为分段状态。
- 关键决策：
  - **上限 6 写进协议常量而不是只写文档**：注入预算与 chip 条可读性都是可被绕过的约束，只有 Schema 拦得住。
  - **去重放在 Schema 层**：重复 `skillId` 会让同一份指令注入两次、绑定快照无法回溯，属于协议非法而非业务偏好。
  - **绑定快照不再容忍伪造 ID**：旧 `resolveSkillInstructions` 在 `getBinding` 返回空时静默回退到当前修订，等于允许一个不存在于库里的 `bindingId`；现在缺快照直接失败。两个原先靠伪造 `createBinding` 存活的测试改为委托真实的 `SkillExecutionService`。
  - **不新增 `ActiveRun.bindingIds`**：工具桥接的权威判据是 SQLite 里的 `binding.run_id`；再存一份内存列表只会在长任务中与库漂移。撤销级联只需要 `skillIds`。
  - 隐式继承的删除同时写成了回归测试的正向断言（后续 Run 拿不到绑定、停用技能不连带无关 Run、无绑定 Run 不被误取消），而不是只删代码。
- 教训：
  - **拆「校验」与「建快照」两步会静默丢掉前置检查。** 我先按 `resolveBindingIds` / `resolveSkillInstructions` 拆分，`resolveBindingIds` 用 `if (!execution) return []` 短路，导致没有执行服务时启用/信任检查一个都不跑——两个既有测试立刻红。改为单次遍历内「先整体校验、后逐个落快照」才成立。跨层重构时不能只保证新路径正确，要逐条问「旧代码的每个分支现在由谁承担」。
  - **同一遍历里失败要回滚到零。** 逐个处理时第一个技能已经把 `run_skill_bindings` 写进去了，第二个才报未信任，于是失败的 Run 带着半截授权记录。ADR 的「整个 Run 不启动」必须先校验完再落盘，并补断言 `listBindingsByRun(runId)` 为空。
- 测试证据：`npm run verify` 退出 0（lint / format:check / typecheck / test / build 全绿），test 为 50 文件 / 411 测试；`db/migrate.test.ts` 未改动、无新表新列；中文内容 `grep` 回读命中且 `\ufffd` 计数为 0。尚未做真实开发窗口的双技能运行，属 B00-5 门槛。
- 相关文件：`packages/agent-protocol/src/index.ts`、`packages/agent-protocol/src/index.test.ts`、`apps/desktop/src/main/services/run-service.ts`、`apps/desktop/src/main/services/run-service.test.ts`、`apps/desktop/src/main/persistence/skill-execution-repository.ts`、`apps/desktop/src/main/ipc/register-ipc.ts`、`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/App.test.tsx`、`docs/02-domain-model.md`、`docs/07-mvp-and-roadmap.md`、`docs/10-ui-ux-system.md`、`docs/adr/0012-composer-capability-binding.md`、`docs/development/README.md`

## 01:11 B00-2 指令注入顺序与运行约定解耦（状态：完成）

- 背景：B00-1 让一次 Run 可以绑定多个 Skill，但运行约定段（`算台运行约定：...`）仍是一整段硬编码字符串，里面混着 `svg-export` / `template-merge` / `pptx-validate` 等 PPT 样本专属口径。任何带命令的 Skill 都会被告知一套并不存在的命令，模型照着编造调用。本卡把通用层与预设层拆开。
- 变更内容：
  - 新增 `apps/desktop/src/main/services/skill-runtime-conventions.ts`：通用运行约定（`skill_execute` 带 `bindingId`、不执行 Shell/pip、`task_write_file` 写 work 目录且覆盖必须 `expectedHash`、`skill_read_resource` 用对应 Skill 的 `bindingId` 且资源只读、不得自行声明验证状态）+ 命令表组装（每项带 `bindingId` + `skillName` + `commandId` + `argumentSchema`）。
  - `apps/desktop/src/main/services/skill-adapter.ts`：`SkillAdapter` 新增可选 `runtimeConventions(commandIds: ReadonlySet<string>): string | undefined`，只允许写该样本的命令该怎么用。
  - `apps/desktop/src/main/services/ppt-generation-preset.ts`：PPT 预设实现 `runtimeConventions`，按命令 id 集合条件输出（svg-export + template-merge 的 attempt 合并口径、template-merge 的 assets/ 公司模板口径、pptx-validate 的 artifact 登记闸门）。
  - `apps/desktop/src/main/services/run-service.ts`：`resolveSkillBindings` 改为按 `snapshot.revision.contentHash` 查适配预设，取回 `runtimeConventions` 后交给 `composeRuntimeConvention` 组装；正文为空的 Skill 不再注入空 system 段。
  - 测试：`skill-runtime-conventions.test.ts`（5 项，含泄漏锁——通用层不得出现 `svg-export` / `template-merge` / `pptx-validate` / `assets/` / `公司模板`）、`ppt-generation-preset.test.ts`（新增 4 项 runtimeConventions 测试）、`agent-engine.test.ts`（新增两技能各一条 system 段、顺序即用户选择顺序、互不串内容）。
  - 文档：`docs/02` §4.1 当前实现状态追加运行约定分层说明；`docs/adr/0012` §实现进度追加 B00-2 段落；`docs/development/README.md` 任务板 B00-2 改 done。
- 关键决策：
  - **通用层与预设层拆成两个文件/接口，而不是在 RunService 内部分支。** 前者是唯一持有通用契约的地方，后者由适配预设按 `contentHash` 匹配后提供。这样即使未来新增样本预设，通用层也不会被样本词汇污染；测试可以直接断言「通用层不含样本词汇」作为回归锁。
  - **命令表每项带 `bindingId` 与 `skillName`，而不是只在段落开头说一次。** 多绑定下模型在推理 `skill_execute` 调用时，需要逐条命令寻址到正确 Skill；只在段落开头说一次不够，模型容易拿错 `bindingId`。
  - **正文为空的 Skill 不注入。** `readSkillInstruction` 读 SKILL.md 后 `stripFrontmatter`，若结果为空字符串，agent-core 的 `buildSkillMessages` 仍会产出一条只有标题的 system 段。在 RunService 层过滤更干净。
  - **不在 RunService 加 model seam 做「Fake Provider 断言 messages 结构」。** agent-core 已有 `recordingModel`，B00-2 新增的 agent-engine 测试直接断言两技能各一条 system 段、顺序正确、互不串内容。RunService 到 agent-core 是 plain array pass-through，无分支，纯函数测试已足够。
- 教训：
  - **`indexOf` 跨字符串比较无意义。** 我写 `expect(systemMessages[1]?.content.indexOf('PPT 专属口径')).toBeLessThan(systemMessages[2]?.content.indexOf('研究助手口径'))`，两个 `indexOf` 返回的是各自字符串内的位置，不可比。改为断言数组顺序 + 互不包含对方内容。
  - **`toContain` 不能用在 `undefined` 上。** `adapter.runtimeConventions?.(new Set(['svg-export']))` 返回 `undefined`（因为 `svg-export` 单独不触发任何预设口径），`expect(undefined).not.toContain('attempt')` 报错。改为 `toBeUndefined()`。
- 测试证据：`npm run verify` 退出 0（lint / format:check / typecheck / test / build 全绿），test 为 51 文件 / 421 测试（新增 10 项：skill-runtime-conventions 5 项、ppt-generation-preset 4 项、agent-engine 1 项）；中文内容 `grep` 回读命中且 `\ufffd` 计数为 0。
- 相关文件：`apps/desktop/src/main/services/skill-runtime-conventions.ts`、`apps/desktop/src/main/services/skill-runtime-conventions.test.ts`、`apps/desktop/src/main/services/skill-adapter.ts`、`apps/desktop/src/main/services/ppt-generation-preset.ts`、`apps/desktop/src/main/services/ppt-generation-preset.test.ts`、`apps/desktop/src/main/services/run-service.ts`、`packages/agent-core/src/agent-engine.test.ts`、`docs/02-domain-model.md`、`docs/adr/0012-composer-capability-binding.md`、`docs/development/README.md`
