# 2026-09-07 工作日志

## 20:54 全仓代码质量与规范审查（状态：完成）

- 背景：用户要求全面检查代码质量和规范遵循，先提供报告讨论改进。
- 变更内容：新增 `docs/reviews/2026-09-07-code-quality.md`；未修改业务源码、工程配置、产品数据库或既有改动，未提交。
- 关键决策：以 `e39a854` 为审查基线；区分隔离复现、静态发现和规划缺口。建议优先修复文件边界、Run 归属校验、多工具调用，再处理流式完成/取消、成果与界面错误、IPC 和迁移规范；暂不进行大规模重构。
- 测试证据：`npm run verify` 退出码 0，lint、format:check、typecheck、21 文件/133 测试及 build 全通过；构建只有两条已知 Zod PURE 注释警告。临时合成数据确认符号链接越界、多工具仅执行最后一个、搜索取消仍等待、Task/Session 错配写入、提前 EOF 产生 done；内存 SQLite 确认 foreign_key_check 能看到同事务删除。实验不触网，临时文件已清理。
- 验证边界：文档登记的产品应用库只读打开失败；检查了开发日志，未查询产品业务内容。未做真实桌面、多主题、服务商、安装包或依赖漏洞审计；报告已说明。
- 相关文件：`docs/reviews/2026-09-07-code-quality.md`、`docs/logs/2026-09-07.md`。

## 20:59 修复 Run 权限边界与多工具调用（状态：完成）

- 背景：落实代码质量审查中优先级最高的三项：符号链接越界、Renderer 指定工作区根及 Task / Session 错配、同轮多个 ToolCall 丢失。
- 变更内容：`StartRunRequest` 删除 `workspacePath` 并拒绝额外字段；`RunService` 从持久化 Task / Session / Workspace 关系取得工作区根并在建 Run 前校验归属；`read_text_file` 用真实路径校验符号链接边界；Agent Engine 收集并按请求顺序执行同轮全部 ToolCall。
- 关键决策：保留 Agent Core 的 `workspacePath` 输入，但只允许 Application 层注入；多个工具调用串行执行，保持事件顺序和现有单线程 Tool 语义。
- 测试证据：新增符号链接越界、Task / Session 错配、协议拒绝客户端工作区根、同轮两次工具调用的回归测试；`npm run verify` 通过（21 个测试文件、137 个测试、lint、format、typecheck、build）。构建仍只有已知 Zod PURE 注释警告。
- 相关文件：`packages/agent-protocol/src/index.ts`、`apps/desktop/src/main/persistence/task-repository.ts`、`apps/desktop/src/main/services/run-service.ts`、`packages/tool-runtime/src/read-text-file.ts`、`packages/agent-core/src/agent-engine.ts`、`docs/05-capability-system.md`。

## 21:05 收敛流完成、超时、取消与进度语义（状态：完成）

- 背景：落实代码质量审查中运行可靠性的优先项：模型流提前结束被当作完成、模型请求没有超时、联网搜索无法随 Run 取消、Tool 进度直到结束才广播。
- 变更内容：OpenAI-compatible Provider 使用可注入 fetch，并将 Run 取消与 120 秒流超时合并；仅在收到 `[DONE]` 或 `finish_reason` 后完成，提前 EOF 失败。Web Search 将 Run signal 传入搜索客户端并与 20 秒超时合并。Agent Engine 在 Tool 执行期间立即产出 `tool.progress`，取消时不再等待不响应的 Tool Promise。
- 关键决策：模型流采用整体 120 秒上限，覆盖首包与后续读取；兼容未发送 `[DONE]` 但提供标准 `finish_reason` 的服务商。多个 ToolCall 仍维持串行事件顺序。
- 测试证据：新增模型提前 EOF、finish reason、请求取消、超时、搜索 HTTP 取消及实时 Tool 进度时序测试；`npm run verify` 通过（21 个测试文件、144 个测试、lint、format、typecheck、build）。构建仍只有已知 Zod PURE 注释警告。
- 相关文件：`packages/agent-core/src/openai-compatible-provider.ts`、`packages/agent-core/src/agent-engine.ts`、`packages/tool-runtime/src/web-search.ts`、`apps/desktop/src/main/services/search-engine-service.ts`、`docs/03-system-architecture.md`。

## 21:18 收敛成果与界面异步状态（状态：完成）

- 背景：落实代码质量审查中成果与界面的第 6–8、10 项：成果误存中间文本、版本错误不可见、异步切换可能被旧响应覆盖，以及确认对话框的重复确认和键盘焦点缺口。
- 变更内容：成果保存仅使用 `run.completed.finalContent`；Run 快照与加载期间到达的事件按稳定 id 合并。Artifact、Task 运行历史和 Evidence 的异步请求均带请求代号，忽略过期响应；Artifact 预览区始终展示版本加载错误。ConfirmationDialog 改为 Portal，并提供 inert、初始焦点、Escape、Tab 环绕和关闭后焦点恢复；移除 Knowledge 操作中的原生二次确认。
- 关键决策：首次 Renderer 测试采用 React Testing Library 与 jsdom，保留现有 Node 默认测试环境，仅对 `.test.tsx` 文件声明 jsdom；受控 Promise 覆盖 Artifact 列表的乱序响应，避免依赖真实定时器。
- 测试证据：新增最终回复选择与事件合并、Artifact 版本加载错误、Artifact 列表过期响应、ConfirmationDialog 焦点与键盘交互测试；`npm run verify` 通过（25 个测试文件、149 个测试、lint、format、typecheck、build）。构建仍只有已知 Zod PURE 注释警告。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/hooks/use-artifact-viewer.ts`、`apps/desktop/src/renderer/src/components/ConfirmationDialog.tsx`、`apps/desktop/src/renderer/src/lib/run-events.ts`、`docs/10-ui-ux-system.md`、`docs/11-qoder-handoff.md`、`docs/12-engineering-standards.md`。

## 21:28 收敛 IPC、迁移完整性与持续验证（状态：完成）

- 背景：落实代码质量审查中规范闭环的第 9、11 项，并补齐持续集成与首个端到端用户旅程。
- 变更内容：共享协议新增 IPC 返回值 Schema；三个注册 helper 都在输入解析后校验输出，文件选择与资料导入改为走 `handleNoInput`。迁移在打版本戳后、提交前执行 `foreign_key_check`，发现孤儿行即回滚。新增 GitHub Actions，在 Pull Request 与 `main` 推送执行 `npm run verify`。
- 关键决策：首个端到端旅程使用 Electron IPC 替身调用已注册 handler，真实走 Workspace、Task、Fake Run、Artifact、人工修订和导出，不依赖桌面窗口或网络；真实桌面自动化作为后续专项保留。
- 测试证据：IPC 覆盖非法输入、无入参通道、输出形状、来源白名单及完整本地旅程；迁移覆盖制造外键孤儿时不打版本戳且回滚。`npm run verify` 通过（25 个测试文件、155 个测试、lint、format、typecheck、build）。构建仍只有已知 Zod PURE 注释警告。
- 相关文件：`packages/agent-protocol/src/index.ts`、`apps/desktop/src/main/ipc/register-ipc.ts`、`apps/desktop/src/main/ipc/register-ipc.test.ts`、`apps/desktop/src/main/db/migrate.ts`、`.github/workflows/verify.yml`、`docs/07-mvp-and-roadmap.md`。

## 22:49 合并 Task 下所有 Run 到主对话视图 + 跨 Run 对话记忆（状态：完成）

- 背景：用户发现同一 Task 下多轮对话只能在右侧「执行记录」中逐条切换查看，且连续 Run 之间不传递对话上下文——模型对上一轮内容完全无记忆。
- 变更内容：
  - **UI 合并展示**：`App.tsx` 新增 `taskAllRuns` / `taskAllEvents` 状态和 `loadAllTaskRuns` 方法，主对话区域按时间顺序遍历所有 Run，每组渲染用户提问 → 助手回复（MarkdownPreview）→ 工具卡片，Run 之间用带时间戳的分隔线区分；最新已完成 Run 下方显示「保存为成果」按钮；新增自动滚动效果。
  - **跨 Run 记忆**：`RunRepository` 新增 `listByTask` 方法（按创建时间正序返回全部 Run）；`RunService.buildPreviousMessages` 从同一 Task 下已完成的旧 Run 提取 user/assistant 消息对，通过 `engine.run({ messages })` 传入引擎，引擎已有的 `[system, ...input.messages, user]` 拼接逻辑自动生效。
  - **辅助函数**：`run-events.ts` 新增 `extractAssistantText` 和 `extractCompletedTools`，供合并视图按 Run 提取内容。
  - **清理**：移除不再使用的 `sentPrompt`、`assistantText`、`assistantDisplayText`、`completedTools`、`toolNameById`、`isCompletedRun`、`completedRunContent`；`saveCurrentArtifact` 改用 `latestCompletedRun`（从合并视图中取最新已完成 Run 的 ID 和内容）。
- 关键决策：跨 Run 历史只包含 user/assistant 消息，不含 tool 调用事件，避免跨 Run 的 toolCallId 配对问题；失败/取消的 Run 不纳入历史；动画时长使用 `--motion-overlay` Token 而非硬编码。
- 测试证据：`npm run verify` 通过（25 个测试文件、155 个测试、lint、format、typecheck、build）。构建仍只有已知 Zod PURE 注释警告。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/lib/run-events.ts`、`apps/desktop/src/renderer/src/styles.css`、`apps/desktop/src/main/persistence/run-repository.ts`、`apps/desktop/src/main/services/run-service.ts`。
