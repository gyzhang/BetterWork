# 2026-09-14 工作日志

## 10:34 专家召唤与对话入口修订（状态：完成）

- 背景：用户评审指出专家页“返回任务准备”导航混乱、启动前配置过重，确认改为参考 ClawBible 的“召唤 → 直接对话”。
- 变更内容：设计文档升为 v0.2；独立专家列表主要按钮改为召唤，编辑配置独立退出回专家页；删除任务准备表单与工作依据预览门槛；期间与数据缺项通过对话补充，材料用途默认推导、详情按需查看。同步 UI 规范、路线图、B/C 规划及 Proposed ADR-0014。
- 关键决策：业务输入不足不阻止理解与澄清对话；模型配置、授权、Skill 信任/依赖和无效绑定仍按发送边界检查。新交互已确认，其余架构建议仍待评审。
- 草图：新增当前任务可视化目录下 expert-summon-v02.html，保留 v0.1 草图供对照。默认专家列表；支持召唤、独立配置编辑、示例对话、材料增删、能力简要查看、历史草稿切换及成果复用。全部合成数据，仅当前草图内保存状态。
- 测试证据：5 份相关文档链接检查、草图 JS 语法编译、36 个唯一 DOM ID 与 75 处字面量元素引用检查通过；git diff --check 通过。沿用前轮本地 URL 受浏览器策略限制的记录，本轮未绕过或重试浏览器预览，未完成实际浏览器交互与视觉验收。仅文档与独立草图修改，未运行产品构建。
- 相关文件：docs/designs/experts-and-task-materials.md、docs/adr/0014-expert-context-and-material-binding.md、docs/10-ui-ux-system.md、docs/07-mvp-and-roadmap.md、docs/development/phase-b-c-roadmap.md。


## 11:23 专家设计定稿与开发计划（状态：完成）

- 背景：用户通过专家与任务材料 v0.2 评审，要求落实设计并形成开发计划。
- 变更内容：设计标记 Accepted/待开发，ADR-0014 接受并登记；AGENTS、领域、架构、能力、知识/记忆、UI 规范和路线图同步正式方向；新增 tasks-experts.md 的 27 张任务卡、唯一任务板、五个里程碑、前置核对与旧 B/C 归属映射。ADR-0012 仅追加后续扩展关系，不重写历史决策。
- 关键决策：E00 核对 A/macOS 与 B0 真实验收尾项；E1 先交付召唤/配置/执行，后续 E2 材料、E3 记忆、E4 MCP/网页正文、E5 Office 输入和两期交付。27 项全部 todo，设计通过不等于业务实现。记忆投影、MCP 接入和 Office 解析由明确任务完成技术定案，不重复询问已接受产品范围。
- 测试证据：静态源码/任务板基线 94263d2；任务板与卡片 27/27 一一对应、依赖有效且无环；19 份当前变更/新增 Markdown 本地链接检查通过；git diff --check 通过。仅文档变更，未运行产品测试/构建或真实验收，未修改业务代码。
- 相关文件：docs/development/tasks-experts.md、docs/designs/experts-and-task-materials.md、docs/adr/0014-expert-context-and-material-binding.md、AGENTS.md 及关联正式文档。

## 13:40 E00 前置核对与 macOS 启动证据（状态：完成）

- 背景：按专家开发计划先核对阶段 A/macOS 与 B0 尾项，再进入专家契约；不把设计接受或自动测试误写成真实产品验收。
- 变更内容：E00 任务板改为 done；B00-5 因真实双技能运行与重启回看没有可追溯记录，恢复为 doing。没有修改业务代码。
- 关键决策：`npm run verify` 的 52 个测试文件 / 433 项测试、类型检查、构建均通过；`bash scripts/dev-start.sh` 成功启动真实 Electron 窗口，`/tmp/betterwork-dev.log` 记录主进程注册内置 Skill 且无运行时错误；在窗口中核对 `+ → 技能`、可用/不可用状态、选择 `ppt-generation-expert` 后回到任务编辑器并显示可移除 chip。双技能真实执行与重启后的历史绑定恢复仍保留人工验收缺口，不伪造通过。
- 测试证据：`npm run verify` 退出 0；开发窗口启动退出码保持运行；UI 走查使用当前 macOS 窗口完成。停止条件仍是 B00-5 人工双技能旅程，后续可在有第二个可执行 Skill 与可用 Provider 时补做。
- 相关文件：docs/development/tasks-experts.md、docs/development/README.md、/tmp/betterwork-dev.log（运行时证据）。

## 13:44 E10 专家与任务上下文契约（状态：完成）

- 背景：E00 前置核对完成后，先把 Expert、ExpertRevision、TaskContextRevision 与启动边界定成可实现契约，避免 E11–E15 各自猜字段或提前建未来切片的空表。
- 变更内容：新增 [expert-contracts.md](../development/expert-contracts.md)，定义 Expert 生命周期与不可变修订、顺序 Skill 预设、内置工具 allow-list、模型引用、E1 任务草稿、CAS 冲突、启动事务、旧任务通用助手迁移、稳定错误码和消费者。同步领域模型与系统架构，任务板 E10 改为 done。
- 关键决策：通用助手无专家/无 Skill 是合法路径；Expert 修订只在显式应用后进入新任务上下文；Skill 预设最终转换为现有 `StartRunRequest.skillBindings`，不创建第二套授权；E10 不新增 SQLite 表、IPC 或材料/记忆/MCP 占位字段。旧任务不从 prompt、Skill、模型或路径推断历史专家。
- 测试证据：Markdown 本地链接与 `git diff --check` 待提交前执行；本卡仅文档与契约，未改业务代码，不跑产品构建。
- 相关文件：docs/development/expert-contracts.md、docs/development/tasks-experts.md、docs/02-domain-model.md、docs/03-system-architecture.md。

## 13:54 E11 Expert 管理与不可变修订（状态：完成）

- 背景：按 E10 契约先交付专家身份与配置管理，保证后续召唤和执行注入有真实持久化对象；本卡不把 Expert 接进 Run，也不创建 TaskContext、材料、记忆或 MCP 表。
- 变更内容：新增 app schema v9 的 `experts` / `expert_revisions`；实现 ExpertRepository、ExpertService、ExpertErrorCode；共享协议增加 Expert 生命周期、修订草稿、Skill 预设、内置工具策略、模型引用和 IPC channel；注册 Main IPC 与 Preload API；新增迁移、Repository、Service 覆盖。
- 关键决策：修订只增不改；内置 Expert 只读、复制生成 user Expert；归档保留历史；Skill/模型缺失允许保存但在摘要显示阻塞原因；未知内置工具在管理边界拒绝；模型密钥不进入 Expert 数据；乐观修订冲突拒绝覆盖。
- 测试证据：`npm run verify` 退出 0（lint、格式、类型、54 个测试文件 / 441 项测试、Electron build）；定向 Expert/迁移/IPC 测试 37 项通过；迁移回滚/外键/重开由现有门禁覆盖。
- 已知限制：当前没有 Expert 列表/编辑 UI，Expert 不影响 Run 的人格、工具或模型；召唤和执行注入留 E12–E14。
- 相关文件：apps/desktop/src/main/db/app-schema.ts、apps/desktop/src/main/persistence/expert-repository.ts、apps/desktop/src/main/services/expert-service.ts、packages/agent-protocol/src/index.ts、apps/desktop/src/main/ipc/register-ipc.ts、apps/desktop/src/preload/index.ts。

## 14:04 E12 专家执行注入与能力裁决（状态：完成）

- 目标：把 E10 的显式 TaskContextRevision 接入真实 Run，固定本次 Expert 人格、模型引用、Skill 顺序和内置工具集合。
- 变更内容：新增 app schema v10 的 `task_context_revisions` 与 `TaskContextRepository`；支持按任务读取、顺序绑定、CAS 修订和重复 Skill 拒绝。`RunService` 在启动边界校验上下文归属、Expert 生命周期/修订，并将人格合成为单一 system 指令；模型 profile 与内置工具 allow-list 在执行时生效。Agent Core 增加 Expert 指令槽位，保持普通 Run 兼容。
- 验证：定向 Agent Core、RunService、TaskContextRepository、迁移测试 4 文件共 62 项通过；`npm run verify` 退出 0（55 个测试文件 / 448 项测试 / Electron build）。
- 已知限制：召唤入口和对话草稿 UI 仍由 E13/E14 实现；材料、记忆、MCP 未提前暴露，当前 Knowledge/Web 工具仍是应用级能力。
- 相关文件：packages/agent-protocol/src/index.ts、packages/agent-core/src/agent-engine.ts、apps/desktop/src/main/persistence/task-context-repository.ts、apps/desktop/src/main/services/run-service.ts、apps/desktop/src/main/db/app-schema.ts。

## 14:15 E13 专家召唤、草稿与身份恢复（状态：完成）

- 目标：按已确认的 ClawBible 式入口，从专家列表直接召唤到工作对话，不增加“返回任务准备”或二次开始步骤。
- 变更内容：新增专家列表视图和只读召唤卡片；扩展 TaskContext IPC/Preload，首条消息在创建 Task 后保存通用助手或 ExpertRevision、Skill 顺序和来源，再以修订号启动 Run。任务选择与重开会恢复已保存的 Expert/Skill，上下文不足时仍保留旧任务的通用助手兼容。Composer 的 `+ → 专家` 打开同一专家列表。
- 验证：新增 App 召唤/首条消息/任务上下文恢复测试和 IPC CAS 测试；`npm run verify` 退出 0（55 个测试文件 / 451 项测试 / Electron build）。
- 已知限制：专家配置编辑仍由 E14 实现；材料、成果选择、记忆和 MCP 不在本卡提前显示或生效。
- 相关文件：apps/desktop/src/renderer/src/views/ExpertsView.tsx、apps/desktop/src/renderer/src/hooks/use-experts.ts、apps/desktop/src/renderer/src/App.tsx、apps/desktop/src/main/ipc/register-ipc.ts、packages/agent-protocol/src/index.ts。

## 14:21 E14 专家管理与按需配置 UI（状态：完成）

- 目标：把专家配置作为独立管理空间，编辑人格、Skill 预设和内置工具，并保持召唤入口只进入对话。
- 变更内容：专家页增加新建、详情、编辑修订、内置专家复制、启停/归档；编辑器按需展开 Skill 与工具选项，保存复用 E11 的版本冲突和不可变修订 IPC。配置编辑不修改已存在 TaskContext 或运行中的输入。
- 验证：App 测试覆盖配置加载、编辑和修订保存；`npm run verify` 退出 0（55 个测试文件 / 452 项测试 / Electron build）。
- 已知限制：模型 profile 的高级选择、材料/记忆/MCP 配置仍留在各自 E20/E30/E40 切片，不在专家编辑器放置未生效字段。
- 相关文件：apps/desktop/src/renderer/src/views/ExpertsView.tsx、apps/desktop/src/renderer/src/App.test.tsx、apps/desktop/src/renderer/src/styles.css。

## 14:26 E15 内置专家分发与 E1 收口（状态：完成）

- 目标：把首个可用 Expert 作为安装资源稳定分发，重启不重复创建，内置原件继续保持只读。
- 变更内容：新增 `resources/experts/release-manifest.json`，启动时由 Main 读取并幂等注册 stable builtin Expert；electron-builder 将 `resources/experts` 复制到安装包；ExpertService 在管理边界验证清单，内置 Expert 与用户 ID 冲突时拒绝静默覆盖。
- 验证：内置注册幂等、只读和复制行为测试通过；`npm run verify` 退出 0（55 个测试文件 / 453 项测试 / Electron build）。
- 已知限制：内置样本是研究分析人格，真实 PPT/经营分析 Skill 组合仍需后续 E20–E55 的材料与 Office 路径；不以示例 Expert 宣称完整业务交付。
- 相关文件：resources/experts/release-manifest.json、apps/desktop/src/main/services/expert-service.ts、apps/desktop/src/main/index.ts、apps/desktop/electron-builder.yml。

## 14:35 E20 材料、快照与运行来源契约（状态：完成）

- 目标：在实现 E2 材料功能前固定候选、任务选择、版本快照、运行读取和成果输入的事实边界，避免把 Workspace 目录或整个 Knowledge Vault 当成专家权限。
- 变更内容：新增 [material-contracts.md](../development/material-contracts.md)，定义 Knowledge revision、ArtifactVersion、Workspace 输入快照、六类材料用途、Workspace 候选与 Task 授权、RunContextSnapshot、输入快照状态/恢复/回收、两库边界、RunMaterialRead 和 ArtifactInputRelation；列明 `read_text_file`、`knowledge_search`、成果读取、Skill/Worker 和来源登记的范围校验落点。
- 关键决策：候选、选择、读取和成果采用分别记录；内容修订不追随最新；跨 Workspace 只有显式来源选择可用；归档版本在有历史引用时保留；缩小范围创建新上下文段。明确现有 `read_text_file` 与 `knowledge_search` 仍未过滤，留给 E23；脚本按本机用户权限运行，不宣称 OS 沙箱。
- 验证：Markdown 本地链接检查与 `git diff --check` 通过；本卡仅契约和架构文档变更，未运行产品构建或测试。
- 相关文件：docs/development/material-contracts.md、docs/development/expert-contracts.md、docs/development/tasks-experts.md、docs/02-domain-model.md、docs/03-system-architecture.md、docs/04-knowledge-and-memory.md、docs/adr/0014-expert-context-and-material-binding.md。

## 15:04 E21 知识修订、输入快照与恢复（状态：完成）

- 目标：让材料在进入任务前拥有可重现的内容版本和受管文件副本，处理文件变化、取消、断电/重启和孤儿资产。
- 变更内容：知识库迁移 v3 新增 `knowledge_revisions` / `knowledge_revision_chunks`，历史库自动生成 revision 1，刷新按内容哈希追加修订；应用库迁移 v11 新增 Workspace 所属 `input_snapshots`；新增 `InputSnapshotRepository` 与 `InputSnapshotService`，接通稳定读取、普通文件/符号链接/Workspace 边界检查、SHA-256 内容寻址复制、状态收口、恢复和孤儿清理，并在主进程启动装配恢复。
- 关键决策：知识原件继续只引用原路径；输入快照是受管只读输入，不登记为 Artifact；快照缺失/哈希不符标记失败而不静默换源；相同内容可复用物理文件但保留 Workspace 记录。材料选择、运行范围过滤、读取足迹和成果输入关系留在 E22–E24。
- 验证：`npm run verify` 退出 0（56 个测试文件 / 459 项测试 / Electron build）；定向知识、迁移和输入快照测试 29 项通过。
- 相关文件：apps/desktop/src/main/db/knowledge-schema.ts、apps/desktop/src/main/db/app-schema.ts、apps/desktop/src/main/persistence/input-snapshot-repository.ts、apps/desktop/src/main/services/input-snapshot-service.ts、apps/desktop/src/main/services/knowledge-vault.ts。

## 15:19 E22 材料选择与草稿持久化（状态：完成）

- 目标：把“候选材料”和“本次任务实际选择”分开，重启后恢复精确来源、用途和版本，并在 Main 保存边界拒绝失效或越界引用。
- 变更内容：TaskContextRevision 迁移 v12 新增 `materials_json`；共享协议增加 `MaterialReference`、六类 `MaterialPurpose`、候选和输入快照类型；新增 `TaskMaterialService`，提供 Knowledge revision、Workspace ArtifactVersion 和 ready 输入快照候选，校验内容哈希、重复项、快照完整性和 Workspace 归属；新增候选查询、Workspace 文件快照 IPC/Preload；App 保存并恢复材料草稿。
- 关键决策：候选只用于发现，不授予读取权；Artifact 跨 Workspace 只有显式全局选择入口允许，输入快照必须属于当前 Workspace；材料中的文本不能修改专家或工具授权。现有 `read_text_file`/`knowledge_search` 仍未接 Run 范围过滤，留给 E23。
- 验证：`npm run verify` 退出 0（57 个测试文件 / 463 项测试 / Electron build）；定向材料、TaskContext、IPC 和协议测试 23 项通过。
- 相关文件：packages/agent-protocol/src/index.ts、apps/desktop/src/main/persistence/task-context-repository.ts、apps/desktop/src/main/services/task-material-service.ts、apps/desktop/src/main/ipc/register-ipc.ts、apps/desktop/src/preload/index.ts、apps/desktop/src/renderer/src/App.tsx。

## E23 宿主范围与上下文收缩（状态：完成）

- 应用库 v13 新增 `run_context_snapshots`，Run 启动事务保存 Task、Workspace、TaskContextRevision、材料引用和上下文段。
- `RunService` 将 Knowledge 搜索限制在所选 revision，将工作区文本读取限制到所选输入快照的受管副本，新增精确 Markdown `read_artifact` 读取；越界路径、未选成果和失效材料在宿主边界拒绝。
- 材料集合严格缩小时切换上下文段，旧段的助手回复不会再次进入模型输入；未绑定材料的旧任务保持旧历史兼容。
- 新增运行范围、快照仓储和工具工厂测试；`npm run verify` 退出 0（59 文件 / 470 测试 / Electron build），2026-09-14。

## E24 读取足迹与成果输入来源（状态：完成）

- 应用库 v14 新增 `run_material_reads` 与 `artifact_input_relations`；协议增加读取足迹、输入关系和成果登记输入字段。
- RunService 对 Knowledge 搜索、输入快照和 Markdown ArtifactVersion 读取记录具体材料、定位、内容哈希和摘要哈希；普通旧 Run 的全局知识检索也会登记可解析的精确 revision。
- Markdown 与文件成果登记可显式声明输入材料或同 Run Evidence，Main 校验它们已经由同一 Run 实际读取；选中但未读取、旧 Run Evidence 和人工修订不会自动变成来源。
- `npm run verify` 退出 0（60 文件 / 473 测试 / Electron build），2026-09-14。

## E25 材料交互、成果复用与 E2 验收（状态：完成）

- 目标：把已完成的候选、任务授权、运行范围和来源关系接到用户实际工作的入口，并支持从精确成果版本开始下一期任务。
- 变更内容：Composer `+` 菜单新增添加文件、引用知识和引用成果；新任务在首条消息前可按 Workspace 查询候选，文件通过受管输入快照进入材料草稿。已选材料显示紧凑 chip，可调整用途、移除和识别不可读项；右侧资料面板展示本次材料并提供同一组入口。候选成果包含其他 Workspace 的版本并显式标记来源，Office 文件输入在 E51 前显示不可读取。成果详情新增“基于此版本开始新任务”，固定精确 Markdown 版本，沿用仍有效的专家，清空旧上下文、本期输入和期间草稿。
- 关键决策：选择器使用本地草稿，点击“取消”不修改原选项；候选不授予读取权，发送仍由 Main 校验；用途映射为 ArtifactInputRelation，只有同一 Run 实际读过的材料才能在保存成果时登记为来源。新任务不复制旧聊天。
- 验证：新增 Composer 材料选择取消/提交测试；定向 Renderer、服务和成果测试通过；`npm run verify` 退出 0（61 个测试文件 / 475 项测试 / Electron build），2026-09-14。
- 已知限制：PPTX/XLSX 输入解析留 E50/E51；Memory、MCP、网页正文和讨论节点仍按 E30–E56 推进；文件成果输入关系沿用 E24 Main 校验边界。

## E30 记忆范围与治理 ADR（状态：完成）

- 目标：在实现记忆存储前固定范围、确认状态、来源、预算、快照和投影真相源，避免把聊天全文或公司规则静默沉淀为长期事实。
- 变更内容：新增 ADR-0015，定稿 User、Workspace、Expert、Expert×Workspace 四种判别范围；candidate/confirmed/superseded/expired/deleted 状态；来源与修订哈希；16 条/6,000 字符检索预算；Run 级记忆快照和实际读取足迹；范围收缩、并发编辑、投影失败和重启恢复语义。更新 ADR 索引、E3 任务卡、知识记忆文档和 E1–E2 总 ADR。
- 关键决策：SQLite 是唯一真相源，Markdown 只可重建；首版不接受手改 Markdown 回写，不做自动反思、Embedding、向量检索或 Task 长期记忆。E31 实现存储/注入，E32 实现管理 UI 与 E3 验收。
- 验证：Markdown 结构与 `git diff --check` 通过；本卡为文档与 ADR 变更，未改变运行代码。

## E31 记忆存储、检索与运行注入（状态：完成）

- 目标：把 E30 的记忆治理落到真实 SQLite 修订、作用域检索、Run 注入和可重建投影，保证候选不自动进入模型且历史 Run 可追溯。
- 变更内容：应用库迁移 v15 新增 `memory_records` 与 `run_memory_reads`；新增 `MemoryRepository`/`MemoryService`，支持 User、Workspace、Expert、Expert×Workspace 作用域、不可变内容修订、CAS 冲突、状态转移、有效期和来源哈希。confirmed 记忆按稳定优先级最多 16 条、总内容 6,000 字符注入新 Run，实际 revision/content hash 写入读取足迹；`memory/.../index.md` 按作用域原子重建，SQLite 是唯一真相源。共享协议、Main IPC、Preload 和启动恢复已接通。
- 关键决策：记忆只作为工作背景，不改变系统指令、工具授权或材料范围；candidate、deleted 和已过期记录不进入适用集合；投影失败不回滚已提交数据库状态。管理 UI、对话确认、编辑/删除入口和“本任务不用”留给 E32。
- 验证：`npm run verify` 退出 0（63 个测试文件 / 480 项测试 / Electron build）；新增协议、仓储、投影和 Run 读取足迹测试，2026-09-14。
- 相关文件：packages/agent-protocol/src/index.ts、apps/desktop/src/main/db/app-schema.ts、apps/desktop/src/main/persistence/memory-repository.ts、apps/desktop/src/main/services/memory-service.ts、apps/desktop/src/main/services/run-service.ts。

## E32 记忆管理、对话确认与 E3 最小闭环（状态：完成）

- 目标：让用户可以看见、确认、修改和删除记忆，并在当前任务中明确排除某条背景，避免“记忆生效”变成不可解释的隐式开关。
- 变更内容：TaskContextRevision 迁移 v16 增加 `excluded_memory_ids_json`；设置页新增记忆管理，支持用户作用域记忆创建、candidate 确认、内容修订和删除；已完成的助手消息增加“记住这段经验”确认表单，可选择 User、Workspace 或 Expert×Workspace 作用域；任务资料面板展示适用记忆并提供“本任务不用”，排除项只影响后续该 TaskContextRevision 运行。新增 Renderer、协议、TaskContext 和 RunService 覆盖。
- 关键决策：只有用户显式点击确认后才写入 `confirmed`；对话内容先进入可编辑确认框，不把整段回复静默写入长期记忆；删除继续保留历史修订，当前任务排除不修改全局记忆。自动反思和批量整理留后续阶段。
- 验证：`npm run verify` 退出 0（64 个测试文件 / 483 项测试 / Electron build）；E3 最小记忆闭环完成，2026-09-14。
- 相关文件：apps/desktop/src/renderer/src/views/MemoryView.tsx、apps/desktop/src/renderer/src/hooks/use-memories.ts、apps/desktop/src/renderer/src/components/ContextPanel.tsx、apps/desktop/src/main/persistence/task-context-repository.ts、packages/agent-protocol/src/index.ts。

## E40 MCP 实现 ADR 与离线探测样本（状态：完成）

- 目标：在接入 MCP 代码前先固定首轮传输、工具授权、稳定 ID、取消/断线/退出语义，避免把“发现工具”误当成“已授权能力”。
- 变更内容：新增 [ADR-0016](../adr/0016-mcp-transport-and-lifecycle.md)，选定官方 `@modelcontextprotocol/client` v2.0.0 的首轮 stdio 客户端路径；定义连接状态、`connectionId/toolName` 稳定标识、tools/list 候选与 Expert/Task 授权交集、Schema 变化重新确认和共享连接取消边界。新增 `scripts/fixtures/mcp-finance-readonly-server.mjs` 与 `scripts/mcp-probe.mjs`，离线替身提供只读 `finance.monthly_summary`。
- 关键决策：stdio 子进程复用现有进程监督；首轮只适配只读工具调用，Streamable HTTP、OAuth、写入型业务工具和 Resource/Prompt 留后续；外部账号不可得时不伪造真实业务验收。
- 验证：`node scripts/mcp-probe.mjs` 通过 initialize → tools/list → tools/call → clean close；文档链接与 `git diff --check` 通过。本卡尚未引入 SDK 依赖，E41 再按 ADR 安装并接入客户端。

## E41 MCP 运行设施、工具适配与取消边界（状态：完成）

- 目标：把 E40 选定的 stdio MCP 路径接入真实 Run 工具集合，保持工具发现、授权、进度、超时、取消和退出都可解释。
- 变更内容：新增应用库 v17 的 `mcp_connections` 与 TaskContext `mcp_tool_bindings_json`；引入官方 `@modelcontextprotocol/client@2.0.0`，实现 `McpConnectionRepository`/`McpClientService`，支持连接检测、tools/list、稳定 `connectionId/toolName`、Schema 哈希、受限输出和 `AgentTool` 适配。只有显式绑定的工具进入 Run；共享连接可并发调用，单次 AbortSignal 不关闭其他 Run；应用退出统一关闭 MCP 客户端。IPC/Preload 已接通，E42 再提供配置与选择界面。
- 关键决策：工具描述仍是候选信息，新增工具不自动授权；工具级错误、协议错误、超时和断线都交给现有 Run 失败/取消收口；密钥字段未进入连接摘要或日志，首轮 stdio 配置只保存命令、参数和工作目录。
- 验证：新增协议、迁移、连接服务和 IPC 覆盖；离线财务替身通过发现与调用，`npm run verify` 退出 0（65 个测试文件 / 486 项测试 / Electron build），2026-09-14。真实外部业务账号仍留 E43 人工验收。
- 相关文件：apps/desktop/src/main/services/mcp-client-service.ts、apps/desktop/src/main/persistence/mcp-connection-repository.ts、apps/desktop/src/main/services/run-service.ts、packages/agent-protocol/src/index.ts、apps/desktop/package.json。


## E42 MCP 设置与专家/任务工具选择（状态：完成）

- 目标：把 E41 的运行绑定变成可见、可复用、可按任务调整的产品能力，保持工具发现与实际授权分离。
- 变更内容：应用库迁移 v18 为 ExpertRevision 增加 MCP 工具预设；设置页支持 stdio 连接新增、编辑、删除和检测，状态与工具目录只返回安全摘要。专家编辑器可选择已发现工具，召唤时将预设带入当前任务；当前任务资料面板按连接选择具体工具并写入 TaskContext，历史任务保持原绑定。失效连接/工具显示 `mcp-unavailable`，不会自动替换或加入新工具。
- 验证：新增 ExpertRepository/ExpertService 覆盖同一连接不同工具、失效工具和不可变复制；`npm run verify` 退出 0（67 个测试文件 / 493 项测试 / Electron build），2026-09-14。真实外部业务账号仍留 E43 人工验收。


## E43 网页正文与来源证据（状态：完成）

- 目标：在已有 `web_search` 结果上提供可取消、受限、可回溯的公开网页正文读取，不把外部内容变成授权或业务写入。
- 变更内容：新增 [ADR-0017](../adr/0017-web-fetch-and-evidence-boundary.md)、`web_fetch` Tool Runtime 和 Main `WebFetchService`；只允许 HTTP(S) 公开主机，逐跳校验重定向，15 秒超时、AbortSignal、1 MiB 上限和正文 Content-Type 校验。HTML 去除脚本/样式后提供正文和标题；成功读取登记最终 URL、时间、内容哈希、HTTP/Content-Type 定位及摘录为 `web-page` Evidence。Expert 工具 allow-list、过程标签和失败/取消收口已接通。
- 验证：注入 fetch 替身覆盖正文提取、私网目标、重定向、二进制响应和取消；Tool Runtime、RunService 和证据路径测试补齐。真实搜索→正文→来源旅程仍需外部网络条件下人工验收。


## E50 Office 输入解析定案（状态：完成）

- 目标：为月度报告需要的上期 PPT、本期 XLSX/CSV 输入确定可打包、可取消、可回溯的解析路径，不把缩略图预览当成数据读取。
- 变更内容：新增 [ADR-0018](../adr/0018-office-input-parsing-boundary.md)，选择 JSZip + fast-xml-parser 解析 PPTX 文本/表格/备注，ExcelJS 4.4.0 读取 XLSX Sheet/Range/公式缓存，受限 UTF-8/BOM 读取 CSV；明确不执行宏/外链/公式刷新，不猜图片图表数值，并固定压缩包、解压、文本和页数上限。桌面依赖已锁定。
- 探测与验证：`node scripts/office-input-probe.mjs` 通过合成 PPTX（文本/表格/备注）、XLSX（公式与缓存值）及中文 BOM CSV；输出与 E51 API 形状一致；`npm run verify` 退出 0（67 个测试文件 / 493 项测试 / Electron build）。下一阶段实现受 E21 输入快照约束的真实读取。

## E51 Office 输入读取（状态：完成）

- 变更内容：新增 `OfficeParserService` 与 `read_office_material` Tool Runtime；Run 只从 E21 的受管 Workspace 输入快照或已选的精确 PPTX ArtifactVersion 读取，支持 PPTX 的 slide/table/notes、XLSX 的 Sheet/Range 与公式缓存、CSV 的 rows 定位。材料候选将可解析的输入和 PPTX 成果标记为可读，其他类型仍显示不可读。
- 过程与来源：解析结果带稳定 locator 和公式/不支持特性警告；每个返回片段登记 `RunMaterialRead(operation=parse)`，沿用材料内容哈希。工具校验材料范围、快照完整性和成果版本归属，不执行宏、外链或公式刷新。
- 验证：新增 Tool Runtime、解析器、Run 工具注册和定位/损坏/取消测试；定向测试 4 个文件 / 39 项通过，`npm run verify` 通过（以命令输出为准）。
- 相关文件：`apps/desktop/src/main/infrastructure/office-parser.ts`、`apps/desktop/src/main/services/run-service.ts`、`packages/tool-runtime/src/read-office-material.ts`、`docs/adr/0018-office-input-parsing-boundary.md`。

## E52 讨论节点与重启后继续/返工（状态：完成）

- 变更内容：新增迁移 v19、`DiscussionCheckpointRepository/Service` 和两条类型化 IPC。节点阶段固定为理解、研究完成、报告大纲、报告、PPT 大纲、PPT 完成和迭代；保存结论、反馈、下一步、来源 Run 与精确成果版本。
- 交互：工作页增加轻量讨论节点条，重启任务后从 SQLite 恢复当前节点；记录新节点可用客户端 ID 幂等提交，返工通过 `supersedesId` 标记上一节点为 `superseded`，不覆盖历史成果、不维持 Run 等待。
- 验证：Repository、Service、IPC 注册和 Renderer 组件测试通过；迁移在新库与历史库路径中由全量门禁验证。下一阶段进入 E53 的经营分析方法和确定性数值校验。

## E53 经营分析方法与确定性结果（状态：完成）

- 变更内容：新增 [ADR-0020](../adr/0020-deterministic-business-analysis.md)、`analyze_business_metrics` Tool Runtime 和内置 `builtin-business-analysis` Skill；研究分析专家预设该 Skill，工具纳入内置工具选择与过程摘要。
- 规则：输入显式区分当前、对比和预算数值；输出原始值、变化额、变化率、预算偏差和零基数 warning。缺少当前指标失败，不刷新公式、不猜测单位或财务规则，规则继续来自任务材料。
- 验证：工具测试覆盖正负变化、预算、零基数、缺失值和取消；内置 Skill 内容哈希已更新到分发清单。下一阶段进入 E54 报告/PPT 交付和来源关联。

## E54 报告、PPT 与来源交付（状态：完成）

- 变更内容：成果详情 IPC 现在按精确 ArtifactVersion 返回 `inputRelations`；Markdown 与 PPT 版本页显示本版来源证据和材料输入关系及用途，沿用已有版本化保存、导出、缩略图预览和系统打开路径。
- 约束：输入关系仍在 Main 校验同一 Run 已实际读取，历史版本不被当前版本覆盖；人工修改继续使用 `user-edit` 版本语义，未把 Evidence 元数据扩展成逐句 Citation。
- 验证：协议可选关系字段、IPC 精确版本补充、成果页面来源展示和现有 Artifact 输入关系测试通过。下一阶段进入 E55 连续两期真实桌面验收。
