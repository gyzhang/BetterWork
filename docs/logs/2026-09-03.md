# 2026-09-03

## 23:05 Qoder 交接验收与 Agent 协作资产建立（状态：完成）

- 背景：项目自 Codex 移交至 Qoder（交接基线 `087fa6b`，含交接文档 `83f1f7d`）。借鉴 clawbible-desktop 与 clawbible-cloud 的实践经验，为 BetterWork 建立面向 AI Agent 的协作资产。
- 变更内容：
  - 新增 `.qoder/rules/`：1 个常驻规则（时间纪律、中文编辑纪律、工作日志纪律、场景规则索引）+ 5 个场景规则（GATE-0 诊断、Knowledge 只读、UI Token、启停与提交、IPC 与 Artifact 版本）。
  - `AGENTS.md`：新增「任务路由」章节（按任务类型的必读表 + 单一规则源说明）；参考项目说明加入 ClawBible Cloud；完成定义追加第 7 条（工作日志）。
  - 新增 `docs/logs/`（本目录）：日志制度 + 首篇日志。
  - `docs/09-reference-projects.md` 登记 clawbible-cloud 的协作资产参考维度；`docs/11-qoder-handoff.md` 阅读顺序补充规则入口。
- 关键决策：借思想不抄内容——参考项目的规则全部按 BetterWork 领域与已核实的路径重写；暂不引入 check-rules 门禁、SOP 库与 docs/laws（当前规模收益不足，规则数达到 8~10 个或出现重复任务类型时再评估）。
- 教训：无（本篇为首篇；日志制度本身即为沉淀教训而设）。
- 测试证据：`npm run verify` 全绿（typecheck、32 个测试、构建成功，仅两条已知 Zod `@PURE` 警告）；`git diff --check` 通过；规则与文档中的代码路径、SQLite 位置均已实际核实。
- 相关文件：`.qoder/rules/`、`AGENTS.md`、`docs/logs/`、`docs/09-reference-projects.md`、`docs/11-qoder-handoff.md`

## 23:30 阶段一：资料索引失败/空状态完善（状态：完成）

- 背景：目标（移交后夜间推进）阶段一，对应交接文档 §6「完善资料索引的失败/空状态」。
- 变更内容：
  - `App.tsx`：导入结果新增逐条未导入原因列表（文件名：原因，danger 样式块）；取消导入对话框时给出「已取消导入」反馈；资料刷新/移除的 IPC 调用补上 try-catch（原先 Main 抛错会产生未捕获的 Promise rejection，界面无任何反馈）。
  - `knowledge-vault.ts`：刷新索引前先检查原始文件可访问，缺失/移动时返回友好中文错误且本地索引保持不变（原先会把 `ENOENT: no such file or directory...` 原始报错透传给用户）。
  - `styles.css`：新增 `.knowledge-issues`（仅用 `--danger/--danger-soft` 语义 Token）。
  - 测试 +2：FTS 未命中回退 LIKE、完全不匹配返回空；源文件缺失时刷新不破坏索引。
- 关键决策：不引入统一错误码机制（Phase 1 范围内 skipped/error 结构化字段已够用）；成果页版本列表加载失败的静默问题已记录、留待后续切片。
- 教训：Renderer 多处 `void invoke()` 无 catch 属系统性模式，本次只修了资料流程；其余面板的同类问题在后续切片按流程逐个补。
- 测试证据：`npm run verify` 全绿（typecheck、34 个测试、构建成功）；`git diff --check` 通过；中文改动经 grep 回读与 git diff 逐行核对。
- 人工验收（留给光哥）：导入一个不支持的文件（如 .png）看逐条原因；导入对话框取消看反馈；把已导入资料源文件移走后点「刷新索引」看友好报错且列表不消失。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`、`apps/desktop/src/main/knowledge-vault.ts`、`apps/desktop/src/main/knowledge-vault.test.ts`
