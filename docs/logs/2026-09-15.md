# 2026-09-15

## Expert Run 身份快照（状态：代码完成）

- 变更：应用库 v21 为 `run_context_snapshots` 增加可选 `expert_id` 与 `expert_revision_id` 外键；专家 Run 启动时直接保存实际采用的 Expert 及修订，通用助手保持空值。仓储拒绝只提供一半身份的快照，旧 Run 继续按未记录专家处理。
- 文档：同步材料契约、专家契约、E55 合成验收记录、任务板和交接说明，明确 TaskContextRevision 与 RunContextSnapshot 的职责差异。
- 验证：`migrate.test.ts`、`run-context-snapshot-repository.test.ts`、`run-service.test.ts` 和两期验收定向测试通过；`npm run verify` 通过（74 个测试文件 / 528 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## Expert 快照归属校验（状态：代码完成）

- 变更：RunContextSnapshot 创建时额外校验 `expertRevisionId` 确实属于 `expertId`，拒绝跨 Expert 修订组合；新增真实 SQLite 回归测试。
- 验证：`npm run verify` 通过（74 个测试文件 / 529 项测试 / Electron build）。

## Run 能力配置快照（状态：代码完成）

- 变更：应用库 v22 为 `run_context_snapshots` 增加模型引用、内置工具策略和 MCP 绑定 JSON；RunService 在启动事务中保存当次解析结果，旧通用 Run 保持空值。新增迁移、仓储解析和两期/运行回归覆盖。
- 验证：`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## Expert MCP 契约示例同步（状态：完成）

- 变更：在 `expert-contracts.md` 的 `ExpertRevision` 示例中补齐已落地的 `mcpToolBindings`，明确专家修订可以保存具体 MCP 工具预设。
- 验证：`git diff --check` 通过；仅文档同步，沿用本阶段全仓门禁结果。

## E56 静态资源复核（状态：通过）

- 验证：`npm run expert:preflight` 通过（1 个内置 Expert、2 个内置 Skill，资源清单和 builder 规则均匹配）。
- 限制：macOS 仍锁屏，因此不把这次静态复核扩大解释为签名安装或真实业务验收。

## 通用助手快照契约同步（状态：完成）

- 变更：材料契约示例将 `taskContextRevisionId` 标为可选，和通用助手无草稿、旧任务历史快照缺少上下文的实际兼容语义一致；专家 Run 仍直接固定 Expert 身份与修订。
- 验证：`git diff --check` 通过。

## MCP 同名工具绑定隔离（状态：代码完成）

- 修复：专家配置和任务资料面板之前只按 `toolId` 判断勾选，两个连接提供同名工具时会互相影响；现在统一按 `connectionId + toolId` 匹配、添加和移除。
- 验证：MCP 选择器定向测试通过；`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。

## MCP 主进程绑定去重（状态：代码完成）

- 变更：`McpClientService.createAgentTools` 使用 `connectionId + toolId` 复合键去重，和 Renderer 的选择规则一致；重复提交同一绑定只创建一个 AgentTool。
- 验证：MCP 服务测试补充重复绑定回归，完整门禁沿用本阶段结果。

## 设计与计划基线校准（状态：完成）

- 变更：确认设计稿技术摘要表头无重复；开发计划基线更新为包含 v22 能力快照的 `e283c47`。
- 验证：`git diff --check` 通过。

## E55/E56 外部条件复核（状态：仍待人工条件）

- CUA 检查返回 macOS 锁屏且自动解锁失败；未启动或操作用户 App。
- 模型探针 `curl --connect-timeout 2 --max-time 4 http://10.62.64.38:30808/api/inference/v1/models` 超时，exit 28、0 字节响应。
- 已同步两期验收记录；E55/E56 继续保持 partial，不用合成数据替代真实窗口、材料、MCP、取消和安装验收。

## 架构文档同步（状态：完成）

- 变更：同步系统架构、领域模型与 ADR-0014，明确 RunContextSnapshot 对专家 Run 直接保存实际 Expert 身份和修订；通用助手与没有历史记录的旧 Run 继续保留空值语义。
- 验证：`git diff --check` 通过；本次仅文档同步，代码门禁沿用前一阶段 `npm run verify` 全绿结果。

## E55/E56 可执行条件复核（状态：仍待人工条件）

- `npm run expert:preflight` 重新通过：1 个内置 Expert、2 个 Skill，资源清单和 builder 复制规则匹配。
- 模型探针再次执行：`curl --connect-timeout 2 --max-time 4 http://10.62.64.38:30808/api/inference/v1/models` 返回 curl exit 28，4 秒内 0 字节（HTTP 000）。
- 桌面验收前置仍未满足；不把静态资源预检或合成测试扩大解释为真实模型、MCP、取消/重启和连续两期窗口验收。

## E23 快照集成回归补强（状态：代码完成）

- 变更：RunService 的 MCP 集成和 Expert 工具策略测试现在直接断言启动事务写入 `RunContextSnapshot` 的 TaskContext、Expert 修订、MCP 绑定、模型引用和内置工具策略；覆盖运行接线与审计快照不一致的回归风险。
- 验证：定向 `run-service.test.ts` 通过（36 项）；`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E23 快照归属校验（状态：代码完成）

- 变更：`RunContextSnapshotRepository` 在写入时校验 Run、Task、Workspace 和 TaskContextRevision 的组合归属；新增跨 Task/Workspace 与跨 TaskContextRevision 回归，避免单列外键存在但审计关系错误。
- 验证：定向快照测试通过（6 项）；`npm run verify` 通过（74 个测试文件 / 532 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E24 成果来源归属校验（状态：代码完成）

- 变更：`ArtifactInputRelationRepository` 写入关系前校验目标 `ArtifactVersion` 的 `sourceRunId` 与当前 Run 一致；新增跨 Run 成果版本回归，避免来源关系被写入错误的产生 Run。
- 验证：定向成果来源测试通过（3 项）；`npm run verify` 通过（74 个测试文件 / 533 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## 当前门禁基线同步（状态：完成）

- 变更：将专家计划、B00-5 任务板和交接说明中的当前门禁数字同步为 74 个测试文件 / 536 项测试。
- 验证：文档链接与格式检查通过，代码门禁沿用本阶段全绿结果。

## E42 MCP 绑定唯一性（状态：代码完成）

- 变更：TaskContextRevision 与 ExpertRevision 保存边界拒绝重复的 `connectionId + toolId`，避免配置计数、历史快照和实际工具目录不一致；运行时去重仍保留作为兼容保护。
- 验证：新增 TaskContext 与 ExpertService 回归测试；`npm run verify` 通过（74 个测试文件 / 535 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E31 记忆读取哈希校验（状态：代码完成）

- 变更：`MemoryRepository.recordReads` 写入读取足迹前校验不可变记忆修订 ID 与内容哈希，拒绝调用方传入的过期或伪造哈希。
- 验证：新增记忆读取回归测试；`npm run verify` 通过（74 个测试文件 / 536 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E24 材料读取范围校验（状态：代码完成）

- 变更：有 TaskContext 的 Run 写入 RunMaterialRead 前必须命中快照中的具体材料且内容哈希一致；无上下文的旧通用 Run 保留兼容路径。
- 验证：新增快照范围与旧 Run 兼容测试；`npm run verify` 通过（75 个测试文件 / 538 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## 计划门禁基线同步（状态：完成）

- 变更：将 E23、E31 任务卡中的验证数字同步为当前 75 个测试文件 / 538 项测试。
- 验证：文档格式检查通过；代码门禁沿用本阶段全绿结果。

## E43 Evidence 归属校验（状态：代码完成）

- 变更：Evidence 仓储写入前校验来源 Run 存在且属于声明的 Task，避免来源证据被跨任务登记。
- 验证：新增跨任务 Evidence 回归测试；`npm run verify` 通过（75 个测试文件 / 539 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E52 讨论节点归属校验（状态：代码完成）

- 变更：讨论节点仓储在写入时校验 Task 存在，以及可选 Run、被替代节点和 ArtifactVersion 均属于当前 Task。
- 验证：新增跨 Task Run/ArtifactVersion 回归测试；`npm run verify` 通过（75 个测试文件 / 541 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E55/E56 门禁复核（状态：等待人工条件）

- 验证：`npm run expert:preflight` 与完整 `npm run verify` 均通过；当前门禁为 75 个测试文件 / 541 项测试，Electron build 成功。
- 环境：CUA 仍报告 macOS 处于锁屏，无法启动或操作真实窗口；因此 E55/E56 继续保留 `partial`，不把自动化和静态资源结果写成真实业务或签名安装通过。

## E52 客户端 ID 跨任务归属修复（状态：代码完成）

- 修复：讨论节点仓储的幂等路径现在校验已有客户端 ID 的 Task 归属；同一 ID 属于另一 Task 时拒绝请求，避免直接返回别的任务节点。
- 验证：新增真实 SQLite 回归测试，讨论节点定向测试通过（4 项）。

## E23 Run/Session 归属校验（状态：代码完成）

- 修复：`RunRepository.create` 现在校验 Session 确实属于声明的 Task，避免仅依赖两列外键而形成跨 Task 的 Run 关系。
- 验证：新增真实 SQLite 回归测试；`npm run verify` 通过（75 个测试文件 / 543 项测试 / Electron build）。

## E32/E42 TaskContext IPC 字段接线修复（状态：代码完成）

- 修复：`SaveTaskContext` IPC 之前丢弃 `excludedMemoryIds` 与 `mcpToolBindings`，导致界面选择只停留在 Renderer；现在完整传入仓储并回读持久化结果。
- 验证：IPC 回归测试断言两类字段均跨边界保存；定向 IPC 测试通过，完整门禁在本阶段继续执行。

## E24 材料引用元数据校验（状态：代码完成）

- 修复：材料读取范围校验之前只比较稳定 ID 和内容哈希，可能接受伪造的来源路径、所属工作空间或文件键；现在对快照中的完整材料引用逐字段比对。
- 验证：新增知识修订来源路径伪造回归测试；定向材料读取测试通过；`npm run verify` 通过（75 个测试文件 / 544 项测试 / Electron build）。

## E12 TaskContext CAS 边界修复（状态：代码完成）

- 修复：TaskContext 首次保存时如果调用方带有期望修订号，之前会在没有当前修订的情况下错误接受；现在将“无当前修订”视为修订号 0 并拒绝非零期望值。
- 验证：新增首次保存 CAS 回归测试，定向 TaskContext 测试通过；`npm run verify` 通过（75 个测试文件 / 545 项测试 / Electron build）。

## E31 记忆读取范围校验（状态：代码完成）

- 修复：记忆读取足迹现在校验 Run 的工作空间与 Expert 范围、确认状态和有效期，拒绝把其他公司空间或无效记忆登记为本次运行已注入内容。
- 验证：新增跨工作空间记忆回归测试；定向 MemoryRepository 测试通过；`npm run verify` 通过（75 个测试文件 / 546 项测试 / Electron build）。


## E23/E24 输入快照实体归属校验（状态：代码完成）

- 修复：任务材料引用此前只检查引用声明的 Workspace；现在同时校验被引用输入快照实体的 Workspace 与格式，拒绝把其他工作空间的快照伪装成当前任务输入。
- 验证：新增跨工作空间输入快照引用回归测试；定向 TaskMaterialService 测试通过，完整门禁随后执行。

## E22/E24 知识修订来源路径校验（状态：代码完成）

- 修复：任务材料服务此前只校验 Knowledge revision 的修订 ID 与内容哈希；现在同时校验 `sourcePath`，避免伪造来源路径后进入 TaskContext 和 RunMaterialRead。
- 验证：新增知识修订来源路径伪造回归测试；定向 TaskMaterialService 测试与 `npm run verify` 均通过（75 个测试文件 / 546 项测试 / Electron build）。

## E55/E56 外部条件复核（状态：仍待人工条件）

- CUA 再次确认 macOS 处于锁屏且自动解锁失败；模型端点 `10.62.64.38:30808` 在 4 秒超时内返回 0 字节（curl exit 28）。
- 结果：没有启动真实窗口或伪造模型成功；E55/E56 继续保持 `partial`，证据追加到两期验收记录。


## E23 输入快照路径歧义保护（状态：代码完成）

- 修复：同一相对路径存在多个已选输入快照时，`read_text_file` 之前会由 Map 静默覆盖并读取不确定版本；现在明确拒绝歧义路径并提示只选择一个版本。
- 验证：新增两期同路径快照回归测试；定向 RunService 测试通过，`npm run verify` 通过（75 个测试文件 / 547 项测试 / Electron build）。
