# 2026-09-15

## Expert Run 身份快照（状态：代码完成）

- 变更：应用库 v21 为 `run_context_snapshots` 增加可选 `expert_id` 与 `expert_revision_id` 外键；专家 Run 启动时直接保存实际采用的 Expert 及修订，通用助手保持空值。仓储拒绝只提供一半身份的快照，旧 Run 继续按未记录专家处理。
- 文档：同步材料契约、专家契约、E55 合成验收记录、任务板和交接说明，明确 TaskContextRevision 与 RunContextSnapshot 的职责差异。
- 验证：`migrate.test.ts`、`run-context-snapshot-repository.test.ts`、`run-service.test.ts` 和两期验收定向测试通过；`npm run verify` 通过（74 个测试文件 / 528 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## Expert 快照归属校验（状态：代码完成）

- 变更：RunContextSnapshot 创建时额外校验 `expertRevisionId` 确实属于 `expertId`，拒绝跨 Expert 修订组合；新增真实 SQLite 回归测试。
- 验证：`npm run verify` 通过（74 个测试文件 / 529 项测试 / Electron build）。

## Run 能力配置快照（状态：代码完成）

- 变更：应用库 v22 为 `run_context_snapshots` 增加模型引用、内置工具策略和 MCP 绑定 JSON；RunService 在启动事务中保存当次解析结果，旧通用 Run 保持空值。新增迁移、仓储解析和两期/运行回归覆盖。
- 验证：`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## Expert MCP 契约示例同步（状态：完成）

- 变更：在 `expert-contracts.md` 的 `ExpertRevision` 示例中补齐已落地的 `mcpToolBindings`，明确专家修订可以保存具体 MCP 工具预设。
- 验证：`git diff --check` 通过；仅文档同步，沿用本阶段全仓门禁结果。

## E56 静态资源复核（状态：通过）

- 验证：`npm run expert:preflight` 通过（1 个内置 Expert、2 个内置 Skill，资源清单和 builder 规则均匹配）。
- 限制：macOS 仍锁屏，因此不把这次静态复核扩大解释为签名安装或真实业务验收。

## 通用助手快照契约同步（状态：完成）

- 变更：材料契约示例将 `taskContextRevisionId` 标为可选，和通用助手无草稿、旧任务历史快照缺少上下文的实际兼容语义一致；专家 Run 仍直接固定 Expert 身份与修订。
- 验证：`git diff --check` 通过。

## MCP 同名工具绑定隔离（状态：代码完成）

- 修复：专家配置和任务资料面板之前只按 `toolId` 判断勾选，两个连接提供同名工具时会互相影响；现在统一按 `connectionId + toolId` 匹配、添加和移除。
- 验证：MCP 选择器定向测试通过；`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。

## MCP 主进程绑定去重（状态：代码完成）

- 变更：`McpClientService.createAgentTools` 使用 `connectionId + toolId` 复合键去重，和 Renderer 的选择规则一致；重复提交同一绑定只创建一个 AgentTool。
- 验证：MCP 服务测试补充重复绑定回归，完整门禁沿用本阶段结果。

## 设计与计划基线校准（状态：完成）

- 变更：确认设计稿技术摘要表头无重复；开发计划基线更新为包含 v22 能力快照的 `e283c47`。
- 验证：`git diff --check` 通过。

## E55/E56 外部条件复核（状态：仍待人工条件）

- CUA 检查返回 macOS 锁屏且自动解锁失败；未启动或操作用户 App。
- 模型探针 `curl --connect-timeout 2 --max-time 4 http://10.62.64.38:30808/api/inference/v1/models` 超时，exit 28、0 字节响应。
- 已同步两期验收记录；E55/E56 继续保持 partial，不用合成数据替代真实窗口、材料、MCP、取消和安装验收。

## 架构文档同步（状态：完成）

- 变更：同步系统架构、领域模型与 ADR-0014，明确 RunContextSnapshot 对专家 Run 直接保存实际 Expert 身份和修订；通用助手与没有历史记录的旧 Run 继续保留空值语义。
- 验证：`git diff --check` 通过；本次仅文档同步，代码门禁沿用前一阶段 `npm run verify` 全绿结果。

## E55/E56 可执行条件复核（状态：仍待人工条件）

- `npm run expert:preflight` 重新通过：1 个内置 Expert、2 个 Skill，资源清单和 builder 复制规则匹配。
- 模型探针再次执行：`curl --connect-timeout 2 --max-time 4 http://10.62.64.38:30808/api/inference/v1/models` 返回 curl exit 28，4 秒内 0 字节（HTTP 000）。
- 桌面验收前置仍未满足；不把静态资源预检或合成测试扩大解释为真实模型、MCP、取消/重启和连续两期窗口验收。

## E23 快照集成回归补强（状态：代码完成）

- 变更：RunService 的 MCP 集成和 Expert 工具策略测试现在直接断言启动事务写入 `RunContextSnapshot` 的 TaskContext、Expert 修订、MCP 绑定、模型引用和内置工具策略；覆盖运行接线与审计快照不一致的回归风险。
- 验证：定向 `run-service.test.ts` 通过（36 项）；`npm run verify` 通过（74 个测试文件 / 530 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E23 快照归属校验（状态：代码完成）

- 变更：`RunContextSnapshotRepository` 在写入时校验 Run、Task、Workspace 和 TaskContextRevision 的组合归属；新增跨 Task/Workspace 与跨 TaskContextRevision 回归，避免单列外键存在但审计关系错误。
- 验证：定向快照测试通过（6 项）；`npm run verify` 通过（74 个测试文件 / 532 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E24 成果来源归属校验（状态：代码完成）

- 变更：`ArtifactInputRelationRepository` 写入关系前校验目标 `ArtifactVersion` 的 `sourceRunId` 与当前 Run 一致；新增跨 Run 成果版本回归，避免来源关系被写入错误的产生 Run。
- 验证：定向成果来源测试通过（3 项）；`npm run verify` 通过（74 个测试文件 / 533 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## 当前门禁基线同步（状态：完成）

- 变更：将专家计划、B00-5 任务板和交接说明中的当前门禁数字同步为 74 个测试文件 / 536 项测试。
- 验证：文档链接与格式检查通过，代码门禁沿用本阶段全绿结果。

## E42 MCP 绑定唯一性（状态：代码完成）

- 变更：TaskContextRevision 与 ExpertRevision 保存边界拒绝重复的 `connectionId + toolId`，避免配置计数、历史快照和实际工具目录不一致；运行时去重仍保留作为兼容保护。
- 验证：新增 TaskContext 与 ExpertService 回归测试；`npm run verify` 通过（74 个测试文件 / 535 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E31 记忆读取哈希校验（状态：代码完成）

- 变更：`MemoryRepository.recordReads` 写入读取足迹前校验不可变记忆修订 ID 与内容哈希，拒绝调用方传入的过期或伪造哈希。
- 验证：新增记忆读取回归测试；`npm run verify` 通过（74 个测试文件 / 536 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E24 材料读取范围校验（状态：代码完成）

- 变更：有 TaskContext 的 Run 写入 RunMaterialRead 前必须命中快照中的具体材料且内容哈希一致；无上下文的旧通用 Run 保留兼容路径。
- 验证：新增快照范围与旧 Run 兼容测试；`npm run verify` 通过（75 个测试文件 / 538 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## 计划门禁基线同步（状态：完成）

- 变更：将 E23、E31 任务卡中的验证数字同步为当前 75 个测试文件 / 538 项测试。
- 验证：文档格式检查通过；代码门禁沿用本阶段全绿结果。

## E43 Evidence 归属校验（状态：代码完成）

- 变更：Evidence 仓储写入前校验来源 Run 存在且属于声明的 Task，避免来源证据被跨任务登记。
- 验证：新增跨任务 Evidence 回归测试；`npm run verify` 通过（75 个测试文件 / 539 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E52 讨论节点归属校验（状态：代码完成）

- 变更：讨论节点仓储在写入时校验 Task 存在，以及可选 Run、被替代节点和 ArtifactVersion 均属于当前 Task。
- 验证：新增跨 Task Run/ArtifactVersion 回归测试；`npm run verify` 通过（75 个测试文件 / 541 项测试 / Electron build）。构建仍有两条来自 Zod 的已知 Rollup `@PURE` 注释警告。

## E55/E56 门禁复核（状态：等待人工条件）

- 验证：`npm run expert:preflight` 与完整 `npm run verify` 均通过；当前门禁为 75 个测试文件 / 541 项测试，Electron build 成功。
- 环境：CUA 仍报告 macOS 处于锁屏，无法启动或操作真实窗口；因此 E55/E56 继续保留 `partial`，不把自动化和静态资源结果写成真实业务或签名安装通过。

## E52 客户端 ID 跨任务归属修复（状态：代码完成）

- 修复：讨论节点仓储的幂等路径现在校验已有客户端 ID 的 Task 归属；同一 ID 属于另一 Task 时拒绝请求，避免直接返回别的任务节点。
- 验证：新增真实 SQLite 回归测试，讨论节点定向测试通过（4 项）。

## E23 Run/Session 归属校验（状态：代码完成）

- 修复：`RunRepository.create` 现在校验 Session 确实属于声明的 Task，避免仅依赖两列外键而形成跨 Task 的 Run 关系。
- 验证：新增真实 SQLite 回归测试；`npm run verify` 通过（75 个测试文件 / 543 项测试 / Electron build）。

## E32/E42 TaskContext IPC 字段接线修复（状态：代码完成）

- 修复：`SaveTaskContext` IPC 之前丢弃 `excludedMemoryIds` 与 `mcpToolBindings`，导致界面选择只停留在 Renderer；现在完整传入仓储并回读持久化结果。
- 验证：IPC 回归测试断言两类字段均跨边界保存；定向 IPC 测试通过，完整门禁在本阶段继续执行。

## E24 材料引用元数据校验（状态：代码完成）

- 修复：材料读取范围校验之前只比较稳定 ID 和内容哈希，可能接受伪造的来源路径、所属工作空间或文件键；现在对快照中的完整材料引用逐字段比对。
- 验证：新增知识修订来源路径伪造回归测试；定向材料读取测试通过；`npm run verify` 通过（75 个测试文件 / 544 项测试 / Electron build）。

## E12 TaskContext CAS 边界修复（状态：代码完成）

- 修复：TaskContext 首次保存时如果调用方带有期望修订号，之前会在没有当前修订的情况下错误接受；现在将“无当前修订”视为修订号 0 并拒绝非零期望值。
- 验证：新增首次保存 CAS 回归测试，定向 TaskContext 测试通过；`npm run verify` 通过（75 个测试文件 / 545 项测试 / Electron build）。

## E31 记忆读取范围校验（状态：代码完成）

- 修复：记忆读取足迹现在校验 Run 的工作空间与 Expert 范围、确认状态和有效期，拒绝把其他公司空间或无效记忆登记为本次运行已注入内容。
- 验证：新增跨工作空间记忆回归测试；定向 MemoryRepository 测试通过；`npm run verify` 通过（75 个测试文件 / 546 项测试 / Electron build）。


## E23/E24 输入快照实体归属校验（状态：代码完成）

- 修复：任务材料引用此前只检查引用声明的 Workspace；现在同时校验被引用输入快照实体的 Workspace 与格式，拒绝把其他工作空间的快照伪装成当前任务输入。
- 验证：新增跨工作空间输入快照引用回归测试；定向 TaskMaterialService 测试通过，完整门禁随后执行。

## E22/E24 知识修订来源路径校验（状态：代码完成）

- 修复：任务材料服务此前只校验 Knowledge revision 的修订 ID 与内容哈希；现在同时校验 `sourcePath`，避免伪造来源路径后进入 TaskContext 和 RunMaterialRead。
- 验证：新增知识修订来源路径伪造回归测试；定向 TaskMaterialService 测试与 `npm run verify` 均通过（75 个测试文件 / 546 项测试 / Electron build）。

## E55/E56 外部条件复核（状态：仍待人工条件）

- CUA 再次确认 macOS 处于锁屏且自动解锁失败；模型端点 `10.62.64.38:30808` 在 4 秒超时内返回 0 字节（curl exit 28）。
- 结果：没有启动真实窗口或伪造模型成功；E55/E56 继续保持 `partial`，证据追加到两期验收记录。


## E23 输入快照路径歧义保护（状态：代码完成）

- 修复：同一相对路径存在多个已选输入快照时，`read_text_file` 之前会由 Map 静默覆盖并读取不确定版本；现在明确拒绝歧义路径并提示只选择一个版本。
- 验证：新增两期同路径快照回归测试；定向 RunService 测试通过，`npm run verify` 通过（75 个测试文件 / 547 项测试 / Electron build）。

## 材料契约同步：同路径快照歧义（状态：完成）

- 变更：在材料契约中记录同一相对路径多版本快照的读取规则；无快照 ID 的 `read_text_file` 必须拒绝歧义，区分版本需只选一个文本快照或使用带快照 ID 的 Office 入口。
- 验证：Markdown 差异检查与 `git diff --check` 通过；实现已在上一阶段的 RunService 回归和全量门禁中验证。


## E41 MCP Schema 与输出边界补强（状态：代码完成）

- 修复：MCP 输入 Schema 之前只进入模型工具描述，调用参数未在 Main 校验；结构化输出也可能绕过 100,000 字符上限。现在发现阶段编译 JSON Schema、调用前校验输入，文本和结构化结果统一受限。
- 验证：新增非法输入与超大输出回归测试；MCP 定向测试通过，`npm run verify` 通过（75 个测试文件 / 549 项测试 / Electron build）。

## E41 MCP 诊断可见性（状态：代码完成）

- 修复：MCP 已知边界错误不再被统一包装成模糊的“调用失败”；Schema 校验和输出上限错误直接保留具体中文原因，未知传输错误仍保留调用工具名并携带原始 cause。
- 验证：MCP 定向测试与 `npm run verify` 均通过（75 个测试文件 / 549 项测试 / Electron build）。

## E40 MCP 探测非法输入路径（状态：代码完成）

- 变更：`scripts/mcp-probe.mjs` 现在在成功握手、发现和调用后，继续发送非法月份参数并要求替身返回 JSON-RPC 错误，探测脚本的非法响应覆盖与 ADR 说明一致。
- 验证：`node scripts/mcp-probe.mjs` 通过；`npm run verify` 通过（75 个测试文件 / 549 项测试 / Electron build）。

## E41 MCP 结构化输出上限回归（状态：代码完成）

- 变更：离线 MCP 替身新增超大 `structuredContent` 响应；回归同时覆盖文本和结构化输出，确保两条路径都不能绕过大小上限。
- 验证：MCP 定向测试与 `npm run verify` 均通过（75 个测试文件 / 549 项测试 / Electron build）。

## E55/E56 外部条件再次复核（状态：仍待人工条件）

- CUA 仍检测到 macOS 锁屏，浏览器策略加载失败；模型端点在 4 秒内无响应（curl exit 28）。
- 结果：未启动真实桌面或网页旅程，E55/E56 继续保持 `partial`；阻塞证据已追加到两期验收记录。


## E43 网页主机与响应边界回归（状态：代码完成）

- 修复：网页地址校验此前对方括号 IPv6 和 IPv4 映射 IPv6 的私网表达不完整；现在先规范化主机并拒绝相关环回/映射形式。
- 验证：新增私网 IPv6、重定向上限和 1 MiB 截断回归；`web-fetch-service` 定向测试通过，`npm run verify` 通过（75 个测试文件 / 550 项测试 / Electron build）。


## E56 发布资源路径泄漏预检（状态：代码完成）

- 变更：`expert-release-preflight` 增加 Skill manifest 路径逃逸检查，并扫描内置 `skills/`、`experts/`、`dependency-locks/` 是否包含当前仓库或用户目录绝对路径。
- 验证：正常 `npm run expert:preflight` 通过；临时打包资源注入开发机路径后预检按预期失败；`npm run verify` 通过（75 个测试文件 / 550 项测试 / Electron build）。

## E55/E56 当前条件复核（状态：阻塞条件持续）

- CUA 当前可见 Chrome 标签页，但原生 macOS 仍锁屏；模型端点再次在 4 秒内无响应（curl exit 28）。
- 自动化代码和资源预检已完成，剩余真实两期桌面、取消/重启、MCP 业务调用及签名安装均需解锁桌面和可用模型端点后执行。

## E55 真实窗口召唤走查（状态：部分完成）

- 条件恢复：CUA 已能操作 macOS，启动开发态 Electron 窗口并进入“专家”页面。
- 实际路径：点击“研究分析专家 → 召唤”，直接进入工作台；当前专家与“经营分析方法” Skill 已注入，未出现任务准备表单。
- 脱敏运行：提交经营分析输入后，Run 先进入进行中再失败；消息中心显示 `无法连接模型服务（http://10.62.64.38:30808/api/inference/v1）：fetch failed`。SQLite `run_events` 记录 `run.started → run.failed`，失败通知与 Run 终态一致。
- 结论：专家召唤、运行状态和失败反馈完成真实窗口核对；模型服务不可用，未把该次失败或合成数据写成连续两期业务成功。E55/E56 仍为 `partial`。

## 复核门禁（状态：通过）

- `npm run expert:preflight` 通过：1 个内置专家、2 个 Skill，资源路径与构建资源校验通过。
- `npm run verify` 通过：75 个测试文件 / 550 项测试，lint、format、typecheck 与 Electron build 全部通过；构建仍有来自 Zod 的两条已知 Rollup `@PURE` 注释警告。
- `git diff --check` 通过；当前分支与 `origin/main` 保持同步。

## E14 专家模型偏好 UI（状态：代码完成）

- 变更：专家编辑器新增“模型偏好”，支持继承应用默认语言模型或选择已配置的语言模型；专家详情显示当前偏好。停用或已删除的配置仍会显示为不可用，不会静默改写历史修订。
- 验证：新增 Renderer 回归测试，断言指定模型 ID 跨保存 IPC；真实 Electron 热更新走查确认下拉显示“应用默认模型”和当前语言模型。`npm run verify` 通过（75 个测试文件 / 551 项测试 / Electron build）。

## 模型端点认证探测（状态：仍阻塞）

- 使用当前语言模型配置的本地凭据发送 `Authorization: Bearer …`，不把凭据写入日志或输出；`GET /api/inference/v1/models` 仍返回空响应（curl exit 52，约 5 秒）。
- 结论：失败不是未认证探测造成的，E55/B00-5 的真实模型成功执行与取消走查继续等待端点恢复。

## 召唤后的专家模型可见性（状态：代码完成）

- 变更：工作台 Composer 现在根据当前专家修订显示实际模型偏好；指定模型缺失或停用时显示“专家指定模型不可用”，避免把应用默认模型误显示为本次执行模型。打开历史任务时只有当前专家修订匹配时才读取该修订的模型偏好。
- 验证：新增召唤工作台 Renderer 回归测试；`npm run verify` 通过（75 个测试文件 / 552 项测试 / Electron build）。

## 历史专家修订的模型标签（状态：代码完成）

- 修复：打开旧任务且当前窗口无法回读对应历史专家修订时，Composer 不再错误显示应用默认模型，改为“专家修订模型（历史版本）”并保留可追溯性。
- 验证：`npm run verify` 通过（75 个测试文件 / 552 项测试 / Electron build）。

## 历史专家修订标签回归（状态：代码完成）

- 变更：新增任务恢复回归，固定历史专家修订 ID 时，Composer 显示“专家修订模型（历史版本）”，不把当前应用默认模型当作历史事实。
- 验证：`npm run verify` 通过（75 个测试文件 / 553 项测试 / Electron build）。

## E56 打包资源复核（状态：静态通过，签名仍阻塞）

- 模型显示改动后重新执行 `npm run dist:mac --workspace @betterwork/desktop`；arm64/x64 DMG 和 App 目录构建成功。
- 两种架构的打包 Resources 预检均通过；`codesign --verify --deep --strict` 仍失败，本机没有 Developer ID 签名身份，未把签名安装或 Gatekeeper 结果写成通过。构建产物已清理。

## 专家详情记忆摘要（状态：代码完成）

- 变更：专家详情增加关联记忆摘要，显示已确认/待确认数量和最多三条内容；“管理记忆”直接进入设置页的记忆面板。召唤仍直接进入对话，不引入任务准备表单。
- 验证：新增 App Renderer 回归测试，覆盖记忆摘要与设置页跳转；定向测试通过，随后执行完整 `npm run verify`（75 个测试文件 / 554 项测试 / Electron build）。
- 人工走查：开发态 macOS 窗口从“专家 → 查看配置”看到空记忆状态，点击“管理记忆”进入设置 → 记忆；未改变召唤直达对话路径。

## 专家记忆工作空间隔离（状态：代码完成）

- 修复：专家详情仅显示专家通用记忆，以及当前工作空间的 Expert×Workspace 记忆；其他工作空间的内容不会出现在专家摘要中。
- 验证：Renderer 回归测试覆盖当前工作空间候选记忆可见、其他工作空间记忆不可见；定向测试、typecheck 与 lint 通过，完整门禁随后执行。

## E56 签名预检入口（状态：代码完成，签名条件仍阻塞）

- 变更：`expert-release-preflight` 新增 `--signed-app`，在 macOS 上统一执行 `codesign --verify --deep --strict`；普通资源预检保持可在开发环境运行。
- 验证：资源预检通过；缺失 `.app` 参数按预期失败。当前 `security find-identity -v -p codesigning` 返回 0 个有效身份，因此未伪造签名成功证据。

## E56 签名预检 CLI 回归（状态：代码完成）

- 变更：将 `scripts/expert-release-preflight.mjs` 纳入 Vitest，覆盖无签名身份时的资源预检、缺失 `--signed-app` 参数和非 `.app` 路径拒绝；`tsconfig` 与 Vitest 配置纳入 scripts 测试目录。
- 验证：定向脚本测试 3 项通过；完整门禁随后执行，当前预期为 76 个测试文件 / 557 项测试。

## E32 专家记忆管理作用域（状态：代码完成）

- 变更：从专家详情进入“管理记忆”时，设置页固定显示该专家的通用记忆和当前工作空间专属记忆；可直接创建对应作用域记忆，也可切回全部记忆。其他专家或工作空间的记录不会混入列表。
- 验证：新增 MemoryView 与 App Renderer 回归测试；定向测试、typecheck、lint 通过，完整门禁随后执行（预期 76 个测试文件 / 558 项测试）。

## E55/B00-5 Expert 取消后重启边界（状态：自动化完成，真实端点仍待验收）

- 变更：RunService 回归覆盖 Expert 运行被用户取消后仍保留 `RunContextSnapshot` 中的 `expertId`、`expertRevisionId` 与 `TaskContextRevision`；同一 Task 使用未变更的固定上下文可再次启动并完成，取消 Run 只保留唯一 `run.cancelled` 终态。
- 验证：`run-service.test.ts` 定向测试通过（38 项）；随后 `npm run verify` 通过（76 个测试文件 / 559 项测试，含 Electron build）；真实模型成功运行、撤销走查和连续两期业务验收仍受当前 endpoint 不可用影响。

## E32 专家记忆管理手工复核（状态：通过）

- CUA 在当前 macOS 开发窗口完成“专家 → 查看配置 → 管理记忆”路径；页面显示“当前范围：研究分析专家 · 当前工作空间”，创建提示明确为该专家的工作方法。
- 点击“查看全部记忆”后范围标题和表单视觉标签恢复为用户范围；未写入或删除任何记忆。专家“召唤”入口仍保持直接进入工作台。

## E55 两期记忆复用与公司范围回归（状态：自动化完成）

- 变更：两期合成验收新增 Expert×Workspace 已确认方法记忆；第二期只允许读取当前公司工作空间的记录，另一工作空间的同一 Expert 记忆被排除，并登记精确 `run_memory_reads` 足迹。
- 验证：`expert-two-period-acceptance.test.ts` 定向测试通过；随后 `npm run verify` 通过（76 个测试文件 / 559 项测试，含 Electron build）；真实模型与网页/MCP 业务旅程仍需外部端点恢复后人工验收。

## E55 跨 Task 历史隔离回归（状态：自动化完成）

- 变更：RunService 使用两个独立 Task/Session 启动同一 Expert 的连续两期请求，检查第二期发送给模型的消息不包含第一期对话，同时保留两期各自的 Expert 修订快照。
- 验证：`run-service.test.ts` 定向测试通过（39 项）；随后 `npm run verify` 通过（76 个测试文件 / 560 项测试，含 Electron build）。

## E55/E56 验收条件预检 CLI（状态：代码完成，外部条件仍阻塞）

- 变更：新增 `scripts/expert-acceptance-preflight.mjs` 与 `npm run expert:acceptance-preflight`，检查 OpenAI-compatible `/models` 端点和 Developer ID Application 签名身份；API Key 只从用户指定的环境变量读取，不写入输出；模型检查有 5 秒 AbortController 超时。
- 验证：CLI 本地替身测试 4 项通过，覆盖成功、连接失败、空响应和参数边界；真实端点预检明确返回“模型端点在 5000ms 内无响应”；`npm run verify` 通过（77 个测试文件 / 564 项测试，含 Electron build）。

## E55 真实材料与模型工具走查（状态：部分完成）

- 条件恢复后，用当前语言模型配置的凭据执行 `npm run expert:acceptance-preflight -- --model-url http://10.62.64.38:30808/api/inference/v1 --api-key-env BETTERWORK_MODEL_API_KEY --skip-signing`，模型 `/models` 检查通过；凭据未写入日志或提交。
- CUA 从“专家 → 研究分析专家 → 召唤”直接进入工作台；临时创建的三份脱敏 Markdown 通过“添加文件”加入同一 Task，并分别设置为规则口径、历史对比和本期输入。
- 真实 Run `55713063-1fbe-41d2-938b-ad8a18fc097c` 完成，SQLite 记录 3 条 `run_material_reads`；Run 事件实际包含 3 次 `read_text_file` 和 1 次 `analyze_business_metrics`，界面输出的 120/110/100/58 与脱敏材料一致。
- 真实网页 Run `c13516ff-2030-4854-a695-c565c2150359` 实际调用 `web_fetch → web_search → web_fetch`；Investopedia 的 403 被明确显示，Microsoft Learn 正文读取成功并登记最终 URL、标题和 Evidence。

## E55 真实取消与重启走查（状态：部分完成）

- Run `eb2fac48-f633-4d14-b297-9edcca697336` 在开发窗口点击“停止”后以唯一 `run.cancelled` 收口；Run `b9bf6d15-3975-45f4-a046-7fd0460b3a43` 使用同一 Task/固定上下文重启并以 `run.completed` 收口，两个快照都保留同一 Expert 修订。
- 重启回复出现脱敏材料中不存在的现金流/续约数字，未将其计入验收结论。该现象说明同一 Task 的历史消息仍可能诱发模型沿用未验证事实；后续需要材料读取足迹驱动的事实约束或引用门禁，再完成 E55 的连续两期通过判定。

## E55/E56 当前遗留条件

- `mcp_connections` 当前为空，未进行真实业务 MCP 调用；离线 MCP 测试仍不能替代该证据。
- `security find-identity -v -p codesigning` 仍找不到 Developer ID Application；E56 签名安装保持 `partial`。模型端点已恢复并通过带凭据预检，下一阶段按 E55 剩余事实约束/独立两期与 E56 签名条件继续。

## 选定材料清单修复与独立任务复测

- 发现：材料绑定和 `run_context_snapshots` 已正确保存，但模型没有收到可执行的材料路径/快照清单，导致一次独立任务自行尝试 `snapshot1/2/3` 等虚假标识并失败。
- 实现：`RunService.buildPreviousMessages` 新增材料清单系统消息；工作区快照暴露相对路径、用途和精确快照 ID，并明确 Markdown 使用 `read_text_file`、Office 使用 `read_office_material`；同时要求只使用选定材料事实，不用猜测补齐。`run-service.test.ts` 新增请求体回归测试。
- 验证：重启开发态 Electron 主进程后，独立 Task `34d21a14-7c8f-42c2-b041-66f581b030b3` 的 Run `31813b3b-a6dc-4306-bd08-d5f439d0e747` 完成。`run_material_reads` 记录三份脱敏 Markdown 的精确读取，工具事件为 `read_text_file ×3`、`analyze_business_metrics ×1`、`task_write_file ×1`；输出数值 130/125/120/60/58 和 +4.0%/+8.33% 均可在材料中核对，界面显示五页 PPT 摘要结构。
- 自动校验：`run-service.test.ts` 定向测试 40 项通过；`npm run verify` 通过（77 个测试文件 / 565 项测试，含 lint、format、typecheck、Electron build）。
- 遗留：`mcp_connections` 仍为空，真实业务 MCP 未验收；Developer ID Application 仍缺失，E56 签名安装未通过。合成样本目录将在提交前删除，不进入 Git。

## 重启上下文事实边界加固

- 调整材料清单消息位置：放到同一 Task 的历史用户/助手消息之后、当前 Run 用户请求之前，并明确旧助手回复不是本次 Run 的证据；每次重启要求重新读取当前清单材料。
- `run-service.test.ts` 增加顺序回归断言；定向 40 项、全量 `npm run verify` 均通过（77 个测试文件 / 565 项测试，Electron build 通过）。
- 这只收紧上下文提示和顺序，未把提示当作逐句事实门禁；真实 MCP 为空、Developer ID 签名缺失，E55/E56 继续保持 `partial`。

## 材料事实边界复测

- 真实复测发现模型会把不同期间的 2 家和 1 家到期客户相加为 3 家，因此在材料清单中加入禁止跨期间相加、禁止把“未提及”解释为续约/流失、缺数据必须写“材料未提供”的规则。
- 同一 Task 的 Run `b67a7b4d-9863-405c-9b86-7647539d31dc` 在读取三份快照并调用 `analyze_business_metrics` 后完成；界面输出保留 2 家/1 家的期间事实，未再输出 3 家，并明确标记费用预算、客户总数、延期金额和未来预测为材料未提供或不可推断。
- 定向 RunService 40 项、全量 `npm run verify`（77 个测试文件 / 565 项测试）通过。该机制仍是上下文指令约束，不是逐句事实硬门禁；E55/E56 的真实 MCP、签名安装条件继续保持 partial。

## 材料事实硬门禁与读取足迹（状态：代码完成，真实 MCP/安装条件仍阻塞）

- 变更：`RunService` 为选定材料的 Run 维护事实账本，记录成功读取材料和 `analyze_business_metrics` 结果；最终输出带业务单位的数字必须有原始或确定性计算依据，客户数只接受材料原数。没有成功材料读取足迹的材料范围 Run 也会在 `run.completed` 发布前失败收口。
- 测试：新增“无材料读取失败”和“客户数越界失败”回归；`run-service.test.ts` 定向 42 项通过。`npm run verify` 通过（77 个测试文件 / 567 项测试 / Electron build）；构建保留两条已知 Zod Rollup `@PURE` 注释警告。
- 真实证据：此前 Run `31813b3b-a6dc-4306-bd08-d5f439d0e747` 的三份材料读取和 Run `b67a7b4d-9863-405c-9b86-7647539d31dc` 的事实边界输出可继续作为样本；新观察到的空跑 Run `842445fc-7bf5-4c02-b45c-d1b9cbb0aa7e`、`6792caad-1704-46f5-a6dc-def6e2c726db` 未计入成功证据，门禁已覆盖该形态。当前 macOS 随后进入锁屏，无法继续做桌面复测。
- 限制：E55 仍为 `partial`，因为真实业务 MCP、连续两期在硬门禁下的成功运行和 Developer ID 签名安装尚未完成；非数字定性主张尚未纳入本账本。

## 硬门禁后真实材料成功路径（状态：一期通过，E55 仍 partial）

- 重启开发态 Electron 后，真实窗口启动 Run `65000f96-d031-4298-a234-916f41d9c40e`。SQLite 状态为 `completed`；`run_material_reads` 记录 `financial-rules.md`、`2026-09-report.md`、`2026-10-data.md` 三个精确快照读取。
- 运行事件按预期完成 `read_text_file ×3` → `analyze_business_metrics ×1` → `run.completed`。工具结果为收入 130、上期 120、预算 125、变化额 10、预算差异额 5、变化率 0.083333…、预算差异率 0.04；最终摘要没有跨期间合计客户数，费用预算、延期回款金额、客户总数和未来预测均保留为材料未提供/不可推断。
- 这次复测确认硬门禁在真实模型窗口中允许合规材料路径完成，同时留下可查询的读取足迹和工具事件。E55 仍缺真实业务 MCP、连续两期硬门禁成功与更广泛的非数字主张审计；E56 仍缺 Developer ID Application 签名安装。

## 定性事实硬门禁（状态：代码完成）

- `MaterialFactLedger` 新增关键定性状态账本，覆盖已续约、已流失、平均客单价、续约率/流失率和客户总数；材料中没有肯定事实时，最终输出的对应主张会失败收口，缺失说明和待确认表述保留可用。
- 新增回归覆盖“材料只写续约意向待确认、输出已续约”的失败路径。`run-service.test.ts` 43 项通过；全量 `npm run verify` 通过（77 个测试文件 / 568 项测试 / Electron build）。
- 该边界只针对当前经营分析中的高风险事实短语，E55 仍等待真实 MCP、连续两期硬门禁成功和更广泛语义审计；E56 的签名安装条件未改变。
- 真实窗口复测 Run `9cbb50ff-0baf-4de0-944b-eb02b4bac59a` 成功登记三份材料读取；模型拒绝把“续约意向待确认”改写成“该客户已续约”，因此正常完成而没有触发失败门禁。该记录证明最新桌面代码已加载，但不作为 `run.failed` 硬门禁样本。
- 细化：当前请求明确给出的定性事实也进入账本，避免用户提供的本期事实在材料范围 Run 中被错误拒绝；未在请求或材料中出现的肯定式状态继续失败。
- 边界修正：把“是否已续约”、核实/跟进/建议及条件句识别为非断言，避免合规行动建议被定性门禁误报；全量验证仍为 77 个测试文件 / 568 项测试，Electron build 通过。

## 数字单位边界（状态：代码完成）

- `MaterialFactLedger` 为 `家/户/客户` 数量建立独立原始集合，金额、百分比和日期数字不能跨单位复用；当前请求中明确的计数单位和客户数标签仍可作为本次输入。
- 新增“收入 130 万元不能推出客户数 130 家”回归；定向 44 项、全量 77 个测试文件 / 569 项测试和 Electron build 通过。E55 的真实 MCP、连续两期和 E56 签名条件仍未改变。

## 百分比单位边界（状态：代码完成）

- 事实账本新增百分比集合：原始金额不能冒充百分比；材料中的百分比和确定性工具由 130/125 推出的 104% 可以通过。
- 新增“收入 130 万元不能推出增长率 130%”回归；全量验证通过（77 个测试文件 / 570 项测试 / Electron build）。

## A19 成果页系统打开与导出 spot-check（状态：部分证据）

- 在开发态 Electron 的“成果”页打开 `图形元素全面测试页 - 中电金信模板`，详情正确显示 `PPTX · v1`、文件大小 `208.8 KB`、结构校验“通过”、视觉检查“未检查”和单页预览；版本历史显示只有 v1，未串到其他成果。
- 点击“打开”后，PowerPoint 新窗口标题为 `output - 只读`，窗口 URL 指向 `.../desktop/artifact-files/fdb5d9b4-b14a-4fb5-92fd-79145ae8f562/output`，缩略图和状态栏均显示 1 页。由此确认详情页传入的是当前版本的不可变存储文件，系统打开路径可用。
- 点击“导出文件”后，系统保存面板显示当前版本的默认文件名 `图形元素全面测试页 - 中电金信模板.pptx` 和 PPTX 格式；CUA 在保存面板中途失去可访问性，未确认最终写盘，因此不把导出成功、PowerPoint 编辑或另存结果写成通过。
- A19 继续保持 `doing`。剩余手工证据是完成一次导出并重新打开导出副本，验证文本/图形可编辑、另存后可再次打开，以及旧版本/Markdown 回归；A20/A21 不因本次 spot-check 提前升级。

## A19 Markdown 版本切换与编辑入口 spot-check（状态：部分证据）

- 在同一成果页打开已有 Markdown 成果，确认版本历史同时显示 v1（AI 生成）和 v2（人工修订）。切换 v1 后页眉明确显示“Markdown · v1 · 历史版本”，正文、代码块和表格仍正常渲染，证明历史版本选择没有串到 v2。
- 从 v1 历史版本进入编辑入口，表单显示当前标题和 Markdown 内容，并明确提示保存会创建 v3 人工修订版本；随后点击“取消”，数据库中的版本数保持 2，没有产生副作用。
- 这部分满足 A19 的 Markdown 查看、历史版本切换和编辑入口走查；PPTX 导出副本的写盘、PowerPoint 可编辑/另存和全量人工清单仍待完成，A19 继续保持 `doing`。
