# 2026-09-26 工作日志

## 07:53 记忆治理页检索与滚动区验收收口（状态：完成，提交待指令）

- 背景：昨夜 23:21 一节记录的改造（分组收成页签、页面唯一滚动区、`memory:list` 后端全库检索）等待光哥在真实窗口走查；本节记录验收结果与压测数据清理。
- 变更内容：无源码改动。清理开发库内直投的压测数据——按 `/tmp/betterwork-memseed-ids.json` 的 id 清单删除 66 行 `memory_records`（60 条样例，加上光哥走查时在「压测样例 01／02」两条上追加的 6 个修订）与 60 行 `memory_operations`。删除前的引用守卫由「任何指向本批修订的引用」收窄为「本批之外的行指向本批修订」，样例自身的修订链属于同一批，随批删除。
- 关键决策：不做级联清理，遇到集合外引用即停下报告；宁可先查引用来源，也不放宽条件硬删，避免误伤真实记录。
- 教训：其一，`COALESCE(r.topic_key, ?)` 的回退值若绑成检索词本身，`topic_key IS NULL` 的行会恒真命中——断言「`%` 不是通配符」时必须钉住具体命中集合，只断言「不为空」抓不到这类错，本次正是 `%` 用例把缺陷钉住。其二，跨零点会话里每个时间都要重新 `date`：23:21 与 07:53 分属两个日志文件，不能凭上下文续写。其三，并行会话在途时仓库级门禁（lint／format／typecheck）会因他人文件变红，归属要用文件 mtime 与报错路径逐条核对后再说话，不得顺手修别人的半成品。
- 测试证据：光哥真实窗口走查通过——页签一次只渲染一组、滚动条落在版心右缘、搜索能命中未加载的记录、超出一页时给出说明。清理后库内 `memory_records` 共 1 行（`confirmed` 1 行），样例残留 0、操作残留 0，真实记录「收入按回款到账金额统计，不使用签约金额」仍为 revision 1；清理前备份在 `/tmp/betterwork-db-backup-20260925-2155.sqlite`。
- 相关文件：`docs/logs/2026-09-25.md`、`docs/07-mvp-and-roadmap.md`、`docs/10-ui-ux-system.md`、`docs/development/memory-contracts.md`、`/tmp/memclean.mjs`、`/tmp/betterwork-memseed-ids.json`。

## 08:44 KM15 前置对账与知识改动本地提交（状态：走查开始，人工栏进行中）

- 背景：光哥逐条核对 [KM15 清单](../development/knowledge-km15-checklist.md) §0 前置——0.1 开发实例能起、能操作、无报错（通过）；0.3 系统内已配 3 个模型且均可用，其中 1 个是向量模型；并问 0.4 的 20 题与标注是否现在必须准备。
- 关键决策：0.4 **不阻断** §1–§11 与 §13，只卡在 §12 之前；一旦开跑不得回改标注，所以约定等人工 UI 栏走完再一起定题，先出草稿题供他改定、确认后我才跑语义栏。0.3 补一条落地口径：开跑前要在知识页「索引与模型」的「嵌入模型」下拉里确认选中的就是那个向量模型（下拉只列已启用且已配凭据的嵌入角色 profile），选中即视为本轮 §12 的真实调用授权范围。
- 变更内容：清单 §0 四行改为真实状态；任务板 KM15 行追加 08:40 前置对账一句。知识改动按任务边界本地提交 `9771b0a`（4 个 renderer 文件＋知识 CSS 追加段＋清单＋任务板＋2026-09-25 日志，8 文件 750 插入／108 删除），未推送。
- 测试证据：提交前 `npm run verify` 退出 0（136 文件／1325 测试）；`use-knowledge-library.test.ts` 27/27、`KnowledgeView.test.tsx` 13/13。提交后 `bash scripts/dev-start.sh` 起开发实例（pid 53340），日志只到 builtin Skill/Expert 注册，无报错。
- 边界：本轮我不驱动界面，§1–§9 由光哥操作并按节回填「结果记录」；`.app` 冷启动与旧库升级（§13）单独一轮；`artifact:declare-sources`/`get-run-declarations` 仍无界面消费者，要人工验收它需先补只读的「本次运行声明记录」界面，等光哥拍板。

## 08:51 §12 语义验收题集草稿（状态：待光哥改定冻结）

- 背景：光哥确认清单 §0.3——系统内 3 个模型均可用且只有 1 个向量模型，并已在知识页「嵌入模型」下拉核对选中项，即视为本轮真实向量调用授权。他此前问 0.4 是否现在必须准备，答案是「只卡在 §12 之前」，但他选择先把题准备好。
- 变更内容：新增 `docs/development/knowledge-km15-semantic-set.md` 与合成语料 `docs/development/fixtures/km15-semantics/K01–K10`（10 份 Markdown，7.6 KB，全部虚构的银行/金融科技日常口径，不含真实客户或行内数据）。题集含 14 道同义改写题（题面与资料几乎不共用字面词，用来检验语义这一路的增量价值）＋ 6 道字面重叠题（关键词基线应当命中，两列并列才成对照），逐题预标注到 K01–K10；判定口径写明「按资料身份进入前 5 即命中，不按命中块数计次」，过栏线混合列 ≥16/20，失败归因四选一。
- 关键决策：语料用合成而不是真实库——清单 §12 原文即「共同确认的合成中文资料」，且合成语料可整批重建、可复现，不牵涉把生产资料发给模型服务；若光哥要换成真实资料，题面作废、由他在开跑前重新标注。执行载体给两条路（界面手跑 40 次／`scripts/` 探针脚本直连 Vault 与真实向量模型），探针那条需要单独授权读取凭据，且明确它替代不了 §8 的人工 UI 栏。
- 相关文件：`docs/development/knowledge-km15-semantic-set.md`、`docs/development/fixtures/km15-semantics/`、`docs/development/knowledge-km15-checklist.md`（§0.3／§0.4／§12 改为真实状态并指向题集）、`docs/development/tasks-knowledge.md`（KM15 行）。

## 08:55 知识页「索引与模型」首行折行修复（状态：完成，待光哥窗口回看）

- 背景：光哥走查 KM15 §8 时发现「语义检索」后面的「嵌入模型」标签与右侧提示都在同一行内逐字折行，而该行右侧仍有大量留白，质疑为什么非要折行。
- 根因：共享下拉 `.field-select-trigger` 按整行表单字段写成 `width:100%`；放进 `.knowledge-admin-profile` 这条弹性行后它成为 flex-basis 为 100% 的项，仅它一项就占满父容器，标签与 `<small>` 被压向 min-content——中文可在任意字间断行，min-content 约等于一个字，于是标签塌成两字宽。截图佐证：下拉实测约 300px 而文案只有约 110px，箭头被 `justify-content:space-between` 推到最右，说明宽度是被 100% 吃掉的，不是行不够宽。
- 变更内容：`apps/desktop/src/renderer/src/styles.css` 在 `.knowledge-admin-profile` 作用域下补三条覆盖——trigger 回到内容宽（min 200／max 280，超长模型名在按钮内走省略号而不是把提示挤到下一行），标签与提示 `flex:none` 保住单行自然宽。JSX 与组件未动。
- 关键决策：不改 `.field-select-trigger` 的全局 `width:100%`——知识页筛选器、成果页与记忆编辑器都依赖整行形态；只在内联场景由调用方作用域收回。也不借这次改动顺手动别的样式。
- 教训：`.knowledge-admin-row small` 只设颜色没设 `font-size`，UA 的 `small`（约 9.6px）低于 docs/12 §8 的 12px 底线，而字号护栏只扫显式声明，抓不到这种「没写」的违规；是否补齐等光哥拍板。
- 测试证据：`npx prettier --check` 通过；`standards/coding-standard.test.ts` 22/22；`KnowledgeView.test.tsx` 13/13。布局效果需真实窗口确认，按约定不由我驱动界面。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`apps/desktop/src/renderer/src/views/KnowledgeView.tsx`、`apps/desktop/src/renderer/src/components/FieldSelect.tsx`。

## 09:21 纵向间距贴死全仓整改与规范落条款（状态：完成，待光哥窗口回看）

- 背景：光哥指出知识页「全部资料」下拉与搜索框 0 间距贴合、视觉上令人窒息，要求先修这一处，再举一反三把全部界面查一遍，并把这个初级要求写进界面设计规范——上一轮整改不彻底。
- 根因：`.page-toolbar` 是 `components/layout/PageToolbar.tsx` 的骨架容器，styles.css 里只有 `flex:none` 与一条下边距，没有任何纵向堆叠机制，子控件全靠各自 margin 凑缝；同时 `FieldSelect` 自带 `width:100%`，在弹性行里等于以父容器宽为基准，把同排的标签与提示挤到逐字断行。
- 变更内容：① `display:flex` + `gap:12px` 收进 `.page-toolbar` 基类，删掉 `.knowledge-search` 自带下边距与记忆页的 `display:grid`/`gap:10px` 覆写（只留 `margin-bottom:0` 并注明本页节奏归 `.memory-settings`）；② `.field-select-trigger` 去掉 `width:100%`——纵向容器由 `stretch` 撑满、横向行按内容取宽，成果页「采用来源」行里同类的标题挤压一并消失，这条推翻了 08:55 一节「不改全局 `width:100%`」的决策；③ `.knowledge-admin-toggle` 补 `align-self:flex-start`，因为纵向 flex 默认 `stretch` 会把按钮拉满整行；④ docs/10 §9.8 新增四条硬约束（纵向不得贴死、一道缝只有一个所有者、骨架自带机制、控件不写 `width:100%`）并改写落地现状，§8.3 补「页面不得替骨架补纵向间距」，docs/12 §8 与 `.qoder/rules/betterwork-ui.md` 同步；⑤ `standards/coding-standard.test.ts` 新增「界面间距与骨架纪律」两条护栏。
- 全仓复核结果：真正 0 间距贴死的只有 PageToolbar 一处。其余纵向堆叠都有机制持有——`.skill-rows` gap 12、`.artifact-source-select` gap 8、记忆编辑区 grid gap、成果与知识列表行由 padding 13–15px 加 1px 分隔线成节奏、浮层 header/footer 由 padding 加分隔线。
- 关键决策：护栏只锁「装控件的骨架容器必须有 gap」与「页面不得替骨架补 gap」两条，不推广到所有 column 容器。`scroll-region`、`view-container-list` 的节奏分别来自版心区块内距与行自身 padding，一律要求 gap 会让分隔线脱离下一行，那样的白名单比重叠规则更糟。
- 教训：docs/10 §9.8 上一轮（2026-09-05）写的「仓库中不再有偏离标尺的间距值」不成立——`gap` 取值仍有 2/3/5/6/7/9/10/11/14/15/20px，偏离标尺 118 处、在标尺上 109 处。规范里的「落地现状」必须由护栏或可复现的统计支撑，不能凭一次局部整改下结论；本轮已把该句推翻重写，水平微调收进标尺另立任务，不在本轮顺手改。
- 测试证据：`prettier --check` 全绿；`standards/coding-standard.test.ts` 24/24，并对两条新护栏做变异验证（删掉 `.page-toolbar` 的 gap 让第一条转红、加 `.memory-settings .page-toolbar { gap:10px }` 让第二条转红），验证后已还原并复核文件尾；`typecheck`、`build` 退出 0；全量 `npm test` 1324/1327，3 红为 office-parser、memory-retrieval 计时用例与 KM14 基准（p95 1725ms／预算 1000ms），单独重跑前两项转绿，当时负载均值 16–22 且有开发实例与 knowledge-worker 在跑，与本次 renderer CSS／文档／护栏改动无因果路径。`npm run lint` 的 7 条错误全部在未跟踪的 `scripts/km15-semantic-probe.ts`（mtime 09:15，并行会话在途文件），未代为修改。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`standards/coding-standard.test.ts`、`docs/10-ui-ux-system.md`、`docs/12-engineering-standards.md`、`.qoder/rules/betterwork-ui.md`。

## 11:06 字号基线与间距标尺全仓收口（状态：完成，提交待指令）

- 背景：光哥对 09:21 一节留给他的两项都拍了「做」——① 说明文字太小要补；② 偏离标尺的间距值要收进标尺。
- 变更内容：① `styles.css` 新增 `small { font-size: 12px }` 全局基线，全仓 80 处 `<small>` 一次性转可读，知识页「索引与模型」的说明文字从 9.6px 回到 12px；既有豁免不受影响（品牌字标 `.brand small` 仍 10px，特异性更高）。顺手核过 `<code>`／`<kbd>`／`<pre>` 各 1 处，它们继承父级字号，没有同类问题。② 118 处偏离标尺的 `gap` 按映射表归格（2/3→4、5/6/7/9→8、10/11→12、14/15→16、20→24），收口后只剩 4（34）/8（123）/12（56）/16（16）/24（2）五档、偏离 0 处；批量改写走 `/tmp/gap-snap.mjs`，脚本对未映射取值直接中止不写入。③ `standards/coding-standard.test.ts` 新增「间距只用标尺上的档位」与「次要文本 small 有 12px 字号基线」两条护栏，该 describe 共 4 条。④ docs/10 §9.8 记下映射表与真实分布，docs/12 §8 与 `.qoder/rules/betterwork-ui.md` 同步。
- 关键决策：5 归到 8 而不是就近归到 4——5 与 6 是同一语义档（标签与控件、图标与文字），纯就近取整会把它们拆成 4 与 8，把原本差 1px 的两处放大成差 4px。标尺的作用是消除近似值，不是保住每一位原值，所以按「同档归一格」映射。护栏只锁 `gap` / `row-gap` / `column-gap`，`margin` 与 `padding` 仍是分散裸像素，纳入同一标尺要逐页回看，另立一轮。
- 教训：字号护栏只扫得到显式声明，抓不到「压根没写」的 UA 默认值——这类「缺省即违规」的洞要靠基线规则加「钉住基线存在」的护栏收口，继续在各组件补 `font-size` 只会漏下一次。同理，文档里写「已全量对齐」之前必须先有可复现的统计或护栏，上一轮就是靠一次局部整改下了全称结论。
- 测试证据：`npm run verify` 退出 0（136 文件／1329 测试，含 lint、format:check、typecheck、test、build；只剩两条已知的 Zod Rollup `@PURE` 注释警告）。四条间距护栏逐条做过变异验证：删 `.page-toolbar` 的 gap、加 `.memory-settings .page-toolbar { gap:10px }`、写入 `gap: 6px`、删 `small` 基线，分别让对应护栏转红，验证后从备份还原并复核文件尾与 `mutation-probe` 残留为 0。`prettier --check` 全绿；中文改动逐处 `grep` 回读。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`standards/coding-standard.test.ts`、`docs/10-ui-ux-system.md`、`docs/12-engineering-standards.md`、`.qoder/rules/betterwork-ui.md`、`/tmp/gap-snap.mjs`。

## 16:27 知识卡片「更多」浮层改回基座（状态：完成，第 2 条待光哥定产品口径）

- 背景：光哥回看知识页提三条——① 「更多」下拉的字号比它的触发按钮还大；② 「最近作业（含已取消）」在作业跑完、通知已进消息中心之后仍常驻界面；③ 菜单点别处不收起，而且能同时打开两张卡片的「更多」。
- 根因（①③同源）：`KnowledgeDocumentCard` 的「更多」不是 `PopoverMenu` 基座，而是自造的 `<details>` + `.knowledge-actions-menu-panel`（`position:absolute` 加 overlay 阴影）。`<details>` 没有「点外面收起」的语义，于是 ③ 成立；面板里的 `button` 没写 `font-size`，`button { font: inherit }` 一路继承到 body 的 14px，而触发按钮 `.knowledge-more-button` 是 12px，于是 ① 成立；每张卡片各持一个 `<details>`，互不知晓，于是能开两个。三条症状是同一个实现选择的结果，不是三次疏忽。
- 变更内容：① 卡片「更多」改用 `PopoverMenu`（`align="end"`、`aria-expanded`，背板收起、Esc、焦点归还、方向键与视口碰撞随基座一并获得），`.knowledge-actions-menu*` 死样式全部删除；② 基座补两项能力：打开时读取触发控件的计算字号并内联镜像到浮层根，`PopoverMenuItem` 增加 `tone: 'danger'`（自造面板原有的红色危险项语义不能丢）；③ `.popover-menu` 给 13px 兜底，`.popover-menu-item` 不再自带字号；④ docs/10 §10.1 新增「浮层一律复用 `PopoverMenu` 基座」条款，并修正「Popover 尚未落地」的过期记载，`.qoder/rules/betterwork-ui.md` 同步；⑤ 护栏新增两条：`.popover-menu-item` 不得自带字号、overlay 阴影只允许出现在登记过的 6 个浮层表面（白名单逐项写理由）。
- 关键决策：不在自造面板上补 outside-click 与字号——那是补丁式修复，基座的背板／Esc／焦点／碰撞语义仍会缺一份；能力补进基座、页面复用基座。
- 教训：**给已有测试文件加用例前，先确认该文件是否已存在**。本轮我用整文件写入的方式给 `PopoverMenu.test.tsx` 加三条，把原有 14 条（定位、键盘导航、焦点归还、背板、ARIA）整体覆盖掉了，靠 `git diff --stat` 里它是 `M` 而非新增才发现，回滚后改为在原 describe 上追加 2 条——其中「背板点击收起」原文件已有，我的重复用例删掉。写文件工具不会为已存在的文件向我二次确认，这一步只能自己查。
- 测试证据：`npm run verify` 退出 0（137 文件／1336 测试）。新增 `KnowledgeDocumentCard.test.tsx` 3 条（不再渲染 `<details>`、点开出现背板与 3 个 menuitem、选中即回调并收起、busy 时禁用项点不动）；`PopoverMenu.test.tsx` 14→16 条（字号镜像、danger 类）。两条新护栏做变异验证：写 `.mutation-overlay` 的 overlay 阴影、给 `.popover-menu-item` 补 `font-size: 13px` 分别转红，验证后从备份还原并确认残留 0。
- 未收口：第 ② 条「最近作业」常驻是 KM15 刻意补齐、写进验收标准的功能（任务板 KM15 ③，清单 §1.2／§1.5／§8.8 要求「作业收口后进入最近作业」「重启后仍在」「已取消的不消失」），与光哥「成功就该消失」的预期冲突，属产品口径。三个方案已列给他选，未动代码。库内事实：`knowledge_jobs` 两条均为 `succeeded`（08:47 语义重建、11:02 来源检查），`notifications` 41 条，说明消息中心那条路已经走通。
- 相关文件：`apps/desktop/src/renderer/src/components/KnowledgeDocumentCard.tsx`、`apps/desktop/src/renderer/src/components/PopoverMenu.tsx`、`apps/desktop/src/renderer/src/components/PopoverMenu.test.tsx`、`apps/desktop/src/renderer/src/components/KnowledgeDocumentCard.test.tsx`、`apps/desktop/src/renderer/src/styles.css`、`standards/coding-standard.test.ts`、`docs/10-ui-ux-system.md`、`.qoder/rules/betterwork-ui.md`。

## 17:42 知识页「最近作业」改为默认折叠（状态：完成，提交待指令）

- 背景：16:27 一节把第 ② 条的三个口径交给光哥选，他选「全部保留，但默认折叠」。
- 变更内容：终态作业收进 `<details className="knowledge-recent-jobs">`，标题写成「最近作业（含已取消）· N 条」并默认收起；进行中的作业不折叠，仍带「取消」与「查看条目」。`.knowledge-jobs-heading` 死样式删除，换成折叠组收起／展开两态文字颜色，沿用 `.task-run-history summary` 已有的内联披露样式。验收清单 §1.2／§1.5／§8.8 与来源说明按新渲染文案改写。
- 关键决策：这里用 `<details>` 而不是再造一套状态——它是文档流内的折叠块，不是浮层，正好落在 16:27 那条「浮层必须复用 PopoverMenu」禁令的适用范围之外，且与仓库既有写法一致；背板、Esc、焦点归还那套语义是浮层的需求，折叠块只需要一个开合。
- 测试证据：`KnowledgeView.test.tsx` 13/13，其中「已取消的作业留在最近作业里」升级为断言默认不带 `open`、点击 summary 后展开（实测 jsdom 会切 `open`）；`npm run verify` 退出 0（137 文件／1336 测试）。
- 相关文件：`apps/desktop/src/renderer/src/views/KnowledgeView.tsx`、`apps/desktop/src/renderer/src/styles.css`、`apps/desktop/src/renderer/src/views/KnowledgeView.test.tsx`、`docs/development/knowledge-km15-checklist.md`。

## 17:55 UI 一致性评估报告（状态：完成，等光哥定改进排期）

- 背景：光哥追问「为什么你总要自造 UI 组件」，要求举一反三查全仓所有界面，并出一份 UI 一致性评估报告与改进建议，可对照 ClawBible Cloud 的组件设计。
- 变更内容：新增 [UI 一致性评估](../reviews/2026-09-26-ui-consistency.md)（141 行）：根因分析、组件台账（docs/10 §10.1 声明 17 个基础组件 vs 实际 4 个基座）、六类缺陷（浮层与模态 4 套实现、9 处原生 `<select>` 与 `FieldSelect` 并存、Tabs 三套语义、行卡片 9 套几何、`min-height` 21 种／`border-radius` 12 种／`z-index` 10 种／焦点环 3 种、`SettingsView` 零骨架使用）、与 Cloud 的对照（值得借 5 条、明确不照搬 5 条）、P0–P3 分期计划。docs/10 §10.1 加了指向该报告的入口。
- 关键决策：报告的每条结论都要自己复核后才写。子代理给了两条错误结论——「9–10px 违规小字号 6 处」实际全部落在图形化徽标豁免内；「`SkillsView` 的 `role="group"` + `aria-pressed` 是缺陷」实际是视图模式切换的正确语义。两条都在报告 §6「核实修正」里显式记下，不随调研原样落文档。
- 教训：**白名单写成了豁免清单，等于把已知缺陷合法化**。本轮新增的 `OVERLAY_SURFACES` 护栏把 `.notification-panel`（缺 `aria-expanded`、无 Esc、无焦点管理）和 `.slide-viewer` 登记成"合法浮层表面"，护栏只会拦新增，不会推动存量收敛。正确形态是 Cloud 的棘轮：记录当前数量、只许降不许升、降了要求收紧基线，并对每一项标注「待迁入哪个基座」。凡加白名单，先问一句"这是在描述现状，还是在承诺收敛"。
- 测试证据：纯文档产出，未改代码；`prettier --check` 通过；报告内引用的 file:line 逐条 grep 核实（原生 `<select>` 9 处、`.text-button` 定义两次、`ConfirmationDialog` 有 inert/Esc/焦点归还而 `ModelEditorSheet` 没有、`notifications.tsx:225-227` 无 `aria-expanded`、行卡片 9 套几何取值表）。
- 相关文件：`docs/reviews/2026-09-26-ui-consistency.md`、`docs/10-ui-ux-system.md`、`/Users/kevin/Dev4AI/ClawBible.AI/clawbible-cloud/frontend/`（只读对照）。

## 09:27 §12 执行载体 B 落地为探针，关键词基线预跑（状态：混合列待光哥启动）

- 背景：光哥定了「题目不改、载体选 B」，并授权脚本读取本机模型凭据发起真实向量调用。实现时撞到一条硬边界：模型 Key 由 Electron `safeStorage`（macOS 钥匙串）在 Main 进程解密，非 Electron 进程取不出明文；我去定位凭据存储目录时被安全策略拦下，随即停手改方案，不绕钥匙串、不抄密文自解。
- 关键决策：endpoint／模型标识／Key 改由启动者在命令行用 `KM15_EMBED_*` 环境变量给同一个真实模型，Key 只进请求头，不打印不落盘；跑法由光哥自己执行一次，凭据不经过我。探针不起第二套检索实现，直接复用生产侧同一条管线（受管提取 Worker、FTS5、`/embeddings`、RRF、向量扫描 Worker），并默认在 `/tmp` 新建一次性 Vault 导入合成资料，不碰正式库。
- 变更内容：新增 `scripts/km15-semantic-probe.ts` 与冻结题集 `docs/development/fixtures/km15-semantics/questions.json`；`package.json` 增 `km15:semantics`（build → esbuild 打 CJS → 执行，与既有 `office:probe` 同类口径）；题集文件 §5 改写为已选定的载体、命令、凭据边界与代价说明。
- 教训：esbuild 打成 ESM 会在 exceljs 的动态 require 处直接崩（`Dynamic require of "crypto" is not supported`），改 `--format=cjs` 后可跑，代价是 `import.meta.url` 不可用，改为「必须在仓库根目录启动」并用 `process.cwd()` 取路径＋缺文件时明确报错。另外 `no-console` 只允许 warn/error，探针的正常输出必须走 `process.stdout.write`。
- 测试证据：无凭据预跑通链——导入作业 10/10、关键词基线列命中 **5/20**（Q15/Q16/Q17/Q19/Q20 命中；Q18「双录 质检 抽样 比例」未命中，文档原句是"百分之五随机抽取"，没有"比例"这个字面词，而关键词路要求所有分词都命中），明确标注这不是 §12 正式结论。`npm run typecheck` 退出 0、`eslint` 对新文件退出 0；`npm run verify` 唯一红项是 KM14 性能基准（并发下 p50 902.1／p95 1011.7／setup 17.7s，超 1000ms 线 11.7ms），单跑同一文件复绿 3/3（p50 270.7／p95 291.1／setup 6.8s，RSS 2.9MiB），p50／p95／setup 同比例放大且当时 5 分钟负载 11.78，按既有口径归因为机器负载，未改阈值未改规则。
- 边界：本条日志与并行记忆会话的 08:55 小节同在一个 `docs/logs/2026-09-26.md` 工作树改动里，本轮只提交探针、题集、`package.json` 与题集说明文档，日志文件留待两拨改动分开落定；真实混合列未跑，清单 §12 保持待验；未推送。

## 20:04 界面观感基线：控件几何、叠放层级与页签语义（状态：完成，待光哥窗口回看）

- 背景：光哥定了「今晚先收拾界面，当前界面感觉不统一」，并在两个口径上拍板——控件高度以 **32px 为常规档**（密集行内 28px、主行动 36px），顺序是**先统一观感、再补基座**。
- 变更内容：
  - `styles.css` 的 `:root` 新增 `--control-height-sm` / `--control-height` / `--control-height-lg`（28/32/36）、`--control-radius` 6px、`--control-border`，以及 `--z-*` 11 档命名层级；**50 处**控件边框/圆角/高度声明改取档位，**13 处** `z-index` 全部换成命名档，焦点环收为唯一写法 `2px solid var(--focus-ring)`（`.material-chip select` 原为 1px，深色底上几乎看不见）。相对叠放顺序沿用改造前的实际结果，唯一有意改变是模态一档高于浮层（此前模态背板与菜单同为 12，靠 DOM 顺序决胜）。
  - `.text-button` 从「前后两条规则各写一半」合并为单一定义（25px 无框小胶囊 → 28px 带边框按钮）；`.primary-button` 成为 `--control-height-lg` 的真实消费者、圆角 8px→6px；`.secondary-button`/`.context-toggle` 圆角 7px→6px、边框改用控件边框档。
  - `MemoryView` 记忆分组页签补 `role="tablist"` 与 `role="tab"`、`aria-selected`，回归断言同步改写——`getByRole('button')` 在 `role="tab"` 下不再命中，这类改动若没有测试会静默通过。
  - docs/10 新增 §9.10 控件几何与浮层字号、§9.11 叠放层级两张档位表，§10.1 建**组件台账**（声明 17 个基础组件 vs 实际 4 个基座）；`.qoder/rules/betterwork-ui.md` 第一条改为「写 UI 交互前先查台账」。[UI 一致性评估](../reviews/2026-09-26-ui-consistency.md) 的 P0 五项全部收口，页签方向键与 roving tabindex 显式并入 P2 的 `Tabs` 基座（`ContextPanel` 那处页签同样缺键盘逻辑，两处一起收，避免各写一遍）。
- 关键决策：**"裸值残留 0"这类结论必须先说清统计口径**。把 `.primary-button`/`.secondary-button`/`.text-button` 三个按钮类加进护栏选择器后，当场抓出 4 处漏网（主行动按钮 8px 圆角、次级按钮 7px 圆角与 `--border` 边框、文字按钮 `--border` 边框）——此前那句"表单控件裸值残留 0"只在 `input/select/textarea` 口径下成立。护栏没写到的地方，规范就等于没生效。
- 遗留（新发现，建议进 P3 门禁项）：三个墙钟预算门（KM14 p95、`memory-retrieval` 1,000 条排序、office-parser 解压预算）在 137 文件并发跑时不稳定通过，负载 15 以上时连单跑都红；它们与 `npm run verify` 同属一道门，等于给提交加了随机红。方向不是放宽阈值，而是**把计时基准挪进独立串行档**（`npm run verify` 跑功能测试，`npm run bench` 单独串行跑基准并打印样本），提交门禁只认前者。本轮未擅自改动门禁构成，待光哥定。
- 教训：给主行动按钮的透明边框留豁免时，用**值精确匹配**（`1px solid transparent`）而不是按选择器整条放行，否则 `--control-*` 的其余约束一起失效。填充式按钮的透明边框只为与带边框控件保持同一盒几何，颜色由填充色负责。
- 测试证据：`standards/coding-standard.test.ts` 32/32、`MemoryView.test.tsx` 22/22；变异验证把 `.primary-button` 的边框改回 `1px solid var(--border)` 后护栏转红并精确指到 `styles.css:425`，恢复后复绿。最后一轮改动后单独复跑 `npm run lint`、`format:check`、`typecheck`、`build` 全部退出 0。全量 `npm test` 连跑四轮，红项都在**三个墙钟预算门**上，且组合每次都变：第 1 轮 KM14（p95 9839.2）＋ office-parser 解压预算 30s 超时，第 2 轮只剩 KM14（p95 1930.9），第 3 轮（负载 4.33 时起跑）KM14 p95 2899.0，第 4 轮 KM14（p95 5247.3）＋ `memory-retrieval` 的 1,000 条排序预算（281.5ms > 200ms）＋ office-parser 超时。单跑同一文件曾全绿：office-parser＋KM14 串行 9/9（p50 359.8／p95 459.0／setup 8.5s）、KM14 单跑 3/3（p50 579.1／p95 650.4，当时 1 分钟负载 9.07），而负载升到 15 以上时 KM14 单跑也红（p95 2054.3）。未改阈值、未改规则、未跳过用例。
- 边界：本轮只动观感与护栏，未新建任何基座；P1（`Modal`/`Sheet`、`Field` + 9 处原生 `<select>`）与 P2（`Tabs`/`ListRow`/`Badge`/`EmptyState`、23–40px 密集高度档 30 处残留、卡片与徽标圆角）待光哥定排期。UI 观感需光哥在已开的应用里回看确认，我不用自动化驱动界面验证；改动未提交、未推送。
