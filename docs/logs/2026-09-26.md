# 2026-09-26 工作日志

## 07:53 记忆治理页检索与滚动区验收收口（状态：完成，提交待指令）

- 背景：昨夜 23:21 一节记录的改造（分组收成页签、页面唯一滚动区、`memory:list` 后端全库检索）等待光哥在真实窗口走查；本节记录验收结果与压测数据清理。
- 变更内容：无源码改动。清理开发库内直投的压测数据——按 `/tmp/betterwork-memseed-ids.json` 的 id 清单删除 66 行 `memory_records`（60 条样例，加上光哥走查时在「压测样例 01／02」两条上追加的 6 个修订）与 60 行 `memory_operations`。删除前的引用守卫由「任何指向本批修订的引用」收窄为「本批之外的行指向本批修订」，样例自身的修订链属于同一批，随批删除。
- 关键决策：不做级联清理，遇到集合外引用即停下报告；宁可先查引用来源，也不放宽条件硬删，避免误伤真实记录。
- 教训：其一，`COALESCE(r.topic_key, ?)` 的回退值若绑成检索词本身，`topic_key IS NULL` 的行会恒真命中——断言「`%` 不是通配符」时必须钉住具体命中集合，只断言「不为空」抓不到这类错，本次正是 `%` 用例把缺陷钉住。其二，跨零点会话里每个时间都要重新 `date`：23:21 与 07:53 分属两个日志文件，不能凭上下文续写。其三，并行会话在途时仓库级门禁（lint／format／typecheck）会因他人文件变红，归属要用文件 mtime 与报错路径逐条核对后再说话，不得顺手修别人的半成品。
- 测试证据：光哥真实窗口走查通过——页签一次只渲染一组、滚动条落在版心右缘、搜索能命中未加载的记录、超出一页时给出说明。清理后库内 `memory_records` 共 1 行（`confirmed` 1 行），样例残留 0、操作残留 0，真实记录「收入按回款到账金额统计，不使用签约金额」仍为 revision 1；清理前备份在 `/tmp/betterwork-db-backup-20260925-2155.sqlite`。
- 相关文件：`docs/logs/2026-09-25.md`、`docs/07-mvp-and-roadmap.md`、`docs/10-ui-ux-system.md`、`docs/development/memory-contracts.md`、`/tmp/memclean.mjs`、`/tmp/betterwork-memseed-ids.json`。

## 08:44 KM15 前置对账与知识改动本地提交（状态：走查开始，人工栏进行中）

- 背景：光哥逐条核对 [KM15 清单](../development/knowledge-km15-checklist.md) §0 前置——0.1 开发实例能起、能操作、无报错（通过）；0.3 系统内已配 3 个模型且均可用，其中 1 个是向量模型；并问 0.4 的 20 题与标注是否现在必须准备。
- 关键决策：0.4 **不阻断** §1–§11 与 §13，只卡在 §12 之前；一旦开跑不得回改标注，所以约定等人工 UI 栏走完再一起定题，先出草稿题供他改定、确认后我才跑语义栏。0.3 补一条落地口径：开跑前要在知识页「索引与模型」的「嵌入模型」下拉里确认选中的就是那个向量模型（下拉只列已启用且已配凭据的嵌入角色 profile），选中即视为本轮 §12 的真实调用授权范围。
- 变更内容：清单 §0 四行改为真实状态；任务板 KM15 行追加 08:40 前置对账一句。知识改动按任务边界本地提交 `9771b0a`（4 个 renderer 文件＋知识 CSS 追加段＋清单＋任务板＋2026-09-25 日志，8 文件 750 插入／108 删除），未推送。
- 测试证据：提交前 `npm run verify` 退出 0（136 文件／1325 测试）；`use-knowledge-library.test.ts` 27/27、`KnowledgeView.test.tsx` 13/13。提交后 `bash scripts/dev-start.sh` 起开发实例（pid 53340），日志只到 builtin Skill/Expert 注册，无报错。
- 边界：本轮我不驱动界面，§1–§9 由光哥操作并按节回填「结果记录」；`.app` 冷启动与旧库升级（§13）单独一轮；`artifact:declare-sources`/`get-run-declarations` 仍无界面消费者，要人工验收它需先补只读的「本次运行声明记录」界面，等光哥拍板。

## 08:51 §12 语义验收题集草稿（状态：待光哥改定冻结）

- 背景：光哥确认清单 §0.3——系统内 3 个模型均可用且只有 1 个向量模型，并已在知识页「嵌入模型」下拉核对选中项，即视为本轮真实向量调用授权。他此前问 0.4 是否现在必须准备，答案是「只卡在 §12 之前」，但他选择先把题准备好。
- 变更内容：新增 `docs/development/knowledge-km15-semantic-set.md` 与合成语料 `docs/development/fixtures/km15-semantics/K01–K10`（10 份 Markdown，7.6 KB，全部虚构的银行/金融科技日常口径，不含真实客户或行内数据）。题集含 14 道同义改写题（题面与资料几乎不共用字面词，用来检验语义这一路的增量价值）＋ 6 道字面重叠题（关键词基线应当命中，两列并列才成对照），逐题预标注到 K01–K10；判定口径写明「按资料身份进入前 5 即命中，不按命中块数计次」，过栏线混合列 ≥16/20，失败归因四选一。
- 关键决策：语料用合成而不是真实库——清单 §12 原文即「共同确认的合成中文资料」，且合成语料可整批重建、可复现，不牵涉把生产资料发给模型服务；若光哥要换成真实资料，题面作废、由他在开跑前重新标注。执行载体给两条路（界面手跑 40 次／`scripts/` 探针脚本直连 Vault 与真实向量模型），探针那条需要单独授权读取凭据，且明确它替代不了 §8 的人工 UI 栏。
- 相关文件：`docs/development/knowledge-km15-semantic-set.md`、`docs/development/fixtures/km15-semantics/`、`docs/development/knowledge-km15-checklist.md`（§0.3／§0.4／§12 改为真实状态并指向题集）、`docs/development/tasks-knowledge.md`（KM15 行）。
