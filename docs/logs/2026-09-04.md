# 2026-09-04

## 06:37 UI 反馈修复：窗口标题栏与 SVG 图标体系（状态：完成）

- 背景：光哥晨间人工验收提出四项反馈：①红绿灯与品牌区拥挤；②双击标题栏无法最大化；③界面图标是 Unicode 字符不是 SVG，观感业余；④询问通知机制设计现状。
- 变更内容：
  - 窗口：macOS 增加 `trafficLightPosition {x:16, y:18}`；`.task-header` 与非工作视图顶部 24px 拖拽条设为 `-webkit-app-region: drag`（此前整个窗口没有任何拖拽区，连移动窗口都不行）；新增 `window:toggle-maximize` IPC（协议定义 + Zod + preload `chrome.toggleMaximize()`），双击标题栏遵循系统 `AppleActionOnDoubleClick` 偏好（缩放/最小化/无操作）；侧栏品牌区下移避开红绿灯。
  - 图标：新建 `renderer/src/icons.tsx`（14 个内联描边 SVG 组件，currentColor、24 网格），替换 App.tsx 全部字符图标（▣◇▤⚙◀✓＋‹›×↑⌄），grep 确认无残留；补对齐样式。品牌字标「算」与格式徽标（MD/PDF）保留文字形式。
  - `docs/10-ui-ux-system.md` §12 新增图标规范与 macOS 窗口交互规范两条底线。
- 关键决策：手写 SVG 图标集而非引第三方库（14 个图标规模不值得加依赖，与 LobsterAI 同思路）；双击行为读系统偏好而非硬编码最大化。
- 教训：交接时的「UI Foundation 已完成」不等于窗口基础交互（拖拽区）已覆盖；今后窗口层改动要实际拖一下、双击一下再交付。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；残留字符图标 grep 为 0。**UI 效果未经桌面确认，需光哥人工验收**（我不驱动应用界面）。
- 人工验收（留给光哥）：①顶部拖拽移动窗口；②双击标题栏最大化/还原（含系统偏好为「最小化」时的行为）；③红绿灯与「算台」间距；④各页面图标观感（尤其侧栏、工具卡片、成果卡片）。
- 遗留：④通知机制——当前产品没有设计（docs/10 仅把「通知」列入 UI-4 远期范围），已向光哥提交最小方案待拍板，未实现。
- 相关文件：`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`packages/agent-protocol/src/index.ts`、`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/icons.tsx`、`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`

## 06:49 SVG 图标规范下沉到规则层（状态：完成）

- 背景：光哥复核「图标必须 SVG」是否已成体系约束。核实结果：docs/10 §12 有（晨间已加），但 `.qoder/rules/betterwork-ui.md`（改 renderer 文件时自动加载的场景规则）没有，存在回退风险。
- 变更内容：`betterwork-ui.md` 补充强制条目——系统操作、导航、按钮等界面功能图标一律使用内联 SVG（图标集 `icons.tsx`，currentColor、24 网格），禁 Unicode 字符/emoji，新增图标先进图标集；品牌字标与 MD/PDF 徽标除外。AGENTS.md 不重复此条（宪法层靠任务路由到 docs/10）。
- 关键决策：规则分层各司其职——docs/10 是详细规范真相源，rules 文件是触发时的强制清单，两层对齐但不互相复制全文。
- 教训：新增设计规范时要同步检查是否需要下沉到规则层，只写 docs 会在日常改码时看不见。
- 测试证据：grep 回读确认新条目字面正确；纯文档变更，不涉及代码。
- 相关文件：`.qoder/rules/betterwork-ui.md`、`docs/10-ui-ux-system.md`、`docs/logs/2026-09-04.md`

## 06:53 标题栏改为贯穿窗口的连续拖拽区（状态：完成）

- 背景：光哥指出对「标题栏」的理解分歧——他（Windows 惯例）指贯穿窗口顶部的一整条，我此前只把中栏任务头当标题栏，左（导航区）右（产物面板）顶部拖不了。他的理解是对的。
- 变更内容：
  - 拖拽区贯穿三栏：侧栏整体可拖（所有按钮 no-drag，含红绿灯周围）、中栏任务头/顶部条（原有）、上下文面板头部与折叠态可拖。
  - 双击最大化统一走模块级 `handleTitlebarDoubleClick`，带交互元素守护；顺带修掉「双击标题栏里的按钮（模型芯片等）会冒泡触发最大化」的隐藏 bug。
  - 修正过程发现并修复一处作用域错误：处理器初版定义在 App 组件内而 ContextPanel 引用不到，已提升到模块级。
- 关键决策：不做覆盖整个窗口的固定 overlay 条（会拦截顶部按钮点击），而是让三栏各自的头部区域成为拖拽段，视觉与手感等效于贯穿横条。
- 教训：与用户讨论窗口层概念时先对齐「标题栏」的指代；Electron 无边框窗口的拖拽区规划要一开始就按全窗口思考，而不是按主工作区组件思考。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过。拖拽手感需光哥人工验收。
- 人工验收：从侧栏空白处、红绿灯周围、右侧面板头部拖动窗口；双击三栏标题区最大化/还原；双击标题区里的按钮不再触发最大化。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`

## 07:00 修正：标题栏行为收缩回各栏顶部标题带（状态：完成）

- 背景：光哥验收发现侧栏中部空白区双击也会最大化窗口（"什么牛马设计"）——上一条把整个侧栏和折叠态细条整条设为拖拽+双击区，判定区扩得没有道理，是对的批评。
- 变更内容：拖拽/双击收缩回各栏顶部标题带——侧栏 = 顶部 40px 透明条 + 品牌行（中部、任务列表、底部完全无窗口行为）；中栏 = 任务头 / 24px 顶部条（不变）；右栏 = 面板头部 / 折叠态仅顶部 24px（整条细条不再响应）。折叠态收起按钮与拖拽条做了 z-index 分层避免拦截。
- 关键决策：判定区模型固定为「窗口顶部横带」，与 Windows/mac 常规标题栏一致；任何不在顶部带内的空白区域永远不承载窗口行为。
- 教训：给区域加窗口行为前先问「用户在这个位置双击，预期会发生什么」；预期外的位置一律不加。判定区宁可小而标准，不可大而便利。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过。手感需人工验收。
- 人工验收：①侧栏中部空白双击无任何反应（重点）；②顶部品牌行与红绿灯周围可拖、双击最大化；③右侧面板头部/折叠条顶部 24px 同上；④中栏任务头同上。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`

## 07:04 口号标点与展示字体修正（状态：完成）

- 背景：光哥指出欢迎页三处文案/排版问题：①口号末尾不该有句号；②口号字体难看；③副文案第一个句号应为逗号。
- 变更内容：
  - 口号「以我所知，成我所作」去掉末尾句号，同步 5 处（App.tsx、docs/08-brand.md、docs/01-product-definition.md、README.md、AGENTS.md），并在 docs/08 §3 立规矩：独立口号末尾不加句号。
  - 副文案改为「从一个清楚的问题开始，算台会协助你把过程沉淀为可以继续使用的成果。」
  - 展示字体从 `Georgia + 宋体(Songti SC)` 换为 `Georgia + 楷体(Kaiti SC/STKaiti/KaiTi)`（styles.css 5 处：品牌字标、欢迎口号、空态大标题、成果详情标题、Markdown 预览标题）；docs/10 §9.7 补充规定展示标题用楷体衬线栈、不用宋体。
- 关键决策：选楷体而非 modern 无衬线，理由是楷体笔锋与「算」的品牌气质契合且 macOS 自带零依赖；如光哥偏好现代观感，回退方案是把衬线栈换成 PingFang SC Semibold（一处改动）。
- 教训：宋体在大号加粗下笔画僵硬，不适合做展示字体；产品语言（口号标点）变化要按变更纪律同步 README/产品定义/品牌文档/AGENTS 四处。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；Songti 残留 grep 为 0；口号 5 处 grep 回读正确。字体观感需人工验收。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`、`docs/08-brand.md`、`docs/01-product-definition.md`、`docs/10-ui-ux-system.md`、`README.md`、`AGENTS.md`

## 07:11 首版品牌标志定稿并接入应用（状态：完成）

- 背景：光哥授权主导设计产品 logo；要求简单明了、承载产品定义、背景必须透明；替换左上角「算」字。
- 变更内容：
  - 设计定稿：圆角方框（工作台/台面）+ 横梁算珠（算盘）+ 三实珠（以我所知·已知积累）+ 玉绿核心珠（成我所作·被激活的知识）。完全落在 docs/08 §4 既有「算盘 × 智能工作台」方向与「一个玉绿色核心珠」的既定意象内。
  - 新增 `docs/assets/betterwork-logo.svg`（彩色主文件：墨色 `#1D2420` + 玉绿 `#2F6B5D`，透明背景，无底板无文字）。
  - 新增 `renderer/src/brand-logo.tsx`（单色变体：`currentColor` 适配四套色系，核心珠转空心描边），侧栏品牌位替换「算」字为标志（24px），`.brand-mark` 去掉实底方块与字体。
  - `docs/08-brand.md` 新增 §4.1 首版标志定稿：构成语义、彩色/单色变体、使用规则（背景必须透明、16px 最小尺寸、不拉伸不加底板不放文字、字标不与标志合并绘制）。
  - README 顶部展示标志。
- 关键决策：①单色版用「空心描边」区分核心珠而非靠细缝隙，16px 下仍可读；②应用内用 `currentColor` 单色渲染而非常驻玉绿，避免与暖砂/远洋等色系冲突——彩色版保留给 README、未来 Dock 图标等品牌场景；③去掉侧栏实底方块，标志自身的圆角框就是「台」。
- 教训：先读品牌文档再动笔——「图标方向」一节早已锁定算盘意象与核心珠规则，设计是收敛已有的方向，不是自由发挥。
- 测试证据：`xmllint` 校验 SVG 通过；typecheck、42 个测试、构建全绿；`git diff --check` 通过。**标志观感与 16px 可读性需人工验收**。
- 人工验收：①侧栏左上角标志观感（浅/深色与四套色系下切换看适配）；②README 顶部渲染；③把 SVG 缩到 16px 看识别度。
- 遗留：Dock/应用打包图标（.icns）需要 PNG 导出管线，待打包阶段处理。
- 相关文件：`docs/assets/betterwork-logo.svg`、`apps/desktop/src/renderer/src/brand-logo.tsx`、`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`、`docs/08-brand.md`、`README.md`

## 07:26 对话界面三处修正（状态：完成）

- 背景：光哥截图反馈三处问题：①助手回复卡片顶部大片空白（询问是否因思考过程未显示）；②「保存为 Markdown」通栏横幅突兀，常见产品没有这种设计；③问题已发送并回答后输入框仍保留上一轮问题，点开历史任务还会反向填充。
- 排查：按 GATE-0 先查 SQLite——最新 run 的前两条 `message.delta` 就是 `"\n\n"` 与 `"\n"`，确认空白是模型输出自带开头空行被 `white-space:pre-wrap` 原样渲染，不是预留思考区（`reasoning.delta` 按纪律本就不渲染）。
- 变更内容：
  - 助手回复渲染与「保存为成果」内容统一 `trim()`，不再为模型输出开头空行留白；SQLite 事件流保持原始输出，不动 Provider。
  - 通栏横幅改为气泡下方低调小按钮「保存为成果」（12px 描边、ArtifactIcon SVG、与气泡左缘对齐），保存结果以小字注记跟在按钮旁，成功绿、失败红；对齐 LobsterAI 消息操作按钮模式。
  - 新增 `sentPrompt` 承载已发送问题，与输入框草稿 `prompt` 分离：发送后清空输入框；`selectRun`/`selectTask` 不再把旧问题填回输入框；切换/新建任务时清理注记；欢迎页判定改为 `events.length === 0 && !activeRunId`，避免发送瞬间闪回欢迎页。
- 关键决策：trim 放渲染层与 Artifact 保存边界，事件流保持模型原始输出作为真相源；注记区分成败两态（旧实现失败消息也走绿色 inline-message）。
- 教训：输入框草稿与会话记录共用一个 state 是「发送后不清空」这类产品缺陷的典型根源；`pre-wrap` 渲染流式文本必须处理模型输出的首尾空白。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；改动中文片段 grep 回读逐字通过；开发实例已重启待人工验收。
- 人工验收（留给光哥）：①新跑一个计算任务，助手卡片顶部无空白；②回复完成后气泡下方出现小按钮，点击保存后有注记并跳到成果面板；③发送后输入框清空、气泡仍显示问题；④点历史任务，输入框保持为空。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`

## 07:30 展示字体收缩为仅欢迎页口号一处（状态：完成）

- 背景：光哥在成果详情页发现标题「计算: (128 + 72) / 4」用了楷体衬线，明确系统内只有欢迎页口号「以我所知，成我所作」允许特殊字体，其余一律默认字体。
- 变更内容：移除 `.empty-page h1`（空态大标题）、`.artifact-detail-page h1`（成果详情标题）、`.markdown-preview h1/h2/h3`（成果内标题）三处 `Georgia + 楷体` 衬线栈，改为系统默认字体（保留原字号/字重/行高）；`font:` 简写因必须携带字体族，拆为 `font-size/weight/line-height`。`.welcome h2` 口号保留衬线。docs/10 §9.7 同步改写为「全系统唯一展示字体位置是欢迎页口号」。
- 关键决策：字体纪律入文档真相源而非只改样式——特殊字体的适用范围从「一类元素」收紧为「一个元素」，文档与实现必须同步，否则下次又会铺开。
- 教训：07:04 把楷体铺到 5 处展示位是过度泛化——光哥拍板的是「口号用楷体」，不是「展示标题都用楷体」；特殊设计获批时按最小范围落地，扩展前再确认。
- 测试证据：Georgia/Kaiti 全文件 grep 仅剩 `.welcome h2` 一处；docs/10 改动 grep 回读正确；typecheck、42 个测试、构建全绿；`git diff --check` 通过。观感需光哥人工验收。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`

## 07:31 标志重做 v4（负形算盘，渲染自检后定稿）（状态：完成）

- 背景：光哥否决 07:11 首版标志（细线框小珠"也算设计？"），授权摆脱既有文档的算盘方向限制、以小米式极简为标杆自由发挥。
- 变更内容：标志重做为 v4——玉绿圆角台面 + 镂空横梁 + 梁上一珠、梁下二珠；珠梁加粗放大、圆角放大趋近超椭圆；全部负形，实心色块撑起小尺寸。`docs/assets/betterwork-logo.svg`（彩色主文件，透明底）与 `brand-logo.tsx`（currentColor 单色版）同步更新；docs/08 §4.1 更新构成语义与更迭记录。
- 关键决策：①引入**渲染自检环节**——qlmanage 渲染 16/32/128px 与深色底 PNG 后亲自查看再交付，这是上一版流程上缺失的一步；②横向探索并否决四个撞型方案：贯通梁（似汉堡）、居中珠叠梁（似头像/个人中心）、横排三珠（似加载点）、细线框（v1 噪点）；③约束放开后仍保留算盘意象——梁分上下是可成立的极简几何，前提是 execution 到位。
- 教训：小尺寸标志的铁律是大色块 + 负形，细线叙事必死；动笔前先排查常见图标语义占位（头像/汉堡/加载/显示器全被占用）；自我渲染是标志设计唯一的真验收。
- 测试证据：xmllint 通过；typecheck、42 个测试、构建全绿；16/32/128px 与深色底渲染人工查验；`git diff --check` 通过。最终观感需光哥人工验收。
- 人工验收：侧栏左上角实际观感；候选对比图与被否方案渲染在 `/tmp/logo-preview/`（`open /tmp/logo-preview/sheet.svg.png` 可看 B/C 候选对比）。
- 相关文件：`docs/assets/betterwork-logo.svg`、`apps/desktop/src/renderer/src/brand-logo.tsx`、`docs/08-brand.md`

## 07:44 统一页面骨架：消除导航切换时的版面跳跃（状态：完成）

- 背景：光哥指出工作/成果/知识三个主内容页版心宽度不一致（对话列 780、成果列表 860、成果详情与知识 900），且只有工作视图有贯穿页头带，其余页面标题缩在居中列内，切导航时主工作区左上角与宽度都在跳动；判定为实现缺陷而非设计。拍板：版心统一 860px，用 layout 骨架统一，规范入档。
- 变更内容：
  - styles.css 新增三个 layout 类：`.page-header`（贯穿页头带：同高、左缘 28px、同分隔线、拖拽区，左「返回+eyebrow+标题」右「操作区」）、`.page-scroll`（页头下独立滚动区）、`.page-body`（860px 版心，窄窗两侧各留 24px）；删除 completed-work/knowledge/artifact-detail 三页各自为政的盒模型与页内 header 规则；composer 与消息列同宽（同一版心公式）；`.task-header`/`.task-actions` 更名并入骨架。
  - App.tsx：工作视图消息区套 page-body；知识、成果列表、成果详情三页改为「页头带 + page-scroll + page-body」结构，页内标题/说明上移页头带或改为 page-intro；仅设置视图保留顶部拖拽条（声明特例，二级导航布局未统一）。
  - 规范：docs/10 §8.1 版心改为统一 860px，新增 §8.3「统一页面骨架」；betterwork-ui.md 加强制条目（新增/修改页面必须复用骨架，禁止自定版心与页头）。
- 关键决策：用共享 layout 类而非 React 壳组件——四页结构已统一为「header + scroll(body)」，类约束即可防止各自发挥，改动面最小；工作视图对话列从 780 放宽到 860 换取全局一致。
- 教训：三个页面分三批实现、docs/10 §8.1 只给了宽泛阅读宽度范围没定版心，是跳跃的根源；「建议区间」不等于「统一取值」，布局规范必须给出唯一值和唯一实现来源。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；中文片段逐条 grep 回读正确；旧类名 task-header/task-actions/artifact-heading 残留 grep 为 0。版面观感需人工验收。
- 人工验收（留给光哥）：①工作/成果/知识/成果详情四个页面来回切换，标题坐标、版心宽度、分隔线完全对齐无跳跃；②对话消息与输入框左右边缘对齐；③成果详情页标题在页头带内、返回键同坐标；④窗口缩窄时版心两侧留白不小于 24px。
- 遗留：设置页（二级导航布局）未纳入本次骨架，属声明特例；CompletedWorkPage 为未被引用的遗留组件，本次未动。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`、`.qoder/rules/betterwork-ui.md`

## 07:49 右栏分隔线与页头带贯通对齐（状态：完成）

- 背景：光哥截图指出主工作区页头带底边与右侧上下文面板的分隔线不在一条水平线（目测差约 6px），观感山寨。核算：主区页头带为 min-height 70px 实际 70px；右栏为「当前任务行 36px + 页签 29px + 底距 10px + 边框 1px」共 76px。
- 变更内容：`.page-header` 高度锁定 `height:70px`（不再用 min-height，防止内容变化后漂移）；`.context-tabs` 底距 10px 收为 4px，右栏顶部两段总高 36+29+4+1=70px，与页头带底边构成同一条贯通横线。docs/10 §8.3 页头带条目补入「高度固定 70px」与右栏顶部区等高对齐的硬性要求。
- 教训：骨架规范写了「同高」没写数值，右栏顶部区又是独立实现的另一段布局，两边各算各的就会差几像素；跨栏对齐的尺寸必须给唯一数值并写进同一条规范条目。
- 测试证据：构建通过；`git diff --check` 通过；docs/10 改动 grep 回读正确；HMR 已生效（7:49:26 styles.css）。对齐观感需光哥人工验收。
- 人工验收（留给光哥）：工作视图右侧面板展开时，主区页头带底线与右栏页签下分隔线连成一条直线；三个页签（过程/资料/成果）下都成立。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`

## 07:53 消除非工作视图右侧的预留空栏（状态：完成）

- 背景：光哥在成果页发现主区右侧有一条空黑带、页头带在半路截断——`.app-shell` 三栏网格固定预留 380px 第三列，但成果/知识/设置视图不渲染上下文面板，形成死栏。
- 变更内容：第三列从固定 `380px` 改为 `auto`，`.context-panel` 显式 `width:380px`、`.context-collapsed` 显式 `width:48px`——面板在则占列，不在则整列消失，主工作区占满剩余宽度。顺带修复：宽窗口下「收起上下文」原先不回收宽度（收起后仍占 380px 列），现在收起即归还。docs/10 §8.3 新增「上下文面板按场景出现，布局网格不得预留空栏」条目。
- 关键决策：面板按 AGENTS.md「按场景出现」原则处理——成果/知识/设置没有任务上下文，不该有右栏；修复方式是让列跟随内容存在，而不是给非工作视图硬造右栏内容（后者是新功能，走路线图）。
- 教训：网格固定列宽 + 条件渲染子元素 = 幽灵空栏；「按场景出现」的元素必须由布局自动跟随，不能靠固定轨道预留。
- 测试证据：构建通过；`git diff --check` 通过；docs/10 改动 grep 回读正确；HMR 已生效（7:52:46）。各视图宽度观感需人工验收。
- 人工验收（留给光哥）：①成果/知识/设置页主区占满剩余宽度，右侧无空栏，页头带贯通到右边；②工作视图右栏仍为 380px、分隔线对齐不受影响；③工作视图点「收起上下文」后主区变宽、48px 细条贴边；④窄窗口（<1280px）下面板仍为覆盖式抽屉。
- 相关文件：`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`

## 07:58 移除工作页头带的模型芯片（状态：完成）

- 背景：光哥指出页头带右上角「DeepSeek-V4-Flash」芯片突兀、完全没逻辑。核实：它是教学期留下的入口，长得像下拉选择器，点击却只是跳设置页——affordance 失真；且 Composer 页脚已常驻显示「provider · 模型」，信息重复。
- 变更内容：删除工作视图页头带的模型芯片按钮与 `.model-chip` 全部样式，移除 `ChevronDownIcon` 引用（图标本身保留在 icons.tsx 集内）。当前模型信息由 Composer 页脚承载，模型管理仍走侧栏「设置」，符合「模型配置使用独立设置空间」纪律。
- 关键决策：不做头部快速切换器——真下拉是新功能且与「模型配置独立设置空间」边界冲突；假下拉不如没有。
- 教训：长得像选择器的导航按钮比死按钮更伤信任；复用早期阶段 UI 前先问它的交互如今是否名副其实。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；model-chip/ChevronDownIcon 残留 grep 为 0；HMR 已生效（7:57:45 App.tsx）。观感需人工验收。
- 人工验收（留给光哥）：工作页头带右上角无模型芯片（≥1280px 时右侧为空，仅 <1280px 显示上下文开关）；Composer 页脚仍显示当前模型。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`

## 08:05 标志定稿 v5：负形「工」字（状态：完成）

- 背景：v4 负形算盘被光哥一眼认成滑板车（横梁像踏板、下二珠像轮子），并明确要求放弃算盘意象、从现代含义「知识工作者的工作台」出发重新设计；授权不受既有文档约束。
- 变更内容：标志改为玉绿圆角方块内镂空负形「工」字——一笔双关：既是「工作台」的「工」，又是知识工作者的文本光标（I-beam），知识经书写成形为成果。`brand-logo.tsx` 掩膜从梁+三珠改为三段「工」字笔画（掩膜 id 改为 `bw-logo-gong`）；`docs/assets/betterwork-logo.svg` 主文件同步；docs/08 §4 图标方向改写为「从现代含义出发、不绑定算盘」，§4.1 更新为 v5 规格并在更迭记录补记 v4 滑板车否决原因。
- 关键决策：先横评两个候选（「工」字/文本光标 vs 台面上悬浮方块）再定稿——悬浮方块读不出含义（似上传按钮）当场弃；「工」字双关直接命中产品定位。撞语义字典新增：梁+下二珠=滑板车。
- 教训：光哥此前已授予「不被文档算盘方向约束」的自由，我却还在算盘词汇表里做变体；收到设计否决时要连底层方向一起推翻，不是修修补补。
- 测试证据：候选经 qlmanage 32/128px 及深色底渲染自检后人工判读；`bw-logo-beads` 全仓 grep 为 0；docs/08 中文片段 grep 回读逐字通过；xmllint 校验 SVG 通过；typecheck、42 个测试、构建全绿。
- 人工验收（留给光哥）：侧栏左上角新标志观感；若「工」字方向仍不满意，下一轮换语义源（工作台台面+成形物）而不是再调笔画。
- 相关文件：`apps/desktop/src/renderer/src/brand-logo.tsx`、`docs/assets/betterwork-logo.svg`、`docs/08-brand.md`

## 08:06 面板收起后完全移除，不再留 48px 残条（状态：完成）

- 背景：光哥质问收起上下文面板后右侧为什么还留一条细条，「你去观察一下有道龙虾，产物面板关闭起来是这样留一条在右边的吗」。查证：LobsterAI 的产物面板由 `shouldRenderArtifactPanel` 条件渲染，关闭后整个卸载、不留任何残条，重开靠头部开关/快捷键——细条是我的自创。
- 变更内容：`ContextPanel` 关闭时 `return null`，删除 `.context-collapsed` 全部样式与 JSX；「查看上下文」开关改为常驻页头带右侧操作区（删除 ≥1280px 隐藏规则），成为唯一重开入口。docs/10 §8.3 面板条目补入「收起后整体移除、不留残条，重开入口固定为页头带开关」。
- 教训：「收起」的通行语义是元素消失并归还空间，不是缩成图标条——残条是我自作主张发明的第三种状态，既不是展开也不是收起；对照参考实现时连「关闭后的状态」也要对照，不只对照展开态。
- 测试证据：typecheck、42 个测试、构建全绿；`git diff --check` 通过；context-collapsed 残留 grep 为 0（顺带修复 ContextPanel 返回类型为可空以通过 tsc）。收起/重开交互需人工验收。
- 人工验收（留给光哥）：①收起面板后右侧完全干净、主区占满、页头带贯通到右缘；②页头带「查看上下文」开关常驻可见，点击可重开；③面板内右上角关闭按钮行为一致；④窄窗口（<1280px）下面板仍为覆盖式抽屉、开关同样可开合。
- 相关文件：`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/styles.css`、`docs/10-ui-ux-system.md`

## 10:46 标志定稿 v6：书上碰撞火花（状态：完成）

- 背景：v5 负形「工」字被光哥评「太抽象」，并有工商银行标志联想；随后按 24 方向整版出图，光哥初选「墨滴落台」并指示改造为「下面翻开的书 + 上面火苗/火花」，寓意「知识的灵感，碰撞的火花」。
- 变更内容：`brand-logo.tsx` 掩膜改为「翻开书 + 四芒星 + 两粒外溅火星」（掩膜 id `bw-logo-spark`），整体图形较选中稿下移 0.8 格做视觉重心微调；`docs/assets/betterwork-logo.svg` 主文件同步；docs/08 §4 增补「AI sparkle / 铅笔」等新撞语义禁令，§4.1 更新为 v6 规格与更迭记录。
- 关键决策：魔法棒替换案经真实渲染验证读作「铅笔+书」被否（光哥提案、数据否决）；纯四芒星撞「AI 功能按钮」俗套，在星两侧加两粒溅点后「火花」从 UI 符号变为物理碰撞，保留选中稿的轻盈气质；星与书不接触，规避「焚书」误读。
- 教训：判断俗套不能靠嘴辩，把提案真渲染出来给对方看，一轮图片胜过十轮争论；设计在「用户选中的方向」上做减法微调，比推翻重来更容易达成一致。
- 测试证据：16/24/32/128px 与深色底 qlmanage 渲染自检（16px 下星与书可辨、溅点按预期弱化）；`bw-logo-gong` 源码 grep 为 0（仅旧构建产物与历史日志残留）；docs/08 中文 grep 回读逐字通过；xmllint 通过；typecheck、42 个测试、构建全绿。
- 人工验收（留给光哥）：侧栏左上角 v6 观感；若小尺寸下读出「人脸」联想（双点似眼），可去溅点退化为纯星版，一句话即可切换。
- 相关文件：`apps/desktop/src/renderer/src/brand-logo.tsx`、`docs/assets/betterwork-logo.svg`、`docs/08-brand.md`

## 13:58 标志 v6 侧栏实装修正：28px 满版去溅点简版（状态：完成）

- 背景：v6 实装后光哥反馈侧栏观感不好、问是否因为太小。诊断：侧栏 `BrandLogo size={24}`，且构图白形仅占约六成宽度（等效细节约 14px），「星+双溅点+书」四元素在 24px 下互相干扰，溅点糊成噪点——不只是尺寸问题，是元素密度问题。
- 变更内容：做 1:1 真实尺寸侧栏模拟对比图（六方案 + 三张放大结构图），光哥选定 E 方案；`brand-logo.tsx` 改为满版去溅点几何（星与书放大、占满版面）、默认尺寸 28，`App.tsx` 品牌位 24→28；docs/08 §4.1 落「双版本规则」——标准版（含溅点，主文件）用于 ≥64px，简版（满版去溅点）用于 <64px 应用内界面，更迭记录补记实装修正过程。
- 关键决策：双版本而非全局二选一——溅点的「碰撞」叙事在大尺寸保留，小尺寸让位于可读性；这是响应式标志系统的通行做法，且 docs/08 原有「溅点小尺寸允许弱化」条款顺势升级为明确规则。
- 教训：渲染自检用的 128px 观感无法预测 24px 实装效果，品牌落地前必须按真实使用尺寸做 1:1 模拟；「是不是太小了」这类体感反馈，用真实尺寸对比图定位比口头解释快。
- 测试证据：1:1 侧栏模拟对比图人工判读（A–F 六方案）；docs/08 中文 grep 回读逐字通过；typecheck、42 个测试、构建全绿；`git diff --check` 通过。
- 人工验收（留给光哥）：侧栏 28px 简版观感（HMR 已生效）；确认后此轮标志工作收口，剩余待办为打包阶段的应用图标（.icns）导出。
- 相关文件：`apps/desktop/src/renderer/src/brand-logo.tsx`、`apps/desktop/src/renderer/src/App.tsx`、`docs/08-brand.md`
