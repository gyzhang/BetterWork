# 2026-09-21

## 08:36 设置导航菜单改名（状态：完成，排序调整待确认）

- 背景：用户反馈设置左栏导航中英文混排、命名与内容不协调（「模型与能力」只配模型、「能力 Skill」中英重复、「MCP 工具」与其余中文项不齐）。
- 变更内容：
  - `SettingsView.tsx`：导航项「模型与能力」→「模型」、「能力 Skill」→「技能」、「MCP 工具」→「MCP」；对应分区 eyebrow 同步为「模型」「MCP」。
  - `ContextPanel.tsx`、`ExpertsView.tsx`：引导文案「请先在设置 → MCP 工具中配置」→「设置 → MCP」，避免指向已不存在的菜单名。
  - `docs/10-ui-ux-system.md` §6.1 设置分区清单与 §7.5 标题、`docs/08-brand.md` §6 信息架构树同步新命名。
- 关键决策：
  - 「技能」与 Composer 能力选择器已有的 `label: '技能'` 对齐，不新造第三种叫法。
  - 仅改导航与分区标题文案，不改 `SettingsTab` 的 tab key（`models` / `skills` / `mcp`），协议与持久化状态无关。
  - 顶栏一级导航「能力」（指向 Skill 页）与设置内「技能」仍存在叫法分叉，已向用户提出，未擅自改动。
- 测试证据：`npm run typecheck` 通过（exit 0）；`prettier --check` 5 个改动文件全部通过；中文文案逐条 `grep` 回读校验字面正确；全仓已无 `模型与能力` / `能力 Skill` 残留（`docs/logs/` 历史记录除外）。未跑 `npm run verify` 全量门禁（改动仅为界面文案与文档）。
- 相关文件：
  - apps/desktop/src/renderer/src/views/SettingsView.tsx
  - apps/desktop/src/renderer/src/components/ContextPanel.tsx
  - apps/desktop/src/renderer/src/views/ExpertsView.tsx
  - docs/10-ui-ux-system.md
  - docs/08-brand.md

## 08:51 设置页反馈通道对齐统一通知机制（状态：完成）

- 背景：CF11 真机验收过程中，用户发现模型「连接成功」以常驻横幅同时显示在编辑弹窗内与模型列表顶部，质疑是否绕过了统一通知机制（消息中心 + 右下角 toast）。核查 docs/10 §11.5 与代码确认：连接测试属短时操作，正确落点是局部 `TransientToast`（自消、不落库），而 model/search/MCP 三处设置页各自用了常驻 `inline-message` state，属「顶部成功横幅」违规；且真相源 §11.5 只写了全局通知 toast、未描述 `TransientToast` 这一层，规则不足以让人「马上确定」用哪种通知。
- 变更内容：
  - 规则固化（先做）：docs/10 新增 §11.5.1「反馈通道决策表」，钉死事件类型→通道→组件→是否落库→例，并明确「两个 toast」区别（`TransientToast` 局部自消 vs `ToastHost` 是已持久化通知的投影）与「`.inline-message` 只保留错误态」；`.qoder/rules/betterwork-ui.md` 与 `AGENTS.md` §6 路由句改为指向 §11.5.1。
  - 新增 `hooks/use-transient-toast.ts`：共享的短时确认状态原语（`toast`/`showToast`/`dismissToast`），供三处设置页复用。
  - `use-model-settings.ts` + `ModelEditorSheet.tsx` + `SettingsView.tsx` + `App.tsx`：把单个常驻 `message` 拆成 `toast`（成功/信息/拒绝，走 TransientToast）+ `error`（表单内联，仅 `.inline-message.error`）；删除模型列表顶部横幅；测试成功→toast、失败→弹窗内联。
  - `use-search-engine-settings.ts` + `SearchSettings`：同一拆分；「正在连接搜索服务…」常驻文案改为按钮 `busy` 态（禁用 + 「连接中…」）。
  - `McpSettings`：同一拆分；校验错误内联、保存/检测/删除结果走 toast，失败由 `trackAction` 静默改为 `reportAction` 呈现。
- 关键决策：
  - 连接测试**不进消息中心**（非长操作），也**不走全局通知 toast**（会连带落库污染回溯列表），只用局部 `TransientToast`。
  - 卡片上的 `connectionStatus`（连接成功/失败/尚未验证）是持久状态标签，不是通知，保持不动。
  - 复用仓库既有的 skills 范式（`toast`+`error` 分离、`.inline-message` 仅错误），不新造第四种反馈形态；新增共享 hook 收敛三处重复。
- 测试证据：新增 `SettingsView.test.tsx`（3 例：模型成功→toast 且无 `.inline-message:not(.error)`、模型失败→内联 alert 且不弹 toast、搜索成功→toast），对齐 `SkillsView.test.tsx` 的护栏断言；`npm run verify` 退出 0（lint+format+typecheck+test+build，82 文件 / 614 测试，Electron build 成功）。中文文案逐条 `grep` 回读、U+FFFD=0。
- 边界：本轮只改反馈通道与规则文档，未动 IPC/协议/持久化/凭据链路；未提交、未推送。工作树中另有用户 08:36 的「设置导航菜单改名」未提交改动（`ContextPanel.tsx`/`ExpertsView.tsx`/`docs/08-brand.md` 及 `SettingsView.tsx`/`docs/10` 的导航文案部分），与本任务无关，未夹带、未覆盖。
- 下一步：等用户确认后提交本反馈修复（建议与用户的改名改动分开提交）；CF11 第二次重启验收仍待用户在真机完成。
- 相关文件：docs/10-ui-ux-system.md、.qoder/rules/betterwork-ui.md、AGENTS.md、hooks/use-transient-toast.ts、hooks/use-model-settings.ts、hooks/use-search-engine-settings.ts、components/ModelEditorSheet.tsx、views/SettingsView.tsx、App.tsx、views/SettingsView.test.tsx。

## 09:48 收敛 Skill 重复入口，一级导航「能力」改名「技能」（状态：完成，覆盖 08:36 节的设置分区清单）

- 背景：用户追问「能力就是技能，为什么要出现在两个地方」。git 溯源：`11403e6`（2026-09-08 22:07）先把 Skill 管理放进设置二级标签，`72580b3`（同日 22:22）因手工验收发现「设置里找不到入口」把它提升为主导航，当天日志原文写明「保留『设置 → 能力 Skill』兼容入口」——有意保留但没有定义退出条件，实质是遗留重复入口；设计真相源 docs/10 §6.1 从未把 Skill 列入设置分区。08:36 那节把「技能」写进 §6.1，等于把遗留追认成设计意图，本节一并回退。
- 变更内容：
  - `lib/view-types.ts`：`SettingsTab` 去掉 `'skills'`，剩余分区按新顺序排列。
  - `views/SettingsView.tsx`：删除「技能」导航按钮、`tab === 'skills'` 分支、`SkillsPage` 与 `SkillsState` 导入及 `skills` prop；导航重排为 模型、搜索、MCP、记忆、外观、通用。
  - `App.tsx`：`SettingsPage` 不再接收 `skills`；一级导航「能力」→「技能」。
  - `views/SkillsView.tsx`：页头 eyebrow「能力 · Skill」→「技能」；空态 eyebrow「能力」→「技能」，文案「内置能力」→「内置技能」。
  - 测试：`App.test.tsx` 点击一级导航的按钮名 `'能力'` → `'技能'`；`SkillsView.test.tsx` 注释术语同步。
  - 文档：docs/10 §6.1 一级导航补入技能与专家、设置分区改为六个并写明分组依据、新增「能力只作总称」与兼容入口收敛说明；§6.1.1「现有能力入口」→「现有技能入口」；docs/08 §6 IA 树合并实装入口并加同一条命名约束。
- 关键决策：
  - Skill 只留一级导航一个家，不做「设置里放跳转链接」的折中——同一页面两扇门没有语义差别。
  - 「能力」归还总称：设置面板标题「偏好与能力」、Composer 的「添加能力 / 已选能力 / 能力类型」、专家详情 `<h2>能力</h2>` 全部保留，只从页面名位置上撤出。
  - 设置分区顺序按配置动作分组：模型 + 搜索（填 Key 即用）· MCP（连接外部工具）· 记忆（我的数据）· 外观 + 通用（偏好）。
  - 正文里的领域术语 `Skill`（「导入 Skill」「还没有 Skill」、卡片副标题「内置 Skill」）不改，本轮只统一导航与页面命名。
- 教训：
  - 改名类任务必须把「文档追认」和「代码现状」分开核对。上一轮为消除文档滞后，把当时的代码现状（含遗留重复入口）直接写进设计真相源，等于用规范文档固化缺陷；正确顺序是先判定哪个入口该留，再让文档记录判定结果。
  - 与 08:51 任务并行改动同一工作树时，`npm run verify` 结果会受 CPU 争用影响：本轮第一次全量 verify 通过（82 文件 / 614 测试），第二次出现 6 个与本次改动无关的超时失败（preflight CLI、mac-process-supervisor、run-service、真实 venv 准备、skill-service），第三次重跑恢复 82 / 614 全绿。超时型失败先复跑再定性，不即兴改被测代码。
- 测试证据：`npm run verify` 退出 0（lint、format:check、typecheck、82 个测试文件 / 614 个测试、build）；`git diff --check` 通过；定向复跑 `App.test.tsx`、`SettingsView.test.tsx`、`SkillsView.test.tsx`、`ComposerCapabilityPicker.test.tsx` 共 32 例通过；中文改动逐条 `grep` 字面回读；`grep` 确认全仓已无 `tab === 'skills'`、无指向设置页的 Skill 引导文案，`onRequestSkillDetail` 仍走 `setView('skills')`。构建保留既有 Zod Rollup 注释警告。未启动应用，界面效果由用户手工验收。
- 相关文件：`apps/desktop/src/renderer/src/lib/view-types.ts`、`apps/desktop/src/renderer/src/views/SettingsView.tsx`、`apps/desktop/src/renderer/src/views/SkillsView.tsx`、`apps/desktop/src/renderer/src/views/SkillsView.test.tsx`、`apps/desktop/src/renderer/src/App.tsx`、`apps/desktop/src/renderer/src/App.test.tsx`、`docs/10-ui-ux-system.md`、`docs/08-brand.md`。

## 11:10 CF11 主线真机验收：结果、两处发现与后续队列（状态：进行中，本卡不得关单）

- 背景：用户要求把主线测试验证工作记下来。CF11（legacy 明文密钥入库）代码已提交（`122fba3`），卡片剩余项是「真机重启两次不重复加密 + 真实模型/搜索可用验收」。本轮按 GATE-0 顺序（先查 SQLite → 再看 dev 日志 → 最后看代码）做实测核查。
- 已验证通过（本机 `$HOME/Library/Application Support/@betterwork/desktop/betterwork.db` 只读查询 + `/tmp/betterwork-dev.log`）：
  - 07:56:06 首启迁移：日志 `凭据迁移：done=4 failed=0 remaining=0 skipped=false`；`credentials` 4 行（3 个 `model-profile` + 1 个 `api-service-profile/baidu_qianfan`）全部 `version=1`，密文 51B/83B；journal 4 行全部 `done`。
  - 三条 `model_profiles.api_key` 长度 0（明文已清空），模型「测试连接」成功（11:04 截图 toast「模型连接成功」）→ 新读优先链路真实可用。
  - schema v24 + v25 已落库；Keychain 授权无弹窗阻断。
- 发现一（安全回归，待修）：连接检测把已迁移的明文密钥回写数据库。`baidu_qianfan` 的 `api_key` 在 08:00:46 重新变为 75 字节明文，`connection_status=connected`，journal 仍 `done` 故不会再被清理。根因是 `TestSearchEngine` 把从 credentials 解出的密钥传给 `searchEngines.recordConnection(provider, status, apiKey)`，而该仓储 UPSERT 含 `api_key = excluded.api_key`；模型侧 `recordConnection(id, status)` 不收密钥，所以三条模型列仍为 0。修复：删除搜索侧 `apiKey` 形参与该赋值（全仓仅一个调用点）+ 回归测试「journal done 后连接检测，明文列仍为空」+ 直接清空现存这条泄漏（凭据回环校验可用，不重跑迁移）。
- 发现二（展示层误报，待修）：`apiKeyConfigured` 仍由 `row.api_key !== ''` 推导，明文列清空后设置页恒显示「未配置凭据」——正是用户 11:04 截图的疑问。这是「清点所有读取方」时漏掉的第三个读者（运行态与 IPC 测试两处已转）。修复：模型与搜索两处摘要改由 `CredentialRepository.status` 判定 + 测试「明文列为空但凭据存在 → 已配置凭据」。`ModelEditorSheet` 的「留空则保持原有凭据」只由 `editing` 推导，与此无关。
- 关键判断：发现一违反 CF11 卡片第 3 条自身口径（双写仅限「迁移未完成时」窗口），因此两处修复都在 CF11 范围内，不新开了卡、不动协议字段；卡片保持 `doing`，两处发现已写入 [tasks-capability-foundation](../development/tasks-capability-foundation.md) 的「§CF11 验收发现」，避免只留在日志里丢失。
- 主线后续队列（按依赖排序）：
  1. 修 CF11 两处发现（含回归测试）→ 清掉 `baidu_qianfan` 明文 → 真机重启两次确认不重复加密（`credentials.version` 仍为 1、journal 不变）→ CF11 关单。
  2. CF12 M1 里程碑：Keychain 解锁/锁定两态、替换/清空凭据、旧 Run 取消一次且新 Run 用新版本、换机恢复的 `credential_missing`、docs/12 §6 密钥口径与 ADR-0024 转 Accepted。
  3. CF20 API service profile（须沿用 `api-service-profile/owner_id=provider` 映射）。
  4. A16 / A17 真实 PPT Skill 试跑取证、B00-5 双 Skill + 撤销走查：**阻塞已解除**——CF00 记录的模型端点 `10.62.64.38:30808`（curl exit 52）今天实测三档模型连接成功、任务能跑完，CF00 的该条描述需要在下次核对时更新。
  5. A12 绑定/依赖 UI 桌面回归（需人工走查窗口）；E55 真实业务 MCP 连续两次验收（缺真实业务 MCP，离线 mock 不计）；A21 / E56 仍卡 Developer ID（`security find-identity` 0 身份，外部申请动作）。
- 教训：验收「看起来通过」不等于通过。迁移把明文列清空后，任何把密钥当参数传给「顺手记录状态」的写入路径都会静默重建明文；清空共享明文列时必须把**写入方**（不只是读取方）一起清点，尤其是那些参数里捎带密钥的 update/UPSERT。
- 测试证据：本轮为只读诊断，未改代码与数据；`sqlite3` 全部走 `file:...?mode=ro`；`grep` 定位到 `register-ipc.ts:956`、`search-engine-repository.ts:159-173`、`model-repository.ts:47`、`search-engine-repository.ts:44` 四处证据点。文档改动（任务卡）已逐条 `grep` 字面回读，U+FFFD 为 0。
- 相关文件：`docs/development/tasks-capability-foundation.md`、`apps/desktop/src/main/ipc/register-ipc.ts`、`apps/desktop/src/main/persistence/search-engine-repository.ts`、`apps/desktop/src/main/persistence/model-repository.ts`、`apps/desktop/src/main/services/credential-migration-service.ts`、`apps/desktop/src/renderer/src/views/SettingsView.tsx`。

## 11:33 修复发现二「未配置凭据」误报 + CF00 端点结论更新（状态：完成）

- 背景：用户确认导航到位后，按 11:10 商定的周边收尾顺序推进：修徽标误报 → 拆提交 → 更新 CF00 过期结论。发现一（连接检测回写明文）属主线，本轮不动。
- 变更内容：
  - `persistence/credential-repository.ts`：新增同步 `hasSecret(ref)`（只查行存在性与密文非空，不解密、不触碰受保护存储）；`API_KEY_SLOT` 与 `CredentialAvailability` 移到持久层——原定义在 `services/credential-access.ts`，而持久层不得反向引用 services。
  - `persistence/model-repository.ts`、`persistence/search-engine-repository.ts`：构造函数接收可选 `CredentialAvailability`；`apiKeyConfigured = 旧明文列非空 || 凭据库已有密文`。未注入受保护存储时完全退回旧行为，不弄断无 Keychain 环境。
  - `persistence/index.ts`：`credentials` 仓储先于模型/搜索仓储构建，`hasSecret` 作为谓词注入两处摘要。
  - `services/run-service.ts`、`ipc/register-ipc.ts`、`services/credential-access.test.ts`：改从持久层引用 `API_KEY_SLOT`。
  - 测试：`app-store.test.ts` 新增用例——内存库 + 假受保护存储复现「密文已入 credentials、明文列已清空」，断言 `getWithSecret().apiKey === ''` 同时 `apiKeyConfigured === true`（模型与搜索各一），并保留「从未配过凭据 → false」的假阳性护栏。
  - 文档：CF11「验收发现」标注发现二已修并补「发现二修复落点」；CF00 行追加【2026-09-21 已失效】——`GET http://10.62.64.38:30808/api/inference/v1/models` 实测 HTTP 401（可达且要求鉴权，不再是 exit 52 空响应），本机三档模型连接成功、任务可跑完，A16/A17/B00-5 的端点阻塞解除。
- 关键决策：
  - 摘要用「或」而不是「只信凭据库」：迁移窗口内明文列可能仍有值而凭据未落库，只信一边会误报未配置。
  - `API_KEY_SLOT` 迁到持久层而不是让 `persistence` 反向 import `services`；v25 迁移 SQL 里的 `'api-key'` 字面量保持不动——迁移脚本必须冻结，不跟随常量演化。
  - 不改协议字段、不改 IPC channel、不动 `recordConnection`（那是发现一的落点）。
- 教训：中文注释错字又出现一次——把「绝不输出 Key」写成「继不输出 Key」，靠回读 `grep` 才发现。印证常驻铁律二：工具返回 success 与 diff 都不算证据，字面回读才算。
- 测试证据：`npm run verify` 退出 0（lint、format:check、typecheck、82 个测试文件 / **615** 个测试、build），较上一轮新增 1 例即本次回归用例；`git diff --check` 通过；中文改动逐条 `grep` 回读，U+FFFD 为 0。界面「已配置凭据」由用户手工验收（需重启应用读取新构建）。
- 提交拆分结果：本工作树混有今天三个任务的改动。原计划拆三个提交，实际只能拆两个：「导航/信息架构」与「反馈通道」在 `SettingsView.tsx`、`App.tsx`、`docs/10` 里落在同一个 hunk（例如 `SettingsPageProps` 相邻两行一边删 `skills` 一边删 `modelMessage`），再分开必须对用户未提交的工作树做逐行 patch 手术，风险高于收益，因此合并为一个提交并在提交信息里写明原因；凭据摘要修复文件完全不相交，单独一个提交。两个提交共享同一次 `npm run verify`，未对第一个提交单独构建。发现一修复仍为主线待办。
- 相关文件：`apps/desktop/src/main/persistence/credential-repository.ts`、`model-repository.ts`、`search-engine-repository.ts`、`index.ts`、`app-store.test.ts`、`apps/desktop/src/main/services/credential-access.ts`、`credential-access.test.ts`、`run-service.ts`、`apps/desktop/src/main/ipc/register-ipc.ts`、`docs/development/tasks-capability-foundation.md`。
