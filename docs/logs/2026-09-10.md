# 2026-09-10 工作日志

## 01:06 A20 内置资源与依赖制品打包（状态：完成）

- 背景：阶段 A 需要把 `resources/skills`、`resources/dependency-locks`、`resources/wheelhouse` 随安装包一起分发，安装后通过 `process.resourcesPath` 寻址，与开发态 `resources/` 行为一致。
- 变更内容：
  - 新增 `apps/desktop/electron-builder.yml`：配置 `extraResources` 把三个资源目录复制到安装包 `resources/`，排除 OS 元数据，`wheelhouse` 允许缺失。
  - `apps/desktop/package.json`：新增 `dist` / `dist:mac` / `dist:win` 脚本，新增 `electron-builder` 开发依赖。
  - `apps/desktop/src/main/index.ts`：启动时根据 `app.isPackaged` 选择 manifest 路径，读取并调用 `skillService.registerBuiltinRelease()`，错误不阻塞启动。
  - `resources/skills/sample-assistant/SKILL.md`：占位内置 Skill，用于验证打包管线。
  - `resources/skills/release-manifest.json`：填入 sample-assistant 的完整条目（contentHash / profileHash / dependencyFingerprint / scopeHash 均已计算）。
  - `apps/desktop/src/main/services/skill-service.test.ts`：新增 6 项测试——开发根优先、安装根回退、缺资源拒绝、用户副本升级后资源独立、同名 user/builtin 并存、资源目录无敏感材料。
- 关键决策：
  - 占位 Skill 是最小 placeholder，不放入真实 PPT 引擎（外部/私有）。
  - 启动注册是 fire-and-forget，错误只记日志不阻塞启动。
  - `console.log` 改为 `console.warn` 以符合 ESLint 规则。
- 测试证据：`npm run verify` 退出 0（39 文件 / 348 测试）。
- 相关文件：
  - `apps/desktop/electron-builder.yml`
  - `apps/desktop/package.json`
  - `apps/desktop/src/main/index.ts`
  - `resources/skills/sample-assistant/SKILL.md`
  - `resources/skills/release-manifest.json`
  - `apps/desktop/src/main/services/skill-service.test.ts`
