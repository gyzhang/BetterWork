# 2026-09-25 工作日志

## 知识基础闭环 KM00 核对 + KM01 实施（Qwen 连续推进授权第一轮）

- 授权：光哥 2026-09-24 目标指令批准 ADR-0027 与产品规范 §5 文档线框，KM00 后连续实施 KM01–KM14。
- 基线核对（KM00，done）：HEAD `767130e`，`f59c71a..HEAD` 仅 docs 无知识代码漂移；Vault v3、应用库 v29；开工定向测试 51/51 退出 0。
- KM01（done，人工待 KM15）：
  - 协议：`knowledgeRevisionSummarySchema`（textHash/sectionCount/warnings）、`knowledgeSpanSchema`、`knowledgeCursorSchema`、`knowledgeTextPageSchema`（complete 与 nextCursor 互斥）、预览请求 Schema、分页预算常量；`knowledgeMaterialReferenceSchema` 导出复用，不新建第二套材料身份。
  - Vault v4 迁移：修订唯一键 `(document_id, content_hash, parser_version, chunking_version)`，同解析身份 textHash 不一致 → `PARSER_NONDETERMINISTIC`；已存 section 离线回填 textHash/段数，不重解析原件。
  - 正文分页纯函数 `services/knowledge-text.ts`：码点口径、段末游标规范化、跨修订拒绝、20 片段/8,000 码点上限、excerptHash；管理预览 `previewRevision` 不占 Run 预算、不落足迹。
  - 选材身份（契约 §2.4）：`knowledge.list` 摘要接通 `currentRevisionId`，管理 `knowledge.search` 每命中带同快照完整 reference + textHash，供 KM03 直接消费。
  - IPC/Preload：`knowledge:list-revisions`、`knowledge:preview` 两个新通道走 handleInput 边界校验；preload invokeValidated。
  - 验证：`knowledge-text/knowledge-vault/migrate` 定向 61/61 退出 0；回归 `task-material-service/run-service/knowledge-search` 104/104；lint、format:check、typecheck、build 退出 0；`git diff --check` 干净。
- 基线缺陷归类（不属 KM 范围）：全量 `npm test` 1027/1028，唯一失败 `App.test.tsx > offers Expert-scoped memory capture for an Expert task` 在干净 HEAD worktree 复现——9b93312「记忆编辑器下拉框改用 FieldSelect」后测试仍按原生 combobox 断言，属记忆 UI 系列测试债，移交该会话处理；本轮未改动 `App.test.tsx`。
- 提交：KM01 聚焦本地提交（不推送）。下一卡 KM02：`read_knowledge` 工具 + Evidence/RunMaterialRead 应用库迁移 + 精确审计。

## KM02 read_knowledge 工具与精确审计（done，人工待 KM15）

- 应用库 v30：Evidence 增 `knowledge_source_json`/`dedupe_key`，旧 unique 拆两组部分唯一索引；`run_material_reads` 重建移除表级 `UNIQUE(run,material_key,operation,locator)`，新增 toolCall/片段序号/span/textHash/evidenceId 五字段组 CHECK 与部分唯一索引；旧行不回填虚构值。
- `KnowledgeAudit`（services/knowledge-audit.ts）：搜索与读取回调在应用库同一事务内先落整组 Evidence+足迹再返回模型结果；审计失败 `KNOWLEDGE_AUDIT_FAILED` 零正文；Run 预算 60,000 码点事务内重算，超支拒绝不伪装完成；同 toolCall 结果重复消费整组核验一致才复用不重复计量；跨修订游标/伪造引用/范围外材料分别 `KNOWLEDGE_REVISION_MISMATCH`/`KNOWLEDGE_NOT_SELECTED`；取消统一 `isAbortError`。
- `knowledge_search`：Run 摘要改为可回算的精确 span+excerptHash；管理搜索保留旧算法；无 TaskContext 旧 Run 改为可解释空结果（不再隐式全库）。
- `read_knowledge` 工具全链接线：tool-runtime 导出、createRunTools 注入、Expert BUILTIN_TOOL_NAMES（不扩已保存白名单）、材料清单提示、tool.completed 通用消费者只验证不重复插入、事实审计用实际返回文本、TOOL_LABELS/tool-summary 中文标签。
- 验证：knowledge-audit 9/9、knowledge-audit-schema（v30 约束）4/4、run-service 51/51、coding-standard 22/22、全量 `npm test` 1044/1045、typecheck/build/lint/format 退出 0。唯一失败仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话，修复方案已在其域内讨论）。
- 提交：bb6ef2b。应用库迁移推进至 v30，Vault 保持 v4。

## KM03 真实材料研究草稿（done，人工待 KM15）

- 应用库 v31：`research_draft_operations` 回执表（operation_id PK、input_hash、task/context 级联 FK、prompt）。
- `ResearchDraftService`：契约 §4 全路径——输入规范化（默认 background、global-search、完整身份排序去重、冲突用途拒绝）→ inputHash 先查回执（同 hash 直接返回原草稿，不重新要求当前登记；异 hash `OPERATION_CONFLICT`）→ 首次创建核对 Vault 当前登记与修订归属 → 同事务 Task/Session/完整 TaskContext/回执；失败整体回滚，不产生可发送半成品；不自动调用模型。
- Renderer：知识页每条命中勾选、全选结果、清除选择、禁用无选择的研究按钮；切换搜索清空勾选并局部反馈；运行中拒绝且保留选择；失败不清空当前任务；迟到成功不抢导航、提示可从最近任务找回；成功后设置草稿 prompt 且材料进 Composer。
- 验证：research-draft-service.test 7/7 连跑 3 次稳定；全量 `npm test` 1051/1052（唯一红为基线既有 `App.test.tsx` 记忆 combobox，与本卡无关，移交记忆会话）；typecheck、build 退出 0。
- 提交：KM03 聚焦本地提交。应用库迁移推进至 v31。下一卡 KM04：previewRunSource + 来源面板按 Run 回看。

## KM04 按运行回看搜索与读取来源（done，人工待 KM15）

- 协议：`previewRunSourceRequestSchema` + `runSourcePreviewSchema`（`exact`/`legacy` 可辨识联合；exact 复用 KM01 `knowledgeTextPage`，`complete: true` 且无 `nextCursor`，Schema 层互斥守卫已覆盖）、新 channel `knowledge:preview-run-source`、preload `previewRunSource` 双向 Zod 收口。
- 主进程：`KnowledgeAudit.previewRunSource` 四道拒绝——证据不属于该 Run、证据修订不在该 Run 材料快照、修订身份或 textHash 与保存文本不符、区间回算与证据摘录不一致——任一失败都以 `KNOWLEDGE_REVISION_MISMATCH` 拒绝，绝不拼一个假区间；legacy 证据原样返回，不猜 reference/span。回看是只读路径，不新增 Evidence 或足迹。
- Renderer：ContextPanel 资料页按 Run 分组，默认只呈现当前 Run，历史来源折叠需显式展开；知识证据按 `operation` 标注「摘要来源/正文来源」并展示修订前缀与段内码点区间；「查看区间」调 `previewRunSource`，预览区止于该区间、明示不提供续读，可关闭；旧数据标「历史范围未记录」，无区间入口但保留「原文」（主进程白名单）；新 hook `use-run-source-preview` 以请求代号收口，切 Run/收起后迟到响应不覆盖当前选择，来源区块以 `activeRun.id` 为 key 重挂载。
- 验证：knowledge-audit 13/13、register-ipc 27/27（含新通道输入/响应校验）、ContextPanel 15/15；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1060/1061，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话）。
- 提交：KM04 聚焦本地提交（不推送）。下一卡 KM05：成果显式采用声明（协议 + declare 工具 + 继承矩阵 legacy/none/inherited）。

## KM05 成果显式采用声明（done，人工待 KM15）

- 协议：`artifactSourceDeclarationKindSchema`（model/user/inherited/legacy/none）进入版本与成果 DTO（省略仅代表迁移前历史数据）；`declareArtifactSourcesRequestSchema`（同 Run 账本，空数组＝主动清除）、`getRunArtifactDeclarationsRequestSchema`、`artifact:declare-sources`/`artifact:get-run-declarations` 两个通道与 preload 双向收口；`saveMarkdownArtifact` 增补「新建成果没有可核验来源运行就不能声明」的 superRefine。
- 存储：应用库 v32——`artifact_versions.source_declaration` 五值 CHECK，迁移时只把「迁移前已存在输入关系」的版本标 legacy（历史关联，不等于采用），其余默认 none；新增 `run_artifact_source_declarations` 账本（run 主键、后写覆盖、级联删除）与 `RunArtifactSourceDeclarationRepository`。
- 服务：`ArtifactDeclarationService` 承担契约 §6.2 归属核验——整份材料必须在该 Run 固定材料内且有正文读取足迹（只搜过拒绝），证据必须属于同 Run 且带精确定位；同输入两种关系拒绝、完全重复幂等合并；`planForWrite` 实现 §6.1 矩阵：assistant-run 显式=model／省略或空=none，user-edit 显式=user（沿祖先链取来源 Run 校验，旧版本不回写）、省略时 legacy→legacy、model/user/inherited→inherited 复制前版关系、none→none，声明失败在同一事务外抛错不写任何关系。
- 工具与生产链：`artifact_declare_sources`（runId 由宿主 context 注入，模型自带 runId 被 schema 拒绝）、`artifact_register_file` 的 Zod/JSON schema/注册输入/回调全链接通 inputRelations；RunService 删除「把访问过的材料自动绑成采用关系」的旧映射，自动 Markdown 只读账本声明，文件成果按显式声明判定；材料提示补「搜索过不等于采用」的交付要求。
- Renderer：成果详情拆成「声明采用依据 · 种类」与「运行访问记录」两区，legacy 显示「历史关联，未核实采用」且不进声明区，整份知识修订标注「文档级依据，非全文已读」；新 hook `use-artifact-source-selection` 只把带精确定位的访问记录列为候选，未改动＝省略字段沿用前版，勾选/取消＝提交 user 声明（取消全部即空数组清除），并提供「改为沿用上一版声明」撤销入口；App 侧移除按材料用途自动生成关系的旧路径。
- 验证：`artifact-declaration-service` 9/9、`artifact-declaration-schema`（v32 迁移与 legacy 判定）2/2、`artifact-declare-sources` 5/5（含伪造 runId、取消、JSON/Zod schema 一致）、`run-service` 52/52（含工具注册与「未声明则为 none、零关系」）、`register-ipc` 29/29、`file-artifact-service` 16/16（文件成果声明与静默登记两条链）、`use-artifact-source-selection` 5/5、`ArtifactView` 11/11；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1086/1087，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话）。
- 提交：KM05 聚焦本地提交（不推送）。批次 A（KM01–KM05）自动链路收口；下一卡 KM06：嵌入模型批量请求适配与向量空间指纹。

## KM06 批量嵌入适配与模型身份（done，端到端展示在 KM07/KM09）

- 协议：新增嵌入预算与身份常量（每批 ≤16 输入／≤16,000 码点、查询 1 输入、响应 ≤4 MiB、维度 1–4,096、批 30 秒／查询 10 秒、指纹版本 `knowledge-embedding-v1` 与固定请求格式 `float`）＋无密钥的 `embeddingModelSnapshotSchema`（profileId、provider、endpointFingerprint、model、modelFingerprint、可选 dimension）与 `embeddingUsageSchema`；快照里不含完整 endpoint 或凭据，由测试用 `JSON.stringify` 反向核验。
- Main：`embedding-client.ts` 只复用既有入口——`ModelRepository.getWithSecret/getDefaultProfileId`（本卡为「先把具体 profileId 固定下来」新增该查询）与 `CredentialAccess` 的同一迁移口径（pending/failed 拒绝且不回落明文，done 从凭据库解析）。`embeddingEndpointOf` 沿用 http/https 校验、拒绝 URL userinfo、保留影响路由的 query；向量空间指纹为固定字段顺序 JSON 的 SHA-256，`updatedAt`／名称／priority／连接探测结果／API Key 均不参与。
- 适配器：请求体只有 `{ model, input, encoding_format: 'float' }`；响应流式累计读取，达到 4 MiB 即 `reader.cancel()`；`data` 条数必须等于输入条数，index 唯一且覆盖 `[0,n)` 后按 index 重排；每个向量要求有限数值、维度在上限内、范数大于 0，归一化为 Float32 单位向量后再查有限性与非零；批内维度一致并与快照锁定维度比对。usage 只接受服务返回的非负整数，否则省略，不用字符数伪造。
- 失败收口：配置不存在／停用／角色不符／凭据不可用／endpoint 或 model 变化 → `EMBEDDING_MODEL_UNAVAILABLE`，零 HTTP、不顶替 profile、不自动重试；`EMBEDDING_TIMEOUT` 与用户取消分开，后者走 agent-core 统一 `abortError`，取消词汇不再本地定义；HTTP 错误只保留状态码与安全展示值。
- 验证：`embedding-client.test.ts` 22/22（真实隔离 SQLite＋注入 fetch 替身；乱序重排与归一化、换 Key/重存不改指纹而改 endpoint/model 必变、越界快照与停用删除零 HTTP、迁移 pending 不解析明文、超时恰好一次请求、取消语义、8 组非法响应、503 不回显正文与密钥、流式超限、维度锁定、批次数/码点/空输入零 HTTP）；回归 `model-provider-factory/model-connectivity/persistence/agent-core` 110/110、`standards/coding-standard` 22/22；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1108/1109，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（移交记忆会话）。
- 提交：KM06 聚焦本地提交（不推送）。下一卡 KM07：Vault 作业/共享向量空间/代次与 Worker 调度（含 `electron.vite.config.ts` 构建入口与打包预检）。

## KM07a 导入与索引作业：Vault 存储与生命周期（done，Worker 卸载留给 KM07b）

- 拆卡：光哥 2026-09-25 确认 KM07 拆为 07a（存储与作业，本卡）与 07b（提取分块与向量扫描卸载到独立 Worker＋构建入口与打包预检）。
- 协议：作业 Schema 全集（kind/status/item status/phase/failure/summary/item summary/detail/ack/import ack `{cancelled, jobId?}`/cancel result/cursor/分页/重试请求）、`knowledgeSearchSettingsSchema`＋CAS 保存请求、重建 discriminatedUnion（keyword 目标或精确修订／semantic 只带 `resetSemanticSpace`）、来源核对请求，八个新通道＋`KnowledgeJobEvent` 推送；`KNOWLEDGE_VECTOR_MAX_PUBLISHED` 收口发布容量。
- 存储：Vault v5 迁移（`db/migrate.test.ts` 补真实升级用例）——`knowledge_jobs`（按 job 单调 sequence、retry_of_job_id、space_id）、`knowledge_job_items`（`UNIQUE(job_id,target_json)`、阶段与安全 failure）、跨修订共享 `knowledge_embedding_spaces`（epoch＋current 部分唯一索引、dimension 由首个合法批次 CAS 锁定）、`knowledge_index_generations`（active 部分唯一）、`knowledge_chunk_vectors`、派生检索块 `knowledge_retrieval_chunks`＋FTS、`knowledge_search_settings` 单例 revision；`knowledge-chunks.ts`（knowledge-chunks-v1，1000 窗口/100 重叠，稳定 chunkId）。
- 服务：`KnowledgeJobStore` 聚合规则（全成功 succeeded／混合 partial／全失败 failed／取消优先 cancelled＋保留既成事实／重启 interrupted 收口终态不改）；`KnowledgeIndexStore` staging→单修订原子发布（发布前全量重校验维度与覆盖，不匹配批次拒绝，不发布半份向量）、强制重建同事务退役旧空间并开 epoch+1 新空间、旧空间立即退出查询与覆盖率；`KnowledgeIndexService` Main 单作业排队、排队期取消即时、批间让位给查询、语义失败不回滚已发布关键词块、手动重试创建引用原作业的新 job 且仅允许 failed/interrupted/cancelled 条目、越界 cursor 与已退役空间拒绝（`INDEX_ITEM_NOT_RETRYABLE`/`INDEX_CONFIGURATION_CHANGED` 入契约 §13.1）。
- IPC/Preload/Hook/主进程：导入与刷新改为作业回执，8 个新 handle 全部走注册 helper 与双向 Zod；preload `jobs/job/cancelJob/retryJob/onJobEvent/settings/saveSettings/rebuildIndex/checkSources`；Hook `trackJob/reportJob` 以 `pendingJobs` 过滤事件、终态回读详情回填 message/issues、卸载退订；主进程启动 `recoverInterrupted` 收口＋`notifyKnowledgeJob` 进消息中心（queued/running/cancelled 不发通知）。
- 修正的用例前提：作业列表断言原按「插入序」期望 [second, first]，契约排序实为 `(createdAt,id)` 倒序——同毫秒以 id 决胜，改为集合相等断言并注明；服务测试替身的取消错误改用 agent-core `abortError()`（护栏测试再次抓到本地 `'AbortError'` 字面量）。
- 验证：`knowledge-job-store`/`knowledge-index-store`/`knowledge-index-service`/`knowledge-chunks` 共 32+ 用例（含发布前故障、维度锁、强制重建隔离、三种可重试状态与成功/运行拒绝、事件顺序、重启恢复）；`register-ipc` 33/33（新增知识作业通道 5 用例：取消对话框零作业、CAS 与语义拒绝、非法重试、分页与越界 cursor、设置不可用时启用语义被拒）；`use-knowledge-library` 4/4；App.test 补 `job/onJobEvent` mock 恢复其余套件绿色；全仓 `npm run verify` 至 test 阶段仅余基线已知红（记忆 combobox，移交记忆会话），lint/format:check/typecheck/build 退出 0；`npm run build` 单独确认（产物 `apps/desktop/out/`）。
- 提交：KM07a 聚焦本地提交（不推送）。下一卡按依赖序为 KM07b（Worker 卸载与构建/打包入口）或 KM08（混合检索）；07b 先收口提取链路的进程边界，不留半接线。

## KM07b 提取卸载到独立 Worker 与构建入口（done，向量扫描 op 随 KM08 接入）

- 边界：Main 独占数据库写入与凭据；Worker 只处理给定字节，逐行 JSON 走 stdin/stdout，不读文件路径、不访问网络、不写数据库（契约 §8.2）。
- 实现：提取核心从 `knowledge-vault.ts` 抽到 `services/knowledge-extract.ts`（Main 与 Worker 共用同一份，不出现第二套解析器）；新入口 `infrastructure/knowledge-worker.ts` 校验请求 Schema 与本次启动 nonce，不符即 `WORKER_NONCE_MISMATCH`/`WORKER_REQUEST_INVALID`；`knowledge-worker-runner.ts` 按作业登记唯一子进程（同作业复用、换作业先优雅收口旧进程），请求携带 `{jobId, attempt}`，base64 超 32 MiB 前置拒绝且零启动，响应逐行 O(n) 累积、单行超 64 MiB 判不可信并终止，取消在途请求以统一 `abortError` 收口（不算失败），1 秒宽限只终止已登记 pid，应用退出走 `shutdown`。
- 接线：`KnowledgeVault` 构造可注入提取器且 `importSource` 透传条目作业上下文；`KnowledgeIndexService.cancelJob` 经 `onJobCancel` 通知运行器收口；`main/index.ts` 注入 runner 并挂 before-quit；`electron.vite.config.ts` 新增 knowledge-worker 独立构建入口（与 skill-guardian 同法：开发/测试跑 TS 源，打包跑构建 JS）。
- 配置与契约：tsconfig 开启 `allowImportingTsExtensions`（原生 ESM 要求 Worker 入口链相对导入带 `.ts` 后缀；理由与限制写入 docs/12 §4，不放宽其他文件）；契约 §13.1 增补 `WORKER_TIMEOUT`、`WORKER_UNAVAILABLE`、`WORKER_EXTRACT_FAILED`。
- 修正过程：Worker 入口起初以无扩展名相对导入在原生 Node 下 `ERR_MODULE_NOT_FOUND`，子进程启动即退；响应缓冲先前每次 data 都 `Buffer.concat` 全量（O(n²)），64 MiB 守卫 30 秒内触发不了，改为增量 chunk 数组＋仅换行时 concat；测试两个用例在 spawn 完成前读 `activePid`，改 `waitForPid` 轮询并等一个宏任务再取消。
- 验证：`knowledge-worker-runner.test.ts` 7/7（真实子进程提取与同作业复用、坏 docx 错误响应进程存活可续、超限零启动、卡死替身取消收口、溢出替身判不可信终止、线协议拒绝坏 JSON 与伪 nonce、退出码 0）；`knowledge-index-service` 18/18（含取消同步通知 Worker）、`knowledge-vault` 13/13（含注入提取器与上下文透传）；打包预检 `npm run build`（产物 `apps/desktop/out/main/knowledge-worker.js`）；全仓 `npm run verify` 1147/1148，唯一红为基线既有 `App.test.tsx` 记忆 combobox（移交记忆会话）。
- 提交：KM07b 聚焦本地提交（不推送）。至此 KM07 两片全部收口；下一卡 KM08 统一混合检索（含 Worker 向量扫描 op 与 RSS/延迟实测）。
