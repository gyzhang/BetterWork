# 2026-09-25 工作日志

## 知识基础闭环 KM00 核对 + KM01 实施（Qwen 连续推进授权第一轮）

- 授权：光哥 2026-09-24 目标指令批准 ADR-0027 与产品规范 §5 文档线框，KM00 后连续实施 KM01–KM14。
- 基线核对（KM00，done）：HEAD `767130e`，`f59c71a..HEAD` 仅 docs 无知识代码漂移；Vault v3、应用库 v29；开工定向测试 51/51 退出 0。
- KM01（done，人工待 KM15）：
  - 协议：`knowledgeRevisionSummarySchema`（textHash/sectionCount/warnings）、`knowledgeSpanSchema`、`knowledgeCursorSchema`、`knowledgeTextPageSchema`（complete 与 nextCursor 互斥）、预览请求 Schema、分页预算常量；`knowledgeMaterialReferenceSchema` 导出复用，不新建第二套材料身份。
  - Vault v4 迁移：修订唯一键 `(document_id, content_hash, parser_version, chunking_version)`，同解析身份 textHash 不一致 → `PARSER_NONDETERMINISTIC`；已存 section 离线回填 textHash/段数，不重解析原件。
  - 正文分页纯函数 `services/knowledge-text.ts`：码点口径、段末游标规范化、跨修订拒绝、20 片段/8,000 码点上限、excerptHash；管理预览 `previewRevision` 不占 Run 预算、不落足迹。
  - 选材身份（契约 §2.4）：`knowledge.list` 摘要接通 `currentRevisionId`，管理 `knowledge.search` 每命中带同快照完整 reference + textHash，供 KM03 直接消费。
  - IPC/Preload：`knowledge:list-revisions`、`knowledge:preview` 两个新通道走 handleInput 边界校验；preload invokeValidated。
  - 验证：`knowledge-text/knowledge-vault/migrate` 定向 61/61 退出 0；回归 `task-material-service/run-service/knowledge-search` 104/104；lint、format:check、typecheck、build 退出 0；`git diff --check` 干净。
- 基线缺陷归类（不属 KM 范围）：全量 `npm test` 1027/1028，唯一失败 `App.test.tsx > offers Expert-scoped memory capture for an Expert task` 在干净 HEAD worktree 复现——9b93312「记忆编辑器下拉框改用 FieldSelect」后测试仍按原生 combobox 断言，属记忆 UI 系列测试债，移交该会话处理；本轮未改动 `App.test.tsx`。
- 提交：KM01 聚焦本地提交（不推送）。下一卡 KM02：`read_knowledge` 工具 + Evidence/RunMaterialRead 应用库迁移 + 精确审计。

## KM02 read_knowledge 工具与精确审计（done，人工待 KM15）

- 应用库 v30：Evidence 增 `knowledge_source_json`/`dedupe_key`，旧 unique 拆两组部分唯一索引；`run_material_reads` 重建移除表级 `UNIQUE(run,material_key,operation,locator)`，新增 toolCall/片段序号/span/textHash/evidenceId 五字段组 CHECK 与部分唯一索引；旧行不回填虚构值。
- `KnowledgeAudit`（services/knowledge-audit.ts）：搜索与读取回调在应用库同一事务内先落整组 Evidence+足迹再返回模型结果；审计失败 `KNOWLEDGE_AUDIT_FAILED` 零正文；Run 预算 60,000 码点事务内重算，超支拒绝不伪装完成；同 toolCall 结果重复消费整组核验一致才复用不重复计量；跨修订游标/伪造引用/范围外材料分别 `KNOWLEDGE_REVISION_MISMATCH`/`KNOWLEDGE_NOT_SELECTED`；取消统一 `isAbortError`。
- `knowledge_search`：Run 摘要改为可回算的精确 span+excerptHash；管理搜索保留旧算法；无 TaskContext 旧 Run 改为可解释空结果（不再隐式全库）。
- `read_knowledge` 工具全链接线：tool-runtime 导出、createRunTools 注入、Expert BUILTIN_TOOL_NAMES（不扩已保存白名单）、材料清单提示、tool.completed 通用消费者只验证不重复插入、事实审计用实际返回文本、TOOL_LABELS/tool-summary 中文标签。
- 验证：knowledge-audit 9/9、knowledge-audit-schema（v30 约束）4/4、run-service 51/51、coding-standard 22/22、全量 `npm test` 1044/1045、typecheck/build/lint/format 退出 0。唯一失败仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话，修复方案已在其域内讨论）。
- 提交：bb6ef2b。应用库迁移推进至 v30，Vault 保持 v4。

## KM03 真实材料研究草稿（done，人工待 KM15）

- 应用库 v31：`research_draft_operations` 回执表（operation_id PK、input_hash、task/context 级联 FK、prompt）。
- `ResearchDraftService`：契约 §4 全路径——输入规范化（默认 background、global-search、完整身份排序去重、冲突用途拒绝）→ inputHash 先查回执（同 hash 直接返回原草稿，不重新要求当前登记；异 hash `OPERATION_CONFLICT`）→ 首次创建核对 Vault 当前登记与修订归属 → 同事务 Task/Session/完整 TaskContext/回执；失败整体回滚，不产生可发送半成品；不自动调用模型。
- Renderer：知识页每条命中勾选、全选结果、清除选择、禁用无选择的研究按钮；切换搜索清空勾选并局部反馈；运行中拒绝且保留选择；失败不清空当前任务；迟到成功不抢导航、提示可从最近任务找回；成功后设置草稿 prompt 且材料进 Composer。
- 验证：research-draft-service.test 7/7 连跑 3 次稳定；全量 `npm test` 1051/1052（唯一红为基线既有 `App.test.tsx` 记忆 combobox，与本卡无关，移交记忆会话）；typecheck、build 退出 0。
- 提交：KM03 聚焦本地提交。应用库迁移推进至 v31。下一卡 KM04：previewRunSource + 来源面板按 Run 回看。

## KM04 按运行回看搜索与读取来源（done，人工待 KM15）

- 协议：`previewRunSourceRequestSchema` + `runSourcePreviewSchema`（`exact`/`legacy` 可辨识联合；exact 复用 KM01 `knowledgeTextPage`，`complete: true` 且无 `nextCursor`，Schema 层互斥守卫已覆盖）、新 channel `knowledge:preview-run-source`、preload `previewRunSource` 双向 Zod 收口。
- 主进程：`KnowledgeAudit.previewRunSource` 四道拒绝——证据不属于该 Run、证据修订不在该 Run 材料快照、修订身份或 textHash 与保存文本不符、区间回算与证据摘录不一致——任一失败都以 `KNOWLEDGE_REVISION_MISMATCH` 拒绝，绝不拼一个假区间；legacy 证据原样返回，不猜 reference/span。回看是只读路径，不新增 Evidence 或足迹。
- Renderer：ContextPanel 资料页按 Run 分组，默认只呈现当前 Run，历史来源折叠需显式展开；知识证据按 `operation` 标注「摘要来源/正文来源」并展示修订前缀与段内码点区间；「查看区间」调 `previewRunSource`，预览区止于该区间、明示不提供续读，可关闭；旧数据标「历史范围未记录」，无区间入口但保留「原文」（主进程白名单）；新 hook `use-run-source-preview` 以请求代号收口，切 Run/收起后迟到响应不覆盖当前选择，来源区块以 `activeRun.id` 为 key 重挂载。
- 验证：knowledge-audit 13/13、register-ipc 27/27（含新通道输入/响应校验）、ContextPanel 15/15；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1060/1061，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话）。
- 提交：KM04 聚焦本地提交（不推送）。下一卡 KM05：成果显式采用声明（协议 + declare 工具 + 继承矩阵 legacy/none/inherited）。

## KM05 成果显式采用声明（done，人工待 KM15）

- 协议：`artifactSourceDeclarationKindSchema`（model/user/inherited/legacy/none）进入版本与成果 DTO（省略仅代表迁移前历史数据）；`declareArtifactSourcesRequestSchema`（同 Run 账本，空数组＝主动清除）、`getRunArtifactDeclarationsRequestSchema`、`artifact:declare-sources`/`artifact:get-run-declarations` 两个通道与 preload 双向收口；`saveMarkdownArtifact` 增补「新建成果没有可核验来源运行就不能声明」的 superRefine。
- 存储：应用库 v32——`artifact_versions.source_declaration` 五值 CHECK，迁移时只把「迁移前已存在输入关系」的版本标 legacy（历史关联，不等于采用），其余默认 none；新增 `run_artifact_source_declarations` 账本（run 主键、后写覆盖、级联删除）与 `RunArtifactSourceDeclarationRepository`。
- 服务：`ArtifactDeclarationService` 承担契约 §6.2 归属核验——整份材料必须在该 Run 固定材料内且有正文读取足迹（只搜过拒绝），证据必须属于同 Run 且带精确定位；同输入两种关系拒绝、完全重复幂等合并；`planForWrite` 实现 §6.1 矩阵：assistant-run 显式=model／省略或空=none，user-edit 显式=user（沿祖先链取来源 Run 校验，旧版本不回写）、省略时 legacy→legacy、model/user/inherited→inherited 复制前版关系、none→none，声明失败在同一事务外抛错不写任何关系。
- 工具与生产链：`artifact_declare_sources`（runId 由宿主 context 注入，模型自带 runId 被 schema 拒绝）、`artifact_register_file` 的 Zod/JSON schema/注册输入/回调全链接通 inputRelations；RunService 删除「把访问过的材料自动绑成采用关系」的旧映射，自动 Markdown 只读账本声明，文件成果按显式声明判定；材料提示补「搜索过不等于采用」的交付要求。
- Renderer：成果详情拆成「声明采用依据 · 种类」与「运行访问记录」两区，legacy 显示「历史关联，未核实采用」且不进声明区，整份知识修订标注「文档级依据，非全文已读」；新 hook `use-artifact-source-selection` 只把带精确定位的访问记录列为候选，未改动＝省略字段沿用前版，勾选/取消＝提交 user 声明（取消全部即空数组清除），并提供「改为沿用上一版声明」撤销入口；App 侧移除按材料用途自动生成关系的旧路径。
- 验证：`artifact-declaration-service` 9/9、`artifact-declaration-schema`（v32 迁移与 legacy 判定）2/2、`artifact-declare-sources` 5/5（含伪造 runId、取消、JSON/Zod schema 一致）、`run-service` 52/52（含工具注册与「未声明则为 none、零关系」）、`register-ipc` 29/29、`file-artifact-service` 16/16（文件成果声明与静默登记两条链）、`use-artifact-source-selection` 5/5、`ArtifactView` 11/11；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1086/1087，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（9b93312 引入，移交记忆会话）。
- 提交：KM05 聚焦本地提交（不推送）。批次 A（KM01–KM05）自动链路收口；下一卡 KM06：嵌入模型批量请求适配与向量空间指纹。

## KM06 批量嵌入适配与模型身份（done，端到端展示在 KM07/KM09）

- 协议：新增嵌入预算与身份常量（每批 ≤16 输入／≤16,000 码点、查询 1 输入、响应 ≤4 MiB、维度 1–4,096、批 30 秒／查询 10 秒、指纹版本 `knowledge-embedding-v1` 与固定请求格式 `float`）＋无密钥的 `embeddingModelSnapshotSchema`（profileId、provider、endpointFingerprint、model、modelFingerprint、可选 dimension）与 `embeddingUsageSchema`；快照里不含完整 endpoint 或凭据，由测试用 `JSON.stringify` 反向核验。
- Main：`embedding-client.ts` 只复用既有入口——`ModelRepository.getWithSecret/getDefaultProfileId`（本卡为「先把具体 profileId 固定下来」新增该查询）与 `CredentialAccess` 的同一迁移口径（pending/failed 拒绝且不回落明文，done 从凭据库解析）。`embeddingEndpointOf` 沿用 http/https 校验、拒绝 URL userinfo、保留影响路由的 query；向量空间指纹为固定字段顺序 JSON 的 SHA-256，`updatedAt`／名称／priority／连接探测结果／API Key 均不参与。
- 适配器：请求体只有 `{ model, input, encoding_format: 'float' }`；响应流式累计读取，达到 4 MiB 即 `reader.cancel()`；`data` 条数必须等于输入条数，index 唯一且覆盖 `[0,n)` 后按 index 重排；每个向量要求有限数值、维度在上限内、范数大于 0，归一化为 Float32 单位向量后再查有限性与非零；批内维度一致并与快照锁定维度比对。usage 只接受服务返回的非负整数，否则省略，不用字符数伪造。
- 失败收口：配置不存在／停用／角色不符／凭据不可用／endpoint 或 model 变化 → `EMBEDDING_MODEL_UNAVAILABLE`，零 HTTP、不顶替 profile、不自动重试；`EMBEDDING_TIMEOUT` 与用户取消分开，后者走 agent-core 统一 `abortError`，取消词汇不再本地定义；HTTP 错误只保留状态码与安全展示值。
- 验证：`embedding-client.test.ts` 22/22（真实隔离 SQLite＋注入 fetch 替身；乱序重排与归一化、换 Key/重存不改指纹而改 endpoint/model 必变、越界快照与停用删除零 HTTP、迁移 pending 不解析明文、超时恰好一次请求、取消语义、8 组非法响应、503 不回显正文与密钥、流式超限、维度锁定、批次数/码点/空输入零 HTTP）；回归 `model-provider-factory/model-connectivity/persistence/agent-core` 110/110、`standards/coding-standard` 22/22；lint、format:check、typecheck、build 退出 0；全量 `npm test` 1108/1109，唯一红仍为基线既有 `App.test.tsx` 记忆 combobox（移交记忆会话）。
- 提交：KM06 聚焦本地提交（不推送）。下一卡 KM07：Vault 作业/共享向量空间/代次与 Worker 调度（含 `electron.vite.config.ts` 构建入口与打包预检）。

## KM07a 导入与索引作业：Vault 存储与生命周期（done，Worker 卸载留给 KM07b）

- 拆卡：光哥 2026-09-25 确认 KM07 拆为 07a（存储与作业，本卡）与 07b（提取分块与向量扫描卸载到独立 Worker＋构建入口与打包预检）。
- 协议：作业 Schema 全集（kind/status/item status/phase/failure/summary/item summary/detail/ack/import ack `{cancelled, jobId?}`/cancel result/cursor/分页/重试请求）、`knowledgeSearchSettingsSchema`＋CAS 保存请求、重建 discriminatedUnion（keyword 目标或精确修订／semantic 只带 `resetSemanticSpace`）、来源核对请求，八个新通道＋`KnowledgeJobEvent` 推送；`KNOWLEDGE_VECTOR_MAX_PUBLISHED` 收口发布容量。
- 存储：Vault v5 迁移（`db/migrate.test.ts` 补真实升级用例）——`knowledge_jobs`（按 job 单调 sequence、retry_of_job_id、space_id）、`knowledge_job_items`（`UNIQUE(job_id,target_json)`、阶段与安全 failure）、跨修订共享 `knowledge_embedding_spaces`（epoch＋current 部分唯一索引、dimension 由首个合法批次 CAS 锁定）、`knowledge_index_generations`（active 部分唯一）、`knowledge_chunk_vectors`、派生检索块 `knowledge_retrieval_chunks`＋FTS、`knowledge_search_settings` 单例 revision；`knowledge-chunks.ts`（knowledge-chunks-v1，1000 窗口/100 重叠，稳定 chunkId）。
- 服务：`KnowledgeJobStore` 聚合规则（全成功 succeeded／混合 partial／全失败 failed／取消优先 cancelled＋保留既成事实／重启 interrupted 收口终态不改）；`KnowledgeIndexStore` staging→单修订原子发布（发布前全量重校验维度与覆盖，不匹配批次拒绝，不发布半份向量）、强制重建同事务退役旧空间并开 epoch+1 新空间、旧空间立即退出查询与覆盖率；`KnowledgeIndexService` Main 单作业排队、排队期取消即时、批间让位给查询、语义失败不回滚已发布关键词块、手动重试创建引用原作业的新 job 且仅允许 failed/interrupted/cancelled 条目、越界 cursor 与已退役空间拒绝（`INDEX_ITEM_NOT_RETRYABLE`/`INDEX_CONFIGURATION_CHANGED` 入契约 §13.1）。
- IPC/Preload/Hook/主进程：导入与刷新改为作业回执，8 个新 handle 全部走注册 helper 与双向 Zod；preload `jobs/job/cancelJob/retryJob/onJobEvent/settings/saveSettings/rebuildIndex/checkSources`；Hook `trackJob/reportJob` 以 `pendingJobs` 过滤事件、终态回读详情回填 message/issues、卸载退订；主进程启动 `recoverInterrupted` 收口＋`notifyKnowledgeJob` 进消息中心（queued/running/cancelled 不发通知）。
- 修正的用例前提：作业列表断言原按「插入序」期望 [second, first]，契约排序实为 `(createdAt,id)` 倒序——同毫秒以 id 决胜，改为集合相等断言并注明；服务测试替身的取消错误改用 agent-core `abortError()`（护栏测试再次抓到本地 `'AbortError'` 字面量）。
- 验证：`knowledge-job-store`/`knowledge-index-store`/`knowledge-index-service`/`knowledge-chunks` 共 32+ 用例（含发布前故障、维度锁、强制重建隔离、三种可重试状态与成功/运行拒绝、事件顺序、重启恢复）；`register-ipc` 33/33（新增知识作业通道 5 用例：取消对话框零作业、CAS 与语义拒绝、非法重试、分页与越界 cursor、设置不可用时启用语义被拒）；`use-knowledge-library` 4/4；App.test 补 `job/onJobEvent` mock 恢复其余套件绿色；全仓 `npm run verify` 至 test 阶段仅余基线已知红（记忆 combobox，移交记忆会话），lint/format:check/typecheck/build 退出 0；`npm run build` 单独确认（产物 `apps/desktop/out/`）。
- 提交：KM07a 聚焦本地提交（不推送）。下一卡按依赖序为 KM07b（Worker 卸载与构建/打包入口）或 KM08（混合检索）；07b 先收口提取链路的进程边界，不留半接线。

## KM07b 提取卸载到独立 Worker 与构建入口（done，向量扫描 op 随 KM08 接入）

- 边界：Main 独占数据库写入与凭据；Worker 只处理给定字节，逐行 JSON 走 stdin/stdout，不读文件路径、不访问网络、不写数据库（契约 §8.2）。
- 实现：提取核心从 `knowledge-vault.ts` 抽到 `services/knowledge-extract.ts`（Main 与 Worker 共用同一份，不出现第二套解析器）；新入口 `infrastructure/knowledge-worker.ts` 校验请求 Schema 与本次启动 nonce，不符即 `WORKER_NONCE_MISMATCH`/`WORKER_REQUEST_INVALID`；`knowledge-worker-runner.ts` 按作业登记唯一子进程（同作业复用、换作业先优雅收口旧进程），请求携带 `{jobId, attempt}`，base64 超 32 MiB 前置拒绝且零启动，响应逐行 O(n) 累积、单行超 64 MiB 判不可信并终止，取消在途请求以统一 `abortError` 收口（不算失败），1 秒宽限只终止已登记 pid，应用退出走 `shutdown`。
- 接线：`KnowledgeVault` 构造可注入提取器且 `importSource` 透传条目作业上下文；`KnowledgeIndexService.cancelJob` 经 `onJobCancel` 通知运行器收口；`main/index.ts` 注入 runner 并挂 before-quit；`electron.vite.config.ts` 新增 knowledge-worker 独立构建入口（与 skill-guardian 同法：开发/测试跑 TS 源，打包跑构建 JS）。
- 配置与契约：tsconfig 开启 `allowImportingTsExtensions`（原生 ESM 要求 Worker 入口链相对导入带 `.ts` 后缀；理由与限制写入 docs/12 §4，不放宽其他文件）；契约 §13.1 增补 `WORKER_TIMEOUT`、`WORKER_UNAVAILABLE`、`WORKER_EXTRACT_FAILED`。
- 修正过程：Worker 入口起初以无扩展名相对导入在原生 Node 下 `ERR_MODULE_NOT_FOUND`，子进程启动即退；响应缓冲先前每次 data 都 `Buffer.concat` 全量（O(n²)），64 MiB 守卫 30 秒内触发不了，改为增量 chunk 数组＋仅换行时 concat；测试两个用例在 spawn 完成前读 `activePid`，改 `waitForPid` 轮询并等一个宏任务再取消。
- 验证：`knowledge-worker-runner.test.ts` 7/7（真实子进程提取与同作业复用、坏 docx 错误响应进程存活可续、超限零启动、卡死替身取消收口、溢出替身判不可信终止、线协议拒绝坏 JSON 与伪 nonce、退出码 0）；`knowledge-index-service` 18/18（含取消同步通知 Worker）、`knowledge-vault` 13/13（含注入提取器与上下文透传）；打包预检 `npm run build`（产物 `apps/desktop/out/main/knowledge-worker.js`）；全仓 `npm run verify` 1147/1148，唯一红为基线既有 `App.test.tsx` 记忆 combobox（移交记忆会话）。
- 提交：KM07b 聚焦本地提交（不推送）。至此 KM07 两片全部收口；下一卡 KM08 统一混合检索（含 Worker 向量扫描 op 与 RSS/延迟实测）。

## KM08 统一混合检索（2026-09-25）

- 背景与授权：KM07 收口后按连续推进指令实施 KM08（契约 §9 全部＋§8.3 租约），管理页与 `knowledge_search` 工具首次共用同一条检索管线。
- 实现：新增 `services/knowledge-search.ts`——scope 先解析（library＝各文档当前登记修订并集；run＝宿主快照材料，无快照或空选材返回空结果、零模型调用、绝不回退全库）；候选先按允许修订集合过滤后才截断；FTS bm25 关键词路＋零命中子串回退（title 命中数降序→正文首命中升序→无正文最后→身份序）；语义路按 §8.3 租约固定 current space 与各修订 active generation，逐批复核空间身份，两路同权 RRF（k=60）融合、chunkId 去重标 `keyword/vector/both`、同修订同 section 重叠只留名次最高、摘要 span 回算到 section 并重算 excerptHash；七种降级原因全部可解释且保留关键词结果，用户取消一律 `abortError`；`effectiveMode` 按实际生效路报告。
- Worker 扫描：`scan` op 进 Worker 线协议（点积批扫描、分数降序），运行器按 1 MiB 批载荷上限拒超批于启动前；批间检查取消。
- 接线迁移：`KnowledgeAudit.searchForRun` 与 `knowledge_search` 工具改为异步统一检索（证据/足迹精确 span 审计不变）；IPC `SearchKnowledge` 返回带 `results/requestedMode/effectiveMode/degradedReason/coverage/durationMs` 的统一响应并在通道出口过 Schema；Hook/View 消费命中条目（chunk 粒度、按 documentId 关联回资料卡），降级走内联消息；`main/index.ts` 用共享 EmbeddingClient 与 Worker 运行器接两个生产入口。
- 修正过程：`searchChunks` 初版用 `FROM fts f WHERE f MATCH ?`——FTS5 不接受表别名做 MATCH 左值（`no such column: f`），改为表值函数 `knowledge_retrieval_fts(?)` 形式；测试夹具把 `KnowledgeSearch`/`searchForRun` 全链路迁移为异步（run-service/register-ipc/audit/tool/engine 五处 stub）；审计测试的嵌入替身改为「被调用即抛错」，把「关键词路径零 HTTP」变成可执行断言。
- 验证：`knowledge-search.test.ts` 16/16、`knowledge-worker-runner.test.ts` 10/10（真实子进程扫描批三例）、`knowledge-audit.test.ts` 13/13、`register-ipc.test.ts` 35/35（新增 SearchKnowledge 通道两例）、hook 7/7；全仓 `npm test` 1171/1172（唯一红为基线既有 `App.test.tsx` 记忆 combobox，移交记忆会话）；`npm run build` 通过（产物含 `out/main/knowledge-worker.js`）。扫描 warm p95 与 Worker RSS 量化门槛按计划在 KM14 联合自动化实测。
- 提交：KM08 聚焦本地提交（不推送）。下一卡 KM09：知识搜索与索引管理界面。

## KM09 知识检索状态与索引管理界面（2026-09-25）

- 范围：把 KM08 统一检索的真实结果（生效方式、降级原因、向量覆盖）与 KM06/KM07 的设置、作业能力带到知识页；状态与动作全部收在 `useKnowledgeLibrary`，视图零 IPC，遵守 UI 纪律（复用页面骨架、语义 Token、既有反馈通道，不新增 Toast/横幅）。
- 检索投影：搜索后工具栏显示「本次检索方式 · 降级原因（七种枚举逐一中文化）· 向量覆盖 indexed/eligible」；未检索不假称覆盖。检索响应加序号守卫，迟到响应不覆盖当前查询的结果、勾选与状态。
- 索引与模型面板：语义开关（无合格模型时禁用并显示 `unavailableReason`）；嵌入模型 FieldSelect 显式切换（只列启用且已配密钥的 embedding profile，提示切换后需手动重建）；普通重建与强制重建两个入口，确认文案区分（强制重建明确旧语义索引立即停用、取消/部分失败不回退、关键词不受影响）；启用前确认列出模型、资料数与外发费用说明，启用本身不自动补建历史索引（契约 §8.2）。全部走既有 CAS 设置与作业通道。
- 作业面板：挂载读取首页 queued/running 作业并跟踪；事件按作业内 `sequence` 单调合并，迟到小序号不回退面板；终态移出并回读结果；运行中给取消入口（回执 false 如实反馈）；终态把失败/中断/取消条目收为可重试集合，重试提交新作业并跟踪。
- 测试：`KnowledgeView.test.tsx` 4/4、`use-knowledge-library.test.ts` 13/13、`App.test.tsx` 夹具补 settings/jobs。全仓 `npm test` 并行负载下 register-ipc beforeAll 与 memory-retrieval 性能用例抖动，单独复跑 54/54 全绿（未改阈值/超时），唯一持续红为基线 App.test 记忆 combobox；`npm run build` 通过。
- 修正过程：检索 mock 的 `mockImplementationOnce(() => promise)` 触发 no-misused-promises（void 上下文），改 `mockReturnValueOnce`；测试里 `getByRole('checkbox').disabled` 需类型断言而 eslint 又判断言多余（规则间冲突），改用 `hasAttribute('disabled')` 两侧兼容；Hook 测试首版在同一 act 里 setQuery 后立即 onSearch 命中旧闭包（React 状态未刷新），拆成两个 act 后守卫用例真实生效。
- 提交：KM09 聚焦本地提交（不推送）。下一卡 KM10：资料详情、历史预览与来源检查。

## KM10 资料详情、历史预览与来源检查（2026-09-25）

- 范围：Vault v6 迁移补来源检查结论列、`checkSource` 结论持久与语义状态派生、知识页主区详情子视图（不新增第三常驻栏）。
- 实现：`source_status`（枚举 CHECK，历史行一律 unchecked，不伪造「一致」）＋`source_checked_at`；比较当前原件字节哈希得出 unchanged/changed/missing/unreadable 并持久，超导入上限按 changed 处理，只有登记文档缺失才算作业异常；导入/刷新成功即记 unchanged。摘要新增 `sourceStatus/sourceCheckedAt/lexicalState/semanticState`（语义状态由设置＋active generation＋space current 派生，partial/failed 留在作业层不复制成第二套事实）。详情子视图提供固定版本标识、来源状态与检查时间、保存版本列表（旧修订仍读、切换重置分页）、码点分页预览（明示只读已保存文本，零 Run 足迹、零模型调用）与分开的「打开本机原件 / 检查来源 / 刷新内容 / 移出」动作；版本切换与翻页走序号守卫，返回列表保留搜索条件；作业终态统一刷新列表与详情摘要。
- 修正过程：详情状态行最初用多插值 JSX 文本导致文本断成多节点、视图断言找不到目标文案，改为纯函数 `sourceStateLine` 单文案输出；hook 测试一度给 `checkSources` 重复添加 mock 造成 TS2300 重复标识符，改回既有 mock 加 `mockResolvedValueOnce`。
- 验证：`migrate.test.ts` 44/44、`knowledge-vault.test.ts` 15/15、service 检查作业断言改为「结论即完成」、hook 17/17、视图 5/5；全仓 `npm test` 1189/1190（唯一红为基线 App.test 记忆 combobox）、`npm run build` 通过。
- 提交：KM10 聚焦本地提交（不推送）。下一卡 KM11：单层集合管理。

## KM11 单层集合管理与检索前置过滤（2026-09-25）

- 范围：契约 §10.1 单层集合（创建/改名/删除/成员 replace-set/筛选），Vault v7 迁移、协议与 IPC 四通道、检索集合过滤先于截取、知识页筛选与详情勾选；分类只组织不授权，不移动本机文件，不改 Task 材料。
- 实现：v7 建 `knowledge_collections`（name_key UNIQUE＋revision）与 `knowledge_collection_members`（复合主键、双向 CASCADE），documents 补 `membership_revision DEFAULT 1`；名称 NFKC＋trim＋小写唯一，改名/删除要求 expectedRevision，成员保存携带文档级 CAS，两个窗口不会静默互相覆盖。协议加 `knowledgeLibraryFilterSchema`（全部/未分类是派生查询模式，不插集合行）并贯通 `ListKnowledge/SearchKnowledge`；摘要补 `collectionIds/membershipRevision`。`KnowledgeSearchService` 的 library 范围先按筛选解析再做候选收集与截断，未选集合不占 topK 名额；集合被删除后按空范围收口，不报错也不回退全库。UI：工具栏 FieldSelect 集合筛选（切筛选清空搜索与跨查询勾选并给局部反馈），管理面板集合行（新建/行内改名/删除前确认「只解除分类，不删资料与原件」），详情「集合」节勾选后显式保存走成员 CAS。移除资料级联成员，重新导入是新登记、不复活旧成员（设计总稿要求「重新导入不偷偷复活旧成员」，按现状固化为断言）。
- 修正过程：`storeDocument` 事务后的回读 SELECT 漏了 `membership_revision`，导致导入返回的摘要成员版本为 undefined、首次保存分类必报冲突——测试暴露后补齐列；协议手写 `KnowledgeDocumentSummary` 的 `currentRevisionId/sourceCheckedAt/pageCount` 在 exactOptionalPropertyTypes 下与 z.infer 结果不兼容，改为 `?: T | undefined` 对齐（生产构造仍用条件展开）。集合测试夹具一度把「归档」集合误取为列表首项（saveCollection 返回全量列表），改按名称查找。
- 基线修复两处（计入本卡）：`App.test.tsx` 记忆「适用范围」断言仍按原生 select 写，是记忆会话 FieldSelect 改造（9b93312）遗留红，改为按钮＋菜单交互断言（测试侧适配，不动记忆代码）；`run-service.ts` 是本人 KM08 提交时漏跑 prettier 的格式漂移，补跑 `--write`。至此全仓测试基线恢复全绿。
- 验证：`migrate.test.ts` 45/45（v7 默认值/唯一键/双向级联/FK）、`knowledge-vault.test.ts` 17/17（重名规范化、改名与成员 CAS、K-16 删集合保另一集合、移除再导入不复活成员）、`knowledge-search.test.ts` 18/18（筛选先于截取、空范围）、`register-ipc.test.ts` 36/36（4 通道端到端与错误消息可见）、hook 21/21、视图 7/7、`App.test.tsx` 21/21；全仓 `npm test` 1202/1202、lint/format:check/typecheck/build 退出码全 0。
- 提交：KM11 聚焦本地提交（不推送）。下一卡 KM12：Office 解析正确性基础。

## KM12 Office 解析正确性基础（2026-09-25）

- 范围：契约 §11 的复用与正确性基础——字节解析核心、PPTX 演示顺序与备注关系、XLSX Sheet 定位往返、预算先于大分配、取消统一 abortError；本卡不扩 `KnowledgeFormat`（xlsx/csv/pptx 进知识导入是 KM13），不动 `extractDocument`。
- 实现：抽出 `parseOfficeBytes(format, bytes, options)`，`parseFile` 变薄为「读字节＋调核心」，任务读取与后续知识导入共用一份解析器。PPTX 页序按 `presentation.xml` 的 sldIdLst→presentation rels 的关系引用顺序（结构文档必须用保留命名空间的 parser：`removeNSPrefix` 会让 `p:sldId` 的 id 与 r:id 互相覆盖）；备注只沿 slide 自身 `_rels`（Type 尾段 `notesSlide`）解析，删掉「按显示序号猜 notesSlideN.xml」的回退；缺 presentation 或未列入播放列表都报 `unsupported-feature`，绝不假装页序。XLSX 输出定位 `sheet:'表名'!起:止`（含 `!`/引号按 Excel 惯例引号＋翻倍转义），输入端 `parseOfficeXlsxLocator` 同时吃带/不带 `sheet:` 前缀与引号表名，旧 `Sheet1!A1:D12` 兼容；不可识别显式报错替代静默空结果（修复「回显自身输出得 0 节」）；新增 `structuredLocator` 结构化定位；日期序列化为 ISO 文本。解压预算改为按 JSZip 中央目录声明大小在分配完整条目前判定（总量 200 MiB／单条文本 20 MiB），Sheet 数在 ExcelJS 装配前读 `xl/workbook.xml`；取消全部改抛 `abortError()`（含 run-service Office 读取前置检查），落回「取消语义只有一处定义」。
- 停止条件核对：依赖无需更换——声明大小检查已把关键预算移到分配前；ExcelJS 仍需整包驻留（受 50 MiB 输入上限约束），如实记录，不谎称防住全部内存峰值。
- 测试：`office-parser.test.ts` 6/6（页序错位＋备注关系错位、往返/特殊表名/ISO 日期/结构化定位/旧格式、bytes 核心与 parseFile 一致、210 MiB 声明量拒于分配前、21 MiB 单条 XML 拒于解码前、CSV 引号内换行、取消 `isAbortError`）；`run-service.test.ts` 52/52 不回归。全仓 `npm test` 1204/1204、lint/format:check/typecheck/build 退出 0。预算用例构造真实 210 MiB 合成压缩包，首跑在并行负载下超 5s 缺省超时，按仓内 Worker 测试先例给该用例显式声明 30s（真实工作量，未改阈值语义）。
- 提交：KM12 聚焦本地提交（不推送）。下一卡 KM13：xlsx/csv/pptx 导入知识并使用。

## KM13 工作簿、CSV 与演示文稿导入知识（2026-09-25）

- 范围：契约 §11 的格式扩展与分节映射；`KnowledgeFormat` 加 `xlsx|csv|pptx`，穷尽所有消费分支，导入链路复用 KM12 字节核心；不改检索/读取/采用协议（新格式经既有链路自动可用）。
- 实现：协议收口单一 `knowledgeFormatSchema`（文档摘要、提取文档、检索命中、修订摘要四处内联枚举并轨，tool-runtime `KnowledgeSearchItem.format` 与 `ScopedChunkFormat` 同步引用，不留平行枚举）；`extractDocument` 改显式穷尽 switch，新格式绝不落入 PDF 分支。分节按 §11 确定性规则：PPTX 每页/每表/备注一节（表行 ` | ` 连接，pageCount 取播放页号）；XLSX 每非空逻辑行一节（`sheet:'名'!起:止` 复用引号往返定位；单元格 `address=value`，公式 `address=formula（缓存值：x|无缓存值）`，日期 ISO）；CSV 每逻辑记录一节（`rows:N-N`，字段按 CSV 引号规则再序列化）。全空文件报 `no-extractable-text`（warning 枚举新增）不建伪知识；`truncated` 拒绝发布；单节超 2,000,000 码点 `EXTRACTION_LIMIT_EXCEEDED`，绝不截断成完整表。扩展名映射、对话框过滤、不支持提示与页面文案同步。
- Worker 约束修正：office 依赖进入 Worker 原生执行图后暴露两处 strip-only 限制——`KnowledgeServiceError` 的构造参数属性改为体内赋值；`abortError` 走 `@betterwork/agent-core/errors` 子路径导出（agent-core package exports 增补），electron-vite 与 vitest 的字符串别名按声明顺序把子路径放在包名前缀之前。取消定义仍只在 agent-core/errors.ts 一处。
- 修正过程：首跑全仓时 11 个测试文件与 build 同因别名拼接（`index.ts/errors`）失败——补子路径别名后恢复；`.ts` 相对导入用于 Worker 图内新引入的两个本地模块（office-parser、knowledge-errors），与 KM07b 既有约定一致。
- 测试：`knowledge-extract.test.ts` 6/6（错位页序、表/备注节、逐行分节与序列化、引号内换行、no-extractable-text、单节超限）、`knowledge-vault.test.ts` 18/18（三格式真实导入＋检索＋预览 locator；`.ppt`/空 CSV 逐条原因）、`knowledge-worker-runner.test.ts` 10/10（真实子进程提取 csv）、`register-ipc.test.ts` 37/37（对话框过滤）。全仓 `npm test` 1212/1212、lint/format:check/typecheck/build 退出 0，`out/main/knowledge-worker.js` 正常产出。
- 提交：KM13 聚焦本地提交（不推送）。下一卡 KM14：自动跨链路、迁移、性能与打包回归。

## 10:45 工作型记忆设计评价（状态：完成）

- 背景：光哥要求评价现有记忆设计是否完备合理；本轮仅评审，不实施改进。
- 结论：产品边界、用户确认、四作用域、修订与撤销审计设计合理；稳定召回、低负担纠错和真实工作效果仍未闭环。
- 设计取舍：当前词面召回不能保证同义任务命中关键约束；同议题不同正文即判潜在冲突，存在误报与不同议题名漏报的局限；完整依赖继承与连续安全历史后缀防止撤销内容回流，但换期材料或修订变化可能牺牲协作连续性。
- 静态核实的实现偏差：App.tsx:1457 的回答片段保存入口不传 sourceSelector，MemoryEditor.tsx:263 因而声明自主口径，最终 memory-provenance.ts:235–236 写空依赖，与设计 §3.2 普通确认保留资料依赖不符；ContextPanel.tsx:667 仅为预览入选项渲染排除开关，重新试算后已排除项转入无恢复动作的落选区。
- 建议（未批准、未实施）：先补齐来源与恢复入口，再评估关键规则显式固定和相关经验检索的分层策略；用同义表达、跨期复用、规则纠正与撤销的真实旅程验证收益，不以新增向量库或更多审计字段替代产品验收。
- 证据边界：阅读设计、契约、ADR、WM 任务板并抽查生产链路与既有测试；未运行测试、应用或真实模型，以上实现偏差为静态代码证据，不称现场复现；WM16 仍待完整人工及模型语义验收。
- 变更：仅追加本节日志，保留原有未跟踪的 knowledge-foundation.integration.test.ts，不修改代码、任务板或授权状态，不提交、不推送。

## KM14 联合自动验收、规模基准与打包预检（2026-09-25 11:23）

- 范围：契约 §13.3 跨链路矩阵、§14 性能门槛与本卡打包预检；只补测试与本范围内暴露的缺陷，不加功能。
- 联合链路：`knowledge-foundation.integration.test.ts` 1/1——导入→固定修订→草稿幂等/冲突→Run 范围检索（未选资料不出现、空选材不回退全库、hybrid 如实降级 keyword＋semantic-disabled）→逐页游标读取计入预算并逐页落 Evidence→刷新不改既有绑定→移除后禁新草稿但历史可回看→重开 Vault 身份不漂移。分页读取每页需独立 toolCallId（足迹槽位 toolCallId＋partIndex 去重），实现即按真实工具调用建模。
- 规模基准：`knowledge-foundation.perf.test.ts` 3/3——10,375×1,536 真实 Worker 扫描＋RRF 融合，查询嵌入本地替身（排除 HTTP）。单跑 p50 270.3/p95 289.6/max 293.9ms；最终全套并行下 p95 758.8/max 761.7ms（阈值 1,000ms 未动）；Worker 增量 RSS 3.3/60.9MiB（≤256MiB）；4,096 维批上限实测＝64 条整批可扫描、超限拒绝且零新进程；大批量扫描中途取消 abortError 收口、shutdown 后登记进程清空。
- 诊断记录：初跑 p50 4.8s、复跑 1.46s 两个慢窗，用 24 轮稳态探针（~280ms 无劣化、heap 79MiB）判定为并发会话 CPU 争抢，不改阈值不改规则；两次慢窗数据如实登记于此。
- 生产缺陷修复：`buildRetrievalChunks` 每窗口 `Array.from(整节)` 截取（O(n²)），2M 码点节在契约规模下导入不可完成——改为每节一次线性处理（无代理对直接 UTF-16 索引切片，否则码点数组只展开一次），补代理对跨窗回归（chunks 4/4）。
- 打包预检：`npm run verify`（含 `npm run build`）退出 0，产物 `apps/desktop/out/main/knowledge-worker.js` 3.44kB、`out/main/index.js` 约 960kB；exceljs/jszip 仍走 external，未新增打包配置，不安装不发布。
- 验证：全仓 `npm run verify` 退出 0（131 files/1217 tests，含本卡新增联合链路 1＋基准 3＋块化回归 1 例）、`git diff --check` 干净；两库迁移号无变化（Vault v7/App v32）。
- 交付与剩余：KM15 人工清单落 `docs/development/knowledge-km15-checklist.md`（真实模型语义栏待光哥授权）；20,000×4,096 全库级压测超出本卡必测口径，容量守卫与超限降级已有单测，未另行编造数据。KM00–KM14 编码阶段收口，提交不推送。

## 14:13 记忆改进 Spec 与 Qwen3.8-Flash 开发交接（状态：完成）

- 背景：光哥要求将记忆评审结论整理成增量设计和下午可逐卡执行的开发测试计划；本轮只交付文档与模拟原型，不实施业务代码。
- 产物：`docs/designs/memory-improvements.md`、Proposed ADR-0028、既有记忆契约 §11、MI00–MI10 任务板及 Qwen3.8-Flash 提示词；同步路线图、UI §6.1.4、开发入口与 WM 板增量链接，不改变已有任务状态。
- 推荐顺序：MI-M0 先修完整来源继承与排除恢复；随后显式优先规则、冲突来源/并存条件、换期恢复。来源专家身份取来源 Run 快照，当前配置变化须确认，不复制旧权限；不引入记忆向量库或自动恢复旧历史。
- 审查修订：来源选择器之外补 Main 完整依赖门禁；独立排除查询；v1/v2 快照兼容；无词面 Q1–Q8 与技术 R1–R8、负例 N1–N8 分开计数。真实模型 S5 必须在有审计历史的原 Task 续跑；专用空间提炼已关闭且无作业；六对场景理想下限 14 次请求、建议申请 18 次，工具回合及后台请求全部计数，未获授权不执行。
- 检查证据：当前 HEAD `7ba8761`；6 份核心文档/原型共 32 个本地链接有效，主文档 4 个章节锚点有效，任务/场景矩阵条目齐全，UTF-8 与文件尾换行检查通过，中文修改逐段 Grep 回读，`git diff --check` 通过；最终独立只读复核未发现剩余阻断，应用源码无本轮差异。
- 原型证据：`docs/prototype/memory-improvements/index.html` 的定向 Prettier 与内联 JS 语法检查通过；专用 Chrome 标签页实际测试 F1 来源保存/取消、半代理对及超长拒绝、来源缺失禁用，F2 预览失败/无词面下恢复，F3 优先预算与来源门禁，F4 共存条件保存重开，F5 选材/精确 v1 引用返回后草稿与专家保留；控制台为空，已查看截图。SDK 组合截图输出失败后用显式 PNG 输出成功，未改变原型或绕过校验。未做窄屏、深色或完整辅助技术专项验证。
- 验证边界：未运行应用测试、typecheck、build 或 verify；未启动 BetterWork、未调用真实模型、未提交推送。仅启动绑定 127.0.0.1:8768 的文档预览服务，保留专用原型评审页；模拟原型不等于生产 UI 或业务效果验收。D1–D5、所有 MI 开发卡与 ADR-0028 仍待授权。

## 14:35 记忆改进 D1–D5 评审批准记录（状态：完成）

- 审批事实：光哥查看原型后明确「我看了评审原型，批准 D1～D5 的建议。」五项推荐方案均已批准，未变化的方案无需重复确认。
- 变更：同步 Spec、ADR-0028、契约 §11、MI 任务板、Qwen3.8-Flash 提示词、原型标记及路线图/UI/开发/WM 入口；单卡模板改为引用已批准方案，不改设计行为、技术字段、预算或验收门槛。
- 授权边界：MI00–MI10 仍全部 todo，MI00 尚未执行；单卡开发、真实应用验收、真实模型调用及提交发布仍各自授权。ADR-0028 正式状态仍为 Proposed，ADR-0026 与 WM16 及其他系列状态未改。
- 检查证据：HEAD `7ba8761`；5 份核心交接文档共 24 个本地链接、UTF-8、尾换行、契约 §11 锚点和 11 张 todo 卡校验通过；中文 Grep 回读、原型 Prettier 与 `git diff --check` 通过。专用浏览器页确认 D1–D5 均显示已批准；抽查无来源保存禁用、取消显示 0 次保存，控制台为空。只改批准文案，未重跑上一节全部交互场景。
- 未执行：生产代码修改、应用测试/typecheck/build/verify、BetterWork 启动、真实模型调用、提交或推送；保留此前文档与并行工作日志。下一步由光哥向 Qwen3.8-Flash 下达 MI00 只读核对指令。

## 14:46 MI00 记忆改进基线核对（状态：完成）

- 背景：光哥下达「按 `docs/development/memory-improvement-coding-prompts.md` 全面推进后续编码测试落地，合适阶段提交代码」的开工指令，先执行 MI00 只读核对。
- 基线：14:43 `date`；HEAD `7ba8761`，分支 main；`git status --short` 为本增量 6 改 5 新，全部位于 `docs/`，无并行会话未提交源码。
- 真实迁移：`apps/desktop/src/main/db/app-schema.ts` 内联迁移最大版本 **32**（文件内 32 个 `version:` 条目），MI04 列迁移只能取 33 起，不预占、不改历史迁移。
- 链路复核：`memory-provenance-reader.ts:11–18` 仅按 `message.completed` 匹配事件，未验证其为最终无工具回答；`:29–32` 依赖只取 RunContextSnapshot 材料；`memory-service.ts:150–164` 的 `provenanceFromResolved` 固定写 `memoryDependencies: []`；`ContextPanel.tsx:663–667` 可切换行仍仅从 `preview.selectedItems` 渲染。Spec §2 的静态问题全部仍存在，无既有修复可复用。
- 可复用资产：`runMemoryContexts.listDependencyUnion(runId)`（`run-memory-context-repository.ts:266`）已提供材料/记忆依赖并集；`extraction-source-reader.ts:57–73` 的 `?? []` 回退正是 MI01 要消除的降级通路，不直接复用。`SOURCE_REVIEW_REQUIRED` 已存在于协议错误码（`agent-protocol/src/index.ts:1252`）。
- 审批与边界：D1–D5 方案已批准；MI01–MI10 按指令串行开发并分阶段提交；未执行任何真实模型调用、未启动应用、未推送、未发布；ADR-0028 与 ADR-0026 正式状态未改，WM16 与其他系列状态未动。
- 下一步：MI01 先补可失败回归（最终事件门禁、完整依赖继承、缺链拒绝），再实现 Main 闭环；本卡不跑构建。

## 15:40 MI00–MI03 记忆改进落地（状态：自动化完成，UI 待光哥验收）

- 背景：光哥下达「按 `memory-improvement-coding-prompts.md` 全面推进编码测试，合适阶段提交」的指令；按 MI 串行推进 MI00→MI01→MI02→MI03。
- 提交：`c05e6f6` 文档与批准记录（含 MI00 核对）、`cd36d2a` MI01 Main 来源闭环、`c3a5348` MI02 回答捕获、`f914bcc` MI03 独立排除清单。未推送、未发布。
- MI01：`ProvenanceReader` 改为按「completed ＋ 最后一条非工具 `message.completed`」取正文，新增 `runWorkspace/runDependencies/checkpointDependencies/artifactVersionDependencies/memoryRevision` 五个读取口；缺准备快照或记忆审计记录返回 `SOURCE_REVIEW_REQUIRED`，不再 `?? []`；依赖并集去重后递归展开，循环→`SOURCE_DEPENDENCY_CYCLE`、超限→`SOURCE_DEPENDENCY_LIMIT`、501 码点→`SOURCE_MISMATCH`。协议拒绝 `sourceSelector` 与 `asUserInstruction` 并存。
- MI02：捕获入口未确认原文选区不可提交；页面选区仅唯一命中才预填，否则只读原文重选；半代理对边界、重复片段、非 BMP 均有测试；派生内容不再提供 user/expert 全局范围。
- MI03：新增 `memory:task-exclusions` 只读投影（不要求 prompt、不写足迹），`TaskMemoryExclusionItem` 的 `unavailable` 分支不返回正文/标题/来源；写入仍走既有 `SaveTaskContextRequest`＋CAS；面板独立「本任务已排除」段在预览失败时仍给恢复入口。
- 测试证据：每卡定向测试＋`npm run verify` 全绿，最终 133 文件 1244 测试通过、typecheck/build 通过、`git diff --check` 干净；自动测试零外网、零真实模型调用，全部使用真实 SQLite，未 mock 仓储。
- 边界：未修改生产迁移（MI04 起用 33）、未改自动提炼算法、未动 Knowledge、未改 WM16/A/B0/E/CF/KM 状态、未提交用户资料或密钥；ADR-0028 仍 Proposed。
- 下一步：MI-M0 人工验收（见 15:40 应用验收清单），通过后经光哥指令再进入 MI04 迁移卡；MI04–MI09 保持 todo。

## 16:10 MI04–MI05 召回策略字段与 memory-recall-v2（状态：自动化完成，真实请求断言待补）

- 背景：光哥要求持续按卡推进并分阶段提交；在 MI-M0 人工走查尚未回结果时继续做迁移基础与 v2 召回，发现偏差回对应卡补回归。
- MI04（`dd7bb58`）：v33 迁移补 `memory_records.recall_policy`（默认 relevant、CHECK 枚举）；`MemoryState` 贯穿创建/追加修订/治理/替代路径，策略只随显式 patch 变化；协议 `memoryRecordSchema.recallPolicy` 为必填修订字段，旧持久化 DTO 不做隐式兼容后门。
- MI05：协议 `memory-recall-v2` ＋ 优先池常量 ＋ 冻结 v1 快照 Schema ＋ `pinned-rule` 理由，并在 `runMemoryContextSchema` 拒绝版本/快照不一致与 v1 内的 pinned-rule；`MemoryService.update` 实现契约 §11.3 资格判定（含「已 pinned 被编辑成不合格形状」要求先取消优先）；`applyRecallBudgetV2` 与 `pinnedGroupOrder` 实现优先池→偏好池→相关池、组原子、预算先算；`unitsOf` 现按同一组理由整体裁剪。
- 踩坑修正：`unitsOf` 首次改动只识别 `conflict-group` 收集，遇到 `pinned-rule` 单元会生成空单元并把 index 卡住，导致 preview 用例 OOM；修复为按「同一组理由」整体收集，并补 `memory-recall-v2.test.ts` 覆盖该路径。教训：预算裁剪的循环推进必须由收集条件保证，不能只改判入口。
- 测试证据：`memory-recall-v2.test.ts` 9 例全通过（零词面命中入选且分数如实为 0、6/7 条边界、2,000/2,001 码点边界、三分量整体进出、Task 排除不能穿透、真实 SQLite preview 写 v2）；`npm run verify` 全绿，134 文件 1256 测试，typecheck/build/格式/lint 通过；零真实模型调用、零外网、真实 SQLite 未 mock 仓储。
- 边界与下一步：MI05 的「实际 ModelRequest 断言」与 v1/v2 混合库回看仍需在 MI09 联合回归里补齐；MI06 UI、MI07/MI08 未开工；未推送、未发布、未改 ADR/WM/KM/CF 状态；迁移号只用 v33，未预占后续版本。

## 16:22 MI07 冲突裁决可回看（状态：数据层完成，来源展开与 UI 待做）

- 变更：`memoryConflictPairSchema` 改为按 `state` 判别联合——keep-both 必带 1–300 码点的 `applicabilityNote`，unresolved/replaced 严格不带；`listConflictPairsForRevisionIds` 随裁决行回传该说明，缺说明的历史并存裁决降级显示为未裁决（不给空串假说明）。
- 界面：记忆视图冲突卡并列两侧正文＋适用范围＋有效期（复用 `formatValidityRange`），并存时在同一位置显示「并存适用条件」，不改冲突判定算法、不新增裁决表。
- 发现：`memory_conflict_decisions` 早已有 CHECK `(decision='keep-both') = (applicability_note IS NOT NULL)`，因此「缺条件的并存裁决」在生产库中不可能存在；据此删掉不可判定的伪造用例，改测 DTO 分支与真实回传，避免用假数据凑覆盖。
- 测试证据：`npm run verify` 全绿（134 文件 1258 测试）；协议 DTO 分支用例、仓储裁决回传用例、`memory-service.test.ts` 真实 SQLite 并存回看用例通过。
- 缺口：契约 §11.5 的「按需展开精确来源摘要」（复用 `memory:get` 与 provenance）本轮未实现；MI07 保持 doing，UI 走查待光哥。

## 16:32 MI08 换期来源执行器与恢复引导（状态：自动化完成，UI 待验）

- 变更：协议新增 `artifact:get-version-executor` 与 `ArtifactVersionExecutorSummary`（general / expert / unavailable 互斥分支，专家分支同时回传来源修订与当前修订）；`resolveArtifactVersionExecutor` 从精确版本的来源 Run 快照判定身份，user-edit 沿同成果前版回溯，断链/循环/缺快照返回 unavailable，版本不属于所给成果返回 null；Preload `artifacts.getVersionExecutor` 与 App 消费同卡落地。
- 行为差异：「从此版本开始新任务」此前读旧 Task 的当前 executor 草稿并复制其 skillBindings；现在只看来源快照，不复制旧绑定、不复活旧权限，配置更新时提示按当前版本使用，来源不可用时提示原因并回退通用助手。
- 测试证据：`artifact-version-executor.test.ts`（真实 SQLite：快照专家、回溯继承、缺快照拒绝、停用专家、跨成果 null）、`App.test.tsx` 两条更新用例；`npm run verify` 全绿 135 文件 1261 测试。
- 同日累计：MI00 done、MI01 done、MI02/MI03 doing（自动化通过，UI 待光哥）、MI04 done、MI05 doing（真实 ModelRequest 断言留给 MI09）、MI07 doing（来源展开未做）、MI08 doing；提交 `c05e6f6 / cd36d2a / c3a5348 / f914bcc / 6ba610c / dd7bb58 / 84d2753 / fd3a413` 及本卡，未推送、未发布、未调用真实模型。
- 下一步：MI06 策略管理 UI、MI07 两侧来源展开、MI09 离线联合回归（Q1–Q8 / R1–R8 / N1–N8 与真实 ModelRequest 断言）；MI10 仍需光哥人工 UI 与独立的真实模型授权。

## 16:41 MI06 优先规则管理与原因展示（状态：自动化完成，UI 待验）

- 变更：`MemoryView` 记忆卡增加召回策略徽标与「设为优先带入 / 取消优先带入」开关（仅已确认、非终态、非只读分组可见），提交沿用 `state.update` 的 `memory:update`＋新 operationId＋expectedRevision；策略不合格时不在前端另判，直接把 Main 的 `INVALID_TRANSITION` 文案显示在列表内联错误区。
- 文案边界：说明固定为「只免词面门槛，不免范围、有效期、本任务排除、来源与冲突门禁；调整会追加修订并只影响下次运行，也不表示模型一定采用」；测试断言不出现「模型已阅读」这类夸大表述。
- 样式：新增 `.memory-policy-pinned`、`.memory-policy-hint`，只使用既有语义 Token（`--warning`/`--text-secondary`/`--surface`），不新增硬编码色值、不改 Artifact 配色。
- 测试证据：`MemoryView.test.tsx` 新增 4 例通过；`npm run verify` 全绿 135 文件 1265 测试。
- 待办：MI06 的窄屏/键盘路径与真实应用调整走查属人工门槛；MI07 两侧来源展开、MI09 离线联合回归仍未完成。

## 16:51 MI07 补齐两侧来源按需回看（状态：自动化完成，UI 待验）

- 变更：`use-memories` 暴露 `loadRevision(GetMemoryRequest)`，复用既有 `memory:get(id,revisionId)` 只读通路；`MemoryView` 冲突卡新增「查看两侧来源 / 收起两侧来源」开关，按 `pair.leftRevisionId/rightRevisionId` 精确读取，展示来源类型、修订号、材料与记忆依赖计数与摘录前 60 码点，不渲染原始事件载荷。
- 安全边界：只有当前列表内已可管理的一侧才发起请求（不借来源探测其他空间内容）；另一侧不可得时显示占位文案且不泄露其正文；异步链走 `trackAction` 收口，失败也有明确落点。
- 测试证据：`MemoryView.test.tsx` 新增 2 例（展开按精确修订各读一次并显示两行摘要；越范围一侧不请求且只显示占位），`npm run verify` 全绿 135 文件 1267 测试；typecheck 与 `exactOptionalPropertyTypes` 均通过。
- 剩余：MI07 的人工走查、MI09 离线联合回归（Q1–Q8 / R1–R8 / N1–N8 与真实 ModelRequest 断言）、MI10 真实 UI 与另行授权的真实模型语义验收。

## 17:01 MI09 离线联合回归首批（状态：进行中）

- 新增到 `work-centered-memory.integration.test.ts`：Q1–Q8 循环（八种无词面问法全部经生产 RunService 发送链路，断言实际请求含目标规则、审计 reason=pinned-rule、score=0、recallVersion=v2）；R3/R6/R7；N1/N2/N3/N7；AC3 重启后旧 v1 快照原样解析且不回填新字段。
- 修正的测试前提：排除后的请求断言必须剔除本轮 user prompt（否则同义复述会把断言变成假阳性）；R6 需要两侧真的构成同议题潜在冲突对，否则只是两条独立记录；过期用例要先合法设优先再走治理动作，Main 本来就拒绝给已失效记录设优先。
- 修出的实现缺陷：`renderMemoryBlock` 只在 `conflict-group` 理由后追加并存适用条件，导致优先池里的并存分量带正文丢条件；已让 `pinned-rule` 同样携带说明，并由 R6 的实际请求断言守住。
- 测试证据：`npm run verify` 退出 0（135 文件 / 1274 测试），零真实网络与模型调用。
- 剩余：R2/R4 专家范围、R8 换期联合场景、N4 恢复列表专项、N5 材料换版本、AC4 变异验证；MI10 仍需光哥人工 UI 与独立真实模型授权。

## 17:06 MI09 第二批与变异验证（状态：仅剩 R8）

- 新增：R2/R4 专家与专家工作空间优先规则按范围带入且组内选择集精确等于两条；N4 他空间优先规则既不进入请求，也只能以 `unavailable` 占位出现在排除投影（不暴露正文/来源/存在性）；N5 派生记忆的材料换版本后由 `dependency-unavailable` 挡在请求外，且工作区源文件内容仍是本期写入的版本，证明记忆链路没有改写用户资料。
- AC4 变异验证（临时改动不提交）：① 把 MI01 的「缺准备快照/审计记录 → `SOURCE_REVIEW_REQUIRED`」改成 `?? 空依赖`，MI01 集成用例立即失败；② 关掉 MI05 的 pinned 资格门禁，`memory-recall-v2.test.ts` 的资格用例立即失败。两处按原文精确还原，`git status` 只剩本卡新测试文件，未使用 reset 覆盖他人改动。
- 修正的测试前提：N4 需要真正使用该优先规则的 id 参与排除投影断言，否则只是形式断言；变异实验证明断言不是空跑。
- 证据：`npm run verify` 退出 0（135 文件 / 1276 测试）。剩余矩阵行：R8 换期联合场景；MI02/MI03/MI06/MI07/MI08 的 UI 走查与 MI10 真实模型语义验收仍待光哥。

## 17:13 MI08 恢复入口与 MI09 矩阵收尾（状态：离线验收通过）

- `a2c95ac` R8 换期联合场景：自主方法经优先池进请求、上期数字不作为记忆块事实、快照钉住用户选定的成果版本与哈希、未真实读取则不产生读取关系。至此 MI09 的 Q1–Q8、R1–R8、N1–N8 与 AC3/AC4 都由本卡自己跑过，`npm run verify` 退出 0（135 文件 / 1278 测试）。
- `f79eb6e` MI08 恢复入口：历史截断解释后新增「选择本期材料 / 查看上期成果版本 / 到记忆详情保留方法」三个可返回动作，文案明确引用只固定所选版本、标记参考不等于已读取、不自动摘要旧回答；不提供也不暗示「恢复全部历史」。
- 边界：本卡只证明离线技术链路，不宣称真实工作收益；MI10 的八条 UI 路径与六个成对真实模型场景仍需光哥逐项授权。MI02/MI03/MI06/MI07/MI08 的自动化均通过，UI 人工走查待做。

## 17:23 MI03 钩子行为与 MI08 失败守卫补口（状态：自动化补强）

- MI03：新增 `use-task-memory-exclusions.test.ts` 4 例——清单读取不要求 prompt、上下文未建立时静默不请求并丢弃上一任务清单、切任务后的迟到响应必须作废、读取失败给出可重试错误且不与预览错误互相牵连。此前该钩子只有面板级用例，行为层是空白。
- MI08：`startFromArtifactVersion` 改为带请求序号与任务身份双守卫——读取失败不再清空原任务（改抛一次可重试错误，由调用方 `reportAction` 呈现），重复点击时旧响应作废，不会在界面上留下「幽灵专家」。`App.test.tsx` 补 2 例覆盖这两条（AC4/AC5 的取消与迟到分支）。
- 证据：`npm run verify` 退出 0，136 文件 / 1284 测试；中文文案与新注释均 grep 回读确认。仍未做：MI02/MI03/MI06/MI07/MI08 的真实窗口人工走查、MI10 真实模型语义验收（需光哥授权）。

## 17:38 MI 人工验收清单落地（状态：走查可开始，MI10 blocked）

- 新增 `docs/development/memory-mi10-checklist.md`：把 MI-M0 的两条门槛路径、MI02/MI03/MI06/MI07/MI08 的 UI 门槛与任务板 §6.1 八条实际窗口路径，逐格写成「操作 → 该看到什么 → 结果记录」，§1–§7 共四十八格可当场走查；§8 六个成对真实模型场景整节记「未测」，等待配置显示名与整数预算的单独授权。
- 清单里的预期结果全部按当前实现的真实文案核对过，不写理想化描述：`记住这段经验`/`确认选区`/`保留来源并记住`（App.tsx:1463、1510 与 MemoryCaptureSource.tsx:61）、`下次运行可用`/`本任务不用`/`恢复参与选择`/`移除此排除`/`此项当前不可查看`（ContextPanel.tsx:663、744、797–818）、`按相关性选择`/`设为优先带入`/`已确认并存（成对带入，适用条件见裁决记录）`/`查看两侧来源`（MemoryView.tsx:660、741、833、892）、`历史上下文调整`的三个返回入口（ContextPanel.tsx:1037–1043）、`与本任务不相关`/`超出本次预算`落选原因（memory-labels.ts:78–79）。
- 资格拒绝的原因文案同样取自 Main（`memory-service.ts:172–189` 的五条 `pinnedEligibilityProblem`），清单因此能判「界面有没有把拒绝说清楚」，而不是只判「有没有报错」。
- 任务板补口：MI09 行的「剩余：R8」已被 `a2c95ac` 覆盖，改为「剩余＝MI01–MI08 人工 UI 记录」，证据更新为 136 测试文件全通过；MI10 行由 `todo` 改 `blocked` 并写清两条前置（人工走查未做、模型授权未下达）；§6 增加指向清单的一句话，逐格记录只写在清单里，状态仍以任务板为唯一真相源。MI04 行补一句核对结论：`memory_operations.result_json` 只写 `effect` 与 `committedRevisionIds` 等身份、不内嵌 `MemoryRecord`，因此 AC3 的旧回执读取适配实际为空，重放用例已覆盖，不额外造兼容分支。
- 逐卡提示词文件 §4 之后加一行指引：MI-M0 的 A/B 步骤已展开进清单，不再在提示词里维护第二份步骤，避免两处漂移。
- 边界：本轮只动文档，未改代码；`npm run verify` 退出 0（136 文件，构建通过，仅两条已知 Zod `@__PURE__` Rollup 警告）。中文文案四处 Edit 后全部 grep 逐字回读；ASCII 直引号统一为中文引号用一次性脚本处理并断言配对平衡（`UNBALANCED_QUOTES` 未触发），脚本文件用完即删。仍不推送、不发布，也不擅自操作应用界面。

## 18:12 契约回扫与门禁补口（状态：自动化再加一层锚点）

- 人工走查清单落地后（`1dc1f0b`），对本轮 MI 做了一次「契约条款 ↔ 测试断言」回扫：把契约 §11.1–§11.6 的可判定条款逐条列出，再到测试里找断言证据，只保留「条款要求了但没人断言」和「断言判不出条款」两类。回扫由只读子代理汇总，但它报的 10 条缺口和它宣称「已覆盖」的 6 条（回执幂等、CAS 冲突、投影失败仍成功、清单读取不要求 prompt、旧 v1 只读、执行身份 null）都逐条自己 grep 复核后才动手——照单全收会补出重复用例，直接不信又会漏掉真缺口。
- 修出的实现缺陷（`d8c0bd8`）：写入优先门禁只看 `deriveEffectiveStatus`，未到 `validFrom` 的记录也算「已确认」，于是能被设为优先带入；召回侧用的是 `isMemoryEffectiveAt`，同一条规则当时并不会入选。契约 §11.3 的措辞是「confirmed＋当前有效」，口径以召回侧为准，门禁改为两侧同源。证据是新用例先失败（`expected true to be false`）后通过，不是改完补一句断言。
- 补的断言（`8074a97`）：v1/v2 运行快照必须与 `recallVersion` 同分支、v2 缺 `pinnedItemLimit`/`pinnedCodePointBudget` 不合法、v1 上下文里出现 `pinned-rule` 直接拒绝；预算字面量（6/2000/2/600/16/6000/2000/8000）与任务排除上限 100（第 101 个拒绝而非截断）写死，改动必须同时升算法版本；材料侧 200 依赖上限与「摘录区间为空」由 Main 把关；来源执行身份 `general` 分支不再只测 expert/unavailable；替代不继承败者 pinned、改写正文不缩减已登记的资料依赖。
- 判读记录：「用户口径＋带材料依赖」这种形状当前生产路径写不出来（`create` 里 `sourceSelector` 与 `asUserInstruction` 互斥），所以这条按对抗性 fixture 测门禁，并在测试注释里写明它属于防御而非现役路径，避免后来人误以为存在这样的写入口。
- 踩到的一次自伤：在 968 行的协议测试里用后一行做锚点插入时，误删了既有用例的一行解构；`git diff --stat` 当场显示该文件仍为 0 变更才确认已完整还原。教训是在长文件里插入必须把被替换文本原样回放，改完立刻看 diff 而不只看工具返回。
- 证据：`npm run verify` 退出 0，136 文件 / 1295 测试（本轮新增 11 例）；中文用例名与文档句子逐条 grep 回读，直引号统一为中文引号后 prettier 通过。
- 边界与下一步：仍不推送、不发布、不真实调用模型。待光哥按 `docs/development/memory-mi10-checklist.md` 走 §1–§7；18:15 用 `scripts/dev-start.sh` 重启到 pid 42613，主进程与渲染进程都是当前代码（此前只有渲染进程热更新过，`d8c0bd8` 的门禁改在主进程），MI10 §8 需模型配置显示名与整数调用预算的单独授权。

## 18:23 文档散文轴与证据指针复核（状态：口径跟齐，仍待人工走查）

- 扫的两条轴：**证据指针真实性**与**文档散文是否与实现同代**。清单和日志里点名的 16 处 `file:line` 用 `sed -n` 逐行回读，1 处真漂移已修——`d8c0bd8` 在 `pinnedEligibilityProblem` 前加了一行注释，日志里的 `memory-service.ts:171–186` 应为 `172–189`；其余（`App.tsx:1463/1510`、`MemoryCaptureSource.tsx:61`、`ContextPanel.tsx:663/744/797–818/1037–1043`、`MemoryView.tsx:660/741/833/892`、`memory-labels.ts:78–79`）都命中期望文案。核对时自己也漂了一次：把范围起点 `797` 当成单行去找「恢复参与选择」，实际在 `817`，属于探针写错而不是文档写错——范围型引用要按范围核，不能只读首行。
- 更正四处过期口径：`docs/04` §6 原文只说召回「使用固定版本 `memory-recall-v1`」，补上 MI05 的 v2 池序（优先→偏好→相关）、`pinned-rule` 理由、6 条/2,000 码点优先预算、旧 v1 快照原样只读与「版本必须与快照同分支」；`docs/04` §8 增加 MI01–MI08 已实现的来源闭环、独立排除投影、`recallPolicy` 迁移与资格拒绝，并写明人工走查未完成；`docs/10` §6.1.4 的「目前未开工、未实现」改为「界面与主进程链路已实现，逐格走查记录在 MI 清单」；ADR-0028 状态行补一句「代码分阶段提交不自动改变 Proposed，推送与发布仍未授权」。
- Spec 的 F3 补口径：优先资格除「已确认＋来源已核实＋自主工作要求」外，还要当前生效且无材料/记忆依赖，且「当前生效」与召回同区间判定——这正是 `d8c0bd8` 修的缺陷在产品层的表述。
- 未改的设计总稿快照：`docs/designs/work-centered-memory.md` 按既定约定不回写，MI 的增量口径只进 Spec、契约、ADR 与状态板。
- 边界：本轮纯文档，无代码变更；markdown 全部 `prettier --check` 通过，上一轮 `npm run verify` 退出 0（136 文件 / 1295 测试）仍是当前基线。下一步只有光哥的人工走查（清单 §1–§7）与 MI10 §8 的模型授权。

## 18:30 反馈落点与界面读取失败分支（状态：自动化基线 1296）

- 扫的轴：**用户反馈落点是否只有规范允许的三个**（docs/10 §11.5.1）。先在生产源码里 grep `.inline-message`：MI 新增的捕获表单、来源重选、排除清单、策略拒绝、冲突回看全部只用 `.inline-message error`，没有非错误态内联消息、没有自造 Toast，符合规范；但**测试层只断言「文案出现在页面里」**，等于把落点约束留给肉眼。
- 补的断言（`1a530b3`）：捕获表单的「未确认选区」错误必须命中 `.inline-message.error.memory-editor-problems`，且全树不存在 `.inline-message:not(.error)`；记忆页策略拒绝同样收到错误槽；排除清单新增一条读取失败用例——错误留在小节原位、点「重试」只重读清单一次、已经读到的条目不被清空。
- 补用例时发现的真空白：`use-task-memory-exclusions` 的失败串只在钩子层测过，面板层此前完全没有「读取失败仍然可恢复」的用例，而这正是 MI03 AC5 要求的界面行为。
- 判读纪律：负向断言（`.inline-message:not(.error)` 为空）单独写会空过——组件在失败路径下若整段不渲染，断言照样绿。所以每条负向护栏都必须配一条同位正向断言（错误文本确实在 `.inline-message.error` 里）。
- 证据：`npm run verify` 退出 0，136 文件 / 1296 测试；新增/改动的中文用例名与注释逐条 grep 回读，prettier 对全 `docs/` 与改动测试文件通过。
- 边界：界面人工走查（清单 §1–§7）与 MI10 §8 的真实模型语义仍未执行，需要光哥的操作与单独的模型授权；本轮不推送、不发布。

## 18:35 卡片指针与用例账目核对（状态：文档证据对得上账）

- 扫的三条轴：**卡片指针真实性**、**用例数对账**、**新通道四层接线**。
- 指针：任务板里反引号点名的 48 个文件指针逐个核存在，无缺失（另有一条是 `*.test.ts` 通配片段，不是路径）。指针有两种写法混用——仓库根完整路径（`apps/desktop/src/main/...`、`packages/agent-protocol/src/index.ts`）与相对 `apps/desktop/src/{main,renderer/src}` 的简写（`views/MemoryView.tsx`、`services/memory-provenance.ts` 等 15 处）；简写按各自根目录解析后全部命中，没有出现 done 卡片指向从未建立的测试文件。
- 账目：四处用例数已过期并改正——`memory-provenance.test.ts` 19→21（材料 200 上限、空摘录区间）、`memory-recall-v2.test.ts` 9→10（资格拒绝补口）、`artifact-version-executor.test.ts` 3→4（general 分支）、`memory-capture.test.ts` 8→9。数字用 vitest 实际跑出的计数确认，不靠 `grep -c it(` 推断（嵌套 describe 会算错）。
- 接线：`memory:task-exclusions` 与 `artifact:get-version-executor` 两条新通道四层齐全——协议（channel＋请求/结果 Schema＋API 面）、主进程 `register-ipc.ts:537/1398`、preload `index.ts:204/579`、界面消费（`use-task-memory-exclusions.ts` 与 `App.tsx`），不存在「协议声明但无人读」或「界面在猜来源」。
- 变更方式：四处计数用 `sed` 定点替换（都是短 ASCII＋数字片段，不是长中文行），改完 `grep -o` 逐条回读并核对 `git diff --stat` 恰为 4 增 4 删。
- 证据：本轮只动文档；上一轮 `npm run verify` 退出 0（136 文件 / 1296 测试）仍是当前基线。待光哥人工走查与 MI10 §8 授权，未推送、未发布。

## 18:42 真实开发库只读预检（状态：v33 与 MI10 前置已在真机核对）

- 做法：对正在运行的开发实例所用数据库做**只读**核对（`sqlite3` 以 `mode=ro` 打开，只查 schema、迁移台账与计数，绝不读记忆正文、不写任何行）。活跃库定位靠 `-wal/-shm` 的时间戳，不靠文件名猜：真实路径是 `~/Library/Application Support/@betterwork/desktop/betterwork.db`（`@betterwork/` 与 `Electron/` 下的同名文件是旧的遗留实例）。
- MI04 的真机证据：`schema_migrations` 33 行、`MAX(version)=33`、最后一行名为 `MI04 memory recall policy column`、落章时间 2026-09-25 17:13:37 CST（早于本轮 18:14 的重启，说明迁移在应用真实启动路径上跑过）；`memory_records` 的 DDL 里 `recall_policy TEXT NOT NULL DEFAULT 'relevant'` 与枚举 CHECK 都在；`PRAGMA foreign_key_check` 为空；现有 1 条修订的策略值为 `relevant`、无 NULL。此前 MI04 只有合成库的两次迁移用例，现在补上「用户实际库被同一段迁移代码升级过」这一档证据。
- 一处判读纠正：`PRAGMA user_version` 在这套实现里始终是 0——迁移台账用的是 `schema_migrations` 表，不是 user_version。第一枪按 user_version 判"没迁移"是错的，改查台账才看见 33 行。教训是**核对迁移要看本仓的记账方式**，不能按 SQLite 通用惯例假定。
- MI10 前置的真机形态：`workspace_memory_settings` 0 行（没有任何空间开启自动建议）、`memory_extraction_jobs` 0 行（无排队/运行中作业）。这只证明"本机当前形态看起来满足"，仍要光哥确认验收用的工作空间就是这套配置；我没有改任何设置，也没有取消别人的作业。
- 文档回写：任务板 MI04 行加真实环境复核一句，MI10 行把"前置未经确认"细化为已核对的表计数与仍需人工确认的边界；两处都用单行写入，中途一次 Edit 因换行把表格行拆断，已用带断言锚点的脚本合并回一行并核对表头行数（294 行、`| MI` 行全部合法）。
- 边界：本轮无代码改动；基线仍是 `npm run verify` 退出 0（136 文件 / 1296 测试）。待光哥人工走查与 MI10 §8 授权，不推送、不发布。

## 18:47 走查造数段与界面文案再核（状态：清单可自足执行）

- 给清单加了 §0 造数段：此前 §1–§7 假设「样本已经存在」，光哥一打开应用会撞上没有对象可点（要 7 条优先规则、要同议题的两条已确认规则、要未到生效期的一条、要被换版的材料与上期成果版本）。现在按门禁条件给出 10 步界面造数指令，每步标注支撑哪几格，造不出来就当场记不通过——这本身就是 §1/§2/§4 的走查动作。
- 每步条件都回读实现后才落文字，避免「文案点名不存在的控件」这类老毛病：
  - 手工新建即写 `status: 'confirmed'`，所以不要求再点「确认」；分类标签实际是「约束 / 事实 / 历史经验」（不是英文枚举名，也没有「工作要求」这一项）。
  - 表单没有「以我的口径记录」勾选框——`asUserInstruction` 由「有没有来源选择器」推导（`MemoryEditor.tsx:272`），所以 0.2/0.3 改成按入口区分描述，不再让人去找不存在的控件。
  - 日期字段标签是「生效日期（可选）」，议题是「议题（可选）」（aria 名「议题标识」）。
  - 潜在冲突的四个条件是 `topicKey` 同为非空、`normalizedHash` 不同、范围相交、有效期相交（`memory-conflict-policy.ts:85–91`），因此 0.6 明确要求两条正文写成不同句子。
  - 冲突卡片抬头原文「可能冲突：两条口径指向同一议题，需要你澄清」直接写进 4.1 的期望结果。
- 本轮只动清单文档；`prettier --check` 通过，新增中文逐条 grep 回读。待光哥走查与 MI10 §8 授权；不推送、不发布。

## 19:06 矩阵逐行回钉与一处真实缺陷（状态：1300 测试，缺陷已修）

- 扫的轴：**矩阵每一行由哪张卡跑**。MI09 原写「R5/N6/N8 复用 MI01/MI03/MI05 的同目录用例」，逐行打开看后有两行其实没被钉住：
  - R5（非 BMP＋重复句选第二处）只有界面纯函数层用例，Main 的 provenance 解析器没有断言过「第二处区间」；补在 `memory-provenance.test.ts`：正文 `口径已统一。𠮷附注。口径已统一。`，取尾部区间，断言摘录、`excerptHash`、`contentHash` 都来自事件原文，并且 UTF-16 下标会比码点下标多偏移 1（同文不同位的两次保存靠区间区分）。
  - N8（优先组超预算整组落选）只有 `applyRecallBudgetV2` 的单元用例，没证明「词面命中的那条成员不会改走相关池绕开上限」；补在集成用例，先跑**对照组**证明该记忆确实能被召回，再设优先＋判定并存，断言整组不进请求、审计落选原因是 `budget` 而不是 `not-relevant`。
- 写 N8 的过程暴露一个真实缺陷：`manual` 来源的「摘录」就是记忆正文本身，却套用选择器摘录的 1–500 码点上限，于是 501–2,000 码点的合法长记忆在 `MemoryService.create` 里直接抛 ZodError——`mapDomainError` 不认识它，界面拿到的是内部错误而不是领域失败，而编辑器计数条本来就允许写到 2,000。修复是按契约 §11.1 第 3 条把两个上限分开：manual 跟随人工正文 1–2,000，四个带位置的选择器来源仍严格 1–500；契约补口径、协议加双侧边界用例、服务层加 1,100 码点回归（`543872e`）。
- 变异验证：把 manual 的上限临时改回 500 → 新增的两条用例同时变红（`× 来源摘录的两个上限各管各的`、`× 手工保存允许 500 码点以上的长正文`），其余 60 条不受影响；按原文精确还原，`git diff --stat` 只剩本卡意图内的改动。第一次变异脚本因中文锚点写错（摘**取**/摘**录**）而 `ANCHOR_COUNT=0` 直接退出，没有产生半成品改动——断言式锚点＋计数检查这次又一次防止了静默误改。
- 自己的老毛病复现：用「后一行」做插入锚点时再次删掉过既有代码（`memory-service.test.ts` 少了两行 setup），靠 `git diff --stat` 显示「只有新增 31 行」才发现并补回。教训写进项目记忆：插入必须原样回放被替换文本，改完看 diff 而不只看工具返回。
- 证据：`npm run verify` 退出 0，136 文件 / 1300 测试；任务板 MI09 行按上述结论重写（含缺陷来源与修复提交号）。仍待光哥人工走查与 MI10 §8 授权；不推送、不发布。

## 19:19 MI10 证据表与一处静默失效界面（状态：1301 测试）

- 补 §8.5 证据表（`docs/development/memory-mi10-checklist.md`）：任务板 §6.3 要求的字段（场景 ID、HEAD、模型显示名与非敏感指纹、Run ID、上下文修订、入选/排除身份、请求阶段、预期/实际、0/1、重复纠正次数、人工确认、实际请求数、失败归因）以前只有一句话描述，没有可填载体；等授权后再临时造格式容易漏字段。现按六个成对场景排 12 行，基线/改进各一行，S5/S6 的「必须 1」直接写在预期列，另加固定信息表（含 S5 准备轮真实调用数必须为 0、额度与实际发出数）和汇总口径（基线/改进/差值、未测不冒充 0 分、基线本就通过要如实写无增益）。
- 扫的轴：**上限有没有变成静默失效**。冲突卡片的「确认两条并存」原先只按 `notePoints` 把按钮禁掉——写超 300 码点时既不说明原因，也不显示当前长度，等于把 Main 的封闭上限翻译成一个「点了没反应」的哑按钮。修成输入框下方常显「已写 N / 300 码点」，超限时落一条内联错误说明怎么改，并加用例断言计数、内联错误、超限不可提交、修正后可提交（`4dc2b0c`）。清单 4.5 同步改为按新可见行为验收。
- 同类核对通过、未改：捕获摘录计数（`MemoryCaptureSource.tsx` 用 `MEMORY_SOURCE_EXCERPT_MAX_CODE_POINTS`）、记忆正文与议题标识（`MemoryEditor.tsx` 两处都有 problems 文案），界面读的常量与协议 Schema 同一来源，不存在「界面限 A、Main 限 B」的第二套数字。
- 一次误判留档：我原本怀疑并存条件超长会走到 ZodError 内部错误，读码后确认 UI 已有 `canKeepBoth` 拦在提交前，真实缺陷只是「不说明原因」，按实际情形修，没有去改 Main 的错误映射。
- 证据：`npm run verify` 退出 0，136 文件 / 1301 测试；本轮两处 markdown 单行改动用带断言锚点的脚本替换（锚点命中数 ≠ 1 直接退出），行数仍为 294、prettier 通过。待光哥走查与 §8 授权；不推送、不发布。

## 19:28 逐卡交接补齐（状态：提示词 §7 有了真实填写物）

- 补 `docs/development/tasks-memory-improvements.md` §7.1：提示词与任务板 §7 一直只放**空模板**，跑完卡片的交接物在仓库里不存在。现按模板十字段逐卡填写 MI01–MI10，状态与本板一致（MI01/MI04/MI05 done；MI02/MI03/MI06/MI07/MI08/MI09 doing＝自动化通过、UI 未测；MI10 blocked）。
- 每块都写「回归先失败的证据」而不只写「测试通过」：MI02 是未确认选区曾可提交，MI05 是未到 `validFrom` 曾可设优先，MI07 是超上限只静默禁按钮，MI09 是 501–2,000 码点长记忆在 `create` 抛 ZodError，MI01 是缺审计被 `?? 空依赖` 放过（变异实验）。
- 交接里的提交号、日志小节号、测试数、pid 全部当场核对：草稿里留下三处错指针（占位的 `16:xx`、并不存在的 18:55 小节、以及脚本里一条我自己写错的锚点——那行「下一卡与前置」在文件中不存在，脚本按锚点计数≠1 直接报错退出，删掉后才继续）。修完后逐条替换为真实存在的小节号，最终引用集合为 15:40／16:10／16:22／16:32／16:41／16:51／17:01／17:06／17:13／17:23／17:38／18:12／18:30／18:42／19:06／19:19，`grep -c` 确认 0 条残留占位。
- 边界：交接块不复述卡片验收标准，只写本卡实际发生的事实；UI 与真实模型两栏在没跑之前一律写「未测/0 次」，不拿代码完成度冒充业务效果。验证基线仍是 `npm run verify` 退出 0（136 文件 / 1301 测试），本轮纯文档，表格行数与 `| MI` 行合法性复核通过。

## 19:42 逐卡交接补齐后继续回扫 AC（状态：1302 测试）

- 交接块写完后没有停手，按同一口径回扫各卡 AC 的「自动化可判定但未断言」部分，抓到一处：MI02 AC3 的三条界面行为（保存失败不关表单且草稿原样保留、重试复用同一个 `operationId`、未提交点取消不写库）在实现里都在（`MemoryEditor.tsx:211–213` 的按键复用、`App.tsx:335–347` 的失败保留路径），但界面层一条断言都没有。补 `eff8e65`，并顺手把假 API 的 `memories.create` 签名补回来——原来的 `vi.fn(async () => okResult(writeReceipt))` 不收参数，测试在类型层就读不到提交体，这类断言根本写不出来。
- 两条被怀疑的“缺口”经核实是误判，按证据不补：
  - MI03 AC4 的「双击不反转」已有用例（保存中行显示「正在调整…」并带 `disabled`）。
  - 冲突条件的上限并没有落到内部错误：UI 用 `canKeepBoth` 拦在提交前，真实缺陷只是不说明原因，`4dc2b0c` 已按实际情形修完。
- 交接物核对：§7.1 引用的 22 个提交号与 16 个日志小节全部用脚本比对存在性，缺 0 条；MI09 与 §7.1 表头的用例数从 1296→1301→1302 连续跟随实测，不写估算值。
- 证据：`npm run verify` 退出 0，136 文件 / 1302 测试；三处文档替换均用「锚点计数≠预期即退出」的脚本，改完行数与 `| MI` 行合法性复核通过。
- 边界：仍然只剩人工两栏——清单 §0–§7 的真实窗口走查，和 §8 需要模型配置显示名＋整数调用预算的授权。未推送、未发布。
