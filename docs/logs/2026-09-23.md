# Work Log: 2026-09-23

## 01:57 WM15 系统级合成验收（状态：完成）

- 背景：WM01–WM14 按卡交付了治理、召回、审计、历史重放、提炼、简报与界面，但缺一张把这些拼在一起的端到端证据；WM15 要求在真实 SQLite 与可捕获的 Provider 请求上，跨任务、跨空间、跨重启地证明记忆只按范围与依赖进入模型，失败与迟到路径可解释。
- 变更内容：新增 `apps/desktop/src/main/services/work-centered-memory.integration.test.ts`（10 个场景，无生产代码改动）。装配真实 `AppStore.open(文件库)`、`KnowledgeVault`、`InputSnapshotService`、`TaskMaterialService`、`MemoryService`、`MemoryRecallService`、`MemoryExtractionService`、`RunService`，模型走 `ModelProviderFactory` + `fetch` 替身；两个合成工作空间（经营分析、市场研究）共用一个专家、每空间两个独立任务；`restartWorld()` 关掉全部句柄后重新装配来模拟重启。空间、材料、正文全部合成，不引用任何公司真实文件。
- 覆盖：自主口径带入同空间两个独立任务且不泄漏到另一空间；中英混合（`ARR 不含一次性实施费，单位为万元。`）按 `memory-recall-v1` 命中而无关任务不误带；提炼候选确认后只在来源材料被选中时带入，否则 `dependency-unavailable`；旧规则被替代后既不再作为记忆注入、其所在历史轮次也被 `memory-revised` 截断且旧运行审计原样保留；排除/到期/删除各自落到 `task-excluded` 与 `inactive`；重启后记忆、审计、召回一致且启动零网络；取消的 Run 不进入后续历史、迟到取消不改终态；提炼输出非法只记作业失败不改主 Run 终态；投影目录被占用时仍按 SQLite 召回并可见地报 `failed`；改造前旧运行只报 `legacy_unknown` 且不伪造 `requestHash`。
- 关键决策：替身模型必须真的发起一次 `read_text_file` 才能让材料范围 Run 完成——这是 `auditMaterialFacts` 的既定产品行为，不能为了跑通提炼而绕过；替身因此按清单里的 `path="…"` 读取本次选中的材料，读取路径来自请求本身而不是测试写死。
- 关键决策：放弃「把旧规则写进替身回答正文，再断言历史里查无此句」的初版设计。它要求产品改写历史，与「替代不改写历史」直接冲突；正确断言是让旧规则真实成为某一轮的记忆依赖，再证明该轮被安全重放挡下。
- 教训：**并行会话共用同一工作树时，工具返回的搜索结果不可作为唯一依据**——本轮 `grep`/搜索工具多次返回不存在的路径与「无匹配」，据此判断文件内容会直接写错。结论性检查一律改成 `Read` 或 python 断言计数；中文文案改完必须逐处回读字面。
- 教训：诊断顺序仍然有效。两处失败都先用 SQLite 与 Run 事件表定位（`run.failed` 的校验文案、`runMemoryContexts` 的 `selectedItems`/`exclusions`/`replay`），再看代码，最后才改测试；`conflict-unresolved` 把两条同议题记忆一起挡在注入之外这一点，只有读 `decisionSummary.exclusions` 才能看出来，光看请求体推不出来。
- 测试证据：`npx vitest run apps/desktop/src/main/services/work-centered-memory.integration.test.ts` 退出码 0（10 tests passed）；18 个 WM 定向测试文件 `npm test -- …` 退出码 0（Tests 207 passed）；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（Test Files 108 passed，构建完成）；`git diff --check` 退出码 0。提交 `2da9180`。
- 相关文件：`apps/desktop/src/main/services/work-centered-memory.integration.test.ts`、`docs/development/tasks-memory.md`。

## 02:06 WM16 用户验收与交接收口（状态：进行中，人工待验）

- 背景：WM15 完成后需要一次不扩功能的收口：把实际代码版本、命令与退出码、命名偏离与契约缺口写进唯一状态真相源，并明确哪些仍只有自动化证据、没有人工证据。
- 变更内容：`docs/development/tasks-memory.md` 总表改为 WM00–WM15 done、WM16 doing，新增 §15 收口记录（提交时间线、自动化证据表、结构性核对、契约缺口、人工待验表）；同步把已被事实推翻的状态句改口径——AGENTS.md「当前阶段」与任务路由、ADR-0015/0026、[docs/README.md](../README.md)、[docs/adr/README.md](../adr/README.md)、[docs/development/README.md](../development/README.md)、docs/03 §4.3、docs/04 头部与两处小节、docs/07 增量小节、docs/10 §6.1.3、[产品设计](../designs/work-centered-memory.md)状态行、[记忆实施契约](../development/memory-contracts.md)状态行、[记忆编码提示词](../development/memory-coding-prompts.md)依据行。无代码改动。
- 关键决策：ADR-0026 **不自行改判 Accepted**。用户下达的实现指令构成开工授权，但执行手册明确「不得把 Proposed ADR 自行标记为 Accepted」，因此状态写作「Proposed（实施中，接受状态待用户确认）」，并在 docs/03、04、07、10 把「无验收记录、不得当作现状」改为指向任务板 §15 的已实施事实。
- 关键决策：不为凑结构新建 §11 拟新增的 `run-memory-audit.ts`。审计事实已由 `run-memory-context-repository.ts`、`run-history-policy.ts`、`memory-dispatch-gate.ts`、`memory-recall-service.ts` 承载，属命名偏离，记录在 §15.3 而不制造空模块。
- 上报缺口（不在验收卡内解决）：`memory:create` 输入缺 `fromMemoryRevisionId`，而回执有该字段，导致「作为我的工作口径重新保存」提交时带不上审计链；需要走协议/契约变更单独排卡。
- 教训：**验收卡的证据必须是本次实测**，不能引用上一轮结论。本轮据此修正了两处过期认知——`HISTORY_REFERENCE_BLOCKS_DELETE` 早已实现并有测试，早期记录里的「无删除入口」缺口不成立；18 个通道的实际构成是 `memory:*` 14 个加简报与参考版本 4 个。
- 测试证据：文档卡按任务板 §13.4 只跑文档检查——新增/改动中文片段逐处 python 计数回读（`收口记录`、`memory-revised`、`SOURCE_UNAVAILABLE`、`待光哥验收` 等均命中且字面正确）；`grep -c` 逐条核对 18 个通道在 `register-ipc.ts` 与 `preload/index.ts` 各命中一次；迁移 v26–v29 与 `migrate.test.ts` 用例核对；`git diff --check` 退出码 0。未跑构建（无代码改动），沿用 01:57 的 `npm run verify` 退出码 0。
- 相关文件：`docs/development/tasks-memory.md`、`AGENTS.md`、`docs/README.md`、`docs/adr/README.md`、`docs/adr/0015-memory-scope-and-governance.md`、`docs/adr/0026-work-centered-memory.md`、`docs/designs/work-centered-memory.md`、`docs/development/memory-contracts.md`、`docs/development/memory-coding-prompts.md`、`docs/development/README.md`、`docs/03-system-architecture.md`、`docs/04-knowledge-and-memory.md`、`docs/07-mvp-and-roadmap.md`、`docs/10-ui-ux-system.md`、`docs/logs/2026-09-23.md`。
- 下一步：WM16 保持 doing，等光哥按 §15.5 逐项人工验收（治理界面、自动建议质量、两期实际交付、简报与精确版本引用），以及真实模型授权下的语义确认。本轮只本地提交，未推送、未打包、未发布。

## 02:26 §5.3 重新表述审计链补齐（状态：完成）

- 背景：02:06 一节上报的缺口是最后一项「Spec 要求但代码未承载」的内容——契约 §5.3 要求「作为我的工作口径重新保存」在操作回执保留 `fromMemoryRevisionId`，回执类型有该字段，但 `memory:create` 的输入没有，Renderer 提交时带不上，等于契约要求的审计线索断裂。目标模式下这属于 Spec 未完成项，因此补齐而不是继续挂着。
- 变更内容：`createMemoryRequestSchema` 与 `memoryOperationRecordSchema` 各加可选 `fromMemoryRevisionId`；`MemoryService.create` 新增私有 `checkRestatementLink`（只有人工口径可声明来源修订、修订必须真实存在），经 `commitReceipt` 落进 `memory_operations.result_json`，由 `buildReceipt` 回显，因此幂等重放返回同一条线索；`MemoryEditor` 重新表述路径提交 `restateFrom.revisionId`，仍不带 `sourceSelector`；契约 §5.3/§9.2 与任务板 §15.4 同步改口径。
- 关键决策：**不新增迁移、不新增错误码**。`result_json` 是受 `json_valid` 约束的 JSON 列，新字段随回执一起进同一列；失败复用 `NOT_FOUND` 与 `SOURCE_MISMATCH`，避免为一条审计线索扩张协议面。
- 教训：**vitest 通过不等于类型通过**。新测试文件在 vitest 里全绿，`npm run typecheck` 才抓出 fixture 用了不存在的 `provenance.sourceKind`／`effectiveStatus: 'active'` 形状，以及联合属性未收窄就取 `materialDependencies`。新增测试后必须跟一次 typecheck，不能只看定向测试退出码。
- 测试证据：`packages/agent-protocol/src/index.test.ts` 30 passed；`memory-service.test.ts` 13 passed（含新增 2 条）；新增 `components/MemoryEditor.test.tsx` 2 passed；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（109 个测试文件全绿，含 build）；`git diff --check` 干净。
- 相关文件：`packages/agent-protocol/src/index.ts`、`packages/agent-protocol/src/index.test.ts`、`apps/desktop/src/main/services/memory-service.ts`、`memory-service.test.ts`、`apps/desktop/src/main/persistence/memory-operation-repository.ts`、`apps/desktop/src/renderer/src/components/MemoryEditor.tsx`、`MemoryEditor.test.tsx`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：WM16 仍为 doing，缺的是光哥的人工验收（任务板 §15.5）与真实模型授权下的语义确认，这两项无法由自动化证据替代。
