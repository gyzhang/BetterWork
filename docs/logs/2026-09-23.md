# Work Log: 2026-09-23

## 01:57 WM15 系统级合成验收（状态：完成）

- 背景：WM01–WM14 按卡交付了治理、召回、审计、历史重放、提炼、简报与界面，但缺一张把这些拼在一起的端到端证据；WM15 要求在真实 SQLite 与可捕获的 Provider 请求上，跨任务、跨空间、跨重启地证明记忆只按范围与依赖进入模型，失败与迟到路径可解释。
- 变更内容：新增 `apps/desktop/src/main/services/work-centered-memory.integration.test.ts`（10 个场景，无生产代码改动）。装配真实 `AppStore.open(文件库)`、`KnowledgeVault`、`InputSnapshotService`、`TaskMaterialService`、`MemoryService`、`MemoryRecallService`、`MemoryExtractionService`、`RunService`，模型走 `ModelProviderFactory` + `fetch` 替身；两个合成工作空间（经营分析、市场研究）共用一个专家、每空间两个独立任务；`restartWorld()` 关掉全部句柄后重新装配来模拟重启。空间、材料、正文全部合成，不引用任何公司真实文件。
- 覆盖：自主口径带入同空间两个独立任务且不泄漏到另一空间；中英混合（`ARR 不含一次性实施费，单位为万元。`）按 `memory-recall-v1` 命中而无关任务不误带；提炼候选确认后只在来源材料被选中时带入，否则 `dependency-unavailable`；旧规则被替代后既不再作为记忆注入、其所在历史轮次也被 `memory-revised` 截断且旧运行审计原样保留；排除/到期/删除各自落到 `task-excluded` 与 `inactive`；重启后记忆、审计、召回一致且启动零网络；取消的 Run 不进入后续历史、迟到取消不改终态；提炼输出非法只记作业失败不改主 Run 终态；投影目录被占用时仍按 SQLite 召回并可见地报 `failed`；改造前旧运行只报 `legacy_unknown` 且不伪造 `requestHash`。
- 关键决策：替身模型必须真的发起一次 `read_text_file` 才能让材料范围 Run 完成——这是 `auditMaterialFacts` 的既定产品行为，不能为了跑通提炼而绕过；替身因此按清单里的 `path="…"` 读取本次选中的材料，读取路径来自请求本身而不是测试写死。
- 关键决策：放弃「把旧规则写进替身回答正文，再断言历史里查无此句」的初版设计。它要求产品改写历史，与「替代不改写历史」直接冲突；正确断言是让旧规则真实成为某一轮的记忆依赖，再证明该轮被安全重放挡下。
- 教训：**并行会话共用同一工作树时，工具返回的搜索结果不可作为唯一依据**——本轮 `grep`/搜索工具多次返回不存在的路径与「无匹配」，据此判断文件内容会直接写错。结论性检查一律改成 `Read` 或 python 断言计数；中文文案改完必须逐处回读字面。
- 教训：诊断顺序仍然有效。两处失败都先用 SQLite 与 Run 事件表定位（`run.failed` 的校验文案、`runMemoryContexts` 的 `selectedItems`/`exclusions`/`replay`），再看代码，最后才改测试；`conflict-unresolved` 把两条同议题记忆一起挡在注入之外这一点，只有读 `decisionSummary.exclusions` 才能看出来，光看请求体推不出来。
- 测试证据：`npx vitest run apps/desktop/src/main/services/work-centered-memory.integration.test.ts` 退出码 0（10 tests passed）；18 个 WM 定向测试文件 `npm test -- …` 退出码 0（Tests 207 passed）；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（Test Files 108 passed，构建完成）；`git diff --check` 退出码 0。提交 `2da9180`。
- 相关文件：`apps/desktop/src/main/services/work-centered-memory.integration.test.ts`、`docs/development/tasks-memory.md`。

## 02:06 WM16 用户验收与交接收口（状态：进行中，人工待验）

- 背景：WM15 完成后需要一次不扩功能的收口：把实际代码版本、命令与退出码、命名偏离与契约缺口写进唯一状态真相源，并明确哪些仍只有自动化证据、没有人工证据。
- 变更内容：`docs/development/tasks-memory.md` 总表改为 WM00–WM15 done、WM16 doing，新增 §15 收口记录（提交时间线、自动化证据表、结构性核对、契约缺口、人工待验表）；同步把已被事实推翻的状态句改口径——AGENTS.md「当前阶段」与任务路由、ADR-0015/0026、[docs/README.md](../README.md)、[docs/adr/README.md](../adr/README.md)、[docs/development/README.md](../development/README.md)、docs/03 §4.3、docs/04 头部与两处小节、docs/07 增量小节、docs/10 §6.1.3、[产品设计](../designs/work-centered-memory.md)状态行、[记忆实施契约](../development/memory-contracts.md)状态行、[记忆编码提示词](../development/memory-coding-prompts.md)依据行。无代码改动。
- 关键决策：ADR-0026 **不自行改判 Accepted**。用户下达的实现指令构成开工授权，但执行手册明确「不得把 Proposed ADR 自行标记为 Accepted」，因此状态写作「Proposed（实施中，接受状态待用户确认）」，并在 docs/03、04、07、10 把「无验收记录、不得当作现状」改为指向任务板 §15 的已实施事实。
- 关键决策：不为凑结构新建 §11 拟新增的 `run-memory-audit.ts`。审计事实已由 `run-memory-context-repository.ts`、`run-history-policy.ts`、`memory-dispatch-gate.ts`、`memory-recall-service.ts` 承载，属命名偏离，记录在 §15.3 而不制造空模块。
- 上报缺口（不在验收卡内解决）：`memory:create` 输入缺 `fromMemoryRevisionId`，而回执有该字段，导致「作为我的工作口径重新保存」提交时带不上审计链；需要走协议/契约变更单独排卡。
- 教训：**验收卡的证据必须是本次实测**，不能引用上一轮结论。本轮据此修正了两处过期认知——`HISTORY_REFERENCE_BLOCKS_DELETE` 早已实现并有测试，早期记录里的「无删除入口」缺口不成立；18 个通道的实际构成是 `memory:*` 14 个加简报与参考版本 4 个。
- 测试证据：文档卡按任务板 §13.4 只跑文档检查——新增/改动中文片段逐处 python 计数回读（`收口记录`、`memory-revised`、`SOURCE_UNAVAILABLE`、`待光哥验收` 等均命中且字面正确）；`grep -c` 逐条核对 18 个通道在 `register-ipc.ts` 与 `preload/index.ts` 各命中一次；迁移 v26–v29 与 `migrate.test.ts` 用例核对；`git diff --check` 退出码 0。未跑构建（无代码改动），沿用 01:57 的 `npm run verify` 退出码 0。
- 相关文件：`docs/development/tasks-memory.md`、`AGENTS.md`、`docs/README.md`、`docs/adr/README.md`、`docs/adr/0015-memory-scope-and-governance.md`、`docs/adr/0026-work-centered-memory.md`、`docs/designs/work-centered-memory.md`、`docs/development/memory-contracts.md`、`docs/development/memory-coding-prompts.md`、`docs/development/README.md`、`docs/03-system-architecture.md`、`docs/04-knowledge-and-memory.md`、`docs/07-mvp-and-roadmap.md`、`docs/10-ui-ux-system.md`、`docs/logs/2026-09-23.md`。
- 下一步：WM16 保持 doing，等光哥按 §15.5 逐项人工验收（治理界面、自动建议质量、两期实际交付、简报与精确版本引用），以及真实模型授权下的语义确认。本轮只本地提交，未推送、未打包、未发布。

## 02:26 §5.3 重新表述审计链补齐（状态：完成）

- 背景：02:06 一节上报的缺口是最后一项「Spec 要求但代码未承载」的内容——契约 §5.3 要求「作为我的工作口径重新保存」在操作回执保留 `fromMemoryRevisionId`，回执类型有该字段，但 `memory:create` 的输入没有，Renderer 提交时带不上，等于契约要求的审计线索断裂。目标模式下这属于 Spec 未完成项，因此补齐而不是继续挂着。
- 变更内容：`createMemoryRequestSchema` 与 `memoryOperationRecordSchema` 各加可选 `fromMemoryRevisionId`；`MemoryService.create` 新增私有 `checkRestatementLink`（只有人工口径可声明来源修订、修订必须真实存在），经 `commitReceipt` 落进 `memory_operations.result_json`，由 `buildReceipt` 回显，因此幂等重放返回同一条线索；`MemoryEditor` 重新表述路径提交 `restateFrom.revisionId`，仍不带 `sourceSelector`；契约 §5.3/§9.2 与任务板 §15.4 同步改口径。
- 关键决策：**不新增迁移、不新增错误码**。`result_json` 是受 `json_valid` 约束的 JSON 列，新字段随回执一起进同一列；失败复用 `NOT_FOUND` 与 `SOURCE_MISMATCH`，避免为一条审计线索扩张协议面。
- 教训：**vitest 通过不等于类型通过**。新测试文件在 vitest 里全绿，`npm run typecheck` 才抓出 fixture 用了不存在的 `provenance.sourceKind`／`effectiveStatus: 'active'` 形状，以及联合属性未收窄就取 `materialDependencies`。新增测试后必须跟一次 typecheck，不能只看定向测试退出码。
- 测试证据：`packages/agent-protocol/src/index.test.ts` 30 passed；`memory-service.test.ts` 13 passed（含新增 2 条）；新增 `components/MemoryEditor.test.tsx` 2 passed；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（109 个测试文件全绿，含 build）；`git diff --check` 干净。
- 相关文件：`packages/agent-protocol/src/index.ts`、`packages/agent-protocol/src/index.test.ts`、`apps/desktop/src/main/services/memory-service.ts`、`memory-service.test.ts`、`apps/desktop/src/main/persistence/memory-operation-repository.ts`、`apps/desktop/src/renderer/src/components/MemoryEditor.tsx`、`MemoryEditor.test.tsx`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：WM16 仍为 doing，缺的是光哥的人工验收（任务板 §15.5）与真实模型授权下的语义确认，这两项无法由自动化证据替代。

## 02:55 收口复核：规模压测与依赖闭包补测（状态：完成）

- 背景：15.4 补齐契约缺口后，按 Spec 原文再做一次逐条复核（不引用上一轮结论），发现两项只有实现、没有自动化证据：Spec §16 要求「规模压测先使用 1,000 条有效记忆的合成集合记录排序耗时」，§13.1 测试矩阵要求「依赖递归/循环」。
- 变更内容：`memory-retrieval.test.ts` 新增 `memory-recall-v1 scale` 用例，1,000 条有效记忆（两种范围、40 个议题、中英混合）跑确定性排序，断言条数、最高分范围、两次排序完全一致并设 200ms 上界；新增 `memory-recall-service.test.ts` 两条用例，覆盖链路末端删除后的传递失效与两条当前修订互相引用时的环终止。任务板新增 §15.6 记录本轮证据。
- 关键决策：**不给生产代码开测试后门**。记忆依赖闭包的实现里环保护是真实存在的，但正常写入路径产不出环（新修订只能引用已存在的修订），因此环状态由测试自己打开同目录 SQLite 文件改写 `provenance_json` 造出对抗状态，而不是给仓储加 `rewriteDependenciesForTest` 之类的测试专用方法。第一版草稿正是这么写的，读完真实 API 后推翻重写。
- 教训：**fixture 的形状必须来自已通过的测试，不能凭印象**。草稿里 `store.memories.setStatusOf`、`memoryRecords` 表列名、`committedMemoryIds` 三处都是不存在的 API；`sourceAvailable` 会真的回查 `run-assistant` 事件与 `manual` 操作记录，所以来源必须走真实写入路径产出，否则测的是 `source-unavailable` 而不是依赖闭包。
- 测试证据：`npm test -- apps/desktop/src/main/services/memory-retrieval.test.ts apps/desktop/src/main/services/memory-recall-service.test.ts` → 21 passed（含正控制：三条记忆在删除前都被选中）；`npx eslint` 两份文件无告警；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（110 个测试文件全绿）；`git diff --check` 干净。实测排序耗时约 10ms/1,000 条，低于可接受主线程开销，按 Spec 不返回设计评审、不引入向量库或长期后台扫描。
- 相关文件：`apps/desktop/src/main/services/memory-retrieval.test.ts`、`apps/desktop/src/main/services/memory-recall-service.test.ts`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。提交 `e543e95`。
- 下一步：复核中发现 Spec 第 212 行／契约 §5.3 要求「创建时展开来源依赖、拒绝循环」，而错误码 `SOURCE_DEPENDENCY_CYCLE` 只在协议枚举里存在、生产代码从不产出——写时环检测疑似未实现，需先取证再决定补实现还是记录偏差。

## 03:05 写时拒环：让 `SOURCE_DEPENDENCY_CYCLE` 真正产出（状态：完成）

- 背景：02:55 一节记的下一步疑似项经查证成立。契约 §5.3／Spec 第 212 行要求「在创建时展开来源依赖、拒绝循环」，但 `SOURCE_DEPENDENCY_CYCLE` 只在协议枚举里存在、生产代码从不产出：提炼服务入队与执行前都只校验依赖修订存在且哈希一致，从未把来源依赖展开成图。
- 变更内容：`memory-extraction-service.ts` 新增模块级纯函数 `hasMemoryDependencyCycle`（三色 DFS：`onPath` 命中即成环，`settled` 避免重复展开）与私有箭头 `revisionDependencies`（读精确修订的 verified 来源取下一跳，legacy 来源无可展开依赖）；入队前 `enqueueAutomatic` 返回 `notEnqueued('SOURCE_DEPENDENCY_CYCLE')`，作业执行前的依赖复证同样展开，成环则 `finish(job, 'skipped', 'INPUT_LIMIT')`。契约 §5.3 与任务板 §15.7 记录两处落点。
- 关键决策：**不为作业阶段扩协议枚举**。`memoryJobErrorCodes` 是刻意收窄的一份作业可见码表，来源类失败在那里一律折进 `INPUT_LIMIT`（既有的超额、哈希不符都是这么处理的），新代码沿用同一约定，只在注释里写明原因。
- 教训：**自动排空会让「入队后再改库」的测试写不出来**。第一版作业阶段用例走 `requestExtractionForRun`，但 `enqueueAutomatic` 里的 `scheduleDrain()` 是同步启动、`executeJob` 在第一个 `await` 前就跑完步骤 1–3，等测试拿到控制权时依赖复证已经结束，用例只能以 `MODEL_FINISH_UNKNOWN` 失败。改用既有的 `seedQueuedJob` 仓储直连登记（该 helper 的注释本来就写着是为了绕开非阻塞排空抢先 claim），并把 `memoryDependencies` 加进它的 overrides。
- 测试证据：`npm test -- apps/desktop/src/main/services/memory-extraction-service.test.ts` → 27 passed（新增 2 条：入队阶段返回 `SOURCE_DEPENDENCY_CYCLE` 且 0 入队 0 调用模型；入队后被改出环的作业落 skipped／`INPUT_LIMIT`、不调用模型、不建候选）；`npx eslint` 退出码 0；`npm run verify` 退出码 0（110 个测试文件全绿）。环状态由测试改写 `memory_records.provenance_json` 造出，未在生产代码开后门。
- 相关文件：`apps/desktop/src/main/services/memory-extraction-service.ts`、`memory-extraction-service.test.ts`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：§13.1 矩阵逐条复核的最后一处 PARTIAL 是召回期 conflict 组装配（`buildConflictMap` → `conflict-unresolved`／keep-both 同组注入），补测试中。

## 03:08 §13.1 测试矩阵逐条复核与召回期冲突组补测（状态：完成）

- 背景：02:55 一节把 §13.1 的 16 项列为待复核。本轮对当前工作树逐条取证（每项回读到具体 `it(...)` 与断言，不看用例名推断），15 项已有自动化证据，只有第 9 项「conflict 组」是 PARTIAL。
- 变更内容：`memory-recall-service.test.ts` 补两条召回期冲突装配用例——同议题、有效期重叠、无裁决的两条口径都不注入且 `conflictReviewRequired` 为真；`resolveConflict('keep-both')` 之后两条以 `conflict-pair` 一起回来、记忆块含适用条件、`conflictReviewRequired` 归零。文件头注释与 `describe` 标题同步扩到「依赖闭包与冲突组装配」。任务板新增 §15.8 记录 16 项取证结论。
- 关键决策：冲突用例复用同一份 store fixture 而不是新建测试文件——召回过滤顺序（相关性→来源→依赖→冲突→预算）是一条流水线，把依赖闭包与冲突装配放在同一处才能看出彼此的排除理由没有串台。
- 教训：**拼文件末尾要按结构而不是按字符数裁**。用 python 在 `describe` 末尾插入用例时按 `[:-2]` 裁掉收尾，结果留下一个孤立的 `}`，prettier 直接报语法错。这类插入应先断言结尾形状，再按整行替换。
- 测试证据：`npm test -- apps/desktop/src/main/services/memory-recall-service.test.ts` → 4 passed；`npx eslint` 退出码 0；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（110 个测试文件全绿）。
- 相关文件：`apps/desktop/src/main/services/memory-recall-service.test.ts`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：Spec 侧自动化证据已闭环（§15.6／§15.7／§15.8），WM16 保持 doing 等待光哥按 §15.5 人工验收与真实模型授权下的语义确认。

## 03:23 分页默认值收口到协议常量，并补齐交接模板的风险条目（状态：完成）

- 背景：03:08 之后继续按 Spec 原文复核，另一路全量审计发现协议导出的 `LIST_PAGE_DEFAULT_LIMIT = 50` 在生产代码里没有任何消费者——`memory:list` 的真实默认值是 `MemoryRepository.listPage` 写死的 `?? 50`。行为正确，但常量与实现各说一套，改常量不会改行为。
- 变更内容：`memory-repository.ts` 改为 `parsed.limit ?? LIST_PAGE_DEFAULT_LIMIT`；`memory-repository.test.ts` 新增用例，种 `LIST_PAGE_DEFAULT_LIMIT + 1` 条记忆后断言第一页正好是常量条数、给出游标、第二页返回剩余 1 条且不再给游标、两页并集不重不漏。契约 §9 写明「默认值与上限只认协议常量」。任务板新增 §15.9 记录该收口，§15.10 补齐 §14 交接模板里此前缺失的「风险」与「下一卡前置」两项。
- 关键决策：不为这一处新增错误码或迁移，只做「常量取代字面量＋回归用例」。同一文件族的作业列表本来就是从 `MEMORY_JOB_LIST_DEFAULT_LIMIT` 取默认值，说明这是既有约定，属漏项而不是设计选择。
- 教训：**引用章节号要现场核对而不是凭印象**。初稿把 ListPage 写成「Spec §8」，`grep -n "^## "` 按行号回查才知道 §9 才是 IPC/DTO 契约；中文文档里的章节引用同样要回读校验。
- 测试证据：`npm test -- apps/desktop/src/main/persistence/memory-repository.test.ts` → 16 passed；`npx eslint --fix` 后复跑 lint 退出码 0；`npm run typecheck` 退出码 0；`npm run verify` 退出码 0。
- 相关文件：`apps/desktop/src/main/persistence/memory-repository.ts`、`apps/desktop/src/main/persistence/memory-repository.test.ts`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：处理全量审计报出的五处缺口（焦点重查、派发闸门核对记忆块修订、投影清理负向用例、`IPC_FAILURE` 命名、专家不可用提示与用例覆盖），逐处取证后按阶段提交。

## 03:42 全量审计五处缺口逐条收口（状态：完成）

- 背景：03:23 一节列出的五处缺口来自「契约句子 ↔ 生产代码 ↔ 自动化用例」三方对齐审计，共同特征是契约写了但只有实现或只有文档，缺可执行证据。
- 变更内容：① 派发闸门补上契约 §6.4 的「核对记忆块修订」——新增 `assertMemoryBlockMatches`，在写 `request-prepared` 之前先要求请求里带 `MEMORY_BLOCK_HEADER` 的系统消息与已落库快照逐字相同，不符抛 `MemoryBlockMismatchError`，既不写审计也不发包；`run-service.ts` 把同一份 `RunMemoryPreparation.memoryBlock` 传给闸门。② §7.3 的「窗口重获焦点查询状态」补 `window` `focus` 监听，重查设置／作业／候选，面板不可见或卸载即摘监听。③ §5.6 的「只清理 manifest」补负向用例，预埋用户文件证明不被遍历删除。④ §9.3 的 transport 兜底码由 `TRANSPORT_FAILURE` 改名为契约写明的 `IPC_FAILURE` 并导出常量，新增 `memory-result.test.ts` 覆盖 transport 拒绝／领域失败透出／成功保留警告三条路径。⑤ 设计稿 §3.6 的「来源专家不可用改用通用助手并提示」补 `TransientToast` 与 `App.test.tsx` 两条用例。契约 §5.6／§6.4／§7.3／§9.3 各补一句实现落点。
- 关键决策：`MEMORY_BLOCK_HEADER` 从模块私有常量改为导出而不是复制一份表头字面量——两处表头是同一个算法契约的两半，分头写迟早漂移。§3.6 里「沿用来源 Run 快照中的专家身份」没有按原文改：渲染进程拿不到运行快照的专家身份，要新增 IPC＋协议 Schema＋ADR（属 E 系列范围），已在任务板 §15.12 记为口径偏离等光哥判定，不静默当完成。
- 教训：**没有 RTL 自动清理的仓库里，加一个全局事件监听就会暴露测试污染**。`vitest.config.ts` 用 `environment: 'node'` 且未开 `globals`，`renderHook` 的挂载不会自动卸载，上一例遗留的 `focus` 监听继续命中当前例的共享替身，报错表现为「期望 2 次实得 6 次」这种看似产品问题的数字。修法是 `afterEach` 里补 `cleanup()`，属测试基础设施缺陷，不是绕开产品问题。
- 测试证据：`memory-dispatch-gate.test.ts` 6 passed、`memory-result.test.ts` 4 passed、`use-memory-suggestions.test.ts` 9 passed、`memory-service.test.ts` 14 passed、`App.test.tsx` 21 passed；`work-centered-memory.integration.test.ts` ＋ `run-service.test.ts` 61 passed（证明真实请求路径通过新的逐字核对）；`npm run verify` 退出码 0（Test Files 111 passed）。
- 相关文件：`apps/desktop/src/main/services/memory-dispatch-gate.ts`、`memory-recall-service.ts`、`run-service.ts`、`memory-service.test.ts`、`apps/desktop/src/renderer/src/lib/memory-result.ts`、`memory-result.test.ts`、`hooks/use-memory-suggestions.ts`、`App.tsx`、`App.test.tsx`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：把同类缺陷一次扫清——协议导出常量的消费情况全量扫描。

## 03:52 召回与提炼阈值收口到协议常量并加护栏（状态：完成）

- 背景：03:42 收口的五处里有一处是命名漂移，而 15.9 那类「常量与实现各说一套」只修了一处。本轮做全量扫描：协议 57 个 `UPPER_SNAKE` 导出常量中，9 个在 `index.ts` 内只出现在自己的导出行、生产源码里无人引用，值却在 `memory-retrieval.ts` 与 `memory-extraction-prompt.ts` 里各写了一份字面量。
- 变更内容：`RECALL_BUDGET` 的 12 个字段、两个分词权重、打分基数与命中阈值全部改取 `MEMORY_RECALL_*` 常量；`MEMORY_RECALL_VERSION` 与两份停用词表不再在 `memory-retrieval.ts` 本地导出，测试改从协议引用。`EXTRACTION_LIMITS` 逐项改取 `MEMORY_EXTRACTION_*` 与 `MEMORY_CANDIDATE_CONTENT_MAX_CODE_POINTS`，删掉无人消费且与协议重复的 `outputTokensMax`／`timeoutMs` 两个字段；三个片段类上限归并到 `MEMORY_EXTRACTION_FRAGMENT_CODE_POINT_LIMIT`。顺带把 `matchedTokens < 2 && query.length !== 1` 写成两个常量分别表达。契约 §6.1／§7.2 补落点，任务板新增 §15.13。
- 关键决策：逐值比对确认相等后才动代码，因此这是纯接线、零行为变化，不需要新用例证明「值没变」；但要证明护栏不是摆设，所以做了一次负向验证——临时加一个无人消费的 `GUARD_PROBE_ORPHAN` 常量让新护栏失败，再撤销。护栏明确写了它不覆盖 15.9 那一类（协议内已被 Schema 消费、存储层另写字面量），避免把局部自动化当成系统性保障。
- 教训：**`String.prototype.match` 不带 `/g` 只返回一次匹配**，用它统计出现次数会把 22 处引用的常量误判成孤儿——第一次跑新护栏报了 9 个「假孤儿」，真因在计数写法。计数一律用 `matchAll`，且带 `/g` 的 `test()` 有 `lastIndex` 状态，跨字符串复用同一正则也要避免。
- 测试证据：`npm test -- standards/coding-standard.test.ts` → 22 passed（含新护栏）；`memory-retrieval.test.ts`／`memory-extraction-prompt.test.ts`／`memory-recall-service.test.ts`／`memory-extraction-service.test.ts` → 4 files／65 passed；`npx eslint`、`npm run typecheck` 退出码 0；`npm run verify` 退出码 0（Test Files 111 passed）。
- 相关文件：`standards/coding-standard.test.ts`、`apps/desktop/src/main/services/memory-retrieval.ts`、`memory-retrieval.test.ts`、`memory-extraction-prompt.ts`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：WM 侧 Spec 自动化证据到此闭环，剩余只有 §15.5 的五项人工验收与真实模型授权下的语义确认；等光哥验收后 ADR-0026 转 Accepted、WM16 标 done。

## 03:57 Spec §15 文档验证：429 条内部链接与锚点逐条取证（状态：完成）

- 背景：Spec §15「文档验证」要求内部链接与标题锚点逐一确认，此前只有 WM00 归档时的人工核对，没有可复现的证据；今天又改了三份文档，正好一起做。
- 变更内容：写一次性脚本扫描 WM 五份文档＋12 个接入入口（18 份、429 条内部链接），校验文件目标存在与 GitHub slug 锚点命中；结果 0 断链。任务板新增 §15.14 记录取证方式与结论。文档本身未作修改。
- 关键决策：脚本用完即弃，不入库——它服务的是这次收口取证，而不是要新增一条每次提交都要跑的门禁；断链防护在仓库里已由 prettier/护栏与人工评审承担，加一份按 slug 规则猜测的解析器反而会带来假阳性维护成本。
- 教训：**先怀疑工具再改文档**。首轮报的 4 条「断链」全部是脚本没剥掉中文标题里的句点（`## 12. WM 唯一任务板…` 的 slug 是 `12-wm-…`），若照单改文档就会把正确的链接改坏。判据是回读目标文件的真实标题行，而不是相信脚本输出。
- 测试证据：脚本输出 `checked 429 internal links across 18 docs / broken: 0`；`grep` 证明 `memory-coding-prompts.md` 不含任何预算数字或字段定义（Spec §15 要求的两份一致性由「没有第二份定义」保证）；`git diff --check` 退出码 0；本轮无代码改动，上一次 `npm run verify` 退出码 0 仍适用。
- 相关文件：`docs/development/tasks-memory.md`、`docs/logs/2026-09-23.md`。
- 下一步：WM 侧只剩 §15.5 的五项人工验收与真实模型语义确认，等光哥给证据后才能把 ADR-0026 转 Accepted、WM16 标 done。

## 04:44 冲突判定收口、待澄清冲突可见性与记忆界面样式（状态：完成）

- 背景：目标模式继续推 WM16。上一轮 04:28 的全量测试已经绿（112 files／1004 tests），这一轮做的是同一条审计线（契约句子 ↔ 生产代码 ↔ 用例）上还剩的缺口。
- 变更内容：① 抽出 `memory-conflict-policy.ts`，把 §5.5 的「同议题＋作用域交集＋有效期交集＋规范化哈希不同」判定从召回与工作空间简报的两份内联实现收成一份，并纠正简报把「作用域交集」写窄成「同一规范范围」的口径；② 让 `unresolved` 冲突状态第一次真正被产出——`MemoryService` 按当前全部在效记录派生未裁决对，两侧都带进治理列表，光哥的裁决界面（并列呈现＋替代/并存）才可达；③ 新增查询派生字段 `duplicatesConfirmedMemoryId`，候选与已确认记忆重复时在行内提示后果；④ 按语义 Token 补齐记忆治理页、工作空间简报、经验建议三块界面缺失的 66 个类定义，并把 `suggestion-settings` 的同名冲突改成 `suggestion-consent`。契约 §5.5／§5.6／§9.1／§10 补落点，任务板新增 §15.15。
- 关键决策：§10 的「重复/冲突待处理计数」实现落点选在治理页而不是 `WorkspaceBrief`，因为简报字段清单在契约里是封闭的、计数也不是「现有事实的只读投影」；这条连同新增字段一起写成「需要光哥判定的偏离」，不自称已批准。可见性判定用「当前全部在效记录」而不是「当前页」，否则分页会把一对冲突拆成两页各看不见。
- 教训：**枚举值没有生产者是一类真实缺陷**，`unresolved` 在协议、召回排除、界面裁决三处都被引用，却没有任何代码产出它，所以整条裁决路径是死代码——只有从「契约句子里的每个状态谁写谁读」倒查才会暴露。另一条：`vitest` 不做类型检查，新写的治理用例在 `npm run typecheck` 里报了可选链后的数组下标未收窄，测试全绿时它一直是绿的。
- 测试证据：`npm run verify` 退出码 0（Test Files 112 passed／Tests 1004 passed）；`memory-conflict-policy.test.ts` 8、`memory-service.test.ts` 16、`memory-recall-service.test.ts` 4、`workspace-brief-service.test.ts` 10、`MemoryView.test.tsx` 11；界面四份（含 `coding-standard` 护栏）58 passed。中文文案与注释逐条 `grep -n` 回读；`git diff` 对 `styles.css` 是 689 增 0 删（纯追加，未动既有规则）。
- 相关文件：`apps/desktop/src/main/services/memory-conflict-policy.ts`（新增）及其用例、`memory-recall-service.ts`、`workspace-brief-reader.ts`、`memory-service.ts`、`memory-service.test.ts`、`packages/agent-protocol/src/index.ts`、`views/MemoryView.tsx`、`views/MemoryView.test.tsx`、`components/MemorySuggestionList.tsx`、`renderer/src/styles.css`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：WM16 只剩 §15.5 的五项人工验收与真实模型授权下的语义确认；界面样式的实际观感需要光哥在应用里核对。提交发布与推送仍是单独授权。

## 04:51 §13.3 九类观察事件逐条取证（状态：完成）

- 背景：目标模式继续 WM16 收口审计。任务板 §13.3 照抄了 Spec 的九个观察事件名，但从没逐条写明每条事件由哪张表、哪个字段产生，属于「契约句子有、落点无」的同一类缺口。
- 变更内容：① 用当前代码逐条核对九个事件语义，把「生产记录与字段＋定向测试文件」写成表格落进任务板 §15.16，并在 §13.3 段落末尾加了指向本节的指针；② 核对属性口径——审计行只存修订身份、真实 usage 来自模型响应、无任何第三方埋点依赖；③ 修掉核对时发现的唯一真实缺口：`governanceAction` 是「候选确认」与「候选拒绝」两个事件在审计里的唯一区分点，此前只有服务层写入与仓储层解析，没有任何用例断言它能穿过 `result_json` 往返，补了一条真实 SQLite 往返用例并断言回执不含记忆正文。
- 关键决策：只补断言与取证，不新增事件表或遥测通道——Spec 明确要求复用 operations/jobs/run contexts 记录。表格里的测试文件逐条 `grep` 实测过归属，没有把未覆盖的用例算作证据。
- 教训：照抄需求条目里的名词列表不等于完成。九个事件名各自有没有生产者，必须逐条从写入代码查到断言代码；`memory_excluded` 这一行我第一版把测试文件写成了 `task-context-repository.test.ts`，`grep` 才发现该文件并不覆盖排除，指针写错会让后来人验证方向跑偏。
- 测试证据：`npm test -- memory-operation-repository.test.ts` 退出码 0（9 passed，较上轮 +1）。`npm run verify` 退出码 0（lint＋format:check＋typecheck＋test＋build，Test Files 112 passed／Tests 1005 passed）。中文表格与注释逐条 `sed -n` 回读。
- 相关文件：`docs/development/tasks-memory.md`（§13.3 指针＋§15.16）、`apps/desktop/src/main/persistence/memory-operation-repository.test.ts`。
- 下一步：WM16 仍剩 §15.5 五项人工验收与真实模型语义确认；ADR-0026 与 §15.12／§15.15 三处偏离等光哥判定。不推送、不打包、不发布。

## 05:03 枚举值生产者全量扫描与两处收口（状态：完成）

- 背景：WM16 审计连续两次靠人工发现「枚举值没有生产者」（`unresolved`、写时抑制提示无源），本轮改成全量扫描：取出协议里 23 个 memory/brief/reference/job 相关枚举，逐值在非测试源码里找写入点。
- 变更内容：① 扫描只剩两处无生产者，其中 `memoryOperationKind` 的 `set-settings` 是真缺陷——契约 §5.6 要求所有用户写命令按 `operationId` 重放，`applySettings` 的注释也写明回执由调用方补写，但服务层只回显 `operationId`；现在同一事务内 `claim`→写设置→`append`，重放返回原提交效果且不再取消第二轮作业，同 ID 换内容返回 `IDEMPOTENCY_CONFLICT`。② `jobFailurePhaseSchema` 的 `publish` 属于 Skill 作业运行时，不在 WM 范围，只记录不顺手改。③ 收口 §7.2 之外的第二条凭据路径：提炼服务六条 `console.error(describeError(error))` 会把厂商异常里的 `Authorization: Bearer ...` 写进本地日志，改为统一经 `diagnosticOf`（命中敏感形态只留原因码，未命中截断 300 码点）。
- 关键决策：重放时 `cancelledJobCount` 返回 0，因为重放本身不产生写入、取消只发生在首次提交；设置页展示状态读回当前那一行而不是快照。`mapError` 把异常文本作为错误消息返回界面这条全仓既有口径本轮不动，避免只在提炼服务一处收口造成不一致，已写成待判定项。
- 教训：把一次性发现改成可重复的扫描动作才算收口方法；「不落库」不等于「不外泄」，日志和错误消息是同一份敏感性的另外两条出口，检查同一份文本要用同一份谓词。
- 测试证据：`memory-extraction-service.test.ts` 28 passed（新增开关重放用例、改造日志凭据用例）、`memory-extraction-repository.test.ts` 9、`register-ipc.test.ts` 26、集成测试 10、typecheck 0；`npm run verify` 退出码 0（Test Files 112／Tests 1006）。中文注释与文案 `grep`/`sed -n` 回读。
- 相关文件：`apps/desktop/src/main/services/memory-extraction-service.ts`、其测试、`apps/desktop/src/main/index.ts`、`ipc/register-ipc.test.ts`、`services/work-centered-memory.integration.test.ts`、`docs/development/memory-contracts.md`（§5.6、§7.2）、`docs/development/tasks-memory.md`（§15.17）。
- 下一步：WM16 仍是 §15.5 五项人工验收＋真实模型语义确认；`mapError` 的错误消息收口与 §15.12／§15.15 偏离一起等光哥判定。不推送、不打包、不发布。

## 05:07 错误码清单逐值核对（状态：完成）

- 背景：§15.17 的枚举扫描顺势扩到错误码，确认契约列出的码是否都能真的返回给用户。
- 变更内容：41 个记忆错误码逐值扫描，39 个有生产者；`MATERIAL_NOT_ALLOWED`、`MATERIAL_HASH_MISMATCH` 为 0，语义分别由召回排除原因 `dependency-unavailable` 和写入侧 `SOURCE_MISMATCH` 承担。契约 §错误码清单补落点，任务板新增 §15.18。
- 关键决策：不为凑码新增失败路径，也不删契约条目——这是设计词汇比实现多一档，需要光哥判定拆码还是收敛。
- 测试证据：纯文档核对，未改代码；扫描脚本逐值给出结果，落点用 `grep` 复核到具体函数。
- 相关文件：`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：WM16 仍等 §15.5 五项人工验收与真实模型授权。

## 05:16 数字口径全量扫描与三处双源收口

- 背景：§15.17 枚举值、§15.18 错误码两轮都命中了「声明了却没人生产」这一类问题，本轮把同一套扫描动作换成数字：协议 51 个数值型记忆常量逐个统计生产源码引用，再反向核对契约正文写到的每个上限是否既有强制点又有边界用例。
- 变更内容：契约数字全部有强制点，未发现只活在文档里的上限。修掉三处「同一个数写两遍」——`run-history-policy.ts` 自写 8 对／12,000 码点、`memory-recall-service.ts` 自立排除身份上限 50、同意版本在主进程门槛与界面文案各写一份 `1`。协议新增 `MEMORY_SUGGESTION_CONSENT_VERSION = 1`，三处改为导入协议常量；契约 §6.3／§7.3／§8.2 各补落点，任务板新增 §15.19。
- 关键决策：界面侧保留 `MEMORY_CONSENT_VERSION` 这个名字，但让它等于协议常量，与同文件里 `MEMORY_CANDIDATE_PER_JOB_LIMIT = MEMORY_EXTRACTION_MAX_CANDIDATES` 的既有写法保持一致；测试断言继续用字面量（16 条、5,000×2 撑破 12,000），使契约数字变更时红在测试上而不是跟着常量悄悄走。
- 教训：**双源数字的危害不对称——两份运行时字面量相等时只是维护债，跨 IPC 边界的两份「同意版本」却是静默失效**：门槛只认自己那份，界面副本没跟上时开关永远返回 `CONSENT_REQUIRED`，而界面仍显示旧版本号，日志里也没有异常。凡是「一边校验、一边展示或提交」的标识与版本，都必须住在协议里。另外，同一句「数字只有一个真相源」有两半：§15.9 是常量没人读，本轮是两处各读一份，只扫其中一半会漏。
- 测试证据：`npm run typecheck` 通过；`npm run verify` 退出码 0（Test Files 112 passed / Tests 1006 passed）。定向：`run-history-policy.test.ts` 17、`memory-recall-service.test.ts` 4、`memory-extraction-service.test.ts` 28、`work-centered-memory.integration.test.ts` 10、`use-memory-suggestions.test.ts` 9、`memory-suggestions.test.ts` 12。无表结构变化，故无迁移。
- 相关文件：`packages/agent-protocol/src/index.ts`、`apps/desktop/src/main/services/{run-history-policy,memory-recall-service,memory-extraction-service}.ts`、`apps/desktop/src/renderer/src/lib/memory-suggestions.ts`、`docs/development/memory-contracts.md`、`docs/development/tasks-memory.md`。
- 下一步：WM16 仍等 §15.5 五项人工验收与真实模型语义确认；§15.17 的 `mapError` 异常文本外泄界面、§15.18 两个 `MATERIAL_*` 码去留、§15.12／§15.15 各待判定项归光哥拍板。

## 05:26 IPC 双面一致性核对与 docs/04 过期能力描述修正

- 背景：枚举值、错误码、数字三条扫描轴收完后，Spec 还剩两处没被逐条取证：契约 §9 的 18 个通道接口面，以及其他文档对 WM 能力的描述口径。
- 变更内容：18 个记忆通道逐条比对主进程与 preload 所用的请求 Schema 与结果 Schema 标识符，18/18 两端一致；记忆区 60 个顶层对象 Schema 全部 `.strict()`；17/18 通道有界面消费者，仅 `memory:get` 无（界面用 `memory:list` 的完整 DTO，该口保留待光哥判定）。查出并改写 `docs/04-knowledge-and-memory.md` §6 的三处过期描述：「先截 16 条再套预算、不含相关性匹配、不在排除后补位」以及把按任务召回／结构化来源／请求阶段审计写成「WM 拟新增」——这些已由 WM04/WM05/WM06 交付；同节 §6.1、§7 的「拟新增」时态一并改为「已实现（WM…）」。任务板新增 §15.20。
- 关键决策：文档 §6 改写为契约 §6 的真实管线（九类排除 → `memory-recall-v1` 打分 → 16 条／6,000 码点总预算＋2 条／600 码点偏好小池，排除与不胜任记录不占预算），并补两个自动提炼触发的代码落点，避免只说「已实现」而无从核对。
- 教训：**双端同写一份 Schema 标识符的机械结构不会自己烂掉，散文里的能力描述才会**——接口面这轮零缺陷恰恰因为它是可复制粘贴的对称结构，而 docs/04 的「已知边界」段没有进过任何门禁，实现推进一步它就退一步变成假新闻。文档核对应当与前几条轴一样做成可重复的动作，而不是收尾时顺手读一遍。
- 测试证据：本轮纯文档改动，无代码与测试变更；`npx prettier --check docs/04-knowledge-and-memory.md` 通过，`git diff --check` 退出码 0，改写段落整段 `sed -n` 回读校验中文字面；引用的 `run-service.ts:732`、`discussion-checkpoint-service.ts:51` 两个触发点用 `grep -n` 核到具体行。
- 相关文件：`docs/04-knowledge-and-memory.md`、`docs/development/tasks-memory.md`。
- 下一步：WM16 仍等 §15.5 五项人工验收与真实模型语义确认；新增待判定项：`memory:get` 保留为 API 还是并入 `memory:list` 的修订参数。
