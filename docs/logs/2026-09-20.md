# Work Log — 2026-09-20

## 11:45 API/MCP capability design adoption (status: complete)

- Request: adopt the attached API Tools and Remote MCP Capability Design without editing the plan file. Its authorization boundary permits documentation adoption only; product development requires a separate instruction.
- Baseline: `main` at `7f78515`. An untracked `.qoder/plans/` directory was already present at the start of adoption and was left untouched. No existing tracked changes were present.
- Delivered: product design, Proposed ADR-0024 for API service profiles/protected credentials, Proposed ADR-0025 for remote MCP/versioned bindings, and shared capability contracts. Added scoped references to the domain model, architecture, capability system, roadmap, UI specification, Expert contracts, documentation index, and ADR index.
- Scope decisions: curated integrations with Baidu first; multiple named profiles; distinct save/enable/test/default actions; one Main-owned encrypted credential service without merging model/API/MCP configuration; stdio plus Streamable HTTP with none/Bearer/API-key-header authentication; OAuth, legacy HTTP+SSE, arbitrary HTTP tools, and business write tools deferred.
- Execution design: fixed Expert/Task/Run selections, explicit task overrides and empty arrays, reviewed MCP contract hashes, per-Run clients, live revocation, cancelable testing, context segmentation, precise provenance, and recoverable search/model credential migration. Existing Skill execution and A/B0/E acceptance statuses are preserved.
- Consistency checks: read back all four new documents; cross-checked terminology, defaults, credential ownership/rotation, MCP enablement, empty-selection semantics, transport scope, and acceptance mapping. Clarified that new MCP connections start disabled, testing does not enable them, and legacy task-specific search exclusions take precedence during compatibility projection.
- Document validation: inspected local link targets and the linked acceptance/runtime/UI section headings. `git diff --check` emitted no diagnostics. Each of the four new specification files was checked separately with `git diff --no-index --check /dev/null <path>`: exit 1 for new-file differences, with no whitespace diagnostics. A scoped text scan found no replacement characters, accidentally written line-number prefixes, trailing whitespace, or TODO/TBD markers. Chinese headings/context touched by reference insertions were immediately read back.
- Boundaries: no product source, tests, configuration, dependencies, schema, credentials, acceptance cards, book files, or attached plan were modified. No application startup, live service call, Keychain operation, runtime test, build, commit, or push. `npm run verify` was not run because this task changes Markdown documentation only; the documents do not claim runtime acceptance.
- Next step: await a separate development instruction; the proposed implementation slices are recorded in the design and are not a second active task board.
- Related documents: [design](../designs/api-tools-and-remote-mcp.md), [ADR-0024](../adr/0024-api-services-and-credentials.md), [ADR-0025](../adr/0025-remote-mcp-and-capability-bindings.md), [contracts](../development/capability-contracts.md), [ADR index](../adr/README.md).

## 14:07 能力基础开发计划拆分与落盘（状态：完成）

- 背景：用户接受 11:45 落盘的 API/MCP 设计后，要求把后续开发目标拆成任务卡并写入仓库，以便后续以 quest 目标驱动方式逐张推进。上一回复中已把 6 个关键判断点、三套推进策略与推荐顺序列到对话里，本次直接采用推荐顺序，不重新确认。
- 变更内容：新增 [docs/development/tasks-capability-foundation.md](../development/tasks-capability-foundation.md)，共 17 张任务卡，按 CF00–CF51 编号，对应 M0–M5 六个里程碑：CF00 前置收口，CF10–CF12 契约与凭据（含 ADR-0024 升 Accepted），CF20–CF23 API service profile 与百度端到端，CF30–CF33 stdio MCP 版本化与真实业务接入，CF40–CF42 Streamable HTTP 与远程验收（含 ADR-0025 升 Accepted），CF50–CF51 跨类回归与 A21/E55/E56 联合收尾。同时在 [docs/development/README.md](../development/README.md) 顶部 blockquote 与 §2 路由表、[docs/07-mvp-and-roadmap.md](../07-mvp-and-roadmap.md) 顶部 Proposed increment 一节、[docs/README.md](../README.md) 开发执行规划列表、[AGENTS.md](../../AGENTS.md) §2 当前阶段各挂一条入口；均声明 CF 与现有 A/B0/E 并行推进，不重复排期也不改写原卡状态。
- 关键决策：任务编号采用 CF 前缀避开现有 A/B0/E/C 命名空间；M1 完成时同时升 ADR-0024 与改 [docs/12-engineering-standards.md §6](../12-engineering-standards.md) 的“明文存储”描述与对应护栏；CF22 把 `web_search` 从内置工具 allow-list 中迁出，旧专家修订按 capability-contracts §10.3 兼容投影，不回填历史；CF00 只做“核对与补证据”，A12/A16/A17/B00-5 原卡保留在原任务板；Developer ID 外部动作归 CF00 但不同卡不强制依赖；真实业务 MCP 与远程端点不可得时保持 partial，不伪造验收。
- 教训：上一轮插入到 development/README.md 的中文 blockquote 里把“覆盖”误写为“盖盖”，grep 回读时立刻发现并修正；确认“中文字符编辑纪律 → 改完即回读”的必要性；`perl -CSDA` 对“当日日志”/“保证证书”这类合法叠字会误报，不能仅靠“同字相邻”判错；需同时人工 grep 具体字段。
- 测试证据：本次仅文档新增/修改，未运行 `npm run verify`；`git diff --check` 无输出；`grep -c $'\ufffd'` 对 4 份新文档与 5 份中文上下文改动均为 0；对 4 处中文插入逐行 `grep -n` 字面回读无损坏；链接目标文件存在性逐个 `test -f` 确认。
- 边界：未修改产品代码、测试、依赖、配置、数据库、凭据、专家/Skill 实例或 attached plan 文件；不启动应用，不跑真实服务，不提交也不推送。`docs/development/README.md` 与 `AGENTS.md` 中原有中文正文一字未改，只在顶部 blockquote 与§ 2 开头各新增一段。
- 下一步：等用户下一指令；默认建议按 CF00 → CF10 推进，但开发者自行决定先开哪张卡。里程碑卡（CF12/CF23/CF33/CF42/CF51）不自动开工，需单独确认。
- 相关文件：[tasks-capability-foundation.md](../development/tasks-capability-foundation.md)、[development/README.md](../development/README.md)、[07-mvp-and-roadmap.md](../07-mvp-and-roadmap.md)、[docs/README.md](../README.md)、[AGENTS.md](../../AGENTS.md)。
