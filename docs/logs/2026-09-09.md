# 2026-09-09 工作日志

## 00:26 A07 执行协议与持久化生命周期（状态：完成）

- 背景：A06/A1 手工验收于 2026-09-08 23:54 通过后，按阶段 A 顺序开始 A07；只定义协议、仓储、状态转换与可注入 supervisor 的服务接口，不启动真实用户脚本。
- 变更内容：共享协议新增 ScriptExecution 状态与终止原因词汇、JobFailurePhase、JobSpec（protocolVersion=1）、JobResult 判别联合、RunSkillBinding 与 ScriptExecution 形状；`ToolExecutionContext` 增加 `toolCallId` 并由引擎在调用工具时注入；新增 v4 迁移建 `run_skill_bindings` 与 `script_executions`；新增 `SkillExecutionRepository`（终态条件更新防重复结束、启动恢复 interrupted）与 `SkillExecutionService`（登记先于启动、幂等取消、finishRun 有界收口、迟到结果忽略）；新增 `infrastructure/process-supervisor.ts` 的 ProcessSupervisor / SupervisorHandle 契约。
- 关键决策：`environment_id` 暂为可空文本且不加外键，`runtime_environments` 由 A10 引入后按 docs/12 §6 走 rebuildTable 补外键，不在启动代码里探测后 ALTER；取消在等待 supervisor 清理期间独占该执行的终态写入，避免 cancelled 结果抢先落库导致 cleanup failure 记不成 `failed/cleanup-failed`；executable / cwd / env 由宿主调用方解析后传入，服务与模型都提交不了执行规格；A07 不装配 main/index、不接 RunService 收口，接线留给 A13/A15。
- 验证证据：定向测试协议 10、迁移 9、执行服务 7、agent-core 与 tool-runtime 46 均通过；`npm run verify` 退出 0（28 文件 / 191 测试，较 A06 的 179 增加 12）。fake supervisor 覆盖全生命周期、重复结束、迟到结果、跨 Run 归属、错误阶段判别、cleanup 失败、spawn 失败与启动恢复；迁移测试用真实父级链验证级联与外键拒绝。
- 已知限制：ProcessSupervisor 尚无平台实现（A08/A09）；执行服务未被 main 装配，试运行按钮仍禁用；未启动任何真实脚本，不声称执行能力可用。
- 相关文件：`packages/agent-protocol/src/index.ts` 与 `index.test.ts`、`packages/agent-core/src/types.ts` 与 `agent-engine.ts` 及 `agent-engine.test.ts`、`packages/tool-runtime/src/*.test.ts`、`apps/desktop/src/main/db/app-schema.ts` 与 `migrate.test.ts`、`apps/desktop/src/main/persistence/skill-execution-repository.ts` 与 `index.ts`、`apps/desktop/src/main/infrastructure/process-supervisor.ts`、`apps/desktop/src/main/services/skill-execution-service.ts` 与 `skill-execution-service.test.ts`。
