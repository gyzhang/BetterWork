# Work Log — 2026-09-19

## 11:52 智能体工程书本 · 内部培训 PPT 课程大纲（状态：完成）

- 背景：用户请求把 `book/cn/智能体工程：从一次工具调用到可交付的系统.md`（10,263 行 · v1.0 全稿 · 21 章 + 5 附录 + 39 配图）转成一份可讲授的内部培训 PPT 大纲，落地为 Markdown 文件。用户明确「篇幅不限，以讲清楚为目标，受众和书籍的受众一样」「后续会编辑、录制一套课程」。会话中途换过模型，续接后从「读完书本 · 设计课程」阶段继续。
- 交付：`book/cn/课程大纲-智能体工程.md`（v1.0 · 1,116 行）——10 模块 24 课时的 slide 级教学大纲，每课时给出：课时目标 · 12 页 slide 提纲 · 故意失败环节 · 动手练习 · 行动清单。附：结业六条红线 · 教学配套与评估建议 · 课时与书本章节起止行对齐表。
- 关键决策：
  1. **保留书本自身章节结构**：每课时严格遵循「本章承诺 → 读完后能回答 → 定义框 → 核心判断 → 具体示例 → 故意让它失败 → 行业视角 → 在全书中位置 → 小结 → 行动清单」10 块积木的讲授节奏；不改成通用的「概念 → 例子 → 联系」模板。理由：书本本身就是这条节奏的产物，用户之后要「按这本书录课」，PPT 与书对齐最省力。
  2. **两个贯穿案例按书本口径复用**：案例 A（社团文献综述 → 某城商行调研）与案例 B（社团三张表 → 某城商行经营期报）。低领域入口 + 行业变体的双轨在 M2·c2 显式讲一次；机构代称纪律（主线固定为「某城商行」、其他行业只作对照）在大纲开头 0.3 单列一段。
  3. **代码锚点写死**：所有代码走查锚定 BetterWork 提交 `f0245d3b5b5d15d8dc54ee59e804603d39cb2154`（与书本 v1.0 事实快照 2026-09-18 对齐）。避免用户录课时又要回头找 hash。
  4. **不虚构数字**：课程大纲不引入书本里没有的统计数字或基准；书本里已经带时点的数字（如 MCP 2026-07-28 修订、pass^10 数学、检索 token 消耗 4×/15×）按书本口径引用。
  5. **M6 前可选追加 1 课时「数据素养与业务口径」**：为纯工程背景学员准备，用第 14 章社团预算做前置。
- 教训：会话中途换模型时，工作流是「旧模型读完书 · 新模型续接设计 + 撰写」。新模型续接的第一步应该是读 `.qoder/cache/projects/*/conversation-history/*.jsonl` 拿原始 user_query 与 todo 状态，不能只凭 summary 复述。本次按此顺序操作，避免了误任务方向。文件长度超 1000 行的写入按 Write + `append=true` + `continuation_context` 三段完成，未触发单文件写入截断。
- 验证证据：
  - 大纲文件行数：`wc -l book/cn/课程大纲-智能体工程.md` → 1,116（含本次 hash 锚定后的微调）。
  - 关键术语回读：`grep -n "能写程序\|某城商行\|社团预算\|事实与因果假设\|工具的价值不在它能做什么\|数学上不可能不停\|提示词负责.*想对\|代码负责.*错不了\|阶段序列属于"` 命中 25 处，字面正确。
  - 章节起止行与书本 `grep -n "^# 第 [0-9]\|^# 附录"` 结果比对，21 章 + 5 附录行号一致。
  - 参考实现锚定：书本 line 248 明示 `f0245d3b5b5d15d8dc54ee59e804603d39cb2154`，大纲 §0.4 已同步。
- 边界：
  - 本大纲是**可讲授的 PPT 结构**，不是 PPT 成品；用户后续按 slide 逐页制作时，每页正文长度需要按 PPT 呈现规律再切分。
  - 未运行 `npm run verify`——本次只涉及一个新增 Markdown 文件，无代码/构建影响；`.prettierignore` 已排除 `book/` 目录下的 Markdown。
  - 书本 v1.0 与大纲 v1.0 是同一事实快照（2026-09-18）。若书本后续更新到 v1.1，大纲「与书本的对齐表」需要重跑起止行号。
- 相关文件：[课程大纲](../../book/cn/课程大纲-智能体工程.md) · [书本合稿](../../book/cn/智能体工程：从一次工具调用到可交付的系统.md) · [架构与编辑说明](../../book/cn/00-架构与编辑说明.md)
